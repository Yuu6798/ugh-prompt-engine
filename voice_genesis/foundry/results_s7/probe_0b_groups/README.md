# 8-0b 群単位の結果（2026-08-22 再レンダ）

`s7_0b_probe.py` が 1 群（同一話者 × 同一世代）ごとに書く結果 JSON。
セルごとの命令区間（`input_meta`）・全軸値・`wav_sha256` / `samples_sha256`・
provenance を持つ。

**なぜ git に入れるか**: 初回（2026-08-21）のこれらのファイルは作業ディスク上にしか
無く、コンテナ再構築で失った。集約版 `s7_0b_results.json` は残ったが、群単位の
`input_meta`（測定窓）はそこに無く、再構成が要った。**pin・事前登録・測定結果は
git に入れる**という切り分け（runbook §0b）に従い、以後は群単位でも入れる。

**初回との関係**: 初回のレンダ実体は失われたため、ここにあるのは
2026-08-22 の再レンダである。初回の測定値との照合は済んでおり、
360 セルすべてで voicing 3 軸は **max|Δ| = 0（完全一致）**、mel 軸のみ
**max|Δ| = 1.43e-04（ε = 0.0387 の約 1/270）**だった
（`s7_reproducibility_finding.md` §3b）。したがって
[`s7_0b_results.json`](../s7_0b_results.json) の記録と Gate 1 の裁定は有効である。
