# VG-F1 記録 — Foundry Adapter v0（音節規模 unit-selection レンダラ + Performance 遺伝子 v0）

- 日付: 2026-08-15
- 記録者: Claude（Fable 設計 + Sonnet 実装・実行、Claude 完結ルート）
- 設計書: `voice_genesis/foundry/DESIGN_F1_adapter_v0.md`
- 実装: `voice_genesis/foundry/adapter/`（donor_bank / units / joins / perf_genes /
  voice_spec / render + presets 2 種）
- テスト: `voice_genesis/foundry/tests/test_adapter_*.py`（40 テスト、実 vocadito 非依存）

## 1. 実装決定

### 1.1 単位切り出し（donor_bank.py）

- vocadito clip 2 は notes アノテーション CSV（`Annotations/Notes/vocadito_2_notesA1.csv`,
  A2 も存在）が **存在した**。主経路（notes CSV）を採用し、フォールバック
  （エネルギー谷再帰分割）は発動しなかった。annotator は **A1** を採用
  （A1/A2 の選択基準は設計書に無く、実装決定。record §5 に記録）。
- CSV 形式（vocadito README 準拠）: 列 = start_sec, pitch_hz, duration_sec、
  ヘッダ無し。76 ノート、duration 分布は 0.0058s〜1.533s（中央値 0.264s）。
- **実装決定（設計書に無い下限）**: 15ms 未満（`MIN_UNIT_FRAMES_ABS=3` フレーム
  @5ms）になる CSV ノートは単位として採用しない（アノテーション上のグリッチに
  近いと判断）。実測: 76 ノート中 **2 件が drop**（`n_dropped_short=2`）、
  74 単位を採用。クリップ発動（donor 範囲外）は 0 件。
- フォールバック（`units_from_energy_valleys`）は単体テストのみで経路確認済み
  （実 vocadito では未発動）。

### 1.2 単位選択（units.py）

- コスト重み初期値は設計書 §5 Risks の指示どおり `w_p=1.0, w_d=0.3, w_c=1.0`
  のまま使用（耳判定調整は本 record の範囲外）。
- 候補半音レンジの段階拡張ステップ = 3 半音、安全弁 36 半音（3 オクターブ、
  実装決定）。**4 曲 × 2 voice の全 32 note 選択で拡張は 0 回**
  （sakura/umi ともに `selection_stats.n_expansions=0`）。donor の音域が
  score の音域を十分カバーしていたことを意味する。
- 接合コスト用の 24 log 帯域ベクトルの距離は L2（ユークリッド）ノルムを採用
  （設計書に距離関数の指定なし、実装決定）。

### 1.3 伸縮キャップとループ（units.resolve_unit_to_note）

- 比率 [0.5, 2.0] キャップは設計書どおり。**実測が示す重要な事実**: donor
  単位の中央値長（0.264s）が score のノート長（sakura: 1拍0.833s/2拍1.667s/
  4拍3.333s、umi: 1拍0.682s/2拍1.364s/3拍2.045s）より大幅に短いため、
  **往復ループ（`extended_looped`）が主経路になった**:
  - sakura: 20 note 中 17 件が `extended_looped`、3 件が `none`（無キャップ）、
    `compressed_truncated` は 0 件
  - umi: 12 note 中 6 件が `extended_looped`、6 件が `none`、
    `compressed_truncated` は 0 件
  - 圧縮切り詰め（比率 <0.5）は sakura/umi とも 0 件（発生せず）
- **実装決定（設計書に明記の無い edge case）**: 比率 <0.5（過圧縮）の場合は
  キャップ後（比率0.5）の結果を先頭から target 長へ切り詰め、万一それでも
  不足すれば末尾フレームを保持して埋める。今回のデータでは未発動。

### 1.4 接合クロスフェード（joins.py）

- 30ms（6 フレーム @5ms）を境界直前フレームに適用し、直前 unit の実フレームを
  直後 unit の先頭フレームへ向けてブレンドする方式（境界そのものは無改変 =
  直後 unit の最初のフレームと連続）。**実装簡略化（設計書に具体アルゴリズム
  指定なし）**: true overlap-add ではなく単側ブレンド。総フレーム数は timeline
  由来のまま変化しない。
- フレーズ内連続 note のみ対象（`has_join_to_prev = not is_phrase_first`）。
  実測: sakura 14 件 / umi 9 件のジョインを適用、スキップ（対象 unit が
  crossfade 幅未満）は 0 件。
- フレーズ間ブレスは carry-forward（直前有効フレーム保持）で埋めた:
  sakura `n_gap_frames=250`、umi `n_gap_frames=100`。

### 1.5 Performance 遺伝子 v0（perf_genes.py）

- singer/performance.py の note track（vibrato_depth_cents=0 でベース取得・
  ポルタメント 55ms 込み）に onset_glide / vibrato（ノート毎位相リセット）/
  drift / jitter を加算合成（cents 空間）してから乗算で f0 に反映。
- drift・jitter は glottal.py の `[UNDERSPEC-S1-4]` 教訓を踏襲し、cumsum
  ランダムウォークではなく**ブロック単位の i.i.d. 乱数 + 線形補間**方式を採用
  （drift はブロック長 = 1/(2*rate_hz) 秒 ≈ 1.25s @0.4Hz、jitter は 10ms 固定）。
- seed は `spec.seed`（drift）/ `spec.seed+1`（jitter）で分離（glottal.py の
  seed+1/seed+2 慣習を踏襲）。

## 2. Provenance

- ドナー: vocadito clip 2、`Audio/vocadito_2.wav`
  - sha256 = `8dcc99c3b08a9a5800b793e3d65cccfb4464961f15cf8ccde25bd4c8b853d519`
  - 44.1kHz → 24kHz リサンプル: `resample_poly(up=80, down=147)`（F1b/F1c と同一）
- notes CSV: `Annotations/Notes/vocadito_2_notesA1.csv`（annotator A1、76 notes）
- vocadito zip md5（既知値、再取得時照合用）: `dea40fd18f14d899643c4ba221b33a46`
- donor bank npz キャッシュ: `scratchpad/foundry_f1/donor_bank_cache/donor_bank_82837f6f864263c01376b01e.npz`
  （57.9MB・**非コミット**。キャッシュキー = sha256(wav_sha256|notes_csv_path|
  frame_period_ms|N_LOG_BANDS|MIN/MAX_UNIT_SEC|MIN_UNIT_FRAMES_ABS) の先頭 24 桁）
- presets の `donor.sha256` フィールドは上記ドナー wav sha256 と一致させた。

## 3. 4 WAV（sakura / umi × neutral / warped）

生成コマンド:

```
python3 adapter/render.py --score {sakura,umi} --voice adapter/presets/{neutral,warped}.json \
  --wav vocadito_2.wav --notes-csv vocadito_2_notesA1.csv --cache-dir <scratchpad>/donor_bank_cache \
  --out <scratchpad>/foundry_f1/f1_{score}_{voice}.wav
```

出力先: `<scratchpad>/foundry_f1/`（24kHz PCM_16・ピーク 0.6）

| WAV | 秒数 | sha256 |
|---|---|---|
| f1_sakura_neutral.wav | 24.600s | `b787c0539d67c6be4f11abd7052492fdd19ffc913360c465c4558cd480e2aa3a` |
| f1_sakura_warped.wav | 24.600s | `1e3bac5c696ab35c067022955583b7c39282fb9e436af88007f6caf5fa243314` |
| f1_umi_neutral.wav | 11.395s | `8d066b67d5fb2869f8c88d68694546253ef9a6968de4ea822c177e3fba5de785` |
| f1_umi_warped.wav | 11.395s | `c939172167a927dfa59d086d1e2932993539cdcba20058d2631c62c158a09088` |

**決定論契約の実測確認**: 同一 spec+seed で全 4 条件を再実行し、sha256 が
**全て一致**（bit-identical）することを確認した（`rerun/` を一時生成 →
比較 → 削除）。

## 4. 計測（記録のみ・最適化には使わない = §0-5 凍結事項）

`scripts/measure_bands.py`（帯域占有率 + HNR）による計測結果:

| WAV | b0_500 | b500_1k | b1k_3k | tilt(dB/dec) | HNR(dB) |
|---|---|---|---|---|---|
| sakura neutral | 0.242 | 0.233 | 0.521 | -51.87 | 10.06 |
| sakura warped | 0.459 | 0.268 | 0.271 | -57.39 | 9.59 |
| umi neutral | 0.028 | 0.799 | 0.167 | -45.46 | 10.48 |
| umi warped | 0.051 | 0.835 | 0.113 | -51.43 | 10.17 |

生 JSON: `scratchpad/foundry_f1/f1_measure_bands.json`（非コミット）。
warped preset（formant_scale=0.96・tilt=-1.5dB/oct・breath_lift=0.08）は
neutral 比で低域寄り（b0_500 増加）・HNR 微減の方向に動いており、warp 演算子の
作り分けが数値上も効いていることを確認（**判定には使わない、記録のみ**）。

## 5. 単体テスト結果

```
python -m pytest voice_genesis/foundry/tests -q --tb=short
40 passed
```

内訳（設計書 §3 の 5 観点）:

- `test_adapter_donor_bank.py`: 単位切り出し境界（notes CSV / energy valley
  フォールバック、短ノート drop、donor 範囲クリップ、長区間の再帰分割）
- `test_adapter_units.py`: 選択の決定論（再現性・tie-break 最小 index・
  候補拡張・接合コスト連続性優先）、伸縮キャップ + ループ（無キャップ / 延伸
  ループ / 圧縮切り詰め、各 shape・比率・決定論再現）
- `test_adapter_joins.py`: 接合クロスフェードの連続性（境界跳びが内部跳び
  分布の 1.5 倍以内）、無声ギャップ carry-forward、決定論再現
- `test_adapter_perf_genes.py`: vibrato 位相リセット（2 note 同一 pitch/dur
  で cents パターン一致）、seed 再現性（同 seed 一致・異 seed 不一致）、
  onset_glide 形状、drift/jitter i.i.d. ブロックウォークの seed 再現+振幅上限
- `test_adapter_voice_spec.py`: spec ロード/保存ラウンドトリップ、変形演算子
  （freq_warp/spectral_tilt の恒等ケース、breath_lift クリップ）

非破壊確認: `python -m pytest voice_genesis/singer/tests -q --tb=short -x`
→ **38 passed**（既存 singer/ 回帰無し）。`ruff check .`（リポジトリ全体）
→ **All checks passed**。

## 6. 既知の限界

- 単一ドナー = 準クローン（設計書 §0-4 で既知の暫定措置、F1.1 以降で解消予定）
- 単位境界の子音残渣は容認（§4 非目標）
- crossfade は単側ブレンド（overlap-add ではない）の簡略実装。真の overlap-add
  crossfade は将来の改良候補として残す
- CSV ノート単位の中央値長（0.264s）が score ノート長より短く、往復ループが
  主経路になった（sakura 17/20・umi 6/12）。ループの継ぎ目が新たな不自然さ源に
  なりうる（設計書 §5 Risks で既知のリスク、耳判定で確認する）
- annotator A1/A2 の選択基準は設計書に無く実装決定（§1.1）。A2 との比較は
  未実施

## 6b. 耳判定（2026-08-15・判定者 = User・非盲検）

判定者所見（原文）: 「そもそもまず日本語発声になってない。あとさくらの開始 5 秒
ぐらいの音がこもってて比較困難だ。このサンプル群では判断困難」

### 機序判読（記録者）

1. **日本語発声になっていないのは構造的必然だった**: ドナー vocadito clip 2 は
   **スペイン語歌唱**（metadata 実確認）。v0 の unit 選択は音素次元を持たない
   （設計書 §4 で意図的にスコープ外）ため、出力はスペイン語音節の断片の再配列になる。
   vocadito 全 40 クリップ に日本語は存在しない（英 17・仏 9・タガログ 6・北京語 2 ほか）
2. **試験設計の誤り（記録者の責）**: 耳のゲートには階層がある —
   「声か」（F1c で通過）→「**その言語の発声か**」（未着手）→「自然さの精度」。
   下位ゲート未通過のまま上位（接合品質・作り分け）を質問したため判定不能になった。
   S6–S9 の教訓「知覚手がかりの階層を耳が一段ずつ暴露する」の再演であり、
   音素同一性は精度ではなく**ゲート**である
3. こもった開始 5 秒: 開幕の長音ノート群（ループ発動 17/20）に暗い unit が選ばれ
   往復ループで固定された可能性が高い。ただし音素ゲート未通過下では診断優先度低
4. 判定不能も判定である: 「このサンプル群では判断困難」を有効な耳判定として記録し、
   F1 v0 の接合品質・作り分けの評価は**未実施**（≠ 失敗）として扱う

### 帰結 — 次ゲート = 音素同一性（F1.1）

- 好都合な事実: スペイン語は日本語と同じ **5 母音体系**（a/e/i/o/u ≈ あえいおう）。
  現ドナーのままでも「母音骨格」は日本語互換にできる
- F1.1（機械のみ・現ドナー）: unit を包絡 F1/F2 で母音クラス分類 → score の
  母音時系列（さ→a、く→u…）で選択制約 → 子音オンセットは singer/ の S6–S9 資産
  （locus・murmur・noise burst）を WORLD 領域へ移植
- 根治（User 決裁・素材律速）: 日本語歌唱ドナーの導入。vocadito には無いため、
  候補 = User 自身の歌唱録音（ライセンス自明・§8 的にも本人同意で最もクリーン）/
  公開日本語歌唱コーパス（PJS・きりたん歌唱 DB 等 — ライセンス精査要）

## 7. Open Questions

1. **単位選択の距離関数**: 接合コスト（24 log 帯域ベクトル間距離）を L2
   ノルムで実装した。設計書は距離関数を明記していない（L1 等の代替も
   考えられる）。耳判定で問題が出た場合の調整候補。
2. **crossfade の実装方式**: 設計書は「WORLD パラメータ領域の 30ms
   クロスフェード」とのみ指定。本実装は単側ブレンド（§1.4・§6）を採用した。
   真の overlap-add（総フレーム数を一時的に伸長してから縮める方式）の方が
   接合品質が高い可能性があるが、実装複雑度とのトレードオフで見送った。
3. **圧縮切り詰め（比率<0.5）時の埋め方**: 設計書は延伸ループ側（比率>2.0）
   のみ明記。圧縮側は「キャップ後に先頭から切り詰め、不足時は末尾保持」を
   実装決定とした（§1.3）。今回のデータでは未発動のため実測での妥当性検証は
   できていない。
4. **annotator A1 vs A2**: vocadito clip 2 には 2 名分の notes アノテーション
   がある。A1 を主経路に採用したが選定根拠は無い（実装上の最小仮定）。

## 8. 耳判定素材の提出記録

耳判定欄は **空欄**（User 判定待ち）。提出材料:

- `scratchpad/foundry_f1/f1_sakura_neutral.wav`
- `scratchpad/foundry_f1/f1_sakura_warped.wav`
- `scratchpad/foundry_f1/f1_umi_neutral.wav`
- `scratchpad/foundry_f1/f1_umi_warped.wav`
- 比較用に F1c transplant を再掲: `scratchpad/foundry_f1c/f1c_transplant.wav`
  （存在確認済み、F1c は借り物旋律の移植・F1 は自前生成である点が異なる）

### 耳判定（未記入）

判定者:
所見:
