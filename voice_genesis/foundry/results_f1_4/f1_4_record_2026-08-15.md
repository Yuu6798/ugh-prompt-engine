# VG-F1.4 記録 — VCV 遷移内包 unit への転換

- 日付: 2026-08-15
- 記録者: Claude（Sonnet 実装・実行・実測、Claude 完結ルート）
- 設計書: `voice_genesis/foundry/DESIGN_F1_adapter_v0.md` 追補 F1.4
- 背景: `results_f1_3/f1_3_record_2026-08-15.md` 耳判定節（「滑らかさは改善されて
  いるが息の切れ目、繋ぎ目に改善が見られない」）。機序判読: 現行の「母音核 unit +
  子音クリップの挿入」は接合を音韻遷移点（V→C→V、知覚的最敏感部）で行っており、
  VCV 銀行の設計意図（遷移は録音済み・接合は母音定常部中央）と逆だった。
- 実装:
  `voice_genesis/foundry/adapter/donor_bank.py`（`DonorUnit.vowel_core_start_frame`
  新設 + `_unit_vowel_core_range`/`_unit_mean_power` の母音核限定化・F1.4-A/F1.3-B
  整合）、
  `voice_genesis/foundry/adapter/donor_bank_utau.py`（`VCVContext` +
  `_select_wav_subset_for_contexts` + `build_donor_bank_utau_vcv` 新設・v1
  `build_donor_bank_utau` は関数として残置・F1.4-A）、
  `voice_genesis/foundry/adapter/units.py`（`vcv_context_sequence` /
  `VCVTargetNote` / `VCVUnitSelection` / `select_vcv_units` / `resolve_vcv_unit_to_note`
  新設・F1.4-B/C）、
  `voice_genesis/foundry/adapter/render.py`（`_vcv_required_contexts` /
  `_build_vcv_placements` 新設 + `donor="ritsu"` を VCV 経路へ全面差し替え・
  vocadito/pjs 経路は無改変・F1.4-B/D）。
- テスト: `test_adapter_donor_bank.py`（+3）、`test_adapter_donor_bank_utau.py`
  （+7）、`test_adapter_units.py`（+16）、`test_adapter_render_donor_wiring.py`
  （+4）で **既存 150 本 + 新規 30 本 = 180 テスト、全 green**
  （`python -m pytest voice_genesis/foundry/tests -q`）。`voice_genesis/singer/tests`
  38 本、非破壊確認済み。`ruff check voice_genesis/foundry/` clean。

## 1. 実装決定

### 1.1 VCV alias 実在調査（F1.4-A item1/3）

波音リツ強連続音 Ver1.5.1 の oto.ini（A3/F4 各 1237 行）を実測した。

- **「a か」形式（VCV・prev_vowel 付き）**: 各 pitch dir で 1059 行（非フレーズ
  先頭）。sakura/umi が実際に必要とする 27 個の文脈キー（後述 §2）は **A3/F4 の
  両方で 100%（27/27）過不足なく存在**し、重複エイリアスも無い（1 文脈=1 unit）。
- **「- か」形式（フレーズ先頭形）**: 各 pitch dir で 178 行存在。sakura/umi が
  必要とするフレーズ先頭 8 種（さ/や/そ/み/か/う/ひ/お）は全て存在。
- **「a R」等の休止形（フレーズ末用エイリアス）**: **実在しない**（A3/F4 とも
  1237 行中 0 件。alias 末尾トークンが `R`/`r`/`-`/`ー`/`息` になる行を全走査して
  確認）。波音リツ強連続音は「強連続音」（VCV 中心の連続音）バンクであり、
  CVVC 系や一部の単独音バンクに見られる明示的な語尾（ending）録音を持たない
  構成と判断する。
  **[実装決定・record 記録]** 設計書 F1.4-A item3「無ければ減衰テーパで代替」の
  対応として、新規コードは追加しなかった。`singer/performance.py`
  `build_amplitude_envelope` が既に「フレーズ末ノートの減衰（末尾 30% を
  1.0→0.55 へ線形フェード）」を実装しており（F1.3 で「振幅の唯一の権威」に
  確定済み）、これがそのまま代替の減衰テーパとして機能すると解釈した
  （§5 Open Questions #1 参照）。

### 1.2 donor_bank.py: `vowel_core_start_frame`（F1.4-A item1）

`DonorUnit` に `vowel_core_start_frame: Optional[int] = None` を追加した
（unit 先頭からの相対フレームオフセット。`[0, vowel_core_start_frame)` =
録音済み調音遷移＝伸縮しない固定区間、`[vowel_core_start_frame, end)` =
母音定常部＝伸縮/ループ対象）。`None`（vocadito/pjs/旧 UTAU unit）は後方互換で
unit 全体を母音定常部として扱う。

F1.3-B item1「母音核区間の平均パワー」という正規化基準の記述に VCV 化後も
忠実であるため、`_unit_mean_power`（→ `normalize_unit_energy`）を
`vowel_core_start_frame` 以降のみを使うよう変更した（`_unit_vowel_core_range`
新設）。**[実装決定・record 記録]** これにより、unit 先頭の調音遷移（前母音尾+
子音、収録レベルが母音核と異なりうる）が正規化基準を歪めることを防ぐ
（`test_normalize_unit_energy_does_not_distort_by_transition_level` で検算済み）。
ゲイン自体は unit 全体（`start_frame:end_frame`）へ一様に適用する
（単一の連続録音のため、母音核基準のゲインを遷移部にもそのまま使うのが自然
という F1.3 からの方針を継続）。

### 1.3 donor_bank_utau.py v2: VCV unit 化（F1.4-A item1/2）

`build_donor_bank_utau_vcv` を新設した。v1 `build_donor_bank_utau`（母音核のみ
切り出し + 別保持の `ConsonantClip`）は F1.3 までの互換テストのため関数として
残置するが、**render.py の `donor="ritsu"` 経路からはもう呼ばない**。

- unit の frame 範囲を `[offset, cutoff)`（従来の母音核のみ `[consonant_end,
  cutoff)` から拡張）へ変更。これにより録音済みの「前母音尾 + 子音 + 母音
  アタック」の遷移全体が unit 内に保持される（設計書「録音済み調音遷移を切らずに
  保持する」）。
- `vowel_core_start_frame = consonant_end_frame - offset_frame`（unit 内相対値）
  として遷移/母音核境界を記録する。
- `overlap_frames` / `preutterance_frames` は F1.3 と同じ計算式（oto の
  overlap_ms / preutterance_ms をフレーム化）だが、**基準点が unit 先頭
  （= offset）のまま**なので意味が変わらず、そのまま「unit 内オフセット」として
  再利用できる（実装変更不要だった点を記録）。
- 子音は unit に録り込まれるため `ConsonantClip` 別保持は行わない
  （F1.4-B item3「録音子音クリップ挿入経路も VCV 経路では廃止」）。戻り値は
  `(DonorBank, unit_contexts, stats)` の 3 要素（v1 の 4 要素 `consonant_clips`
  を廃止）。

### 1.4 wav 部分集合選択の文脈駆動化（F1.4-A、[実装決定・record 記録]）

v1 の `_select_wav_subset`（オンセット被覆 + 母音あたり最小件数のヒューリス
ティック）は、VCV 経路では「sakura/umi が実際に必要とする文脈キーを過不足なく
含む」ことを保証しない（計算資源境界の設計意図とは無関係な理由で mora が
解決不能になりうる）。そのため `_select_wav_subset_for_contexts` を新設し、
render.py から渡される `required_contexts`（レンダリング対象スコアが必要とする
`(prev_vowel, mora)` の重複無しリスト）を**過不足なく含む最小 wav ファイル集合**
をファイル名昇順の決定論走査で選ぶよう変更した。`required_contexts=None` の
場合は文脈非依存の汎用フォールバック（ファイル名昇順で `max_wav_files` 件）を
維持し、単体テスト・将来スコア向けの汎用性を保った。

実測（sakura×ritsu）: A3/F4 各 21 wav ファイル（sakura 単独）で 17 文脈が過不足
なく解決（`n_wav_files_analyzed=28`、後述 §2）。umi 単独では A3/F4 各 10 wav
ファイルで 11 文脈。WORLD 分析コストは「wav ファイル数」に比例するため
（unit 切り出し方式に依存しない）、F1.3 までの計算資源境界方針は維持される。

### 1.5 units.py: VCV 選択（F1.4-C）

`select_vcv_units` を新設した。候補フィルタは 3 段階:

1. **exact**: `unit_contexts[idx] == (target.prev_vowel, target.mora)` の完全一致
2. **near**（フォールバック）: mora のみ一致（prev_vowel を無視 = 「x 扱い」）
3. **global**（想定外の最終フォールバック）: mora すら一致する unit が皆無の場合、
   全 unit を候補にする

候補内のコストは `w_p*|Δsemitone| + w_d*|log(dur比)|` のみ（**[実装決定・record
記録]** 設計書 F1.4-C は「従来コストの音高項・長さ項は継続、母音ペナルティは
文脈キー一致で自動充足」と述べるのみで concat cost (`w_c`) の扱いには触れて
いない。文脈キー完全一致フィルタにより候補数が通常 1〜2（A3/F4 の 2 音階分）に
絞られるため、concat cost の実質的な弁別力がほぼ失われることと、設計書が
明示的に継続を指定したのが音高・長さ項のみであることから、VCV 選択では
concat cost を計算しない判断とした。§5 Open Questions #2 参照）。tie は
`unit.index` 昇順（決定論）。

### 1.6 units.py: VCV 尺合わせ（F1.4-A item1 / F1.4 §5）

`resolve_vcv_unit_to_note` を新設した。unit を「録音済み調音遷移（先頭
`vowel_core_start_frame` フレーム・**伸縮せずそのまま保持**）」+「母音定常部
（残り・F1.3 と同じ [0.5,2.0] キャップ + 往復ループの overlap-add 機構で目標残長
へ伸縮）」の 2 区間に分けて処理し、単純連結する（同一連続録音由来のため境界に
不連続は生じない）。目標長が調音遷移長を下回る縮退ケース（実コーパスでは
最短ノートでも遷移長よりずっと長いため未発動・後述 §4）は遷移を切り詰め
`head_clipped=True` で記録する設計とした。

### 1.7 render.py: preutterance 消費（F1.4-B item2/3）

`_build_vcv_placements` を新設した。**フレーズ先頭ノート**（`is_phrase_first`）
のみ、配置開始位置を `min(preutterance_frames, 直前のブレスギャップ frames)`
だけ cursor より前へずらす（ブレス 0.25s=50frame の範囲内で自動的にクリップ
される。§2 で実測: sakura 5/6 フレーズ頭で発動・全件無クリップ、umi 1/3
フレーズ頭で発動・無クリップ。umi の残り 2 件はブレスギャップが無い/使い切れ
ないケースではなく、全曲最初のフレーズ（ギャップ 0 のため shift=0 で自然に
不発動）と、必要な shift が実際に 0 未満にならないケースの記録漏れがない
ことを個別確認済み）。`end_frame` は shift の影響を受けない（次フレーズの
ギャップ計算を汚染しないため）。

**[実装決定・record 記録]** フレーズ内部（`has_join_to_prev=True`）のノートには
明示的な cursor シフトを加えていない。理由: `joins.assemble_v2` は run 内部を
`overlap_frames` のみで overlap-add 連結し、`NotePlacement.start_frame/end_frame`
は run 境界（run[0]/run[-1]）以外では未使用（コードパスを検証済み）。実データで
overlap < preutterance < vowel_core_start_frame が常に成立する（§2.1）ため、
overlap-add 接合そのものが「preutterance 相当の lead-in を暗黙に運ぶ」構造に
なっており、追加のシフトは不要と判断した。§5 Open Questions #3 に設計意図との
齟齬可能性を記録する。

### 1.8 consonants.py / 録音子音クリップの不使用（F1.4-B item3）

`donor="ritsu"` の VCV 経路では `consonants.apply_consonant_onset` も
`prepend_recorded_consonant` も呼ばない（子音は unit に録り込み済み）。
`render()` の結果辞書では `consonant_source="vcv"` を報告する（`vocadito`/`pjs`
は従来どおり synthetic/recorded/none を使用・無改変）。

## 2. VCV 被覆統計・文脈解決表（実測）

### 2.1 接合位置の構造不変条件（実測）

波音リツ強連続音 Ver1.5.1 の oto.ini 全 1059 個の非フレーズ先頭 VCV エイリアス
（A3/F4 各 1059）で **`overlap_ms < consonant_ms`（= overlap_frame <
vowel_core_start_frame）が 100% 成立**（`preutterance_ms < consonant_ms` も
100%、`overlap_ms <= preutterance_ms` も 100%）。これは接合フレームが常に
録音済みの前母音尾側（=母音定常部由来）に落ち、子音/遷移領域には踏み込まない
ことを oto の権威あるセグメンテーション情報だけから保証する（帯域指標を使わない
構造検証・`test_vcv_unit_overlap_falls_before_vowel_core_boundary` で
単体テスト化）。sakura/umi の実選択結果（後述）でも個別に再検算し、全 32 件
（sakura 20 + umi 12）で成立を確認した。

### 2.2 文脈解決表

sakura（20 mora）: 全 20/20 が文脈完全一致（`match_kind=exact`）。フォール
バック 0 件。

| # | prev | mora | pitch選択 | overlap/vcore/preutt(frame) |
|---|---|---|---|---|
| 0 | - | さ | F4 | 6/43/26 |
| 1 | a | く | F4 | 20/72/60 |
| 2 | u | ら | F4 | 20/83/60 |
| 3 | - | さ | F4 | 6/43/26 |
| 4 | a | く | F4 | 20/72/60 |
| 5 | u | ら | F4 | 20/83/60 |
| 6 | - | や | F4 | 4/31/5 |
| 7 | a | よ | F4 | 20/89/60 |
| 8 | o | い | F4 | 20/68/60 |
| 9 | i | の | F4 | 20/76/60 |
| 10 | - | そ | F4 | 6/44/30 |
| 11 | o | ら | F4 | 20/78/60 |
| 12 | a | は | F4 | 20/74/60 |
| 13 | - | み | F4 | 2/31/9 |
| 14 | i | わ | F4 | 20/82/60 |
| 15 | a | た | F4 | 20/77/60 |
| 16 | a | す | F4 | 20/70/60 |
| 17 | - | か | F4 | 0/34/13 |
| 18 | a | ぎ | F4 | 20/76/60 |
| 19 | i | り | A3 | 20/72/60 |

umi（12 mora）: 全 12/12 が文脈完全一致。フォールバック 0 件。

| # | prev | mora | pitch選択 | overlap/vcore/preutt(frame) |
|---|---|---|---|---|
| 0 | - | う | F4 | 0/14/1 |
| 1 | u | み | F4 | 20/73/60 |
| 2 | i | は | F4 | 20/76/60 |
| 3 | - | ひ | F4 | 6/32/14 |
| 4 | i | ろ | F4 | 20/83/60 |
| 5 | o | い | F4 | 20/68/60 |
| 6 | i | な | F4 | 20/81/60 |
| 7 | - | お | F4 | 0/21/0 |
| 8 | o | お | F4 | 20/70/60 |
| 9 | o | き | F4 | 20/67/60 |
| 10 | i | い | F4 | 20/60/60 |
| 11 | i | な | F4 | 20/81/60 |

**フォールバック実測**: sakura/umi 合わせて 32 mora 全件が exact 一致（near/
global フォールバック 0 件）。sakura/umi 単独の音域が A3/F4 の 2 音階に近い
ため、ほぼ全ノートで F4（349Hz 近辺）が選ばれ、sakura の最終モーラ「り」のみ
A3 が選ばれた（score 上の主音回帰でピッチが下がるため）。

### 2.3 bank 統計（wav_sha256 は選択された wav 群の合成ハッシュ）

| レンダー | n_units | n_wav_analyzed | wav_sha256(先頭12桁) |
|---|---|---|---|
| sakura×ritsu | 180 | 28 | `99968fc3bf3a` |
| umi×ritsu | 143 | 20 | `bbf741afccf8` |

`n_unmapped_kana`: sakura=7（今回の必須文脈選択では使わない周辺 mora の
かな正規化漏れ・選択結果には無関係）、umi=0。`n_dropped_short`=0（両曲とも）。
`n_negative_overlap_clamped`=0（両曲とも）。

## 3. preutterance 統計（実測）

| レンダー | n_preutterance_applied | n_clipped | shift値(frame) | mean(frame) |
|---|---|---|---|---|
| sakura×ritsu | 5 (/6 フレーズ頭) | 0 | [26, 5, 30, 9, 13] | 16.6 |
| umi×ritsu | 1 (/3 フレーズ頭) | 0 | [14] | 14.0 |

先頭フレーズ（sakura #0, umi #0）は直前ギャップ 0（曲頭のため）で shift=0 と
なり「適用」に数えていない（設計どおり: t=0 の前に食い込む無音は存在しない）。
クリップ発動は 0 件（今回の実コーパスでは preutterance 実値が常にブレス
0.25s=50frame の範囲内に収まった。最大値は sakura #1/#4 の 30frame=150ms）。

`n_stretch`（VCV 母音核の伸縮モード分布）: sakura = none 13 / extended_looped 6 /
compressed_truncated 1、umi = none 6 / extended_looped 3 / compressed_truncated 3。
`head_clipped` 発動: 0 件（両曲とも。最短ノートでも遷移長 90frame≈450ms を
大きく上回るため未発動）。

## 4. E2E（実データ・sakura/umi × ritsu の F1.4 版）

出力先（scratchpad、リポジトリへは非コミット）:
`foundry_f1_4/wavs/{sakura_ritsu_f1_4,umi_ritsu_f1_4,sakura_ritsu_f1_4_warped}.wav`

| ファイル | donor/voice | sha256（先頭12桁） | 長さ(s) | F1.3 版 sha256（先頭12桁） |
|---|---|---|---|---|
| sakura_ritsu_f1_4.wav | ritsu/neutral | `a1d82769d5c5` | 22.785 | `8f5d9a48627f`（F1.3） |
| umi_ritsu_f1_4.wav | ritsu/neutral | `619648245e08` | 10.425 | `49f8195d15ee`（F1.3） |
| sakura_ritsu_f1_4_warped.wav | ritsu/warped | `fa346bcc51f2` | 22.785 | （F1.3 warped 版は未生成・比較対象なし） |

- **決定論確認**: `sakura_ritsu_f1_4` をキャッシュ有効（同一 npz/pkl 再利用）・
  キャッシュ無効（別ディレクトリでフル再計算）の両経路で再実行し、3 回とも
  sha256 完全一致（`a1d82769d5c5...`）を確認済み。
- **F1.3 版との sha 差分**: 全ファイルで sha256 が変化している（unit 内容
  （VCV 全体 vs 母音核のみ）・接合位置・preutterance 消費のいずれもが波形へ
  影響する変更のため、一致しないことは想定どおり）。長さは F1.3 より短縮
  （sakura: 27.125s→22.785s、umi: 11.515s→10.425s）。**[実装決定・record
  記録]** `resolve_vcv_unit_to_note` は各ノートの出力フレーム数を常に
  `target_n_frames`（score 由来のノート長）ちょうどに保つ（head + core_target
  = target_n_frames）ため、note 単位の長さ自体は VCV 化による影響を受けない。
  総尺短縮の要因候補は (a) フレーズ頭の preutterance 消費（cursor を最大
  30frame=150ms 前倒しする、§3）、(b) run 内 overlap-add による毎 join 短縮
  （F1.4 実測: sakura 14 joins 全件 20frame=100ms 均一・§2.2）の 2 つだが、
  選択アルゴリズム自体が w_v ペナルティ方式（F1.3）から文脈完全一致方式
  （F1.4）へ全面変更されているため、同一ノートで選ばれる donor unit が
  F1.3/F1.4 間で一致する保証は無く（F1.3 の overlap 分布は 20×11,10×2,20混在
  ・平均18.57frame で F1.4 の全件20frameとは異なる）、F1.3 との単純な
  overlap 値比較で寄与を分離することはできない。定量分析は未実施
  （§5 Open Questions #4）。
- `measure_bands.py` 計測は記録のみ（§0 凍結どおり判定に用いない）: 生データ =
  scratchpad `foundry_f1_4/measure_bands_f1_4.json`。
  - sakura×ritsu(neutral): RMS -22.71dBFS / crest 18.27dB / HNR中央値 8.88dB
  - umi×ritsu(neutral): RMS -19.43dBFS / crest 14.99dB / HNR中央値 9.75dB
  - sakura×ritsu(warped): RMS -22.61dBFS / crest 18.17dB / HNR中央値 9.03dB

## 5. Open Questions（設計書に無い判断・要 User/Fable 確認）

1. **休止形エイリアス不在時の代替**: §1.1 のとおり、波音リツ強連続音
   Ver1.5.1 には休止形（rest-form）エイリアスが 1 件も存在しない。設計書の
   「無ければ減衰テーパで代替」を、F1.3 で確定済みの
   `performance.build_amplitude_envelope` フレーズ末減衰（既存コード・新規
   追加なし）で充足したと解釈した。専用の VCV 固有テーパ（例えば unit 自体の
   末尾をさらに減衰させる等）を意図していた場合は齟齬がある。
2. **VCV 選択コストから concat cost (w_c) を除外した判断**: §1.5 のとおり、
   設計書が「音高項・長さ項は継続」とのみ書いており concat cost の扱いが
   明示されていない。文脈完全一致フィルタで候補数がほぼ 1〜2 に絞られる
   ため実務上の影響は小さいと考えるが、明示的な設計意図の確認を要する。
3. **フレーズ内部（mid-phrase）ノートに明示的な preutterance シフトを
   加えていない判断**: §1.7 のとおり、`joins.assemble_v2` の run 内部処理が
   `start_frame`/`end_frame` を使わず `overlap_frames` のみで overlap-add
   する構造上、明示シフトは効果を持たない（コード上確認済み）ため未実装
   とした。設計書 F1.4-B item2「各 unit をノート開始より preutterance 分
   だけ前に配置」を全ノートに文字通り適用する別実装（例えば
   `joins.assemble_v2` 自体を preutterance 対応に拡張し、overlap 長を
   `preutterance - overlap` 相当の別概念で再定義する等）を意図していた
   場合は、本実装は設計意図と異なる可能性がある。
4. **F1.3→F1.4 の総尺短縮の要因分解が未実施**: §4 のとおり、総尺短縮
   （sakura -4.34s、umi -1.09s）の要因候補（preutterance 消費 / run 内
   overlap-add の join 長）を列挙したが、選択アルゴリズム自体が F1.3/F1.4 間で
   全面的に異なるため donor unit 選択結果を直接比較できず、寄与を分離した
   定量分析はしていない（設計書に要求が無いため）。
5. **F1.3 warped 版との比較 WAV が存在しない**: §4 の比較表で
   `sakura_ritsu_f1_4_warped.wav` に対応する F1.3 warped 版が無いことを
   記録した（F1.3 では neutral 版のみが `results_f1_3` の比較対象として
   生成されていたため）。耳判定では F1.3 neutral 版・vocadito 基準線との
   比較を優先する想定。

## 6. 耳判定

（**耳判定素材の提出待ち。空欄で用意**）

- 提出予定素材: `sakura_ritsu_f1_4.wav` / `umi_ritsu_f1_4.wav` /
  `sakura_ritsu_f1_4_warped.wav` + 比較用に F1.3 版
  （`sakura_ritsu_f1_3.wav` / `umi_ritsu_f1_3.wav`）と vocadito 基準線
  （`regress_vocadito_sakura_neutral.wav`）を再掲
- 質問（設計書 F1.4 Acceptance より）: 「F1.4 vs F1.3 vs vocadito 基準線」で
  接合点（息の切れ方・つなぎ方）が改善したか
- 判定: _(未実施)_
- コメント: _(未実施)_

## 耳判定（2026-08-15・判定者 = User・非盲検）

判定者所見（原文）: 「改善はあるがまだ未達だな」

- F1.2→F1.3→F1.4 で 3 連続「改善あり・未達」= unit-selection 磨き込みの限界逓減を確認
- 構造的背景: 銀行の情報密度（2 音階・単一音源・数百 unit）が上限を規定しており、
  接合工学では超えられない帯に入ったと判断
- 同時に User から工程再編提案（リファレンスデータセット先行・Phase 0-4 構想）が
  提示された → 設計検討へ移行（本 record はここで close、F1.x 梯子は凍結）

## Codex R1 対応（4 件全採用）

PR #262 の `chatgpt-codex-connector[bot]` レビュー r1（inline comment 4 件、全採用
判定済み・`pr262_codex_review_r1.md`）への対応。ブランチ
`claude/voicegenesis-dev-continue-rfwf3p`。

1. **render.py（P1・F0/振幅の圧縮後タイムライン整合）**: `assemble_v2` が
   フレーズ内 run を true overlap-add で圧縮する一方、`base_f0_persample`/
   `amp_env` は圧縮前の生スコアサンプル時間軸で構築したまま、圧縮後フレーム数
   から作った素朴な等間隔インデックスで読み出していた（join を経るたびに
   ズレが累積するバグ）。修正: `render._note_frame_track` で各ノートの
   perf_genes トラックを圧縮前・実スパン内で抽出し、
   `joins.assemble_control_tracks_v2`（新設、`compute_runs` で run 分割ロジックを
   `assemble_v2` と共有）で sp/ap と**同一の** run/overlap 構造へ通す。
   テスト `test_assemble_control_tracks_v2_matches_sp_ap_frame_for_frame_with_multiple_joins`
   （4 note・3 join）で厳密一致を確認。
2. **units.py（P2・ループ素材の overlap 損失補償）**: `_ping_pong_pad_v2` の
   内部 join 損失 + base↔pad 接合損失を見込まず deficit ちょうどの生ループ材を
   作っていたため、圧縮後は不足し `_force_length` の末尾フレーム複製（凍結
   プラトー）で埋めていた（実測: 10 倍伸長で末尾 325ms=65 frame が完全な静止
   フレーム）。修正: `_loop_pad_raw_target` で内部 join 損失+base 接合損失を
   見込んだ過剰生成を行い、`_force_length` は切り詰め分岐（末尾を捨てるだけ）
   で target へトリムする。10 倍伸長の再検証で凍結補填 1 フレーム以下を確認。
3. **donor_bank 系（P1・キャッシュキーの内容ハッシュ）**: vocadito/UTAU v1/
   UTAU v2(VCV)/PJS の全 builder のキャッシュキーが root path + options のみで、
   `--cache-dir` 再利用時に音源（WAV/oto.ini/.lab/notes CSV）を in-place 編集
   しても古いキャッシュを返すサイレントな provenance 破損があった。修正:
   `donor_bank.aggregate_content_hash` を新設し、各 builder で（oto.ini/.lab/
   notes CSV 解析 + wav 部分集合選択という軽量処理をキャッシュ判定より前倒し
   した上で）選択された WAV 群 + oto.ini/.lab/notes CSV のバイト sha256 を
   sorted 連結して再ハッシュしたものをキー材料へ含めた。fixture の oto.ini/
   notes CSV/.lab を 1 バイト編集してキャッシュキーが変わることをテストで
   確認（4 builder 全件）。
4. **measure_bands.py（P2・単一 read で parse+hash）**: `sf.read` での decode と
   `Path.read_bytes()` での hash が別々の read で、両者の間にファイルが
   書き換えられると記録される sha256 が実際に解析したサンプル列と食い違う
   TOCTOU 窓があった（AGENTS.md Persistent Artifact Safety Gate 項目1 違反）。
   修正: `read_bytes()` を 1 回だけ呼び、同一バイト列から `io.BytesIO` 経由の
   decode と sha256 の両方を導出する。`monkeypatch` で `Path.read_bytes` の
   呼び出し回数が厳密に 1 であることを直接 enforce するテストを追加。

### F1.4 耳判定（未達）の判定条件の訂正記録

**上記「耳判定（2026-08-15）」節の判定は、#1（F0/振幅タイムラインずれバグ）を
含んだ出力に対するものだったことをここに記録する。** バグは「フレーズ内で
join を経るたびに F0/振幅の読み出し位置がスコア時間軸上でズレる」性質のため、
判定者コメント「改善はあるがまだ未達」で観測された接合部の質感の一部（特に
join 2 回以上を含む長いフレーズの後半ノート）は、unit-selection/VCV 化それ自体
の限界だけでなく、このタイムラインずれの影響を受けていた可能性がある。
`sakura_ritsu_f1_4.wav`/`umi_ritsu_f1_4.wav`（耳判定に使われた版）と
`sakura_ritsu_f1_4_fixed.wav`/`umi_ritsu_f1_4_fixed.wav`（本対応後の再生成版）は
別音源であり、上記の「限界逓減」判断（F1.x 梯子凍結の根拠）を無効化するもの
ではないが、耳判定の前提条件として本記録に明記する。再耳判定は本 record の
スコープ外（User 判断待ち）。

### 新旧 WAV sha256（先頭 12 桁）

| ファイル | 版 | sha256(先頭12桁) | 長さ(s) |
|---|---|---|---|
| sakura_ritsu_f1_4.wav | 旧（F0/振幅ずれバグ込み・耳判定対象） | `a1d82769d5c5` | 22.785 |
| sakura_ritsu_f1_4_fixed.wav | 新（Codex R1 対応後・本節） | `e27e80506ead` | 22.785 |
| umi_ritsu_f1_4.wav | 旧（F0/振幅ずれバグ込み・耳判定対象） | `619648245e08` | 10.425 |
| umi_ritsu_f1_4_fixed.wav | 新（Codex R1 対応後・本節） | `ffd1b8ba8510` | 10.425 |

新版は決定論確認済み（同一入力を no-cache 再計算・cache 再利用の 2 経路で
再実行し、旧版生成時と同じ手順で sha256 完全一致を確認: sakura/umi とも
2 回とも同一 sha256）。長さ（22.785s/10.425s）は旧版と一致（#1 の修正は
配置・尺そのものではなく F0/振幅トラックの読み出し位置の補正のため、
n_total_frames は不変）。生成スクリプト・出力先: scratchpad
`foundry_f1_4/run_f1_4_fixed_e2e.py` / `foundry_f1_4/wavs/*_fixed.wav`
（リポジトリへは非コミット・従来どおり）。

### Codex R1 修正版の耳判定（2026-08-15・判定者 = User・非盲検）

判定者所見（原文）: 「fixed は他よりよく聞こえる。まだ改善が必須だが良くなってる」

- F0 タイムライン整合 + ループ凍結修正は**耳で確認できる改善**だった（自動レビュー由来の
  修正が耳判定で裏取りされた初のケース）
- F1.4 の総合判定は「未達」のまま維持 — ただし未達幅は当初判定より小さい
  （当初判定はバグ込み出力への判定だったため）
- F1.x 凍結の裁定は不変（凍結根拠 = 銀行情報密度の構造限界であり本バグではない）。
  S0–S4 実行順も不変

## Codex R2 対応（6 件・全採用）

PR #262 の `chatgpt-codex-connector[bot]` レビュー r2（コミット 9654c3b 以降、inline
comment 6 件）への検証 + 対応。ブランチ `claude/voicegenesis-dev-continue-rfwf3p`
（対応 HEAD: 本節を追記したコミット）。実害基準（AGENTS.md §3）で検証し、6 件とも
実害を確認したため全採用。反証には至らなかった。

### 1. render.py:256 spec.donor 未検証（P1・採用）

**検証**: `render()` は `spec = vs.load_voice_spec(...)` で `spec.donor` を読むだけで
一度も検証していなかった（該当コード確認済み）。実際のドナーは `--donor`/`--wav`/
`--voicebank-root` から独立に決まるため、コミット済み `presets/neutral.json`
（`donor.dataset="vocadito"`、vocadito_2.wav の実 sha256 と一致 = pin 自体は正しい）
を `--donor ritsu --voicebank-root <波音リツ>` と組み合わせて render しても
vocadito の provenance を騙れることを確認した（実際、F1.4 の全 ritsu render は
`run_f1_4_fixed_e2e.py` で `presets/neutral.json` を流用しており、この意味で
**既存の全 F1.4 ritsu 出力の provenance 宣言は F1.4 実施時点から嘘だった**）。

**修正**: `render.py` に `_validate_spec_donor`（新設）を追加し、重い WORLD 分析より
前に fail-closed で照合する。`donor` dict スキーマを dataset 別に拡張（破壊的変更
なし・vocadito 形は不変）:
- `dataset="vocadito"`: 既存どおり。`sha256` が `--wav` の実体 sha256 と不一致なら
  `DonorProvenanceError`
- `dataset="ritsu"`: 新設 `voicebank_sha256`。`donor_bank_utau.voicebank_identity_hash`
  （`--voicebank-root` 配下の全 pitch dir oto.ini を `(相対パス,sha256)` で集約。
  oto.ini は unit セグメンテーションの権威情報 = 意味のある identity pin。wav 全数
  ハッシュより軽量で WORLD 分析より前に検証できる）と不一致なら `DonorProvenanceError`
- `dataset="pjs"`: 新設 `corpus_sha256`。`donor_bank_lab.corpus_identity_hash`
  （全 `pjsNNN/pjsNNN.lab` を集約）と不一致なら `DonorProvenanceError`
- `dataset` が `--donor` と不一致でも即 `DonorProvenanceError`

新設 preset `presets/ritsu_neutral.json`（`voicebank_sha256` = 波音リツ強連続音
Ver1.5.1 実データの実測値 `e722b36c...d00206`）・`presets/pjs_neutral.json`
（`corpus_sha256` = PJS_corpus_ver1.1 実データの実測値 `ed7b6e56...b1547f9`）を追加。
`render()` の戻り値へ `donor_provenance`（照合結果）を追加し CLI 出力にも表示する。
単体テスト 10 本（vocadito/ritsu/pjs の一致・不一致・dataset 不一致・hash キー欠落・
presets 4 件のスキーマ形状）を `test_adapter_render_donor_wiring.py` に追加。

**影響**: 音に影響する修正（bit は不変。fail-closed 検証を通すために
`--voice presets/ritsu_neutral.json` へ切替が必要になった = §「fixed2」参照）。

### 2. render.py:137 preutterance が phrase 頭のみ（P1・採用・実測で確認）

**検証**: 波音リツ強連続音 Ver1.5.1 の oto.ini 全 1059 個の非フレーズ先頭 VCV
エイリアスで `overlap_ms < preutterance_ms`（overlap_frame < preutterance_frame）が
100% 成立することを実測済み（F1.4 §2.1）。典型値は overlap=20frame(100ms)・
preutt=60frame(300ms)。旧 `_build_vcv_placements`/`joins.assemble_v2` は
mid-phrase note の接合を `overlap_frames` のみで overlap-add しており、
`preutterance_frames` を全く消費していなかった（該当コード確認済み）。

数式で検証: run 内 overlap-add で unit i の frame `k` が着地する絶対位置は
`prev_len - overlap_i + k`（`prev_len` = 直前までの累積長）。真のアタック点
= frame `preutterance_i`（overlap ではなく preutterance が「アタック=拍位置」を
表す UTAU の定義）が着地する位置は `prev_len + (preutterance_i - overlap_i)` と
なり、`preutterance_i > overlap_i` のため **`preutterance_i - overlap_i` フレーム分
だけ名目ビートより遅延する**（典型値で 40frame=200ms）。F1.4 Acceptance
「各 unit をノート開始より preutterance 分だけ前に配置」に反する実装だったことを
確認した（実装者自身も §5 Open Questions #3 で齟齬可能性を記録済み）。

**修正**: `joins.py` に `NotePlacement.preutterance_frames`（新フィールド・
`None`=vocadito/pjs で後方互換）+ `overlap_add_concat` の `extra_trim_frames`
引数（新設・省略時 0 で後方互換）+ `_resolve_extra_trim` ヘルパーを追加。
`assemble_v2`/`assemble_control_tracks_v2` の両方（f0/振幅を sp/ap と同一
フレーム数に保つため両方に同じ trim を適用）が、mid-phrase join ごとに
`max(0, preutterance_frames - 実効overlap_frames)` フレームだけ直前
アキュムレータ末尾を追加でハードトリムしてから、従来どおり `overlap_frames`
幅でブレンドする（ブレンド幅=overlap のsonic特性は変えず、ブレンド開始点だけ
前へ動かす）。`render.py` `_build_vcv_placements` は `NotePlacement` へ
`preutterance_frames=sel.unit.preutterance_frames` を渡すだけ（1 行）。

**タイミングテスト**（`test_assemble_v2_preutterance_attack_frame_lands_at_nominal_beat`）:
unit_b の frame[preutterance] が直前 note の累積長（名目ビート位置）へ厳密一致
することを検算済み。`preutterance_frames=None` は trim=0 のまま不変
（`test_assemble_v2_preutterance_none_matches_overlap_only_behavior`）。

**影響**: **音に影響する修正**。F1.4 fixed2 WAV を再生成（下記）。

### 3. render.py:475 出力の atomic 公開（P2・採用）

**検証**: `sf.write(str(out_path), y, SR, subtype="PCM_16")` が最終 out_path へ
直接書き込んでいた（該当コード確認済み。AGENTS.md Persistent Artifact Safety
Gate 項目6「全構築後公開」違反）。プロセス kill・ディスク満杯時に truncated WAV
を残す/既存の有効な出力を破壊しうる。

**修正**: `render.py` に `_atomic_write_wav`（新設。`src/svp_rpe/utils/atomic_io`
と同じ流儀 —— staging tempfile + `os.replace`、`except BaseException` で
staging を best-effort 削除してから re-raise —— だが `src/` は変更禁止のため
adapter 内に独立実装）を追加し、`render()` の書き込み箇所を置き換えた。
単体テスト 3 本: 正常書き込み+tempfile 残留なし、`sf.write` 注入失敗時に
out_path が作られない（AGENTS.md 項目7「公開途中失敗の注入テスト」）、
失敗時に既存の有効な出力を破壊しない。

**影響**: 音・bit には影響しない（書き込み経路のみの変更）。

### 4. donor_bank.py:57 aggregate hash がパス非結合（P2・採用）

**検証**: `aggregate_content_hash` は `hashes: Sequence[str]` を受け取り
`"|".join(sorted(hashes))` するだけで、どのパスがどの digest かを捨てていた
（該当コード確認済み）。2 個の選択済みファイル（同じ hash 集合になるよう中身を
入れ替えても）ファイル名が変わらなければ集約ダイジェストが不変になる
——provenance 破損（Codex 指摘は正しい）。

**修正**: シグネチャを `aggregate_content_hash(paths_and_hashes: Sequence[Tuple[str,
str]])` に変更し、`(相対パス, hash)` ペアを `path:hash` 形式で `path` 昇順ソート
してから連結する。呼び出し元 3 箇所（`donor_bank_utau.py` の v1/v2 builder、
`donor_bank_lab.py`）を全て `(相対パス, sha256)` ペアを渡すよう更新し、cache-key の
`schema=` マーカーを新版へ更新（v1 builder: v2→v3、VCV builder: v2→v3、lab
builder: v1→v2。旧キャッシュとの意味不一致な衝突を避ける）。中身入替検知テスト
2 本（`test_aggregate_content_hash_detects_content_swap_between_same_named_paths`
/ `..._changes_when_path_changes_but_hash_set_same`）を追加。

**影響**: bit には影響しない（cache key の内部計算方式のみの変更。既存 cache は
schema マーカー変更により無効化される=意図どおり）。

### 5. donor_bank_utau.py:193 負 cutoff の意味論（P2・採用・実測で確認）

**検証**: 一次資料 https://utaudb.sakura.ne.jp/knowledge.php?id=21 を確認:
「ブランクが正の数の場合、wavファイルの末尾からの時間[ms]で定義される」
「ブランクが負の数の場合、オフセットからの時間[ms]に-1を乗じたもので定義
される」= `blank_ms = -(cutoff_ms - offset_ms)` すなわち
`cutoff_ms = offset_ms - blank_ms`（Codex 指摘と一致）。旧実装は符号に関わらず
`wav_duration_ms - abs(blank_ms)` としており、負 blank では意味論を誤っていた。

**実 oto 実測**（波音リツ強連続音 Ver1.5.1）: 負 cutoff エントリは **A3: 2 件
（`n ぬA3` offset=2749,blank=-600・`- ぽA3` offset=79,blank=-600）、F4: 1 件
（`- ちょF4` offset=714,blank=-600）**、計 3 件（1237 行中）。VCV 強連続音では
稀（大多数は正号）だが Codex の指摘どおり CVVC/一部音源では多数になりうる構造。
`n ぬA3` の実測: 旧実装 cutoff=3996.24ms・正しくは cutoff=3349ms（647ms=129frame
過大に長い区間を切り出していた = Codex 指摘の実害を数値で確認）。

**sakura/umi への実害**: 3 件の負 cutoff エントリを含む wav ファイル
（`_のぬんなんぬ↓.wav`/`_ぽぷんぱんぷ↓.wav`/`_ちょちゅんちゃんちゅ.wav`）を
実際に `_select_wav_subset_for_contexts`（sakura/umi の必須文脈: 前掲 §2.2）で
再選択して検証したところ、**sakura/umi のどちらの必須文脈（32 mora 全件）にも
この 3 ファイルは選択されない**ことを確認した（sakura/umi 側は「- の」ではなく
「i の」等の別文脈を要求するため）。したがって本バグは実装として確実に存在するが、
**F1.4 の既存 sakura/umi ritsu 出力には影響していない**。

**修正**: `cutoff_position_ms` を `blank_ms>=0` と `<0` で分岐（`>=0`: 従来どおり
`wav_duration_ms - blank_ms`。`<0`: `offset_ms - blank_ms`）。offset/wav_duration
の範囲へのクランプは両分岐で維持。テスト 3 本（正しい値・wav_duration 非依存・
破損値クランプ）を更新/追加。

**影響**: F1.4 の既存 sakura/umi 出力への影響は実測で否定（上記）。ロジック自体は
実害を持つバグとして修正（他 pitch dir/他 UTAU 音源・将来のスコアで負 cutoff
エントリが選択されれば影響しうる）。**fixed2 WAV の再生成は不要**（sha256 不変を
実測確認—後述）。

### 6. render.py:475 --out が入力を上書き（P1・採用）

**検証**: `--out` の衝突検査が皆無で、`--out donor.wav --wav donor.wav` のような
呼び出しが分析後に無条件で `--wav` を上書きする（該当コード確認済み。
`--voicebank-root` 配下への書き込みも同様に無防備）。

**修正**: `render.py` に `_reject_output_collision`（新設）を追加し、`_atomic_write_wav`
呼び出し前に `--wav`/`--voice`（voice_spec_path）/`--notes-csv`/`--voicebank-root`
配下との resolved 衝突（symlink 解決後）を fail-closed で拒否する
（AGENTS.md 永続成果物「公開サイト」設計チェックリスト項目2「衝突ガードは
必須引数」準拠 —— `protected_files`/`protected_roots` はデフォルト無しの必須引数）。
単体テスト 7 本（wav/voice_spec との完全一致衝突・voicebank_root 配下衝突・
無関係パスは素通り・symlink 解決後判定・None 引数はスキップ）。

**影響**: bit には影響しない（書き込み前ガードの追加のみ）。

### テスト・lint・回帰確認

- `ruff check .`（リポジトリ全体）: clean
- `python -m pytest voice_genesis/foundry/tests -q`: **229 件、全 green**
  （既存 196 本 + 新規 33 本。内訳: joins +9 本 / donor_bank +4 本 /
  donor_bank_utau +2 本（既存 1 本の期待値訂正込みで正味 +2） /
  render_donor_wiring +18 本）
- `python -m pytest voice_genesis/singer/tests -q`: 38 本、非破壊確認済み
- `src/svp_rpe/**`・`pyproject.toml` は無変更（本体スイートへの影響なし）

### F1.4 fixed2 WAV（#2 preutterance 消費修正を反映・音に影響）

`presets/ritsu_neutral.json`（#1 の新スキーマ pin）を使い
`--donor ritsu --voicebank-root <波音リツ強連続音Ver1.5.1>` で再生成した
（生成スクリプト: scratchpad `foundry_f1_4/run_f1_4_fixed2_e2e.py`。出力先:
scratchpad `foundry_f1_4/wavs/*_fixed2.wav`・リポジトリへは非コミット・従来どおり）。

| ファイル | 版 | sha256(先頭12桁) | 長さ(s) | fixed(R1)版 長さ(s) |
|---|---|---|---|---|
| sakura_ritsu_f1_4_fixed2.wav | 新（Codex R2 #2 対応後） | `7f4393f209da` | 19.985 | 22.785 |
| umi_ritsu_f1_4_fixed2.wav | 新（Codex R2 #2 対応後） | `a487ccfa0c7f` | 8.625 | 10.425 |

完全 sha256:
- sakura: `7f4393f209da6d8b21d3647e98c33cfe510f5b4d19c78da4d256d7e8a5dcbf48`
- umi: `a487ccfa0c7ffb8aa6a85cd8bdc4dab0d3ef2f713b152897e11e0d94a20ad77c`

**決定論確認**: 両ファイルとも (a) 同一 cache_dir 再利用（cache hit）、
(b) 別 cache_dir でのフル再計算（no-cache）の 2 経路で再実行し、初回と合わせて
**3 回とも sha256 完全一致**を確認済み。

**長さ短縮の内訳**（実測 `join_stats`）: sakura は 14 join × extra_trim 40frame
（=preutterance 60frame − overlap 20frame、oto 実測値どおり一様）= 560frame
（2.8s）分、fixed(R1) 比で追加短縮（19.985s = 22.785s − 2.8s と厳密一致）。
umi は 9 join × 40frame = 360frame（1.8s）分、同様に厳密一致（8.625s = 10.425s
− 1.8s）。donor_provenance は両ファイルとも `declared_sha256 == actual_sha256`
（`presets/ritsu_neutral.json` の pin が実データと一致することを確認済み）。

**#5（負 cutoff）による再生成は無し**: 上記「5.」の実測のとおり、負 cutoff の
3 wav ファイルは sakura/umi のどちらの必須文脈にも選択されないため、#5 単独では
出力 bit に影響しない（fixed2 は #2 の影響のみを含む）。

**再耳判定はスコープ外**（User 判断待ち。F1.x 梯子凍結の裁定・S0–S4 実行順は
本対応で変更しない —— #2 は「凍結後に発見された実装バグの是正」であり、
凍結根拠（銀行情報密度の構造限界）そのものへの反証ではない）。

## Codex R2 追加 2 件 + 総尺回帰の R3 対応

PR #262 の `chatgpt-codex-connector[bot]` レビュー r2 の残り 2 件（cache_hit /
split-read。inline comment、コミット 0b2e401 以降）への対応 + Claude 側の
設計判定による内部発見（総尺回帰）への対応。ブランチ
`claude/voicegenesis-dev-continue-rfwf3p`。

### 修正1（設計判定による内部発見・最優先）: 接合の絶対タイムライン化

**検証**: `joins.assemble_v2`/`assemble_control_tracks_v2`（Codex R2 #2 の
`extra_trim_frames` 機構）は mid-phrase join のたびに直前アキュムレータ末尾を
`max(0, preutterance_frames - overlap_frames)` フレームだけ実際にハードトリム
してから overlap 幅でブレンドする。この「トリムして詰める」方式は単一 run
内では正しく機能するが、`assemble_v2` が複数 run（フレーズ）を合成する際、
run 間ギャップは**常に naive（score 由来の圧縮前）タイムライン基準**で計算
される一方、各 run 自体の長さは trim 分だけ圧縮済みのため、各 run の圧縮が
後続の全 run へ伝播し、総尺が score 総尺から累積的に縮む。実測（fixed2）:
sakura 14 join × 40frame(=preutt60−overlap20、実データで一様) = 560frame
(2.8s) の縮み。score 準拠を要求する F1.4 Acceptance に反する（record §4 の
[実装決定]で「総尺短縮の要因分解は未実施」としていた箇所の実要因）。

**修正**: `joins.py` に絶対グリッド配置版の新関数 `assemble_absolute`/
`assemble_control_tracks_absolute` を新設した（既存 `assemble_v2`/
`assemble_control_tracks_v2` は無変更のまま関数として残置・vocadito/pjs 経路
はそちらを引き続き使う。回帰リスクを avoid するため VCV 専用の新経路とした）。

- 出力バッファを `score_total_frames`（`perf.build_timeline` が返す
  `total_samples` をフレーム化した固定値。trim しない）で先に確保する。
- `render.py` `_build_vcv_placements` を全面改訂: 全ノート（フレーズ先頭・
  mid-phrase 問わず）に対して統一的に「拍位置（score 由来の cursor 位置）−
  lead（選択 unit の `preutterance_frames`）」の絶対座標へ配置する
  （`start_frame`/`end_frame` の両方が同じだけシフトする。R2 までの
  「end_frame は shift 非依存」は撤廃）。フレーズ先頭ノートのみ、直前ブレス
  ギャップ長へ shift をクリップする（無音より前へは食い込ませない・既存
  挙動維持）。
- `assemble_absolute` は、`has_join_to_prev=True` な note が直前までに
  書き込み済みの区間へ実際に重なる場合（lead 差により生じる自然な重なり。
  一様な preutterance の note 同士は重なりゼロ = 単純連結。フレーズ先頭
  直後の最初の mid-phrase note のように preutterance が異なる場合は実際の
  重なりが生じる）のみ、その区間を log-sp/ap クロスフェードでブレンドする
  （トリムではなく同一時間軸上のブレンド）。未配置区間（フレーズ最終ノート
  の末尾に生じうる shift 分の空白等）は末尾 carry-forward（前方 hold）で
  埋める（v1 `assemble` と同じ既存の無声ギャップ充填規約を再利用・追加実装
  なし・[実装決定・record 記録]）。

**テスト**: `test_adapter_joins.py` に `assemble_absolute`/
`assemble_control_tracks_absolute` の単体テストを 8 本追加（(a) 総フレーム数
= 指定バッファ長を厳密 enforce、(b) unit のアタック点が絶対座標上の宣言
位置へ厳密着地することを frame ごとに一意な値で検算、(c) 重なり区間のブレンド
連続性（トリムではないことの確認）、(d) 決定論の repeat 一致）。
`test_adapter_render_donor_wiring.py` の 2 本
（`test_build_vcv_placements_mid_phrase_note_start_unshifted` /
`..._end_frame_unaffected_by_shift`）は R3 の新挙動（mid-phrase note も
start_frame/end_frame が shift される）に合わせて
`..._shifted_by_preutterance`/`..._shifted_with_start_frame` へ改名・
アサーションを更新した（変更理由をテスト docstring に明記）。

**影響**: 音・総尺に影響する修正（fixed3 WAV を再生成・後述）。

### 修正2（Codex R2 追加・P2・`donor_bank_utau.py:507`）: pickle キャッシュ復元時の cache_hit

**検証**: `build_donor_bank_utau_vcv`/`build_donor_bank_utau`（v1）/
`build_donor_bank_lab` の pickle キャッシュ復元パス（`cache_path.exists()`）は
`pickle.load(f)` の戻り値をそのまま return しており、保存時にダンプされた
`stats["cache_hit"]=False`（`bank.stats` 側も同様）がキャッシュ命中時にも
False のまま返る。record/CLI 出力の `cache_hit` を見て新規分析かキャッシュ
再利用かを判別する呼び出し側の前提が壊れる（実測: 既存の
`test_build_donor_bank_utau_vcv_cache_roundtrip` 等は 2 回目呼び出しの
`cache_hit` を検証していなかったため見過ごされていた）。

**修正**: 3 builder すべての復元パスで、返す直前に `stats["cache_hit"]`
（返り値タプルの stats 要素・`bank.stats` の両方）を `True` へ上書きする。

**テスト**: 既存の roundtrip テスト 3 本
（`test_build_donor_bank_utau_vcv_cache_roundtrip`/
`test_build_donor_bank_utau_cache_roundtrip`/
`test_build_donor_bank_lab_cache_roundtrip`）へ、1 回目 `cache_hit is False`・
2 回目 `cache_hit is True`（`stats`/`bank.stats` 双方）の直接検証を追加した
（バグが再発すれば roundtrip テスト自体が落ちるようにし、同種の見過ごしを
再発させない）。

**影響**: record/CLI 出力（`cache_hit` フィールド）にのみ影響。bit には
影響しない。

### 修正3（Codex R2 追加・P2・`donor_bank_utau.py:540`）: ダイジェストとデコードの単一 read 化

**検証**: `build_donor_bank_utau_vcv`/`build_donor_bank_utau`（v1）は prepass
で選択された wav ファイルをハッシュ用に read した後、メイン loop の
`_load_wav_24k`/`sha256_of`（再ハッシュ）がそれぞれ同じファイルをもう一度
read していた（oto.ini も同様: prepass の hash 用 read の後、`parse_oto_ini`
が別 read で decode）。`donor_bank_lab.py`（PJS）・`donor_bank.py`
（vocadito）にも同型の split-read があった（vocadito: wav は
`sha256_of`→`load_donor_24k` の 2 回 read、notes CSV も
`sha256_of`→`load_notes_csv` の 2 回 read）。prepass〜メイン loop の間
（WORLD 分析を挟むため実測で分単位になりうる）にファイルが書き換わると、
キャッシュキーに記録される sha256 と実際に解析されたバイト列が食い違う
TOCTOU 窓が生じる（measure_bands.py の R1 修正と同型の穴。AGENTS.md
Persistent Artifact Safety Gate 項目1「単一 read で parse+hash」違反）。

**修正**: 全 4 builder（UTAU v1/v2(VCV)・PJS・vocadito）で「1 回 read した
バイト列から hash と decode/parse の両方を導出」する形に統一した
（measure_bands.py の R1 修正と同じ流儀。WAV は `io.BytesIO` 経由
`soundfile`、oto.ini/.lab/notes CSV はバイト列から decode）。

- `donor_bank_utau.py`: `parse_oto_ini(path)` を `parse_oto_text(text)` へ
  解析本体を分離（`parse_oto_ini` は薄いラッパとして後方互換維持）。
  `_load_wav_24k(path)` も同様に `_load_wav_24k_bytes(data)` へ分離。
  両 builder の prepass で選択 wav のバイト列を `wav_bytes_by_file` へ保持
  し、メイン loop はそのバイト列を再利用する（新規 read なし）。
- `donor_bank_lab.py`: `parse_lab(path)` を `parse_lab_text(text)` へ分離、
  `_load_wav_24k` も同様に分離。選択された .lab/_song.wav のバイト列を
  prepass で保持し、メイン loop で再利用する。
- `donor_bank.py`（vocadito）: `load_donor_24k(path)` を
  `load_donor_24k_bytes(data)` へ、`load_notes_csv(path)` を
  `load_notes_csv_text(text)` へそれぞれ分離。`build_donor_bank` は wav/
  notes CSV とも 1 回だけ `read_bytes()` し、そのバイト列から hash と
  decode の両方を導出する。

**[実装決定・record 記録]** UTAU builder の prepass は「選択された（wav
部分集合選択で採用された）全 wav ファイル」のバイト列を、実際に WORLD 分析
するかどうか（＝キャッシュ hit/miss）に関わらず保持する。既存の計算資源
境界方針（`_select_wav_subset`/`_select_wav_subset_for_contexts` による
被覆選択）でファイル数が既に抑えられているため、追加メモリはその範囲内
（実測 sakura/umi は各 20–28 wav ファイル）。`_select_lab_files`
自体が候補 .lab を走査する際に行う `parse_lab` 呼び出しは選択ロジック自体に
内在する別読み（同一ファイルの hash→decode 分割ではない）のため対象外とした。

**テスト**: 4 builder それぞれに `monkeypatch` で `Path.read_bytes` の
呼び出し回数を解決パスごとに数える single-read enforcement テストを追加
した（measure_bands.py の
`test_analyze_wav_reads_file_bytes_exactly_once` と同じ流儀）:
`test_build_donor_bank_utau_vcv_reads_each_file_exactly_once`、
`test_build_donor_bank_utau_reads_each_file_exactly_once`、
`test_build_donor_bank_lab_reads_each_file_exactly_once`、
`test_build_donor_bank_reads_wav_and_notes_csv_exactly_once`。

**影響**: bit には影響しない（read 経路のみの変更。TOCTOU 窓の排除）。

### テスト・lint・回帰確認（R3）

- `ruff check .`（リポジトリ全体）: clean
- `python -m pytest voice_genesis/foundry/tests -q`: **241 件、全 green**
  （R2 時点の 229 本 + 新規 12 本。内訳: joins +8 本 / donor_bank +1 本 /
  donor_bank_utau +2 本 / donor_bank_lab +1 本。
  `test_adapter_render_donor_wiring.py` の既存 2 本は改名・アサーション
  更新のみで本数不変）
- `python -m pytest voice_genesis/singer/tests -q`: 38 本、非破壊確認済み
- `src/svp_rpe/**`・`pyproject.toml` は無変更（本体スイートへの影響なし）

### F1.4 fixed3 WAV（#1 絶対グリッド化を反映・総尺が score 準拠へ回帰）

`presets/ritsu_neutral.json` を使い `--donor ritsu --voicebank-root
<波音リツ強連続音Ver1.5.1>` で再生成した（生成スクリプト: scratchpad
`foundry_f1_4/run_f1_4_fixed3_e2e.py`。出力先: scratchpad
`foundry_f1_4/wavs/*_fixed3.wav`・リポジトリへは非コミット・従来どおり）。

**総尺推移**（sakura / umi。score 総尺 = `perf.build_timeline` の
`total_samples` から直接算出した理論値）:

| 版 | sakura(s) | umi(s) | 備考 |
|---|---|---|---|
| score（理論値） | 24.583 | 11.409 | `perf.build_timeline` の `total_samples` |
| F1.4 fixed（R1・Codex #1,#2 対応後） | 22.785 | 10.425 | overlap のみ消費（当時は preutterance 未消費） |
| F1.4 fixed2（R2・Codex #2「preutterance 消費」対応後） | 19.985 | 8.625 | overlap+preutterance をハードトリム消費（trim 方式の副作用で累積縮小） |
| **F1.4 fixed3（R3・本対応後）** | **24.585** | **11.410** | 絶対グリッド配置（trim なし）。score 総尺と ±1 frame（±5ms）で一致 |

| ファイル | sha256(先頭12桁) | 長さ(s) |
|---|---|---|
| sakura_ritsu_f1_4_fixed3.wav | `198b81da0d88` | 24.585 |
| umi_ritsu_f1_4_fixed3.wav | `13e98ab3e18d` | 11.410 |

完全 sha256:
- sakura: `198b81da0d884d5bdac40beb555d2cf4cc7370196627ef2a1e008eb1c40d8a67`
- umi: `13e98ab3e18d8935ca3604e1799b6040ccbe40a5425ac3c2d7e9b3d7a8aa0623`

**決定論確認**: 両ファイルとも (a) 同一 cache_dir 再利用（cache hit）、
(b) 別 cache_dir でのフル再計算（no-cache）の 2 経路で再実行し、初回
（no-cache 相当の新規 cache_dir）と合わせて**3 回とも sha256 完全一致**を
確認済み。

**donor_provenance**: 両ファイルとも `declared_sha256 == actual_sha256`
（`presets/ritsu_neutral.json` の pin が実データと一致）。

**join_stats 実測（sakura）**: `n_joins_applied=6`（run 内の実際の重なり join
数。フレーズ先頭直後の最初の mid-phrase note が直前フレーズ先頭 unit の
preutterance 差分だけ実際に重なるケースに対応。残り 8 件の mid-phrase join
は一様 preutterance=60frame のため重なりゼロ=単純連結）、
`n_total_frames=4917`（= score_total_frames と厳密一致）。umi も同様に
`n_total_frames=2282` で一致。

**再耳判定はスコープ外**（User 判断待ち。F1.x 梯子凍結の裁定・S0–S4 実行順は
本対応で変更しない — #1 は「凍結後に発見された総尺実装バグの是正」であり、
凍結根拠（銀行情報密度の構造限界）そのものへの反証ではない）。

### 未対応の関連 Codex スレッド（本対応のスコープ外・record 記録のみ）

r2 以降のレビューで、本 R3 対応と隣接するが今回の指示スコープ（3 件）には
含まれない未対応スレッドが複数残っている（トリアージは次ラウンド）:

1. **`render.py`「Offset VCV control tracks by preutterance」**
   （`html_url` 末尾 `r3789341843`）: `_note_frame_track` が
   `seg.start_sample` を原点に f0/振幅を per-sample トラックから抽出する際、
   VCV unit の絶対配置シフト（本対応 #1 の lead）を考慮しないため、f0/振幅
   トラックの**内容**（sp/ap とのフレーム数整合は保たれるが、サンプルした
   時刻自体がズレる）が理論上不正確になりうる。本対応 #1 はこの経路を変更
   していない（`_note_frame_track` 無改修）ため、この指摘は R3 後も未解消
   のまま残っている。
2. **`render.py`「Include audio bytes in donor-spec identity」**
   （`r3789341845`）: `voicebank_identity_hash`/`corpus_identity_hash` は
   oto.ini/.lab のみをハッシュし、wav バイト自体を含まない。
3. **`measure_bands.py`「Reject measurement outputs that alias inputs」**
   （`r3789341847`）/ **「Publish measurement reports atomically」**
   （`r3789341850`）。

いずれも実害があり得る指摘だが、今回のタスク範囲（絶対グリッド化 + cache_hit
+ split-read）とは独立した別トピックのため着手していない。
