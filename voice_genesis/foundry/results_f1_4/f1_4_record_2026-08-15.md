# VG-F1.4 記録 — VCV 遷移内包 unit への転換

- 日付: 2026-08-15
- 記録者: Claude（Sonnet 実装・実行・実測、Claude 完結ルート）
- 設計書: `voice_genesis/foundry/DESIGN_F1_adapter_v0.md` 追補 F1.4
- 背景: `results_f1_3/f1_3_record_2026-08-15.md` 耳判定節（「滑らかさは改善されて
  いるが息の切れ目、繋ぎ目に改善が見られない」）。機序判読: 現行の「母音核 unit +
  子音クリップの挿入」は接合を音韻遷移点（V→C→V、知覚的最敏感部）で行っており、
  VCV 銀行の設計意図（遷移は録音済み・接合は母音定常部中央）と逆だった。
- 実装:
  `voice_genesis/foundry/adapter/donor_bank.py`（`DonorUnit.vowel_core_start_frame`
  新設 + `_unit_vowel_core_range`/`_unit_mean_power` の母音核限定化・F1.4-A/F1.3-B
  整合）、
  `voice_genesis/foundry/adapter/donor_bank_utau.py`（`VCVContext` +
  `_select_wav_subset_for_contexts` + `build_donor_bank_utau_vcv` 新設・v1
  `build_donor_bank_utau` は関数として残置・F1.4-A）、
  `voice_genesis/foundry/adapter/units.py`（`vcv_context_sequence` /
  `VCVTargetNote` / `VCVUnitSelection` / `select_vcv_units` / `resolve_vcv_unit_to_note`
  新設・F1.4-B/C）、
  `voice_genesis/foundry/adapter/render.py`（`_vcv_required_contexts` /
  `_build_vcv_placements` 新設 + `donor="ritsu"` を VCV 経路へ全面差し替え・
  vocadito/pjs 経路は無改変・F1.4-B/D）。
- テスト: `test_adapter_donor_bank.py`（+3）、`test_adapter_donor_bank_utau.py`
  （+7）、`test_adapter_units.py`（+16）、`test_adapter_render_donor_wiring.py`
  （+4）で **既存 150 本 + 新規 30 本 = 180 テスト、全 green**
  （`python -m pytest voice_genesis/foundry/tests -q`）。`voice_genesis/singer/tests`
  38 本、非破壊確認済み。`ruff check voice_genesis/foundry/` clean。

## 1. 実装決定

### 1.1 VCV alias 実在調査（F1.4-A item1/3）

波音リツ強連続音 Ver1.5.1 の oto.ini（A3/F4 各 1237 行）を実測した。

- **「a か」形式（VCV・prev_vowel 付き）**: 各 pitch dir で 1059 行（非フレーズ
  先頭）。sakura/umi が実際に必要とする 27 個の文脈キー（後述 §2）は **A3/F4 の
  両方で 100%（27/27）過不足なく存在**し、重複エイリアスも無い（1 文脈=1 unit）。
- **「- か」形式（フレーズ先頭形）**: 各 pitch dir で 178 行存在。sakura/umi が
  必要とするフレーズ先頭 8 種（さ/や/そ/み/か/う/ひ/お）は全て存在。
- **「a R」等の休止形（フレーズ末用エイリアス）**: **実在しない**（A3/F4 とも
  1237 行中 0 件。alias 末尾トークンが `R`/`r`/`-`/`ー`/`息` になる行を全走査して
  確認）。波音リツ強連続音は「強連続音」（VCV 中心の連続音）バンクであり、
  CVVC 系や一部の単独音バンクに見られる明示的な語尾（ending）録音を持たない
  構成と判断する。
  **[実装決定・record 記録]** 設計書 F1.4-A item3「無ければ減衰テーパで代替」の
  対応として、新規コードは追加しなかった。`singer/performance.py`
  `build_amplitude_envelope` が既に「フレーズ末ノートの減衰（末尾 30% を
  1.0→0.55 へ線形フェード）」を実装しており（F1.3 で「振幅の唯一の権威」に
  確定済み）、これがそのまま代替の減衰テーパとして機能すると解釈した
  （§5 Open Questions #1 参照）。

### 1.2 donor_bank.py: `vowel_core_start_frame`（F1.4-A item1）

`DonorUnit` に `vowel_core_start_frame: Optional[int] = None` を追加した
（unit 先頭からの相対フレームオフセット。`[0, vowel_core_start_frame)` =
録音済み調音遷移＝伸縮しない固定区間、`[vowel_core_start_frame, end)` =
母音定常部＝伸縮/ループ対象）。`None`（vocadito/pjs/旧 UTAU unit）は後方互換で
unit 全体を母音定常部として扱う。

F1.3-B item1「母音核区間の平均パワー」という正規化基準の記述に VCV 化後も
忠実であるため、`_unit_mean_power`（→ `normalize_unit_energy`）を
`vowel_core_start_frame` 以降のみを使うよう変更した（`_unit_vowel_core_range`
新設）。**[実装決定・record 記録]** これにより、unit 先頭の調音遷移（前母音尾+
子音、収録レベルが母音核と異なりうる）が正規化基準を歪めることを防ぐ
（`test_normalize_unit_energy_does_not_distort_by_transition_level` で検算済み）。
ゲイン自体は unit 全体（`start_frame:end_frame`）へ一様に適用する
（単一の連続録音のため、母音核基準のゲインを遷移部にもそのまま使うのが自然
という F1.3 からの方針を継続）。

### 1.3 donor_bank_utau.py v2: VCV unit 化（F1.4-A item1/2）

`build_donor_bank_utau_vcv` を新設した。v1 `build_donor_bank_utau`（母音核のみ
切り出し + 別保持の `ConsonantClip`）は F1.3 までの互換テストのため関数として
残置するが、**render.py の `donor="ritsu"` 経路からはもう呼ばない**。

- unit の frame 範囲を `[offset, cutoff)`（従来の母音核のみ `[consonant_end,
  cutoff)` から拡張）へ変更。これにより録音済みの「前母音尾 + 子音 + 母音
  アタック」の遷移全体が unit 内に保持される（設計書「録音済み調音遷移を切らずに
  保持する」）。
- `vowel_core_start_frame = consonant_end_frame - offset_frame`（unit 内相対値）
  として遷移/母音核境界を記録する。
- `overlap_frames` / `preutterance_frames` は F1.3 と同じ計算式（oto の
  overlap_ms / preutterance_ms をフレーム化）だが、**基準点が unit 先頭
  （= offset）のまま**なので意味が変わらず、そのまま「unit 内オフセット」として
  再利用できる（実装変更不要だった点を記録）。
- 子音は unit に録り込まれるため `ConsonantClip` 別保持は行わない
  （F1.4-B item3「録音子音クリップ挿入経路も VCV 経路では廃止」）。戻り値は
  `(DonorBank, unit_contexts, stats)` の 3 要素（v1 の 4 要素 `consonant_clips`
  を廃止）。

### 1.4 wav 部分集合選択の文脈駆動化（F1.4-A、[実装決定・record 記録]）

v1 の `_select_wav_subset`（オンセット被覆 + 母音あたり最小件数のヒューリス
ティック）は、VCV 経路では「sakura/umi が実際に必要とする文脈キーを過不足なく
含む」ことを保証しない（計算資源境界の設計意図とは無関係な理由で mora が
解決不能になりうる）。そのため `_select_wav_subset_for_contexts` を新設し、
render.py から渡される `required_contexts`（レンダリング対象スコアが必要とする
`(prev_vowel, mora)` の重複無しリスト）を**過不足なく含む最小 wav ファイル集合**
をファイル名昇順の決定論走査で選ぶよう変更した。`required_contexts=None` の
場合は文脈非依存の汎用フォールバック（ファイル名昇順で `max_wav_files` 件）を
維持し、単体テスト・将来スコア向けの汎用性を保った。

実測（sakura×ritsu）: A3/F4 各 21 wav ファイル（sakura 単独）で 17 文脈が過不足
なく解決（`n_wav_files_analyzed=28`、後述 §2）。umi 単独では A3/F4 各 10 wav
ファイルで 11 文脈。WORLD 分析コストは「wav ファイル数」に比例するため
（unit 切り出し方式に依存しない）、F1.3 までの計算資源境界方針は維持される。

### 1.5 units.py: VCV 選択（F1.4-C）

`select_vcv_units` を新設した。候補フィルタは 3 段階:

1. **exact**: `unit_contexts[idx] == (target.prev_vowel, target.mora)` の完全一致
2. **near**（フォールバック）: mora のみ一致（prev_vowel を無視 = 「x 扱い」）
3. **global**（想定外の最終フォールバック）: mora すら一致する unit が皆無の場合、
   全 unit を候補にする

候補内のコストは `w_p*|Δsemitone| + w_d*|log(dur比)|` のみ（**[実装決定・record
記録]** 設計書 F1.4-C は「従来コストの音高項・長さ項は継続、母音ペナルティは
文脈キー一致で自動充足」と述べるのみで concat cost (`w_c`) の扱いには触れて
いない。文脈キー完全一致フィルタにより候補数が通常 1〜2（A3/F4 の 2 音階分）に
絞られるため、concat cost の実質的な弁別力がほぼ失われることと、設計書が
明示的に継続を指定したのが音高・長さ項のみであることから、VCV 選択では
concat cost を計算しない判断とした。§5 Open Questions #2 参照）。tie は
`unit.index` 昇順（決定論）。

### 1.6 units.py: VCV 尺合わせ（F1.4-A item1 / F1.4 §5）

`resolve_vcv_unit_to_note` を新設した。unit を「録音済み調音遷移（先頭
`vowel_core_start_frame` フレーム・**伸縮せずそのまま保持**）」+「母音定常部
（残り・F1.3 と同じ [0.5,2.0] キャップ + 往復ループの overlap-add 機構で目標残長
へ伸縮）」の 2 区間に分けて処理し、単純連結する（同一連続録音由来のため境界に
不連続は生じない）。目標長が調音遷移長を下回る縮退ケース（実コーパスでは
最短ノートでも遷移長よりずっと長いため未発動・後述 §4）は遷移を切り詰め
`head_clipped=True` で記録する設計とした。

### 1.7 render.py: preutterance 消費（F1.4-B item2/3）

`_build_vcv_placements` を新設した。**フレーズ先頭ノート**（`is_phrase_first`）
のみ、配置開始位置を `min(preutterance_frames, 直前のブレスギャップ frames)`
だけ cursor より前へずらす（ブレス 0.25s=50frame の範囲内で自動的にクリップ
される。§2 で実測: sakura 5/6 フレーズ頭で発動・全件無クリップ、umi 1/3
フレーズ頭で発動・無クリップ。umi の残り 2 件はブレスギャップが無い/使い切れ
ないケースではなく、全曲最初のフレーズ（ギャップ 0 のため shift=0 で自然に
不発動）と、必要な shift が実際に 0 未満にならないケースの記録漏れがない
ことを個別確認済み）。`end_frame` は shift の影響を受けない（次フレーズの
ギャップ計算を汚染しないため）。

**[実装決定・record 記録]** フレーズ内部（`has_join_to_prev=True`）のノートには
明示的な cursor シフトを加えていない。理由: `joins.assemble_v2` は run 内部を
`overlap_frames` のみで overlap-add 連結し、`NotePlacement.start_frame/end_frame`
は run 境界（run[0]/run[-1]）以外では未使用（コードパスを検証済み）。実データで
overlap < preutterance < vowel_core_start_frame が常に成立する（§2.1）ため、
overlap-add 接合そのものが「preutterance 相当の lead-in を暗黙に運ぶ」構造に
なっており、追加のシフトは不要と判断した。§5 Open Questions #3 に設計意図との
齟齬可能性を記録する。

### 1.8 consonants.py / 録音子音クリップの不使用（F1.4-B item3）

`donor="ritsu"` の VCV 経路では `consonants.apply_consonant_onset` も
`prepend_recorded_consonant` も呼ばない（子音は unit に録り込み済み）。
`render()` の結果辞書では `consonant_source="vcv"` を報告する（`vocadito`/`pjs`
は従来どおり synthetic/recorded/none を使用・無改変）。

## 2. VCV 被覆統計・文脈解決表（実測）

### 2.1 接合位置の構造不変条件（実測）

波音リツ強連続音 Ver1.5.1 の oto.ini 全 1059 個の非フレーズ先頭 VCV エイリアス
（A3/F4 各 1059）で **`overlap_ms < consonant_ms`（= overlap_frame <
vowel_core_start_frame）が 100% 成立**（`preutterance_ms < consonant_ms` も
100%、`overlap_ms <= preutterance_ms` も 100%）。これは接合フレームが常に
録音済みの前母音尾側（=母音定常部由来）に落ち、子音/遷移領域には踏み込まない
ことを oto の権威あるセグメンテーション情報だけから保証する（帯域指標を使わない
構造検証・`test_vcv_unit_overlap_falls_before_vowel_core_boundary` で
単体テスト化）。sakura/umi の実選択結果（後述）でも個別に再検算し、全 32 件
（sakura 20 + umi 12）で成立を確認した。

### 2.2 文脈解決表

sakura（20 mora）: 全 20/20 が文脈完全一致（`match_kind=exact`）。フォール
バック 0 件。

| # | prev | mora | pitch選択 | overlap/vcore/preutt(frame) |
|---|---|---|---|---|
| 0 | - | さ | F4 | 6/43/26 |
| 1 | a | く | F4 | 20/72/60 |
| 2 | u | ら | F4 | 20/83/60 |
| 3 | - | さ | F4 | 6/43/26 |
| 4 | a | く | F4 | 20/72/60 |
| 5 | u | ら | F4 | 20/83/60 |
| 6 | - | や | F4 | 4/31/5 |
| 7 | a | よ | F4 | 20/89/60 |
| 8 | o | い | F4 | 20/68/60 |
| 9 | i | の | F4 | 20/76/60 |
| 10 | - | そ | F4 | 6/44/30 |
| 11 | o | ら | F4 | 20/78/60 |
| 12 | a | は | F4 | 20/74/60 |
| 13 | - | み | F4 | 2/31/9 |
| 14 | i | わ | F4 | 20/82/60 |
| 15 | a | た | F4 | 20/77/60 |
| 16 | a | す | F4 | 20/70/60 |
| 17 | - | か | F4 | 0/34/13 |
| 18 | a | ぎ | F4 | 20/76/60 |
| 19 | i | り | A3 | 20/72/60 |

umi（12 mora）: 全 12/12 が文脈完全一致。フォールバック 0 件。

| # | prev | mora | pitch選択 | overlap/vcore/preutt(frame) |
|---|---|---|---|---|
| 0 | - | う | F4 | 0/14/1 |
| 1 | u | み | F4 | 20/73/60 |
| 2 | i | は | F4 | 20/76/60 |
| 3 | - | ひ | F4 | 6/32/14 |
| 4 | i | ろ | F4 | 20/83/60 |
| 5 | o | い | F4 | 20/68/60 |
| 6 | i | な | F4 | 20/81/60 |
| 7 | - | お | F4 | 0/21/0 |
| 8 | o | お | F4 | 20/70/60 |
| 9 | o | き | F4 | 20/67/60 |
| 10 | i | い | F4 | 20/60/60 |
| 11 | i | な | F4 | 20/81/60 |

**フォールバック実測**: sakura/umi 合わせて 32 mora 全件が exact 一致（near/
global フォールバック 0 件）。sakura/umi 単独の音域が A3/F4 の 2 音階に近い
ため、ほぼ全ノートで F4（349Hz 近辺）が選ばれ、sakura の最終モーラ「り」のみ
A3 が選ばれた（score 上の主音回帰でピッチが下がるため）。

### 2.3 bank 統計（wav_sha256 は選択された wav 群の合成ハッシュ）

| レンダー | n_units | n_wav_analyzed | wav_sha256(先頭12桁) |
|---|---|---|---|
| sakura×ritsu | 180 | 28 | `99968fc3bf3a` |
| umi×ritsu | 143 | 20 | `bbf741afccf8` |

`n_unmapped_kana`: sakura=7（今回の必須文脈選択では使わない周辺 mora の
かな正規化漏れ・選択結果には無関係）、umi=0。`n_dropped_short`=0（両曲とも）。
`n_negative_overlap_clamped`=0（両曲とも）。

## 3. preutterance 統計（実測）

| レンダー | n_preutterance_applied | n_clipped | shift値(frame) | mean(frame) |
|---|---|---|---|---|
| sakura×ritsu | 5 (/6 フレーズ頭) | 0 | [26, 5, 30, 9, 13] | 16.6 |
| umi×ritsu | 1 (/3 フレーズ頭) | 0 | [14] | 14.0 |

先頭フレーズ（sakura #0, umi #0）は直前ギャップ 0（曲頭のため）で shift=0 と
なり「適用」に数えていない（設計どおり: t=0 の前に食い込む無音は存在しない）。
クリップ発動は 0 件（今回の実コーパスでは preutterance 実値が常にブレス
0.25s=50frame の範囲内に収まった。最大値は sakura #1/#4 の 30frame=150ms）。

`n_stretch`（VCV 母音核の伸縮モード分布）: sakura = none 13 / extended_looped 6 /
compressed_truncated 1、umi = none 6 / extended_looped 3 / compressed_truncated 3。
`head_clipped` 発動: 0 件（両曲とも。最短ノートでも遷移長 90frame≈450ms を
大きく上回るため未発動）。

## 4. E2E（実データ・sakura/umi × ritsu の F1.4 版）

出力先（scratchpad、リポジトリへは非コミット）:
`foundry_f1_4/wavs/{sakura_ritsu_f1_4,umi_ritsu_f1_4,sakura_ritsu_f1_4_warped}.wav`

| ファイル | donor/voice | sha256（先頭12桁） | 長さ(s) | F1.3 版 sha256（先頭12桁） |
|---|---|---|---|---|
| sakura_ritsu_f1_4.wav | ritsu/neutral | `a1d82769d5c5` | 22.785 | `8f5d9a48627f`（F1.3） |
| umi_ritsu_f1_4.wav | ritsu/neutral | `619648245e08` | 10.425 | `49f8195d15ee`（F1.3） |
| sakura_ritsu_f1_4_warped.wav | ritsu/warped | `fa346bcc51f2` | 22.785 | （F1.3 warped 版は未生成・比較対象なし） |

- **決定論確認**: `sakura_ritsu_f1_4` をキャッシュ有効（同一 npz/pkl 再利用）・
  キャッシュ無効（別ディレクトリでフル再計算）の両経路で再実行し、3 回とも
  sha256 完全一致（`a1d82769d5c5...`）を確認済み。
- **F1.3 版との sha 差分**: 全ファイルで sha256 が変化している（unit 内容
  （VCV 全体 vs 母音核のみ）・接合位置・preutterance 消費のいずれもが波形へ
  影響する変更のため、一致しないことは想定どおり）。長さは F1.3 より短縮
  （sakura: 27.125s→22.785s、umi: 11.515s→10.425s）。**[実装決定・record
  記録]** `resolve_vcv_unit_to_note` は各ノートの出力フレーム数を常に
  `target_n_frames`（score 由来のノート長）ちょうどに保つ（head + core_target
  = target_n_frames）ため、note 単位の長さ自体は VCV 化による影響を受けない。
  総尺短縮の要因候補は (a) フレーズ頭の preutterance 消費（cursor を最大
  30frame=150ms 前倒しする、§3）、(b) run 内 overlap-add による毎 join 短縮
  （F1.4 実測: sakura 14 joins 全件 20frame=100ms 均一・§2.2）の 2 つだが、
  選択アルゴリズム自体が w_v ペナルティ方式（F1.3）から文脈完全一致方式
  （F1.4）へ全面変更されているため、同一ノートで選ばれる donor unit が
  F1.3/F1.4 間で一致する保証は無く（F1.3 の overlap 分布は 20×11,10×2,20混在
  ・平均18.57frame で F1.4 の全件20frameとは異なる）、F1.3 との単純な
  overlap 値比較で寄与を分離することはできない。定量分析は未実施
  （§5 Open Questions #4）。
- `measure_bands.py` 計測は記録のみ（§0 凍結どおり判定に用いない）: 生データ =
  scratchpad `foundry_f1_4/measure_bands_f1_4.json`。
  - sakura×ritsu(neutral): RMS -22.71dBFS / crest 18.27dB / HNR中央値 8.88dB
  - umi×ritsu(neutral): RMS -19.43dBFS / crest 14.99dB / HNR中央値 9.75dB
  - sakura×ritsu(warped): RMS -22.61dBFS / crest 18.17dB / HNR中央値 9.03dB

## 5. Open Questions（設計書に無い判断・要 User/Fable 確認）

1. **休止形エイリアス不在時の代替**: §1.1 のとおり、波音リツ強連続音
   Ver1.5.1 には休止形（rest-form）エイリアスが 1 件も存在しない。設計書の
   「無ければ減衰テーパで代替」を、F1.3 で確定済みの
   `performance.build_amplitude_envelope` フレーズ末減衰（既存コード・新規
   追加なし）で充足したと解釈した。専用の VCV 固有テーパ（例えば unit 自体の
   末尾をさらに減衰させる等）を意図していた場合は齟齬がある。
2. **VCV 選択コストから concat cost (w_c) を除外した判断**: §1.5 のとおり、
   設計書が「音高項・長さ項は継続」とのみ書いており concat cost の扱いが
   明示されていない。文脈完全一致フィルタで候補数がほぼ 1〜2 に絞られる
   ため実務上の影響は小さいと考えるが、明示的な設計意図の確認を要する。
3. **フレーズ内部（mid-phrase）ノートに明示的な preutterance シフトを
   加えていない判断**: §1.7 のとおり、`joins.assemble_v2` の run 内部処理が
   `start_frame`/`end_frame` を使わず `overlap_frames` のみで overlap-add
   する構造上、明示シフトは効果を持たない（コード上確認済み）ため未実装
   とした。設計書 F1.4-B item2「各 unit をノート開始より preutterance 分
   だけ前に配置」を全ノートに文字通り適用する別実装（例えば
   `joins.assemble_v2` 自体を preutterance 対応に拡張し、overlap 長を
   `preutterance - overlap` 相当の別概念で再定義する等）を意図していた
   場合は、本実装は設計意図と異なる可能性がある。
4. **F1.3→F1.4 の総尺短縮の要因分解が未実施**: §4 のとおり、総尺短縮
   （sakura -4.34s、umi -1.09s）の要因候補（preutterance 消費 / run 内
   overlap-add の join 長）を列挙したが、選択アルゴリズム自体が F1.3/F1.4 間で
   全面的に異なるため donor unit 選択結果を直接比較できず、寄与を分離した
   定量分析はしていない（設計書に要求が無いため）。
5. **F1.3 warped 版との比較 WAV が存在しない**: §4 の比較表で
   `sakura_ritsu_f1_4_warped.wav` に対応する F1.3 warped 版が無いことを
   記録した（F1.3 では neutral 版のみが `results_f1_3` の比較対象として
   生成されていたため）。耳判定では F1.3 neutral 版・vocadito 基準線との
   比較を優先する想定。

## 6. 耳判定

（**耳判定素材の提出待ち。空欄で用意**）

- 提出予定素材: `sakura_ritsu_f1_4.wav` / `umi_ritsu_f1_4.wav` /
  `sakura_ritsu_f1_4_warped.wav` + 比較用に F1.3 版
  （`sakura_ritsu_f1_3.wav` / `umi_ritsu_f1_3.wav`）と vocadito 基準線
  （`regress_vocadito_sakura_neutral.wav`）を再掲
- 質問（設計書 F1.4 Acceptance より）: 「F1.4 vs F1.3 vs vocadito 基準線」で
  接合点（息の切れ方・つなぎ方）が改善したか
- 判定: _(未実施)_
- コメント: _(未実施)_

## 耳判定（2026-08-15・判定者 = User・非盲検）

判定者所見（原文）: 「改善はあるがまだ未達だな」

- F1.2→F1.3→F1.4 で 3 連続「改善あり・未達」= unit-selection 磨き込みの限界逓減を確認
- 構造的背景: 銀行の情報密度（2 音階・単一音源・数百 unit）が上限を規定しており、
  接合工学では超えられない帯に入ったと判断
- 同時に User から工程再編提案（リファレンスデータセット先行・Phase 0-4 構想）が
  提示された → 設計検討へ移行（本 record はここで close、F1.x 梯子は凍結）

## Codex R1 対応（4 件全採用）

PR #262 の `chatgpt-codex-connector[bot]` レビュー r1（inline comment 4 件、全採用
判定済み・`pr262_codex_review_r1.md`）への対応。ブランチ
`claude/voicegenesis-dev-continue-rfwf3p`。

1. **render.py（P1・F0/振幅の圧縮後タイムライン整合）**: `assemble_v2` が
   フレーズ内 run を true overlap-add で圧縮する一方、`base_f0_persample`/
   `amp_env` は圧縮前の生スコアサンプル時間軸で構築したまま、圧縮後フレーム数
   から作った素朴な等間隔インデックスで読み出していた（join を経るたびに
   ズレが累積するバグ）。修正: `render._note_frame_track` で各ノートの
   perf_genes トラックを圧縮前・実スパン内で抽出し、
   `joins.assemble_control_tracks_v2`（新設、`compute_runs` で run 分割ロジックを
   `assemble_v2` と共有）で sp/ap と**同一の** run/overlap 構造へ通す。
   テスト `test_assemble_control_tracks_v2_matches_sp_ap_frame_for_frame_with_multiple_joins`
   （4 note・3 join）で厳密一致を確認。
2. **units.py（P2・ループ素材の overlap 損失補償）**: `_ping_pong_pad_v2` の
   内部 join 損失 + base↔pad 接合損失を見込まず deficit ちょうどの生ループ材を
   作っていたため、圧縮後は不足し `_force_length` の末尾フレーム複製（凍結
   プラトー）で埋めていた（実測: 10 倍伸長で末尾 325ms=65 frame が完全な静止
   フレーム）。修正: `_loop_pad_raw_target` で内部 join 損失+base 接合損失を
   見込んだ過剰生成を行い、`_force_length` は切り詰め分岐（末尾を捨てるだけ）
   で target へトリムする。10 倍伸長の再検証で凍結補填 1 フレーム以下を確認。
3. **donor_bank 系（P1・キャッシュキーの内容ハッシュ）**: vocadito/UTAU v1/
   UTAU v2(VCV)/PJS の全 builder のキャッシュキーが root path + options のみで、
   `--cache-dir` 再利用時に音源（WAV/oto.ini/.lab/notes CSV）を in-place 編集
   しても古いキャッシュを返すサイレントな provenance 破損があった。修正:
   `donor_bank.aggregate_content_hash` を新設し、各 builder で（oto.ini/.lab/
   notes CSV 解析 + wav 部分集合選択という軽量処理をキャッシュ判定より前倒し
   した上で）選択された WAV 群 + oto.ini/.lab/notes CSV のバイト sha256 を
   sorted 連結して再ハッシュしたものをキー材料へ含めた。fixture の oto.ini/
   notes CSV/.lab を 1 バイト編集してキャッシュキーが変わることをテストで
   確認（4 builder 全件）。
4. **measure_bands.py（P2・単一 read で parse+hash）**: `sf.read` での decode と
   `Path.read_bytes()` での hash が別々の read で、両者の間にファイルが
   書き換えられると記録される sha256 が実際に解析したサンプル列と食い違う
   TOCTOU 窓があった（AGENTS.md Persistent Artifact Safety Gate 項目1 違反）。
   修正: `read_bytes()` を 1 回だけ呼び、同一バイト列から `io.BytesIO` 経由の
   decode と sha256 の両方を導出する。`monkeypatch` で `Path.read_bytes` の
   呼び出し回数が厳密に 1 であることを直接 enforce するテストを追加。

### F1.4 耳判定（未達）の判定条件の訂正記録

**上記「耳判定（2026-08-15）」節の判定は、#1（F0/振幅タイムラインずれバグ）を
含んだ出力に対するものだったことをここに記録する。** バグは「フレーズ内で
join を経るたびに F0/振幅の読み出し位置がスコア時間軸上でズレる」性質のため、
判定者コメント「改善はあるがまだ未達」で観測された接合部の質感の一部（特に
join 2 回以上を含む長いフレーズの後半ノート）は、unit-selection/VCV 化それ自体
の限界だけでなく、このタイムラインずれの影響を受けていた可能性がある。
`sakura_ritsu_f1_4.wav`/`umi_ritsu_f1_4.wav`（耳判定に使われた版）と
`sakura_ritsu_f1_4_fixed.wav`/`umi_ritsu_f1_4_fixed.wav`（本対応後の再生成版）は
別音源であり、上記の「限界逓減」判断（F1.x 梯子凍結の根拠）を無効化するもの
ではないが、耳判定の前提条件として本記録に明記する。再耳判定は本 record の
スコープ外（User 判断待ち）。

### 新旧 WAV sha256（先頭 12 桁）

| ファイル | 版 | sha256(先頭12桁) | 長さ(s) |
|---|---|---|---|
| sakura_ritsu_f1_4.wav | 旧（F0/振幅ずれバグ込み・耳判定対象） | `a1d82769d5c5` | 22.785 |
| sakura_ritsu_f1_4_fixed.wav | 新（Codex R1 対応後・本節） | `e27e80506ead` | 22.785 |
| umi_ritsu_f1_4.wav | 旧（F0/振幅ずれバグ込み・耳判定対象） | `619648245e08` | 10.425 |
| umi_ritsu_f1_4_fixed.wav | 新（Codex R1 対応後・本節） | `ffd1b8ba8510` | 10.425 |

新版は決定論確認済み（同一入力を no-cache 再計算・cache 再利用の 2 経路で
再実行し、旧版生成時と同じ手順で sha256 完全一致を確認: sakura/umi とも
2 回とも同一 sha256）。長さ（22.785s/10.425s）は旧版と一致（#1 の修正は
配置・尺そのものではなく F0/振幅トラックの読み出し位置の補正のため、
n_total_frames は不変）。生成スクリプト・出力先: scratchpad
`foundry_f1_4/run_f1_4_fixed_e2e.py` / `foundry_f1_4/wavs/*_fixed.wav`
（リポジトリへは非コミット・従来どおり）。

### Codex R1 修正版の耳判定（2026-08-15・判定者 = User・非盲検）

判定者所見（原文）: 「fixed は他よりよく聞こえる。まだ改善が必須だが良くなってる」

- F0 タイムライン整合 + ループ凍結修正は**耳で確認できる改善**だった（自動レビュー由来の
  修正が耳判定で裏取りされた初のケース）
- F1.4 の総合判定は「未達」のまま維持 — ただし未達幅は当初判定より小さい
  （当初判定はバグ込み出力への判定だったため）
- F1.x 凍結の裁定は不変（凍結根拠 = 銀行情報密度の構造限界であり本バグではない）。
  S0–S4 実行順も不変

## Codex R2 対応（6 件・全採用）

PR #262 の `chatgpt-codex-connector[bot]` レビュー r2（コミット 9654c3b 以降、inline
comment 6 件）への検証 + 対応。ブランチ `claude/voicegenesis-dev-continue-rfwf3p`
（対応 HEAD: 本節を追記したコミット）。実害基準（AGENTS.md §3）で検証し、6 件とも
実害を確認したため全採用。反証には至らなかった。

### 1. render.py:256 spec.donor 未検証（P1・採用）

**検証**: `render()` は `spec = vs.load_voice_spec(...)` で `spec.donor` を読むだけで
一度も検証していなかった（該当コード確認済み）。実際のドナーは `--donor`/`--wav`/
`--voicebank-root` から独立に決まるため、コミット済み `presets/neutral.json`
（`donor.dataset="vocadito"`、vocadito_2.wav の実 sha256 と一致 = pin 自体は正しい）
を `--donor ritsu --voicebank-root <波音リツ>` と組み合わせて render しても
vocadito の provenance を騙れることを確認した（実際、F1.4 の全 ritsu render は
`run_f1_4_fixed_e2e.py` で `presets/neutral.json` を流用しており、この意味で
**既存の全 F1.4 ritsu 出力の provenance 宣言は F1.4 実施時点から嘘だった**）。

**修正**: `render.py` に `_validate_spec_donor`（新設）を追加し、重い WORLD 分析より
前に fail-closed で照合する。`donor` dict スキーマを dataset 別に拡張（破壊的変更
なし・vocadito 形は不変）:
- `dataset="vocadito"`: 既存どおり。`sha256` が `--wav` の実体 sha256 と不一致なら
  `DonorProvenanceError`
- `dataset="ritsu"`: 新設 `voicebank_sha256`。`donor_bank_utau.voicebank_identity_hash`
  （`--voicebank-root` 配下の全 pitch dir oto.ini を `(相対パス,sha256)` で集約。
  oto.ini は unit セグメンテーションの権威情報 = 意味のある identity pin。wav 全数
  ハッシュより軽量で WORLD 分析より前に検証できる）と不一致なら `DonorProvenanceError`
- `dataset="pjs"`: 新設 `corpus_sha256`。`donor_bank_lab.corpus_identity_hash`
  （全 `pjsNNN/pjsNNN.lab` を集約）と不一致なら `DonorProvenanceError`
- `dataset` が `--donor` と不一致でも即 `DonorProvenanceError`

新設 preset `presets/ritsu_neutral.json`（`voicebank_sha256` = 波音リツ強連続音
Ver1.5.1 実データの実測値 `e722b36c...d00206`）・`presets/pjs_neutral.json`
（`corpus_sha256` = PJS_corpus_ver1.1 実データの実測値 `ed7b6e56...b1547f9`）を追加。
`render()` の戻り値へ `donor_provenance`（照合結果）を追加し CLI 出力にも表示する。
単体テスト 10 本（vocadito/ritsu/pjs の一致・不一致・dataset 不一致・hash キー欠落・
presets 4 件のスキーマ形状）を `test_adapter_render_donor_wiring.py` に追加。

**影響**: 音に影響する修正（bit は不変。fail-closed 検証を通すために
`--voice presets/ritsu_neutral.json` へ切替が必要になった = §「fixed2」参照）。

### 2. render.py:137 preutterance が phrase 頭のみ（P1・採用・実測で確認）

**検証**: 波音リツ強連続音 Ver1.5.1 の oto.ini 全 1059 個の非フレーズ先頭 VCV
エイリアスで `overlap_ms < preutterance_ms`（overlap_frame < preutterance_frame）が
100% 成立することを実測済み（F1.4 §2.1）。典型値は overlap=20frame(100ms)・
preutt=60frame(300ms)。旧 `_build_vcv_placements`/`joins.assemble_v2` は
mid-phrase note の接合を `overlap_frames` のみで overlap-add しており、
`preutterance_frames` を全く消費していなかった（該当コード確認済み）。

数式で検証: run 内 overlap-add で unit i の frame `k` が着地する絶対位置は
`prev_len - overlap_i + k`（`prev_len` = 直前までの累積長）。真のアタック点
= frame `preutterance_i`（overlap ではなく preutterance が「アタック=拍位置」を
表す UTAU の定義）が着地する位置は `prev_len + (preutterance_i - overlap_i)` と
なり、`preutterance_i > overlap_i` のため **`preutterance_i - overlap_i` フレーム分
だけ名目ビートより遅延する**（典型値で 40frame=200ms）。F1.4 Acceptance
「各 unit をノート開始より preutterance 分だけ前に配置」に反する実装だったことを
確認した（実装者自身も §5 Open Questions #3 で齟齬可能性を記録済み）。

**修正**: `joins.py` に `NotePlacement.preutterance_frames`（新フィールド・
`None`=vocadito/pjs で後方互換）+ `overlap_add_concat` の `extra_trim_frames`
引数（新設・省略時 0 で後方互換）+ `_resolve_extra_trim` ヘルパーを追加。
`assemble_v2`/`assemble_control_tracks_v2` の両方（f0/振幅を sp/ap と同一
フレーム数に保つため両方に同じ trim を適用）が、mid-phrase join ごとに
`max(0, preutterance_frames - 実効overlap_frames)` フレームだけ直前
アキュムレータ末尾を追加でハードトリムしてから、従来どおり `overlap_frames`
幅でブレンドする（ブレンド幅=overlap のsonic特性は変えず、ブレンド開始点だけ
前へ動かす）。`render.py` `_build_vcv_placements` は `NotePlacement` へ
`preutterance_frames=sel.unit.preutterance_frames` を渡すだけ（1 行）。

**タイミングテスト**（`test_assemble_v2_preutterance_attack_frame_lands_at_nominal_beat`）:
unit_b の frame[preutterance] が直前 note の累積長（名目ビート位置）へ厳密一致
することを検算済み。`preutterance_frames=None` は trim=0 のまま不変
（`test_assemble_v2_preutterance_none_matches_overlap_only_behavior`）。

**影響**: **音に影響する修正**。F1.4 fixed2 WAV を再生成（下記）。

### 3. render.py:475 出力の atomic 公開（P2・採用）

**検証**: `sf.write(str(out_path), y, SR, subtype="PCM_16")` が最終 out_path へ
直接書き込んでいた（該当コード確認済み。AGENTS.md Persistent Artifact Safety
Gate 項目6「全構築後公開」違反）。プロセス kill・ディスク満杯時に truncated WAV
を残す/既存の有効な出力を破壊しうる。

**修正**: `render.py` に `_atomic_write_wav`（新設。`src/svp_rpe/utils/atomic_io`
と同じ流儀 —— staging tempfile + `os.replace`、`except BaseException` で
staging を best-effort 削除してから re-raise —— だが `src/` は変更禁止のため
adapter 内に独立実装）を追加し、`render()` の書き込み箇所を置き換えた。
単体テスト 3 本: 正常書き込み+tempfile 残留なし、`sf.write` 注入失敗時に
out_path が作られない（AGENTS.md 項目7「公開途中失敗の注入テスト」）、
失敗時に既存の有効な出力を破壊しない。

**影響**: 音・bit には影響しない（書き込み経路のみの変更）。

### 4. donor_bank.py:57 aggregate hash がパス非結合（P2・採用）

**検証**: `aggregate_content_hash` は `hashes: Sequence[str]` を受け取り
`"|".join(sorted(hashes))` するだけで、どのパスがどの digest かを捨てていた
（該当コード確認済み）。2 個の選択済みファイル（同じ hash 集合になるよう中身を
入れ替えても）ファイル名が変わらなければ集約ダイジェストが不変になる
——provenance 破損（Codex 指摘は正しい）。

**修正**: シグネチャを `aggregate_content_hash(paths_and_hashes: Sequence[Tuple[str,
str]])` に変更し、`(相対パス, hash)` ペアを `path:hash` 形式で `path` 昇順ソート
してから連結する。呼び出し元 3 箇所（`donor_bank_utau.py` の v1/v2 builder、
`donor_bank_lab.py`）を全て `(相対パス, sha256)` ペアを渡すよう更新し、cache-key の
`schema=` マーカーを新版へ更新（v1 builder: v2→v3、VCV builder: v2→v3、lab
builder: v1→v2。旧キャッシュとの意味不一致な衝突を避ける）。中身入替検知テスト
2 本（`test_aggregate_content_hash_detects_content_swap_between_same_named_paths`
/ `..._changes_when_path_changes_but_hash_set_same`）を追加。

**影響**: bit には影響しない（cache key の内部計算方式のみの変更。既存 cache は
schema マーカー変更により無効化される=意図どおり）。

### 5. donor_bank_utau.py:193 負 cutoff の意味論（P2・採用・実測で確認）

**検証**: 一次資料 https://utaudb.sakura.ne.jp/knowledge.php?id=21 を確認:
「ブランクが正の数の場合、wavファイルの末尾からの時間[ms]で定義される」
「ブランクが負の数の場合、オフセットからの時間[ms]に-1を乗じたもので定義
される」= `blank_ms = -(cutoff_ms - offset_ms)` すなわち
`cutoff_ms = offset_ms - blank_ms`（Codex 指摘と一致）。旧実装は符号に関わらず
`wav_duration_ms - abs(blank_ms)` としており、負 blank では意味論を誤っていた。

**実 oto 実測**（波音リツ強連続音 Ver1.5.1）: 負 cutoff エントリは **A3: 2 件
（`n ぬA3` offset=2749,blank=-600・`- ぽA3` offset=79,blank=-600）、F4: 1 件
（`- ちょF4` offset=714,blank=-600）**、計 3 件（1237 行中）。VCV 強連続音では
稀（大多数は正号）だが Codex の指摘どおり CVVC/一部音源では多数になりうる構造。
`n ぬA3` の実測: 旧実装 cutoff=3996.24ms・正しくは cutoff=3349ms（647ms=129frame
過大に長い区間を切り出していた = Codex 指摘の実害を数値で確認）。

**sakura/umi への実害**: 3 件の負 cutoff エントリを含む wav ファイル
（`_のぬんなんぬ↓.wav`/`_ぽぷんぱんぷ↓.wav`/`_ちょちゅんちゃんちゅ.wav`）を
実際に `_select_wav_subset_for_contexts`（sakura/umi の必須文脈: 前掲 §2.2）で
再選択して検証したところ、**sakura/umi のどちらの必須文脈（32 mora 全件）にも
この 3 ファイルは選択されない**ことを確認した（sakura/umi 側は「- の」ではなく
「i の」等の別文脈を要求するため）。したがって本バグは実装として確実に存在するが、
**F1.4 の既存 sakura/umi ritsu 出力には影響していない**。

**修正**: `cutoff_position_ms` を `blank_ms>=0` と `<0` で分岐（`>=0`: 従来どおり
`wav_duration_ms - blank_ms`。`<0`: `offset_ms - blank_ms`）。offset/wav_duration
の範囲へのクランプは両分岐で維持。テスト 3 本（正しい値・wav_duration 非依存・
破損値クランプ）を更新/追加。

**影響**: F1.4 の既存 sakura/umi 出力への影響は実測で否定（上記）。ロジック自体は
実害を持つバグとして修正（他 pitch dir/他 UTAU 音源・将来のスコアで負 cutoff
エントリが選択されれば影響しうる）。**fixed2 WAV の再生成は不要**（sha256 不変を
実測確認—後述）。

### 6. render.py:475 --out が入力を上書き（P1・採用）

**検証**: `--out` の衝突検査が皆無で、`--out donor.wav --wav donor.wav` のような
呼び出しが分析後に無条件で `--wav` を上書きする（該当コード確認済み。
`--voicebank-root` 配下への書き込みも同様に無防備）。

**修正**: `render.py` に `_reject_output_collision`（新設）を追加し、`_atomic_write_wav`
呼び出し前に `--wav`/`--voice`（voice_spec_path）/`--notes-csv`/`--voicebank-root`
配下との resolved 衝突（symlink 解決後）を fail-closed で拒否する
（AGENTS.md 永続成果物「公開サイト」設計チェックリスト項目2「衝突ガードは
必須引数」準拠 —— `protected_files`/`protected_roots` はデフォルト無しの必須引数）。
単体テスト 7 本（wav/voice_spec との完全一致衝突・voicebank_root 配下衝突・
無関係パスは素通り・symlink 解決後判定・None 引数はスキップ）。

**影響**: bit には影響しない（書き込み前ガードの追加のみ）。

### テスト・lint・回帰確認

- `ruff check .`（リポジトリ全体）: clean
- `python -m pytest voice_genesis/foundry/tests -q`: **229 件、全 green**
  （既存 196 本 + 新規 33 本。内訳: joins +9 本 / donor_bank +4 本 /
  donor_bank_utau +2 本（既存 1 本の期待値訂正込みで正味 +2） /
  render_donor_wiring +18 本）
- `python -m pytest voice_genesis/singer/tests -q`: 38 本、非破壊確認済み
- `src/svp_rpe/**`・`pyproject.toml` は無変更（本体スイートへの影響なし）

### F1.4 fixed2 WAV（#2 preutterance 消費修正を反映・音に影響）

`presets/ritsu_neutral.json`（#1 の新スキーマ pin）を使い
`--donor ritsu --voicebank-root <波音リツ強連続音Ver1.5.1>` で再生成した
（生成スクリプト: scratchpad `foundry_f1_4/run_f1_4_fixed2_e2e.py`。出力先:
scratchpad `foundry_f1_4/wavs/*_fixed2.wav`・リポジトリへは非コミット・従来どおり）。

| ファイル | 版 | sha256(先頭12桁) | 長さ(s) | fixed(R1)版 長さ(s) |
|---|---|---|---|---|
| sakura_ritsu_f1_4_fixed2.wav | 新（Codex R2 #2 対応後） | `7f4393f209da` | 19.985 | 22.785 |
| umi_ritsu_f1_4_fixed2.wav | 新（Codex R2 #2 対応後） | `a487ccfa0c7f` | 8.625 | 10.425 |

完全 sha256:
- sakura: `7f4393f209da6d8b21d3647e98c33cfe510f5b4d19c78da4d256d7e8a5dcbf48`
- umi: `a487ccfa0c7ffb8aa6a85cd8bdc4dab0d3ef2f713b152897e11e0d94a20ad77c`

**決定論確認**: 両ファイルとも (a) 同一 cache_dir 再利用（cache hit）、
(b) 別 cache_dir でのフル再計算（no-cache）の 2 経路で再実行し、初回と合わせて
**3 回とも sha256 完全一致**を確認済み。

**長さ短縮の内訳**（実測 `join_stats`）: sakura は 14 join × extra_trim 40frame
（=preutterance 60frame − overlap 20frame、oto 実測値どおり一様）= 560frame
（2.8s）分、fixed(R1) 比で追加短縮（19.985s = 22.785s − 2.8s と厳密一致）。
umi は 9 join × 40frame = 360frame（1.8s）分、同様に厳密一致（8.625s = 10.425s
− 1.8s）。donor_provenance は両ファイルとも `declared_sha256 == actual_sha256`
（`presets/ritsu_neutral.json` の pin が実データと一致することを確認済み）。

**#5（負 cutoff）による再生成は無し**: 上記「5.」の実測のとおり、負 cutoff の
3 wav ファイルは sakura/umi のどちらの必須文脈にも選択されないため、#5 単独では
出力 bit に影響しない（fixed2 は #2 の影響のみを含む）。

**再耳判定はスコープ外**（User 判断待ち。F1.x 梯子凍結の裁定・S0–S4 実行順は
本対応で変更しない —— #2 は「凍結後に発見された実装バグの是正」であり、
凍結根拠（銀行情報密度の構造限界）そのものへの反証ではない）。

## Codex R2 追加 2 件 + 総尺回帰の R3 対応

PR #262 の `chatgpt-codex-connector[bot]` レビュー r2 の残り 2 件（cache_hit /
split-read。inline comment、コミット 0b2e401 以降）への対応 + Claude 側の
設計判定による内部発見（総尺回帰）への対応。ブランチ
`claude/voicegenesis-dev-continue-rfwf3p`。

### 修正1（設計判定による内部発見・最優先）: 接合の絶対タイムライン化

**検証**: `joins.assemble_v2`/`assemble_control_tracks_v2`（Codex R2 #2 の
`extra_trim_frames` 機構）は mid-phrase join のたびに直前アキュムレータ末尾を
`max(0, preutterance_frames - overlap_frames)` フレームだけ実際にハードトリム
してから overlap 幅でブレンドする。この「トリムして詰める」方式は単一 run
内では正しく機能するが、`assemble_v2` が複数 run（フレーズ）を合成する際、
run 間ギャップは**常に naive（score 由来の圧縮前）タイムライン基準**で計算
される一方、各 run 自体の長さは trim 分だけ圧縮済みのため、各 run の圧縮が
後続の全 run へ伝播し、総尺が score 総尺から累積的に縮む。実測（fixed2）:
sakura 14 join × 40frame(=preutt60−overlap20、実データで一様) = 560frame
(2.8s) の縮み。score 準拠を要求する F1.4 Acceptance に反する（record §4 の
[実装決定]で「総尺短縮の要因分解は未実施」としていた箇所の実要因）。

**修正**: `joins.py` に絶対グリッド配置版の新関数 `assemble_absolute`/
`assemble_control_tracks_absolute` を新設した（既存 `assemble_v2`/
`assemble_control_tracks_v2` は無変更のまま関数として残置・vocadito/pjs 経路
はそちらを引き続き使う。回帰リスクを avoid するため VCV 専用の新経路とした）。

- 出力バッファを `score_total_frames`（`perf.build_timeline` が返す
  `total_samples` をフレーム化した固定値。trim しない）で先に確保する。
- `render.py` `_build_vcv_placements` を全面改訂: 全ノート（フレーズ先頭・
  mid-phrase 問わず）に対して統一的に「拍位置（score 由来の cursor 位置）−
  lead（選択 unit の `preutterance_frames`）」の絶対座標へ配置する
  （`start_frame`/`end_frame` の両方が同じだけシフトする。R2 までの
  「end_frame は shift 非依存」は撤廃）。フレーズ先頭ノートのみ、直前ブレス
  ギャップ長へ shift をクリップする（無音より前へは食い込ませない・既存
  挙動維持）。
- `assemble_absolute` は、`has_join_to_prev=True` な note が直前までに
  書き込み済みの区間へ実際に重なる場合（lead 差により生じる自然な重なり。
  一様な preutterance の note 同士は重なりゼロ = 単純連結。フレーズ先頭
  直後の最初の mid-phrase note のように preutterance が異なる場合は実際の
  重なりが生じる）のみ、その区間を log-sp/ap クロスフェードでブレンドする
  （トリムではなく同一時間軸上のブレンド）。未配置区間（フレーズ最終ノート
  の末尾に生じうる shift 分の空白等）は末尾 carry-forward（前方 hold）で
  埋める（v1 `assemble` と同じ既存の無声ギャップ充填規約を再利用・追加実装
  なし・[実装決定・record 記録]）。

**テスト**: `test_adapter_joins.py` に `assemble_absolute`/
`assemble_control_tracks_absolute` の単体テストを 8 本追加（(a) 総フレーム数
= 指定バッファ長を厳密 enforce、(b) unit のアタック点が絶対座標上の宣言
位置へ厳密着地することを frame ごとに一意な値で検算、(c) 重なり区間のブレンド
連続性（トリムではないことの確認）、(d) 決定論の repeat 一致）。
`test_adapter_render_donor_wiring.py` の 2 本
（`test_build_vcv_placements_mid_phrase_note_start_unshifted` /
`..._end_frame_unaffected_by_shift`）は R3 の新挙動（mid-phrase note も
start_frame/end_frame が shift される）に合わせて
`..._shifted_by_preutterance`/`..._shifted_with_start_frame` へ改名・
アサーションを更新した（変更理由をテスト docstring に明記）。

**影響**: 音・総尺に影響する修正（fixed3 WAV を再生成・後述）。

### 修正2（Codex R2 追加・P2・`donor_bank_utau.py:507`）: pickle キャッシュ復元時の cache_hit

**検証**: `build_donor_bank_utau_vcv`/`build_donor_bank_utau`（v1）/
`build_donor_bank_lab` の pickle キャッシュ復元パス（`cache_path.exists()`）は
`pickle.load(f)` の戻り値をそのまま return しており、保存時にダンプされた
`stats["cache_hit"]=False`（`bank.stats` 側も同様）がキャッシュ命中時にも
False のまま返る。record/CLI 出力の `cache_hit` を見て新規分析かキャッシュ
再利用かを判別する呼び出し側の前提が壊れる（実測: 既存の
`test_build_donor_bank_utau_vcv_cache_roundtrip` 等は 2 回目呼び出しの
`cache_hit` を検証していなかったため見過ごされていた）。

**修正**: 3 builder すべての復元パスで、返す直前に `stats["cache_hit"]`
（返り値タプルの stats 要素・`bank.stats` の両方）を `True` へ上書きする。

**テスト**: 既存の roundtrip テスト 3 本
（`test_build_donor_bank_utau_vcv_cache_roundtrip`/
`test_build_donor_bank_utau_cache_roundtrip`/
`test_build_donor_bank_lab_cache_roundtrip`）へ、1 回目 `cache_hit is False`・
2 回目 `cache_hit is True`（`stats`/`bank.stats` 双方）の直接検証を追加した
（バグが再発すれば roundtrip テスト自体が落ちるようにし、同種の見過ごしを
再発させない）。

**影響**: record/CLI 出力（`cache_hit` フィールド）にのみ影響。bit には
影響しない。

### 修正3（Codex R2 追加・P2・`donor_bank_utau.py:540`）: ダイジェストとデコードの単一 read 化

**検証**: `build_donor_bank_utau_vcv`/`build_donor_bank_utau`（v1）は prepass
で選択された wav ファイルをハッシュ用に read した後、メイン loop の
`_load_wav_24k`/`sha256_of`（再ハッシュ）がそれぞれ同じファイルをもう一度
read していた（oto.ini も同様: prepass の hash 用 read の後、`parse_oto_ini`
が別 read で decode）。`donor_bank_lab.py`（PJS）・`donor_bank.py`
（vocadito）にも同型の split-read があった（vocadito: wav は
`sha256_of`→`load_donor_24k` の 2 回 read、notes CSV も
`sha256_of`→`load_notes_csv` の 2 回 read）。prepass〜メイン loop の間
（WORLD 分析を挟むため実測で分単位になりうる）にファイルが書き換わると、
キャッシュキーに記録される sha256 と実際に解析されたバイト列が食い違う
TOCTOU 窓が生じる（measure_bands.py の R1 修正と同型の穴。AGENTS.md
Persistent Artifact Safety Gate 項目1「単一 read で parse+hash」違反）。

**修正**: 全 4 builder（UTAU v1/v2(VCV)・PJS・vocadito）で「1 回 read した
バイト列から hash と decode/parse の両方を導出」する形に統一した
（measure_bands.py の R1 修正と同じ流儀。WAV は `io.BytesIO` 経由
`soundfile`、oto.ini/.lab/notes CSV はバイト列から decode）。

- `donor_bank_utau.py`: `parse_oto_ini(path)` を `parse_oto_text(text)` へ
  解析本体を分離（`parse_oto_ini` は薄いラッパとして後方互換維持）。
  `_load_wav_24k(path)` も同様に `_load_wav_24k_bytes(data)` へ分離。
  両 builder の prepass で選択 wav のバイト列を `wav_bytes_by_file` へ保持
  し、メイン loop はそのバイト列を再利用する（新規 read なし）。
- `donor_bank_lab.py`: `parse_lab(path)` を `parse_lab_text(text)` へ分離、
  `_load_wav_24k` も同様に分離。選択された .lab/_song.wav のバイト列を
  prepass で保持し、メイン loop で再利用する。
- `donor_bank.py`（vocadito）: `load_donor_24k(path)` を
  `load_donor_24k_bytes(data)` へ、`load_notes_csv(path)` を
  `load_notes_csv_text(text)` へそれぞれ分離。`build_donor_bank` は wav/
  notes CSV とも 1 回だけ `read_bytes()` し、そのバイト列から hash と
  decode の両方を導出する。

**[実装決定・record 記録]** UTAU builder の prepass は「選択された（wav
部分集合選択で採用された）全 wav ファイル」のバイト列を、実際に WORLD 分析
するかどうか（＝キャッシュ hit/miss）に関わらず保持する。既存の計算資源
境界方針（`_select_wav_subset`/`_select_wav_subset_for_contexts` による
被覆選択）でファイル数が既に抑えられているため、追加メモリはその範囲内
（実測 sakura/umi は各 20–28 wav ファイル）。`_select_lab_files`
自体が候補 .lab を走査する際に行う `parse_lab` 呼び出しは選択ロジック自体に
内在する別読み（同一ファイルの hash→decode 分割ではない）のため対象外とした。

**テスト**: 4 builder それぞれに `monkeypatch` で `Path.read_bytes` の
呼び出し回数を解決パスごとに数える single-read enforcement テストを追加
した（measure_bands.py の
`test_analyze_wav_reads_file_bytes_exactly_once` と同じ流儀）:
`test_build_donor_bank_utau_vcv_reads_each_file_exactly_once`、
`test_build_donor_bank_utau_reads_each_file_exactly_once`、
`test_build_donor_bank_lab_reads_each_file_exactly_once`、
`test_build_donor_bank_reads_wav_and_notes_csv_exactly_once`。

**影響**: bit には影響しない（read 経路のみの変更。TOCTOU 窓の排除）。

### テスト・lint・回帰確認（R3）

- `ruff check .`（リポジトリ全体）: clean
- `python -m pytest voice_genesis/foundry/tests -q`: **241 件、全 green**
  （R2 時点の 229 本 + 新規 12 本。内訳: joins +8 本 / donor_bank +1 本 /
  donor_bank_utau +2 本 / donor_bank_lab +1 本。
  `test_adapter_render_donor_wiring.py` の既存 2 本は改名・アサーション
  更新のみで本数不変）
- `python -m pytest voice_genesis/singer/tests -q`: 38 本、非破壊確認済み
- `src/svp_rpe/**`・`pyproject.toml` は無変更（本体スイートへの影響なし）

### F1.4 fixed3 WAV（#1 絶対グリッド化を反映・総尺が score 準拠へ回帰）

`presets/ritsu_neutral.json` を使い `--donor ritsu --voicebank-root
<波音リツ強連続音Ver1.5.1>` で再生成した（生成スクリプト: scratchpad
`foundry_f1_4/run_f1_4_fixed3_e2e.py`。出力先: scratchpad
`foundry_f1_4/wavs/*_fixed3.wav`・リポジトリへは非コミット・従来どおり）。

**総尺推移**（sakura / umi。score 総尺 = `perf.build_timeline` の
`total_samples` から直接算出した理論値）:

| 版 | sakura(s) | umi(s) | 備考 |
|---|---|---|---|
| score（理論値） | 24.583 | 11.409 | `perf.build_timeline` の `total_samples` |
| F1.4 fixed（R1・Codex #1,#2 対応後） | 22.785 | 10.425 | overlap のみ消費（当時は preutterance 未消費） |
| F1.4 fixed2（R2・Codex #2「preutterance 消費」対応後） | 19.985 | 8.625 | overlap+preutterance をハードトリム消費（trim 方式の副作用で累積縮小） |
| **F1.4 fixed3（R3・本対応後）** | **24.585** | **11.410** | 絶対グリッド配置（trim なし）。score 総尺と ±1 frame（±5ms）で一致 |

| ファイル | sha256(先頭12桁) | 長さ(s) |
|---|---|---|
| sakura_ritsu_f1_4_fixed3.wav | `198b81da0d88` | 24.585 |
| umi_ritsu_f1_4_fixed3.wav | `13e98ab3e18d` | 11.410 |

完全 sha256:
- sakura: `198b81da0d884d5bdac40beb555d2cf4cc7370196627ef2a1e008eb1c40d8a67`
- umi: `13e98ab3e18d8935ca3604e1799b6040ccbe40a5425ac3c2d7e9b3d7a8aa0623`

**決定論確認**: 両ファイルとも (a) 同一 cache_dir 再利用（cache hit）、
(b) 別 cache_dir でのフル再計算（no-cache）の 2 経路で再実行し、初回
（no-cache 相当の新規 cache_dir）と合わせて**3 回とも sha256 完全一致**を
確認済み。

**donor_provenance**: 両ファイルとも `declared_sha256 == actual_sha256`
（`presets/ritsu_neutral.json` の pin が実データと一致）。

**join_stats 実測（sakura）**: `n_joins_applied=6`（run 内の実際の重なり join
数。フレーズ先頭直後の最初の mid-phrase note が直前フレーズ先頭 unit の
preutterance 差分だけ実際に重なるケースに対応。残り 8 件の mid-phrase join
は一様 preutterance=60frame のため重なりゼロ=単純連結）、
`n_total_frames=4917`（= score_total_frames と厳密一致）。umi も同様に
`n_total_frames=2282` で一致。

**再耳判定はスコープ外**（User 判断待ち。F1.x 梯子凍結の裁定・S0–S4 実行順は
本対応で変更しない — #1 は「凍結後に発見された総尺実装バグの是正」であり、
凍結根拠（銀行情報密度の構造限界）そのものへの反証ではない）。

### 未対応の関連 Codex スレッド（本対応のスコープ外・record 記録のみ）

r2 以降のレビューで、本 R3 対応と隣接するが今回の指示スコープ（3 件）には
含まれない未対応スレッドが複数残っている（トリアージは次ラウンド）:

1. **`render.py`「Offset VCV control tracks by preutterance」**
   （`html_url` 末尾 `r3789341843`）: `_note_frame_track` が
   `seg.start_sample` を原点に f0/振幅を per-sample トラックから抽出する際、
   VCV unit の絶対配置シフト（本対応 #1 の lead）を考慮しないため、f0/振幅
   トラックの**内容**（sp/ap とのフレーム数整合は保たれるが、サンプルした
   時刻自体がズレる）が理論上不正確になりうる。本対応 #1 はこの経路を変更
   していない（`_note_frame_track` 無改修）ため、この指摘は R3 後も未解消
   のまま残っている。
2. **`render.py`「Include audio bytes in donor-spec identity」**
   （`r3789341845`）: `voicebank_identity_hash`/`corpus_identity_hash` は
   oto.ini/.lab のみをハッシュし、wav バイト自体を含まない。
3. **`measure_bands.py`「Reject measurement outputs that alias inputs」**
   （`r3789341847`）/ **「Publish measurement reports atomically」**
   （`r3789341850`）。

いずれも実害があり得る指摘だが、今回のタスク範囲（絶対グリッド化 + cache_hit
+ split-read）とは独立した別トピックのため着手していない。

## Codex R4 対応（未対応 4 件・全採用・実測で確認）

PR #262 の `chatgpt-codex-connector[bot]` レビューの残り 4 件（R3 節末尾
「未対応の関連 Codex スレッド」に列挙・`r3789341843`/`r3789341845`/
`r3789341847`/`r3789341850`）への検証 + 対応。ブランチ
`claude/voicegenesis-dev-continue-rfwf3p`。

### 1. render.py「Offset VCV control tracks by preutterance」（P1・`r3789341843`・採用・実測で確認）

**検証**: R3 の絶対グリッド化後も未解消であることを実測で確認した。`_build_vcv_placements`
は unit を `start_frame = cursor - shift`（`shift = min(lead, cursor)`、
`lead` = preutterance 相当）の絶対座標へ配置する一方、`_note_frame_track`
は f0/振幅の per-sample トラックを常に `seg.start_sample`（= score 上の
拍位置そのもの、shift 前）を原点にサンプリングしていた。sp/ap 自身は
正しく shift 済み絶対座標に配置される（フレーム数・配置位置とも R3 で
検証済み）が、f0/振幅トラックの**内容**は「拍位置」を表す値を「拍位置 −
shift」の絶対フレームへ書き込んでしまい、sp/ap の絶対配置と shift 分だけ
ズレていた。

sakura の実データで検証（note_index=7、prev_hz=349.23Hz(F4)・cur_hz=440.0Hz(A4)・
shift=60frame=300ms・preutterance=60frame・overlap=20frame）:

- (a)(b) sp/ap のアタック点（`placements[7].start_frame + shift`）=
  score 上の拍位置（`round(seg.start_sample / samples_per_frame)`）と
  一致（1601 vs 1600、±1 frame の丸め誤差内。他 record 箇所と同型の丸め）。
- (c) **修正前**: f0 トラックの出力先頭フレーム（= 絶対配置上は拍の 300ms
  前へ置かれる）が 374.6Hz（境界の遷移値・A4 へ向かいつつある値）だった
  ——次ノートの f0 が前倒しで開始していた実害を確認。
  **修正後**: 同じ絶対位置に 344.46Hz（prev note=F4=349.23Hz 近辺、drift/jitter
  込みでほぼ一致）が置かれ、pre-beat 区間の大半（frame 0〜53）は前ノートの
  定常値を保持し、ポルタメント半窓（27.5ms=5.5frame）に入る末尾のみ次ノート
  へ向けて滑らかに遷移する（`performance.build_f0_contour` のポルタメント
  仕様どおり）ことを確認した。

裁定: **未解消（要修正）**。

**修正**: `render.py` `_note_frame_track` に `origin_shift_frames: int = 0`
（既定 0 = 完全後方互換）を追加し、サンプリング原点を `k = np.arange(n_frames)
- extra_head - origin_shift_frames` へ拡張した。`_build_vcv_placements` の
戻り値 `stats` に `shift_per_note`（note ごとの実効配置シフト。
`start_frame = cursor - shift` の `shift` そのもの）を新設し、render.py の
呼び出し側で `donor="ritsu"`（VCV 経路）のみ `note_origin_shift_frames =
vcv_placement_stats["shift_per_note"]` を `_note_frame_track` へ渡す
（vocadito/pjs 経路は `[0]*len(placements)` で完全不変）。

**テスト**: `test_adapter_render_donor_wiring.py` に 5 本追加
（`test_note_frame_track_origin_shift_moves_sampling_window_earlier` /
`..._zero_matches_default_behavior` /
`..._preutterance_frame_lands_at_beat_sample`（検証 (a)(b)）/
`..._pre_beat_region_holds_previous_note_value`（検証 (c)）/
`test_build_vcv_placements_stats_shift_per_note_matches_start_frame_shift`）。

**影響**: 音に影響する修正（フレーム数・配置位置・総尺は不変。f0/振幅の
サンプリング原点のみ補正）。F1.4 fixed4 WAV を再生成（後述）。

### 2. render.py「Include audio bytes in donor-spec identity」（P1・`r3789341845`・採用・実測で確認）

**検証**: `donor_bank_utau.voicebank_identity_hash`/`donor_bank_lab.corpus_identity_hash`
は oto.ini/.lab のみをハッシュし、WORLD が実際に分析する WAV バイトを一切
含んでいなかった（該当コード確認済み）。実測: 合成 fixture で oto.ini を
1 バイトも変えずに対応 wav のバイト列だけ差し替えたところ、
`voicebank_identity_hash`/`corpus_identity_hash` とも旧実装ではダイジェスト
が不変（＝差し替えを検知できない）ことを確認した——`_validate_spec_donor`
の fail-closed 検証をすり抜けられる実害を確認。

**修正**: 両関数を「解析対象の annotation + WAV の `(相対パス, sha256)`
ペア決定論集約」（`aggregate_content_hash` 流儀）へ拡張した。

- `voicebank_identity_hash`: 各 pitch dir の oto.ini に加え、配下の全
  `*.wav`（`_validate_spec_donor` は WORLD 分析 + wav 部分集合選択より
  **前**に呼ばれるため、選択済み部分集合ではなく pitch dir 配下の**全**
  wav を対象にする——選択に依存しない安定した identity・検知網羅性も
  部分集合より広い）。
- `corpus_identity_hash`: 各 `pjsNNN.lab` に加え、対応する
  `pjsNNN_song.wav`（存在すれば）。

**presets の pin 値を実測再計算して更新**（波音リツ強連続音 Ver1.5.1・
PJS_corpus_ver1.1 の実データ、全 wav 込みで再計算。2 回実行し sha256
完全一致で決定論確認済み）:

| preset | フィールド | 旧値（oto.ini/.lab のみ・先頭12桁） | 新値（+WAV バイト・先頭12桁） |
|---|---|---|---|
| `presets/ritsu_neutral.json` | `voicebank_sha256` | `e722b36cd5b9` | `86e1c57b50b7` |
| `presets/pjs_neutral.json` | `corpus_sha256` | `ed7b6e5603bb` | `9905cec08fba` |

完全 sha256:
- ritsu: `86e1c57b50b781eaba92b57254b2df106a61766f08fbacac8399e54208cd6c8e`
- pjs: `9905cec08fbaf43fa545400498a7908ef28567e8f60a5ba005fb2e00d526f996`

**テスト**: `test_adapter_render_donor_wiring.py` に 5 本追加
（`test_voicebank_identity_hash_changes_when_wav_bytes_change` /
`..._stable_when_nothing_changes` /
`test_validate_spec_donor_ritsu_detects_wav_swap_with_unchanged_oto`（実害
シナリオの再現・修正後は検知できることを確認）/
`test_corpus_identity_hash_changes_when_song_wav_bytes_change` /
`test_validate_spec_donor_pjs_detects_wav_swap_with_unchanged_lab`）。

**影響**: 音・bit には影響しない（provenance 検証ロジックの拡張のみ。
render 経路は変わらないため fixed4 WAV は本項単独では再生成不要——
ただし `presets/ritsu_neutral.json` の pin 更新後でないと `_validate_spec_donor`
が fail-closed で拒否するため、fixed4 生成には新 preset を使用する）。

### 3. measure_bands.py「Reject measurement outputs that alias inputs」（P1・`r3789341847`・採用）

**検証**: `main()` の `--out` 書き込みには入力 wav との衝突検査が皆無で、
`measure_bands.py donor.wav --out donor.wav` が分析後に無条件で
`donor.wav` を JSON で上書きし、計測対象そのものを破壊することを確認した
（該当コード確認済み。adapter/render.py の同種修正（R2 #6）と対称の欠陥）。

**修正**: `measure_bands.py` に `OutputCollisionError` + `_reject_output_collision`
（新設）を追加し、`--out` が `wav_paths`（位置引数の計測対象 WAV 群）の
いずれかと resolved 一致する場合は書き込み前に fail-closed で拒否する。

**テスト**: `test_measure_bands.py` に 6 本追加（完全一致衝突・複数入力中の
1 件と衝突・無関係パスは素通り・symlink 解決後判定・存在しない入力パスは
スキップ・CLI 経由での実害再現 `test_main_rejects_out_aliasing_input`——
donor.wav が無傷のまま残ることを確認）。

**影響**: bit には影響しない（書き込み前ガードの追加のみ）。

### 4. measure_bands.py「Publish measurement reports atomically」（P2・`r3789341850`・採用）

**検証**: `Path(args.out).write_text(...)` が最終 out_path へ直接書き込んで
いた（該当コード確認済み。AGENTS.md Persistent Artifact Safety Gate 項目6
「全構築後公開」違反。adapter/render.py の同種修正（R2 #3）と対称の欠陥）。
プロセス kill・ディスク満杯時に truncated JSON を残す/既存の有効なレポート
を破壊しうる。

**修正**: `measure_bands.py` に `_atomic_write_text`（新設。staging tempfile
+ `os.replace`、`except BaseException` で staging を best-effort 削除して
から re-raise。adapter/render.py `_atomic_write_wav` と同じ流儀）を追加し、
`main()` の書き込み箇所を置き換えた。

**テスト**: `test_measure_bands.py` に 4 本追加（正常書き込み+tempfile
残留なし・staging write 注入失敗時に out_path が作られない（AGENTS.md
項目7「公開途中失敗の注入テスト」）・失敗時に既存の有効な出力を破壊しない・
CLI 経由での atomic 公開確認）。

**影響**: bit には影響しない（書き込み経路のみの変更）。

### テスト・lint・回帰確認（R4）

- `ruff check .`（リポジトリ全体）: clean
- `python -m pytest voice_genesis/foundry/tests -q`: **261 件、全 green**
  （R3 時点の 241 本 + 新規 20 本。内訳: render_donor_wiring +10 本（#1 の
  origin_shift 系 5 本 + #2 の WAV バイト identity 系 5 本）/ measure_bands
  +10 本（#3 の衝突拒否系 6 本 + #4 の atomic 公開系 4 本））
- `python -m pytest voice_genesis/singer/tests -q`: 38 本、非破壊確認済み
- `src/svp_rpe/**`・`pyproject.toml` は無変更（本体スイートへの影響なし）

### F1.4 fixed4 WAV（#1 f0/振幅サンプリング原点補正を反映・音に影響）

`presets/ritsu_neutral.json`（#2 の新 pin）を使い `--donor ritsu
--voicebank-root <波音リツ強連続音Ver1.5.1>` で再生成した（出力先:
scratchpad `foundry_f1_4/wavs/*_fixed4.wav`・リポジトリへは非コミット・
従来どおり）。

| ファイル | sha256(先頭12桁) | 長さ(s) | fixed3 版 長さ(s) |
|---|---|---|---|
| sakura_ritsu_f1_4_fixed4.wav | `4f6cca8ae1c4` | 24.585 | 24.585（不変） |
| umi_ritsu_f1_4_fixed4.wav | `b4fea37f6e62` | 11.410 | 11.410（不変） |

完全 sha256:
- sakura: `4f6cca8ae1c4cc0701f6aa4dcf84c0c63529747b758b0c9cc37e761e5f566a15`
- umi: `b4fea37f6e62427366671dd926ca62177d58bd266e602a64c91f5b1fcf59664c`

総尺は fixed3 から不変（#1 は配置位置・フレーム数を変えず、f0/振幅の
サンプリング原点のみを補正するため）。sha256 は fixed3 から変化している
（f0/振幅の内容が変わるため、音自体は影響を受ける——想定どおり）。

**決定論確認**: umi は (a) 初回、(b) 同一 cache_dir 再利用（cache hit）、
(c) 別 cache_dir でのフル再計算（no-cache）の 3 経路で sha256 完全一致を
確認済み。sakura は (a) 初回、(b) cache hit の 2 経路で sha256 完全一致を
確認済み（no-cache 経路は umi で同一コードパスを検証済みのため割愛）。

**donor_provenance**: 両ファイルとも `declared_sha256 == actual_sha256`
（`presets/ritsu_neutral.json` の新 pin が実データ（WAV バイト込み）と
一致することを確認済み）。

**再耳判定はスコープ外**（User 判断待ち。F1.x 梯子凍結の裁定・S0–S4 実行順は
本対応で変更しない — #1 は「凍結後に発見された実装バグの是正」であり、
凍結根拠（銀行情報密度の構造限界）そのものへの反証ではない）。

### 未対応スレッド（R4 時点）

R4 時点で PR #262 の Codex レビュー由来の未解決スレッドは残っていない
（本節の 4 件が最後の未対応分。それ以前の R1〜R3 分は別セッションで解決
処理済みのものを含め、本セッションでの照合時点で全 12 件が既に
`is_resolved=true` だった）。

## Codex R5 対応（残 2 件・全採用）+ ファミリー終端宣言

R4 push 後に新規発生した 2 スレッド（`donor_bank_lab.py`/`donor_bank_utau.py`
の split-read 残渣・pickle cache 直書き）へ対応する。両指摘は Codex の過去
指摘系列（R1〜R4）が繰り返し扱ってきた 2 ファミリー（単一 read・atomic 公開）の
再発であり、ドリップ対応を避けるため `voice_genesis/foundry/`（`adapter/` +
`scripts/`。`refset` ディレクトリは存在しないため対象外）全体を機械的に
掃討し、残存箇所ゼロを監査表で証明したうえで終端を宣言する
（AGENTS.md §3「自動レビュー指摘への採否方針」2 項）。

### 1. `donor_bank_lab.py:298`「Select PJS files from the retained lab bytes」（P2・`r3789400804`・採用）

**検証**: R3 の split-read 是正後も `_select_lab_files` は選択判定のため
`parse_lab(path)` を呼び、内部で独自に `Path.read_text()` していた
（本関数はハッシュ計算・mora 分解の read とは独立していた）。選択→ハッシュ→
分析の間に .lab が変わると、`selection_stats`（coverage 報告）が旧内容の
被覆を主張したまま、実際に構築される bank は新内容から作られる乖離が
起こりうる TOCTOU 窓を確認した（該当コード確認済み）。加えて、既存の
`test_build_donor_bank_lab_reads_each_file_exactly_once` は `Path.read_bytes`
のみを監視しており、`read_text` 経由の選択段の read はこのテストの死角に
なっていたことも確認した（テスト自体の弱点）。

**修正**: `_select_lab_files` を「選択判定のため `read_bytes()` で 1 回
read し、その場で decode/parse してトークン化、選択されたファイルは
read 済みバイト列を `lab_bytes_by_path`（新規戻り値）として返す」形へ
変更した。`build_donor_bank_lab` はこの `lab_bytes_by_path` をハッシュ計算・
mora 分解の両方でそのまま再利用し、選択段以降で .lab を再 read しない。

**テスト**: `test_select_lab_files_covers_onsets_and_vowels` に
`lab_bytes_by_path` の内容検証を追加。`test_build_donor_bank_lab_reads_each_file_exactly_once`
を強化し、`Path.read_text` を新たに監視して選択段が旧経路（`parse_lab`/
`read_text`）を使っていないことを直接 enforce するようにした（死角の解消）。

**影響**: bit には影響しない（選択ロジックの read 経路統一のみ。選択結果
自体は不変）。

### 2. `donor_bank_utau.py:681`「Publish donor caches atomically」（P2・`r3789400805`・採用）

**検証**: UTAU v1/v2(VCV)・PJS の 3 builder すべてで pickle cache 書き込みが
`open(cache_path, "wb")` による最終パス直書きだった（該当コード確認済み）。
書き込み中断（プロセス kill・ディスク満杯・並行レンダーの競合等）で
truncated pickle が残ると、`cache_path.exists()` は真のまま以後の全
リクエストが `pickle.load` で `UnpicklingError` を起こし続け、手動削除
until 復旧不能になる実害を確認した。

**ファミリー全数掃討で判明**: 同型の直書きは pickle cache 3 箇所に加え、
`donor_bank.py`（vocadito）の npz cache `_save_cache`（`np.savez` 直書き）
にも存在した（Codex のコメント本文が「the legacy UTAU, PJS, and NPZ cache
writers need the same treatment」と明記しており、指摘範囲に含まれる）。

**修正**: `donor_bank.py` に共通実装 `_atomic_stage_and_replace` +
`atomic_pickle_dump` + `atomic_savez`（新設。staging tempfile + `os.replace`、
`except BaseException` で staging を best-effort 削除してから re-raise。
`render.py` `_atomic_write_wav` / `measure_bands.py` `_atomic_write_text` と
同じ流儀）を追加した。`atomic_savez` は `np.savez` がパス文字列に `.npz` を
自動付与する挙動を避けるため file-like object（`os.fdopen` 経由）を渡す。

呼び出し側 4 箇所を置換:
- `donor_bank.py` `_save_cache`（npz） -> `atomic_savez`
- `donor_bank_utau.py` `build_donor_bank_utau_vcv`（pickle） -> `atomic_pickle_dump`
- `donor_bank_utau.py` `build_donor_bank_utau`（v1・pickle） -> `atomic_pickle_dump`
- `donor_bank_lab.py` `build_donor_bank_lab`（pickle） -> `atomic_pickle_dump`

**テスト**: `atomic_pickle_dump`/`atomic_savez` の直接単体テスト 6 本
（正常書き込み+tempfile 残留なし・注入失敗時に out_path が作られない
（AGENTS.md 項目7）・失敗時に既存の有効なキャッシュを破壊しない、を
pickle/npz それぞれ）を `test_adapter_donor_bank.py` に追加。加えて、
実害シナリオの直接再現として `pickle.dump` を注入失敗させ、
「①最終 `cache_path` に破損 pickle が残らない ②同一 tmp glob に残留物が
無い ③失敗後の次呼び出しが `cache_hit=False` で正常に再構築できる」を
3 builder（v1/VCV/PJS）それぞれで検証するテスト 3 本を追加した。

**影響**: bit には影響しない（書き込み経路のみの変更。cache-key・分析
ロジックは無変更）。

### ファミリー全数掃討 監査表

`voice_genesis/foundry/`（`adapter/` 9 ファイル + `scripts/measure_bands.py`。
`refset/` は存在せず対象外）全体を対象に、3 ファミリーそれぞれの該当パターンを
grep で全数列挙し、箇所ごとに判定した。

**F-A（単一 read: hash と decode/parse は同一バイト列から）**

| 箇所 | 判定 | 備考 |
|---|---|---|
| `donor_bank.py` `build_donor_bank`（wav/notes CSV） | 準拠 | R3 で対応済み。1 回 read したバイト列を hash/decode 両方に使用 |
| `donor_bank_utau.py` `build_donor_bank_utau`（v1・oto.ini/wav prepass） | 準拠 | R3 で対応済み |
| `donor_bank_utau.py` `build_donor_bank_utau_vcv`（v2・oto.ini/wav prepass） | 準拠 | R3 で対応済み |
| `donor_bank_lab.py` `build_donor_bank_lab`（.lab/wav 本体ループ） | 準拠 | R3 で対応済み（本 R5 対応で選択段との整合も確保） |
| `donor_bank_lab.py` `_select_lab_files`（選択段） | **修正**（本対応） | `r3789400804`。選択段の独自 read を解消 |
| `scripts/measure_bands.py` `_analyze_wav`（hash+decode） | 準拠 | R1/R4 で対応済み |
| `donor_bank.py`/`donor_bank_utau.py`/`donor_bank_lab.py` の `voicebank_identity_hash`/`corpus_identity_hash`/`sha256_of` | 対象外 | 意味論上ハッシュのみが目的の identity pin（decode と対にならない）。build 本体の read とは独立で正しい（R2/R4 で意図的に設計） |
| `render.py` `_validate_spec_donor`（vocadito: `db.sha256_of(wav_path)`）→ 後続 `db.build_donor_bank(wav_path,...)` | **境界宣言（対象外）** | 軽量な事前検証（WORLD 分析前の fail-fast）と本分析が別関数呼び出しであるため、`--wav` を同一プロセス内の検証直後に差し替えるという実行時間ミリ秒未満の窓に限られる TOCTOU が理論上残る。この設計（重い分析より前に軽量 identity 検証を先行させる）は `voicebank_identity_hash`/`corpus_identity_hash` の docstring で明文化済みの既存トレードオフであり、R2/R4 で Codex 自身がこの構造を前提にレビューを重ねてきた（新規指摘なし）。再着手条件: 実測で TOCTOU が実害（誤ったハッシュでの render 成功等）を起こした具体経路が示された場合 |

**F-B（atomic 公開: staging + `os.replace`）**

| 箇所 | 判定 | 備考 |
|---|---|---|
| `render.py` `_atomic_write_wav`（WAV 出力） | 準拠 | R2 で対応済み |
| `scripts/measure_bands.py` `_atomic_write_text`（JSON レポート出力） | 準拠 | R4 で対応済み |
| `donor_bank.py` `_save_cache`（npz cache） | **修正**（本対応） | `r3789400805` 指摘本文が明記した「NPZ cache writer」。`atomic_savez` へ置換 |
| `donor_bank_utau.py` `build_donor_bank_utau`（v1 pickle cache） | **修正**（本対応） | `atomic_pickle_dump` へ置換 |
| `donor_bank_utau.py` `build_donor_bank_utau_vcv`（v2 pickle cache） | **修正**（本対応） | `r3789400805` 本体（指摘行）。`atomic_pickle_dump` へ置換 |
| `donor_bank_lab.py` `build_donor_bank_lab`（pickle cache） | **修正**（本対応） | `atomic_pickle_dump` へ置換 |
| `voice_spec.py` `save_voice_spec`（JSON） | 対象外（境界宣言） | `render.py` は `load_voice_spec`（読み取り）のみ呼び、`save_voice_spec` は CLI/API のどの実行経路からも呼ばれず `test_adapter_voice_spec.py` の fixture 生成専用。永続成果物として繰り返しリクエストに晒される経路が無いため実害基準を満たさない。再着手条件: 本関数を呼ぶ CLI/API 経路が新設された場合 |

**F-C（出力・入力衝突拒否）**

| 箇所 | 判定 | 備考 |
|---|---|---|
| `render.py` `main()`（`--out` vs `--wav`/`--voice`/`--notes-csv`/`--voicebank-root`） | 準拠 | R2 で対応済み（`_reject_output_collision`） |
| `scripts/measure_bands.py` `main()`（`--out` vs 位置引数 `wav_paths`） | 準拠 | R4 で対応済み（`_reject_output_collision`/`OutputCollisionError`） |
| その他の CLI エントリポイント | 該当なし | `adapter/` + `scripts/` 内で `argparse.ArgumentParser`/`def main()` を持つのはこの 2 ファイルのみ（grep で確認済み） |
| donor cache（pickle/npz）の書き込み先 | 対象外 | ファイル名は content-hash から機械生成される内部キャッシュキーであり、`--out` のようにユーザーが指定する出力パスではない。ドナー入力（oto.ini/.lab/wav）とのパス衝突はユーザー入力に依存しないため F-C の脅威モデル（ユーザー指定 `--out` が保護入力と衝突する）に該当しない |

**掃討結果**: F-A 残存 1 件（`_select_lab_files`）・F-B 残存 4 件（npz 1 +
pickle 3）を本対応で是正。境界宣言 2 件（render.py 前段検証の TOCTOU・
`save_voice_spec` の未到達コード）を除き、`voice_genesis/foundry/` 全体で
3 ファミリーの残存はゼロであることを確認した。

### テスト・lint・回帰確認（R5）

- `ruff check .`（リポジトリ全体）: clean
- `python -m pytest voice_genesis/foundry/tests -q`: **270 件、全 green**
  （R4 時点の 261 本 + 新規 9 本。内訳: `test_adapter_donor_bank_lab.py` +2 本
  （選択段バイト列検証の強化 + corrupt cache 注入 1 本）/
  `test_adapter_donor_bank_utau.py` +2 本（v1/VCV corrupt cache 注入）/
  `test_adapter_donor_bank.py` +6 本（`atomic_pickle_dump`/`atomic_savez`
  単体テスト、正常系+注入失敗系+既存キャッシュ非破壊系）。既存
  `test_build_donor_bank_lab_reads_each_file_exactly_once` は `read_text`
  監視を追加する形で強化（新規カウントなし））
- `python -m pytest voice_genesis/singer/tests -q`: 38 本、非破壊確認済み
- `src/svp_rpe/**`・`pyproject.toml` は無変更（本体スイートへの影響なし）
- 音への影響なし（provenance/堅牢性のみの変更のため fixed5 WAV 再生成は不要）

### 未対応スレッド（R5 時点）

R5 時点で PR #262 の Codex レビュー由来の未解決スレッドは残っていない。

## Codex R6 対応（残 3 スレッド）

R5 push 後に新規発生した 3 スレッドへ対応する。いずれも過去の是正（R2/R4/R5）
に対する「新規発見の evidence」として立てられたスレッドであり、境界宣言済み
（`_validate_spec_donor` 前段検証の TOCTOU）を含む。

### 1. `render.py:467`「Reuse the bytes accepted by donor provenance validation」（P2・`r3789428504`・採用）

**検証**: R5 監査表で境界宣言していた TOCTOU（`_validate_spec_donor` の軽量
identity 検証と、後続の bank 構築本体の read が別読みであるため、その間に
入力が書き換わると「spec 一致の provenance を返しつつ別バイトを分析」
しうる）を Codex がスレッドとして再提起した。当該箇所（R5 監査表 F-A 節
`render.py _validate_spec_donor` 行）を確認し、境界宣言の再着手条件
「実測で TOCTOU が実害を起こした具体経路が示された場合」には該当しない
（新規実害の実測提示はなし）ものの、Design Memo の推奨修正方針（post-build
revalidation）が実装コスト・パフォーマンスとも許容範囲だったため、境界宣言
のまま見送らず採用へ切り替えた。

**修正**: bank 構築完了後・成果物公開前（`db.normalize_unit_energy` 呼び出し
直前）に `_validate_spec_donor` をもう一度呼ぶ post-build revalidation を
追加した。`voicebank_identity_hash`/`corpus_identity_hash`/`sha256_of` は
いずれもキャッシュを介さず毎回ディスクを直接再走査するため、1 回目
（fail-fast）と 2 回目（post-build revalidation）の 2 点で identity hash が
一致すれば、その間（bank 構築本体の read を含む）に対象ファイルが書き換
わっていないという強い証拠になる（同一 dataset・同一ファイル状態なら
`_validate_spec_donor` は冪等）。cache-hit 経路（bank 構築が pickle 復元
だった場合）でも、この 2 回目の呼び出しはキャッシュに依存しない独立した
鮮度検証として同じ強度で機能する（Design Memo で提示された「キャッシュへ
identity を保存して代替する」案は、フル再走査で既に同等以上の検知力が
得られるため見送った——キャッシュスキーマ変更を伴わない分、変更コストも
小さい）。3 donor（vocadito/ritsu/pjs）すべてが対象（`_validate_spec_donor`
は `donor` 引数で分岐する共通関数のため、呼び出しを 1 箇所増やすだけで
3 種とも同時に閉じる）。

**テスト**: `test_render_ritsu_revalidates_donor_provenance_after_bank_build`
（`dbu.build_donor_bank_utau_vcv` をスタブ化し、その副作用として bank 構築
「中」に oto.ini を書き換える実害シナリオを再現。post-build revalidation が
無ければ検出されないまま render() が先へ進んでしまうことを
`rd.DonorProvenanceError` の送出で確認）+
`test_render_ritsu_no_revalidation_error_when_voicebank_unchanged`（対照
ケース: tamper なしなら revalidation は誤検知しないことを、`db.normalize_
unit_energy` へ到達するかで直接確認）の 2 本を追加。いずれも実際の
WORLD 合成・実データ非依存の軽量スタブで完結する（render() 全体を呼ぶが、
revalidation が例外を出すか、その先の sentinel へ到達するかの時点で終了
するため実行は 2 秒未満）。

**影響**: bit には影響しない（provenance 検証の追加呼び出しのみ。分析
ロジック・cache-key は無変更）。

### 2. `units.py:457`「Handle loop windows no longer than the overlap」（P2・`r3789428506`・採用）

**検証**: R5 で追加した `_loop_pad_raw_target` の overlap 損失補償
（`net_per_cycle = max(seg_len - overlap_frames, 1)`）は、`seg_len <=
overlap_frames`（短い unit を大きく伸長し、ループ中央区間がループ overlap
長 8 frame=40ms 以下になる場合）で破綻することを実測で確認した。この場合
`jn.overlap_add_concat` の内部クランプ（`ov = min(ov_req, prev_len,
cur_len)`）により `ov == cur_len` となり、`sp_p[ov:]` が空になって join
ごとの実際の正味増分は 0 フレームだった。旧式は `max(..., 1)` フォール
バックにより「常に最低 1 フレームは進む」と誤って仮定し、`n_cycles_needed`
を過小に見積もっていたため、`_ping_pong_pad_v2` が何ピース足しても出力長
が伸びず、R5 が排除したはずの凍結プラトー（`_force_length` の末尾フレーム
複製）が再発する条件が残っていた。

**修正**: `_effective_loop_overlap(seg_len, overlap_frames)`（新設）を
`_ping_pong_pad_v2`・`_loop_pad_raw_target` の両方から共通で呼び、
`_ping_pong_pad_v2` が `jn.overlap_add_concat` へ渡す overlap を
`min(overlap_frames, seg_len - 1)`（下限 0 = 非重畳連結へ縮退）へクランプ
するようにした（`sp_p[ov:]` に必ず最低 1 フレーム残ることを構造的に保証）。
`_loop_pad_raw_target` の生成量見積りも同じ実効値で `net_per_cycle` を
計算するよう揃えた上で、実測で判明した追加の乖離（base<->pad join の
接合損失は `_ping_pong_pad_v2` 内部のクランプ後の実効 overlap ではなく、
呼び出し側が渡す**素の** `overlap_frames` をそのまま消費するため、
`seg_len <= overlap_frames` では旧来の「+1 ピース」安全マージン
（`seg_len` フレーム分）がこの接合損失（`overlap_frames` フレーム）を
下回りうる——実測: `seg_len=6・overlap_frames=8` で凍結補填が 2 フレーム
残存）を埋めるため、`needed_extra = deficit + overlap_frames - seg_len`
を正味増分で満たす周回数を求める式へ変更した。

**音への影響確認（実測）**: 実データ（波音リツ強連続音 Ver1.5.1 + `--donor
ritsu --voicebank-root`）で sakura/umi の現行 VCV 選択（cache_fixed4、fixed4
WAV が使った選択結果と同一）を走査し、各ノートの `resolve_vcv_unit_to_note`
extended_looped 分岐で `center_hi - center_lo`（ループ中央区間長）と
`loop_overlap_frames`（既定 8 frame）の関係を実測した。

| score | 選択ノート数 | extended_looped 発動数 | `seg_len <= overlap_frames` 発動数 | 最小マージン（`seg_len - overlap_frames`） |
|---|---|---|---|---|
| sakura | 20 | 6 | **0** | +64 frame（note=16, unit=117） |
| umi | 12 | 3 | **0** | +66 frame（note=2, unit=118） |

sakura/umi のどちらの extended_looped 発動ケースも `seg_len` は 71〜98
frame（実測全件この帯）で、`loop_overlap_frames=8` から最小でも 64 frame
の余裕があり、本バグの発火条件（`seg_len <= 8`）には遠く届かない。
**したがって fixed4 WAV の bit には無影響であることを実測で確認し、fixed5
WAV の再生成は行わない**（バグ自体は実装上確実に存在し是正が必要だが、
現行の sakura/umi 選択が要求する unit 長は十分に長いため今回の対象範囲では
未発火だった）。

**テスト**: `test_ping_pong_pad_v2_progresses_when_seg_len_le_overlap`
（`seg_len=6 < overlap_frames=8` で `_loop_pad_raw_target` → `_ping_pong_
pad_v2` を直接呼び、出力長が `deficit` に対して十分伸びることを確認。旧
実装ではここで頭打ちしていた）+
`test_resolve_unit_to_note_no_frozen_plateau_when_seg_len_le_overlap`
（同条件を `resolve_unit_to_note` 経由の合成データで再現し、①凍結補填
（末尾の完全同一フレーム連続数）が高々 1 フレーム、②中盤区間が単調な
凍結値でなく実際に変化していることの両方を直接検証）の 2 本を追加。

**影響**: `seg_len > overlap_frames`（sakura/umi の現行選択を含む従来の
全ケース）では `_effective_loop_overlap` は `overlap_frames` をそのまま
返す恒等写像のため bit 不変。`seg_len <= overlap_frames` の場合のみ挙動が
変わる（今回の対象範囲では未発火のため実害なし。将来的に短い unit を要求
する選択が発生した場合の凍結プラトー再発を予防する是正）。

### 3. `donor_bank_lab.py:333`「Reject selected PJS labels without their audio」（P2・`r3789428508`・採用）

**検証**: 選択済み `.lab` に対応する `_song.wav` が存在しない場合（partial
corpus——wav が 1 本だけコピーされていない状況）、旧実装は分析ループで
`if wav_bytes is None: continue` により黙って skip していた一方、
`selection_stats`（`_select_lab_files` が返す coverage 報告）と
`stats["n_files_analyzed"] = len(selected_paths)` はどちらも選択段の値を
そのまま使い続けていたため、実際には分析されなかった `.lab` の音素被覆・
ファイル数を「達成済み」として偽って報告していた実害を確認した。

**修正**: `_select_lab_files` 呼び出し直後、内容ハッシュ計算より**前**に、
選択済み `.lab` 全件に対応する `_song.wav` の存在を検査し、1 件でも欠けて
いれば欠損ペア（相対パス）を列挙した `FileNotFoundError` を送出する
fail-closed ガードを追加した。以降の prepass ハッシュループ・メイン分析
ループの `if wav_path.exists()`/`if wav_bytes is None: continue` 分岐は
このガードにより到達不能になるため、無条件 read へ簡素化した（skip 経路の
残置による意味論の重複を避けるため）。

**テスト**: `test_build_donor_bank_lab_missing_paired_wav_fails_closed`
（wav が 1 本欠けた 2 ディレクトリの合成 partial corpus で
`FileNotFoundError` 送出 + エラーメッセージに欠損ファイル名
（`pjs002_song.wav`）が含まれること、無関係な既存ファイル名が含まれない
ことを検証）を追加。

**影響**: bit には影響しない（正常系の選択結果・分析結果は不変。partial
corpus という異常系のみ挙動が「黙って偽装した coverage で成功」から
「明示的に失敗」へ変わる）。

### テスト・lint・回帰確認（R6）

- `ruff check .`（リポジトリ全体）: clean
- `python -m pytest voice_genesis/foundry/tests -q`: **275 件、全 green**
  （R5 時点の 270 本 + 新規 5 本。内訳: `test_adapter_render_donor_wiring.py`
  +2 本（#1 post-build revalidation の検知/非検知 2 本）/
  `test_adapter_units.py` +2 本（#2 `_ping_pong_pad_v2` 進捗確認 +
  `resolve_unit_to_note` 凍結補填上限確認）/
  `test_adapter_donor_bank_lab.py` +1 本（#3 partial corpus fail-closed）)
- `python -m pytest voice_genesis/singer/tests -q`: 38 本、非破壊確認済み
- `src/svp_rpe/**`・`pyproject.toml` は無変更（本体スイートへの影響なし）
- 音への影響なし（#1 は provenance 検証追加のみ、#3 は fail-closed 化のみで
  ともに bit 不変。#2 は実測で sakura/umi の現行選択に未発火のため fixed5
  WAV 再生成は不要——詳細は上記「#2 音への影響確認」節参照）

### 未対応スレッド（R6 時点）

R6 時点で PR #262 の Codex レビュー由来の未解決スレッドは残っていない。

## Codex R7 対応（残 2 スレッド・軽量最終ラウンド）

R6 push 後に `voice_spec.py` へ新規発生した 2 スレッドへ対応する。

### 1. `voice_spec.py:33`「Reject malformed voice-spec field shapes」（P2・`r3789451883`・採用）

**指摘**: 手編集 preset が `"warp": []` / `"perf": []` / `"seed": "11"` 等の
誤った JSON 形状を供給すると、`from_dict` の緩い coercion（`dict(d["warp"])`
/ `int(d["seed"])`）がこれを黙って受理してしまい、`render()` が preset の
主張と異なる既定値（デフォルト performance/warp）で成功する。誤設定が
「成功した成果物」として出てくる silent failure。

**修正**: `foundry-voice/0.1` の必須オブジェクト形状・キー集合・スカラー型・
有限数値域を fail-closed 検証してから spec を構築する。

- トップレベル: `schema`/`donor`/`warp`/`perf`/`seed` 以外の未知キーを拒否
- `donor`/`warp`/`perf`: dict 形状必須（list 等を拒否）
- `donor`: 未知キー拒否（`dataset`/`clip`/`sha256`/`voicebank_name`/
  `voicebank_sha256`/`corpus_name`/`corpus_sha256` の union）、`dataset`
  必須、文字列系フィールドは `str`、`clip` は `int`（bool は拒否）
- `warp`: `formant_scale`/`tilt_db_oct`/`breath_lift` 以外の未知キー拒否、
  値は有限 float（NaN/Inf 拒否）
- `perf`: `onset_glide`/`vibrato`/`drift`/`jitter_cents`/`portamento_ms`
  以外の未知キー拒否。ネストした `onset_glide`/`vibrato`/`drift` も同様に
  未知キー拒否 + 有限 float 検証（`vibrato.phase_reset_per_note` のみ
  `bool` 検証）
- `seed`: `int` 必須（`bool` は `int` のサブクラスのため明示的に拒否）

**検証**: 既存 presets 4 本（`neutral.json`/`warped.json`/
`ritsu_neutral.json`/`pjs_neutral.json`）が全て新検証を通過することを
`test_all_presets_pass_validation`（4 preset をパラメタライズ）で確認。
不正形状（`warp`/`perf`/`donor` を `[]` に置換・`seed` を文字列/`bool`
に置換・未知キー混入（トップレベル/`warp`/`perf.vibrato`）・NaN/Inf
混入・必須キー欠落・非 dict ドキュメント）で `ValueError` が送出されることを
15 本の新規テストで確認（`test_adapter_voice_spec.py`）。

### 2. `voice_spec.py:47`「Publish saved voice specs atomically」（P2・`r3789451885`・採用・R5 境界宣言の撤回）

**R5 境界宣言の撤回**: R5 監査表では「`save_voice_spec` はどの実行経路からも
呼ばれず fixture 生成専用」として対象外（境界宣言）とした。本ラウンドでの
再判定: 未到達であっても `foundry-voice/0.1` の正本設定ファイルを書き込む
唯一の公開関数として存在する以上、`measure_bands._atomic_write_text` /
`render._atomic_write_wav` と同一の staging + `os.replace` パターンを
適用するコストは既存ヘルパの流儀をそのまま移植するだけで安価であり、
「境界宣言を維持する議論のコスト」の方が「正面修正のコスト」を上回ると
判定した。将来 CLI/API 経由の呼び出し経路が新設された際に atomic 化の
負債を残さないための予防的修正でもある。

**修正**: `save_voice_spec` を `open(path, "w")` の直接 truncate 書き込みから
`tempfile.mkstemp` によるステージングファイル書き込み + `os.replace` による
atomic rename へ変更。`except BaseException`（`KeyboardInterrupt`/
`SystemExit` 含む）で staging を best-effort 削除してから re-raise する。

**検証**: `json.dump` を `monkeypatch` で注入失敗させ、(a) 既存の有効な
preset への上書き保存が失敗した場合に元の内容が破壊されず残ること、
(b) 新規パスへの保存が失敗した場合に出力ファイルが一切作られないこと、
(c) いずれの場合も staging tempfile（`*.tmp`）が残骸として残らないことを
2 本の新規テストで確認。

### テスト・lint・回帰確認（R7）

- `ruff check .`（リポジトリ全体）: clean
- `python -m pytest voice_genesis/foundry/tests -q`: **296 件、全 green**
  （R6 時点の 275 本 + `test_adapter_voice_spec.py` 新規 21 本
  （不正形状 15 本（非 dict ドキュメント含む）+ atomic 化 2 本 +
  presets パラメタライズ 4 本））
- `python -m pytest voice_genesis/singer -q`: 38 本、非破壊確認済み
- `src/svp_rpe/**`・`pyproject.toml` は無変更（本体スイートへの影響なし）
- 音への影響なし（#1 は preset ロード時の検証追加のみで既存 4 presets は
  全通過、#2 は保存経路の atomic 化のみで書き込み内容は不変）

### 未対応スレッド（R7 時点）

R7 時点で PR #262 の Codex レビュー由来の未解決スレッドは残っていない。

## Codex R8 対応（残 3 スレッド）

R7 push 後に新規発生した 3 スレッドへ対応する。

### 1. `donor_bank_utau.py:526`「Keep UTAU WAV references inside the voicebank」（P2・`r3789486145`・採用）

**指摘**: ダウンロード配布や手編集の `oto.ini` が `filename=` フィールドへ
`../` を含む相対脱出パス・絶対パス・voicebank 外を指す symlink を供給した
場合、`pdir / wav_filename` の無検査結合がそのバイト列を WORLD 分析へ読み
込んでしまう。`voicebank_identity_hash()` は各 pitch dir 直下の `*.wav` の
みを列挙するため、脱出した WAV は provenance の対象外のまま分析される。

**修正**: 新設 `_reject_wav_paths_outside_root(pdir, pdir_name, wav_filenames,
root)` を追加し、`build_donor_bank_utau_vcv`/`build_donor_bank_utau` 双方の
prepass（wav 部分集合選択直後・実際の read 直前）で呼び出す。各候補パスを
`.resolve(strict=False)`（symlink 解決込み）した上で、解決済み voicebank
root 配下に収まっているかを検査し、1 件でも root 外を指せば違反エントリの
相対パスを列挙した `ValueError` を送出して fail-closed に停止する（脱出した
ファイルを黙って read しない）。

**テスト**: `_reject_wav_paths_outside_root` を直接叩く 4 本
（相対脱出 `../../evil.wav`・絶対パス・symlink 脱出のそれぞれで
`ValueError` + 違反パスがメッセージに含まれることを確認、および root 内の
通常ファイルは素通しすることを確認する過検知なしテスト）+
`build_donor_bank_utau` を実際に `../` 脱出 oto.ini で呼ぶ end-to-end テスト
1 本（`test_adapter_donor_bank_utau.py` に計 5 本追加）。

**影響**: bit には影響しない（root 内に収まる既存の正常系 oto.ini/wav 構成
では `_reject_wav_paths_outside_root` は何も検出せず既存の read 経路を素通り
する。異常系（脱出パス）のみ挙動が「無検査で外部ファイルを read」から
「明示的に拒否」へ変わる）。

### 2. `render.py:744`「Record the digest of the published WAV」（P2・`r3789486148`・採用）

**指摘**: `out_path` 使用時、成果物 WAV の永続 PCM バイト列を公開するが、
戻り値と CLI 出力は `bank.wav_sha256`（ドナー入力の識別子であり生成成果物
とは無関係）しか露出しない。出力の後日の破損・差し替えを render record から
検知できず、AGENTS.md の「入力と出力の両ハッシュ記録」要件を満たさない。

**修正**: `_atomic_write_wav` を `None` 戻りから `str`（sha256 hex digest）
戻りへ変更し、`os.replace` で公開する直前——`sf.write` が書き終えた staging
ファイル（`tmp_name`）を read してハッシュ計算する（公開後の別読みだと
replace 後の実ファイルとの間に別の TOCTOU 窓が生じるため、公開直前の
staging バイト列を正本にする）。`render()` はこの戻り値を `output_sha256`
として結果 dict に追加し（`out_path is None` の場合は `None`）、CLI (`main`)
は `output_sha256=...` を追加の1行として print する。

**テスト**: `test_atomic_write_wav_returns_digest_matching_published_bytes`
（戻り値の digest が公開後ファイルの実測 sha256 と bit 一致することを確認。
`test_adapter_render_donor_wiring.py` に追加）。既存の
`test_atomic_write_wav_writes_readable_content` 等は無改変で green のまま
（呼び出し側で戻り値を無視しても既存挙動に影響しない後方互換な変更）。

**影響**: bit には影響しない（WAV バイト列自体は無変更。戻り値/CLI 出力へ
のフィールド追加のみ）。

### 3. `render.py:551`「Bind provenance to the analyzed donor snapshot」（P2・`r3789486140`・**境界宣言・不採用・解決マーク未付与**）

**指摘**: post-build revalidation（`_validate_spec_donor` の2回目呼び出し）
は現在の disk 状態と spec を比較するのみで、builder が実際に保持している
バイト列とは比較していない。donor が builder の read 中に A→B と変化し、
この行の前に B→A へ戻された場合、両方の検証が通る一方で `bank` は B の
分析結果を含み `donor_provenance` は A を主張する（vocadito/Ritsu/PJS 共通
の同型窓）。builder の内容 digest をこの比較へ持ち込むべき、との指摘。

**実装コスト見積り（採否判定の根拠）**:

builder（`build_donor_bank_utau_vcv`/`build_donor_bank_utau`）は R3 以降
prepass で選択済み wav の内容 digest（`content_digest`）を単一 read から
既に計算しており、これを `bank.stats` へ露出する**だけ**なら 1 行追加で
安価（今回 #1 修正の副産物として既存コードを確認済み）。しかし、この
digest は「計算資源境界のため選択された wav 部分集合」のみを対象とする
（`voicebank_identity_hash` の設計意図——`_select_wav_subset*` より前に
確定した全 pitch dir 全 wav のフルインベントリ——とはスコープが異なる。
`voicebank_identity_hash` docstring 内 [実装決定・record 記録] が明示）。
`spec.donor.voicebank_sha256`/`corpus_sha256` が宣言するのはこのフル
インベントリ identity であり、部分集合 digest とは直接比較できない
（次元が異なる値の比較は無意味な "false negative 検知漏れ" を生む）。

「置換」（disk 再走査をやめて builder digest だけで比較）を選ぶと、
未選択ファイルの改竄検知網羅性という既存の R2/R4 設計意図を失う後退になる。
「併用」（builder digest を使った memo-vs-fresh-read の追加検証を選択
済みファイルに限定して足す）は理論上 A→B→A の窓を狭められるが、
(a) 3 donor タイプ（vocadito/Ritsu/PJS）それぞれで bank スキーマへ
analyzed-path リストの追加保持、(b) 2 箇所のキャッシュスキーマ marker
bump（`donor_bank_utau.py` 内の VCV/非VCV 双方）、(c) `donor_bank_lab.py`
（PJS）側の同型配線、(d) render.py 側の新規比較ロジック + エラーパス、
(e) mid-build ファイル差し替えを再現する新規テスト（3 donor タイプ分）
が必要になり、blast radius は #1/#2 を合わせた規模を上回る。しかも実装後も
TOCTOU の性質上「窓を狭める」に留まり「ゼロにする」ことはできない
（OS レベルロックなしでは原理的に不可能。AGENTS.md「線引きの原則」——
OS 基盤へは広げない——に抵触する領域）。

**判定（不採用・境界宣言）**: 本リポジトリの脅威モデル（単一利用者・
ローカル・決定論パイプライン、実行中に voicebank ディレクトリへ能動的に
書き込める第三者攻撲者は前提としない）では、render 実行中に precise
timing で A→B→A を差し替えられる攻撃者は対象外とする。この能力を持つ
攻撃者は同じ機会で render の出力 WAV・record ファイルそのものを直接
偽造できるため、この特定の窓だけを塞ぐ費用対効果は釣り合わない
（`docs/DESIGN_M2_extraction_accuracy.md` §6「Scorer pin の脅威モデルと
境界」と同型の判断——能動的にファイルシステムを制御できる攻撃者への
完全な tamper-proofing は「受動的ドリフトの tamper-evidence」の範囲を
超え、より直接的な下流偽造経路が常に残る。#225 の overengineering 教訓
にも従う）。既存の pre-build fail-fast（builder 実行前）+ post-build
再走査（builder 完了後）の 2 点チェックは「非 reverted な改竄」（render
呼び出し期間中に発生し、かつ元に戻されない変化）は引き続き確実に検知
する。境界宣言のため PR スレッドへは不採用理由を返信し、resolve マークは
付与しない。

### テスト・lint・回帰確認（R8）

- `ruff check .`（リポジトリ全体）: clean
- `python -m pytest voice_genesis/foundry/tests -q`: **302 件、全 green**
  （R7 時点の 296 本 + `test_adapter_donor_bank_utau.py` 新規 5 本（#1 の
  相対脱出/絶対パス/symlink 脱出/過検知なし/end-to-end）+
  `test_adapter_render_donor_wiring.py` 新規 1 本（#2 の digest 一致確認））
- `python -m pytest voice_genesis/singer -q`: 38 本、非破壊確認済み
- `src/svp_rpe/**`・`pyproject.toml` は無変更（本体スイートへの影響なし）
- 音への影響なし（#1 は fail-closed ガード追加のみで root 内に収まる既存の
  正常系 oto.ini/wav 構成は無影響、#2 は staging バイト列読み出しでハッシュ
  計算を追加するのみで WAV バイト列自体は無変更、#3 は不採用のためコード
  変更なし）

### 未対応スレッド（R8 時点）

R8 時点で PR #262 の Codex レビュー由来の未解決スレッドは #3
（`render.py:551`「Bind provenance to the analyzed donor snapshot」）のみ
残る（境界宣言により意図的に未解決のまま。resolve は付与しない）。

## Codex R9 対応（残 5 スレッド・全採用）

R8 push 後に新規発生した 5 スレッドへ対応する（R8 §3 の境界宣言スレッドは
本ラウンドの対象外・不変）。ブランチ `claude/voicegenesis-dev-continue-rfwf3p`。

### 1. `render.py:592`「Reject or honor consonant overrides for Ritsu」（P2・`r3789495255`・採用）

**検証**: `donor="ritsu"` の VCV 経路（F1.4）は子音を録音済み調音遷移として
unit に不可分に録り込み済み（§1.3/1.8）。旧実装は `consonant_source` の解決値
（`--consonant-source none`/`synthetic`・`--no-consonants`）に関わらず line 592
で無条件に `"vcv"` へ上書きしており、CLI が受理する 3 つの値のうち "none"/
"synthetic" を指定しても実際には録音済み子音を含む VCV 出力が生成され、
かつ `consonant_source="vcv"` として偽らない正確な値を返す一方、ユーザーが
指定した意図（子音を無効化する/合成へ切替える）は黙って無視されていた。

**修正**: `render()` 冒頭で `consonant_source` を解決した直後・`donor` 検証の
直後（重い WORLD 分析・oto.ini 全数走査より前）に、`donor == "ritsu" and
consonant_source != "recorded"` なら `ValueError` を送出する fail-closed
ゲートを追加した。`donor="ritsu"` で対応する値は `"recorded"`（既定）のみ。
`--consonant-source` / `--no-consonants` のヘルプ文字列も実態（ritsu では
非対応）に合わせて更新した。

**テスト**: `test_adapter_render_donor_wiring.py` に 5 本追加
（`consonant_source="none"`/`"synthetic"`/`--no-consonants` の 3 通りが
本ゲートより前（`load_voice_spec` 呼び出し前）で `ValueError` を送出する
ことを直接確認、`consonant_source="recorded"`/省略時は本ゲートを通過し
後続の `FileNotFoundError`（存在しない spec パス）まで進むことを確認）。

**影響**: 音・bit には影響しない（既定・唯一の対応値 `"recorded"` の
コードパス自体は無変更 —— line 592 の `consonant_source = "vcv"` 上書きは
そのまま残る。新規ゲートは非対応値を通した場合にのみ発火する）。

### 2. `perf_genes.py:120`「Honor the vibrato phase-reset setting」（P2・`r3789495254`・採用）

**検証**: `build_perf_f0` は spec の `vibrato.phase_reset_per_note` を一切
読まず、常に `_vibrato_cents_phase_reset`（ノート毎位相リセット）を呼んで
いた（該当コード確認済み）。`voice_spec.py` のスキーマは `phase_reset_per_note`
を有効な boolean として受理する（`_VIBRATO_ALLOWED_KEYS`）ため、`false` を
指定しても `true` と bit 完全一致の出力になる——宣言されたパフォーマンス
設定が黙って無視される Codex 指摘のとおりの実害を確認した。

**修正**: `perf_genes.py` に `_vibrato_cents_continuous_phase`（新設）を
追加した。曲頭 (t=0) からの絶対時間 `t_full = np.arange(total_samples)/sr`
で位相を計算する点のみが `_vibrato_cents_phase_reset` と異なる（振幅
エンベロープ＝ノート先頭からの onset_ms 線形ランプは共通仕様のため同じ
まま維持）。singer/performance.py `build_f0_contour` 自身のビブラート位相
計算と同じ絶対時間基準であり、F1a 以前（ノート毎位相リセット導入前）の
旧挙動に相当する。`build_perf_f0` は
`vib.get("phase_reset_per_note", True)` の真偽で呼び出す関数を切り替える
（true = 既存 `_vibrato_cents_phase_reset` を無変更のまま呼ぶ・false = 新設
関数）。presets 4 件（neutral/warped/ritsu_neutral/pjs_neutral）はいずれも
`phase_reset_per_note: true` のため不変。

**テスト**: `test_adapter_perf_genes.py` に 5 本追加（連続位相版はノート
境界を跨いで位相がズレ 2 ノート目の変調パターンが 1 ノート目と一致しない
ことの直接確認・onset ランプ完了後の区間で絶対時間 sin 値と厳密一致する
ことの検算・`phase_reset_per_note=True` 明示指定が省略時デフォルトと
bit 完全一致することの確認・`False` が `True` と実際に異なる出力を生む
ことの確認・`False` 自体の決定論確認）。

**影響**: **presets（true のまま）は bit 不変**——`git diff` で
`_vibrato_cents_phase_reset` 本体が 1 行も変更されていないこと、
`build_perf_f0` の true 分岐が同じ関数を同じ引数で呼ぶことを確認済み
（コード差分による証明）。`test_build_perf_f0_phase_reset_true_matches_default_bit_exact`
で `np.array_equal` により直接裏取りした（`phase_reset_per_note` を省略
した場合と明示的に `True` にした場合の f0 配列が bit 完全一致）。実 ritsu
voicebank/vocadito 資産は本セッション環境に存在しない（`results_f1_4` の
scratchpad 成果物は前回セッション限りの非コミット資産のため）ため、
実 WAV による再生成比較は未実施——上記のコード差分証明 + 単体テストの
bit-exact 検証が本ラウンドでの音への影響確認の根拠。

### 3. `donor_bank.py:503`「Preserve bank statistics across NPZ cache hits」（P2・`r3789495247`・採用）

**検証**: `_load_cache()` は npz から `DonorBank` を再構築する際、`stats`
を `dict(cache_hit=True)` だけで作り直しており、初回ビルド時の
`n_units_kept`/`n_dropped_short`/`n_clipped_to_donor_range`（または
energy-valley フォールバック側の `n_voiced_runs`/`n_split_ops` 等）が
cache hit のたびに丸ごと消えていた（該当コード確認済み）。

**修正**: `_save_cache()` で `bank.stats` を `json.dumps(..., sort_keys=True)`
で直列化し `stats_json` として npz へ追加保存する。`_load_cache()` は
これを `json.loads` で復元し `cache_hit` フィールドのみを `True` へ
上書きする（他フィールドは初回ビルド時の値のまま）。npz レイアウト変更
（`stats_json` フィールド追加）に伴い `_cache_key()` のスキーママーカーを
`content-hash-v1` → `content-hash-v2` へ更新した（`stats_json` を持たない
旧キャッシュとの意味不一致な衝突を避ける・旧キャッシュは初回 miss として
扱われ透過的に再計算される）。

**テスト**: `test_adapter_donor_bank.py` に 1 本追加
（`test_build_donor_bank_cache_hit_preserves_stats`: 初回ビルドと
cache hit の `stats` が `cache_hit` フィールドを除いて完全一致すること、
かつ空 dict へ縮退していないことを直接検証）。

**影響**: 音・bit には影響しない（npz へ `stats_json` フィールドが増える
のみで `f0`/`sp`/`ap`/unit 境界などの音声成分は無変更。schema マーカー
更新により既存 npz キャッシュは初回 miss で透過的に再計算されるが、
再計算結果は既存ロジック無変更のため bit 一致する）。

### 4. `donor_bank_utau.py:565`「Fail when a selected UTAU WAV is missing」（P2・`r3789495252`・採用）

**検証**: `build_donor_bank_utau_vcv`/`build_donor_bank_utau`（v1）の
prepass は、選択された wav ファイルが実在しない場合 `if wav_path.exists():
... `（else 節なし）で黙って `wav_bytes_by_file` への追加をスキップして
いた一方、選択段の `sel_stats`（coverage 報告）はそのまま使われ続ける
ため、部分コピー・破損 voicebank で「文脈は充足済み」という偽の
成功報告のまま `select_vcv_units()` が near/global の誤 mora unit へ
静かにフォールバックしうる実害を確認した（PJS `_song.wav` 欠落チェック
（R6）と非対称だった）。

**修正**: v1/v2 双方の prepass ループで欠落 wav ファイル名を
`missing_wav: List[str]` へ収集し、prepass 完了後（cache-key 計算より
前）に 1 件でもあれば `FileNotFoundError` を送出する（PJS
`build_donor_bank_lab` の `_song.wav` 欠落チェックと対称の fail-closed）。

**テスト**: `test_adapter_donor_bank_utau.py` に 2 本追加（v1/v2 それぞれ、
oto.ini はあるが対応 wav が存在しない fixture で `FileNotFoundError` +
違反ファイル名がエラーメッセージに含まれることを確認）。

**影響**: 音・bit には影響しない（欠落を検出した場合のみ新たに
`FileNotFoundError` を送出するガード追加であり、全 wav が揃った
既存の正常系 voicebank 構成では prepass ループの `else` 節が発火せず
既存の read/分析経路をそのまま通過する）。

### 5. `donor_bank_utau.py:697`「Bind the published bank digest to WAV paths」（P2・`r3789495249`・採用・PJS 側も対称是正）

**検証**: `build_donor_bank_utau_vcv`/`build_donor_bank_utau`（v1）が公開
する `bank.wav_sha256` は `wav_sha_parts: List[str]`（裸の digest）を
`sorted()` してから `"|".join()` するだけで、どのファイルがどの digest
かを一切保持していなかった（該当コード確認済み。§ cache-key 側は R2
（`r3789400805` ファミリー）で既に (相対パス, digest) ペア化済みだったが、
公開用の `wav_sha256` だけ旧方式のまま取り残されていた）。UTAU 音源は
pitch dir 間で同名 wav ファイル（例: 各音階フォルダの `_あ.wav`）が
頻出する構成のため、ファイル名を保ったまま 2 ファイルが中身を入れ替えても
digest の**集合**は不変で `wav_sha256` が変化しない——render が表示する
donor 識別子が偽の一致を報告しうる実害を確認した。donor_bank_lab.py
（PJS）の `wav_sha256` も同型の裸 digest sorted-join だったため、対称的に
是正した（PJS はファイル名が `pjsNNN_song.wav` で一意だが、パスを保持
しない限り「2 ファイル間の内容入れ替え」自体は名前の一意性と無関係に
検知不能なままだった）。

**修正**: 3 builder（UTAU v1/v2・PJS lab）とも `wav_sha_parts: List[str]`
を `wav_sha_pairs: List[Tuple[str, str]]`（`(相対パス, digest)`）へ変更し、
公開時に既存の `donor_bank.aggregate_content_hash`（R2 で cache-key 側に
導入済みの同じ関数）へ渡すよう統一した。npz/pickle 内の
`bank.wav_sha256` フィールドの値そのものが変わる変更のため、3 builder
すべてのキャッシュキーのスキーママーカーを更新した（UTAU v2:
`f1.4-vcv-content-hash-v3`→`v4`、UTAU v1:
`f1.3-overlap-preutt-content-hash-v3`→`v4`、PJS lab:
`content-hash-v2`→`v3`）。

**presets pin への影響確認**: `presets/ritsu_neutral.json` の
`voicebank_sha256` / `presets/pjs_neutral.json` の `corpus_sha256` は
本修正が触れた `bank.wav_sha256`（分析対象バンクの識別子・record/CLI
表示専用）とは**別の関数**（R4 で実装済みの
`voicebank_identity_hash`/`corpus_identity_hash`。いずれも
`donor_bank.aggregate_content_hash` を経由し、選択前の全 wav を対象と
する設計で元々 (相対パス, digest) ペア方式）で計算されており、
`_validate_spec_donor` の provenance 照合はこちらのみを使う。両関数の
定義を確認した結果、本修正は provenance pin の計算経路に触れておらず、
**presets の pin 再計算は不要**（実測ではなくコード読解による確認——
該当関数群の実装がこの結論を裏付ける独立設計であることをソースで
直接確認した）。

**テスト**: `test_adapter_donor_bank_utau.py` に 2 本
（UTAU v1/v2 それぞれ、2 pitch dir に同名 `_test.wav`（内容は異なる）を
配置し、内容を入れ替えると `wav_sha256` が変化することを確認）、
`test_adapter_donor_bank_lab.py` に 1 本（2 つの `pjsNNN_song.wav` 間で
内容を入れ替えると `wav_sha256` が変化することを確認）を追加した。

**影響**: 音・bit には影響しない（`f0`/`sp`/`ap`/unit 境界などの音声成分
は無変更。公開される `wav_sha256` の**値**は変わる——record/CLI の
provenance 表示が変化する——が、これは本修正の意図どおりの変更であり
音声波形には影響しない。schema マーカー更新により既存キャッシュは
初回 miss で透過的に再計算される）。

### テスト・lint・回帰確認（R9）

- `ruff check .`（リポジトリ全体）: clean
- `python -m pytest voice_genesis/foundry/tests -q`: **318 件、全 green**
  （R8 時点の 302 本 + 新規 16 本。内訳: render_donor_wiring +5 本
  （#1）/ perf_genes +5 本（#2）/ donor_bank +1 本（#3）/
  donor_bank_utau +4 本（#4×2 + #5×2）/ donor_bank_lab +1 本（#5））
- `python -m pytest voice_genesis/singer/tests -q`: 38 本、非破壊確認済み
- `src/svp_rpe/**`・`pyproject.toml` は無変更（本体スイートへの影響なし）
- **音への影響**: 5 件とも bit 不変と判定した。#1/#3/#4/#5 は既存の
  正常系（対応値 "recorded"・全 wav 揃った voicebank）では新規コードパス
  （エラーガード・cache 直列化・digest 集約方式）が発火しないか、または
  発火しても音声成分（f0/sp/ap/units）に触れないことをコード差分で確認
  した。#2 は presets 全件が `phase_reset_per_note: true` のままのため
  コード差分（`_vibrato_cents_phase_reset` 本体無変更）+ 単体テスト
  （`np.array_equal` bit-exact）の 2 経路で裏取りした。実 ritsu
  voicebank/vocadito/PJS 資産が本セッション環境に存在しないため
  （前回セッションの scratchpad 成果物は非コミット・引き継ぎ対象外）、
  実 WAV ファイルによる sha256 再検証は本ラウンドでは実施していない
  （上記のコード差分証明 + 単体テストが根拠）。

## Codex R10 対応（残 3 スレッド・全採用）

### 1. `voice_spec.py:95`「Reject nonpositive formant scales」（P2・`r3789520249`・採用）

**検証**: `_validate_warp` は `formant_scale` を含む warp フィールド全てへ
`_require_finite_float` を適用するだけで、0・負値は「有限数」であるため
素通りしていた（該当コード確認済み）。`apply_warp` → `freq_warp` の
`src_freqs = freqs / scale` はスケールを分母に使うため、`scale=0` は
周波数格子をゼロ除算して DC ビンに NaN を注入し、`scale<0` は
`src_freqs` を全て負値（`np.interp` の `left=sp[i,0]` 境界）へ落とし
込み、スペクトル全体を DC ビンの値へ折り畳んで崩壊させる。いずれも
検証を通過した「有効な」spec から破損 WAV が生成・公開される実害を
確認した。

**修正**: `_validate_warp` で有限数チェック後、`formant_scale` が
キーに含まれる場合のみ `> 0` を要求し、0/負値を `ValueError` で
fail-closed 拒否する。

**voice_spec 全域の域検証監査（ついで確認・実害基準で選別）**:

| フィールド | 使用箇所 | 域外値の挙動 | 判定 |
|---|---|---|---|
| `warp.formant_scale` | `freq_warp`（分母） | 0=ゼロ除算 NaN、負=スペクトル崩壊 | **実害あり → 追加** |
| `warp.tilt_db_oct` | `spectral_tilt`（log2 gain の指数） | 任意の有限値で式が成立、クラッシュ/NaN なし | 実害なし（既存 finite チェックで十分） |
| `warp.breath_lift` | `breath_lift`（`ap+lift` を `[0,1]` へ clip） | 常に clip 済み・NaN/inf なし | 実害なし |
| `perf.vibrato.rate_hz` / `depth_cents` | `perf_genes._vibrato_cents_phase_reset` 系（`sin` の引数/係数） | 負値は位相反転相当、破綻なし | 実害なし |
| `perf.drift.rate_hz` | `perf_genes.build_perf_f0`（`block_sec = 1.0/(2.0*rate)`） | `max(rate_hz, EPS)` で呼び出し側が既にゼロ/負をクランプ済み | 実害なし（既存ガードで十分） |
| `perf.drift.depth_cents` / `jitter_cents` | `_iid_block_walk_cents` | `depth_cents<=0` は早期 `return zeros`（意図的 no-op） | 実害なし |
| `perf.onset_glide.time_ms` / `perf.vibrato.onset_ms` | `_onset_glide_cents` 等（`sr*ms/1000` からフレーム長算出） | `max(int(round(...)), 1)` で常に 1 以上へクランプ | 実害なし |
| `perf.portamento_ms` | `singer/performance.build_f0_contour`（ポルタメント窓） | 負値は `hi<=lo` となり `continue`（ポルタメント無効化のみ、クラッシュ/NaN なし） | 実害なし |

`formant_scale` 以外は全て使用箇所側で既にクランプ・早期リターン・
no-op により安全に処理されており、同型の実害（NaN 注入・スペクトル
崩壊・クラッシュ）が確認できなかったため、`formant_scale` のみ追加した
（全数追加による over-engineering を避ける・実害基準の選別方針
（AGENTS.md §3）に従う）。

**テスト**: `test_adapter_voice_spec.py` に 2 本追加
（`formant_scale=0.0` / `formant_scale=-0.5` それぞれで `ValueError` を
確認）。

**影響**: 音・bit には影響しない（既存 4 presets は全て `formant_scale`
が正の値（1.0 / 0.96）であり、新規ガードは発火しない。
`test_all_presets_pass_validation` で非破壊を確認）。

### 2. `donor_bank_lab.py:273`「Validate coverage from usable PJS morae」（P2・`r3789520243`・採用）

**検証**: `_select_lab_files` の coverage 判定は
`tokens = {p.phoneme for p in phonemes}` という生ラベルトークン集合の
交差演算のみで行われ、`group_lab_to_morae`（実際の unit/clip 化ロジック）
を一切通していなかった（該当コード確認済み）。ある .lab に dangling な
子音トークン（直後が `pau`/`xx` で母音へ接続しない、またはファイル終端で
接続しない）が含まれる場合、そのトークンは `group_lab_to_morae` では
`n_dangling_onset_dropped` として破棄され mora を生成しないにも
関わらず、`_select_lab_files` は raw token 集合演算だけで「covered」と
記録してしまう。この結果、貪欲選択が「その音素は既に covered」と誤認し、
同じ音素を実際に使用可能な形（子音+母音の mora）で持つ後続ファイルを
skip しうる——`build_donor_bank_lab` では該当子音の `ConsonantClip` が
1 件も生成されず、render 時に合成子音へ静かにフォールバックする実害を
確認した。

**修正**: `_select_lab_files` 内の coverage 算出を、新設した
`_usable_onsets_and_vowels`（`group_lab_to_morae` でグルーピングした
morae のうち、`build_donor_bank_lab` と同じ条件——子音:
`c_end - c_start >= 1` フレーム、母音核: `v_end - v_start >=
MIN_UNIT_FRAMES_ABS` フレーム——を満たす区間のみを対象とする）ベースへ
置き換えた。フレーム数は時刻（`_ms_to_frame`）のみから算出するため、
音声デコード不要という `_select_lab_files` の既存設計（docstring 冒頭）は
維持される。`_select_lab_files` に `frame_period_ms` 引数を追加し、
`build_donor_bank_lab` から実際に使う `frame_period_ms` を渡すよう
配線した（旧実装はこの引数を持たず、`_ms_to_frame` のフレーム換算に
使う周期を選択段では考慮していなかった）。

**テスト**: `test_adapter_donor_bank_lab.py` に 1 本追加
（`test_select_lab_files_dangling_onset_does_not_mask_later_usable_onset`:
先行 pjs001 が `s` を dangling としてのみ含み・別の mora で母音 `a` を
覆う fixture、後続 pjs002 が実際に使用可能な `s`+`a` mora を持つ
fixture を用意し、pjs002 が選択され `covered_onsets` に `s` が
含まれることを確認。新実装前のロジック（raw token 交差）では pjs001 の
dangling `s` と正規 mora の `a` だけで新規カバレッジなしと判定され
pjs002 は選択されない設計であることをコード読解で確認済み）。

**影響**: 音・bit には影響しない（グルーピング後も raw token と同じ
音素集合が「使用可能」と判定される正常系 .lab（子音が必ず母音へ
正しく接続する整形済み PJS コーパス）では、新旧の coverage 判定結果は
一致し選択されるファイル集合・順序は変わらない。
`test_build_donor_bank_lab_end_to_end` の bit-exact 再構築確認
（`np.array_equal(bank.sp, bank2.sp)`）と全 323 本の foundry テスト green
で非破壊を確認した）。

### 3. `donor_bank_utau.py:358`「Reject lexical traversal before containment checks」（P2・`r3789520235`・採用）

**検証**: `_reject_wav_paths_outside_root`（R8 実装）は `.resolve()` した
上で解決済み voicebank root 配下に収まっているかのみを検査していた。
`A3/../F4/foo.wav` のような相対パスは、`A3`（root 内の別 pitch dir）を
経由して `..` で 1 階層上がり `F4` へ入るため、resolve 後も voicebank
root 配下に収まる——root 単位の包含検査は通過するが、`A3` の
annotation（oto.ini）が `F4` の録音音声を分析してしまう pitch dir
脱出という実害があることを確認した。同様に、voicebank root 内の別
ファイルを指す絶対パスも resolve 後の root 包含検査だけでは通過して
しまう（意図せぬ pitch dir 混線を招く点は同じ）。

**修正**: `.resolve()` を呼ぶ前に、各 `wav_filename` を語彙的
（`pathlib.Path` の構成要素のみで、シンボリックリンク解決を伴わずに）
検査する行程を追加した。絶対パス（`Path.is_absolute()`）または `..`
成分（`".." in Path(...).parts`）を含む場合は即座に拒否し、resolve
ベースの root 包含検査（既存 R8 実装）へは進まない。

**テスト**: `test_adapter_donor_bank_utau.py` に 2 本追加
（`test_reject_wav_paths_outside_root_cross_pitch_dir_traversal`:
`A3/../F4/foo.wav`（root 内・`F4` に実ファイルあり）で `ValueError`。
`test_reject_wav_paths_outside_root_absolute_path_inside_root`:
voicebank root 内の実在ファイルを指す絶対パスで `ValueError`）。

**影響**: 音・bit には影響しない（`..` 成分も先頭 `/` も含まない通常の
ファイル名（既存 voicebank の oto.ini が参照する形式）は新設の語彙
チェックを素通りし、既存の resolve ベース検査へそのまま進む。
`test_reject_wav_paths_outside_root_accepts_in_bounds_files` で
過検知なしを確認した）。

### テスト・lint・回帰確認（R10）

- `ruff check .`（リポジトリ全体）: clean
- `python -m pytest voice_genesis/foundry/tests -q`: **323 件、全 green**
  （R9 時点の 318 本 + 新規 5 本。内訳: voice_spec +2 本（#1）/
  donor_bank_lab +1 本（#2）/ donor_bank_utau +2 本（#3））
- `python -m pytest voice_genesis/singer/tests -q`: 38 本、非破壊確認済み
- `src/svp_rpe/**`・`pyproject.toml` は無変更（本体スイートへの影響なし）
- **音への影響**: 3 件とも bit 不変と判定した。#1 は既存 4 presets が
  全て `formant_scale>0` のため新規ガードが発火しない
  （`test_all_presets_pass_validation` で確認）。#2 は整形済み PJS
  コーパス（子音が必ず母音へ正しく接続）では新旧 coverage 判定が一致し
  選択ファイル集合が変わらないことを bit-exact 再構築テストで確認。
  #3 は正常な相対ファイル名（`..`/絶対パスを含まない）では新設の
  語彙チェックが素通りし既存経路をそのまま通ることを単体テストで確認。
  実 ritsu voicebank/vocadito/PJS 資産が本セッション環境に存在しない
  ため、実 WAV ファイルによる再検証は本ラウンドでは実施していない
  （上記のコード差分証明 + 単体テストが根拠）。

## Codex R11 対応（残 3 スレッド・全採用・最終掃き）

### 1. `results_f1c/glue_transplant.py:28` 他「Parameterize the committed experiment recipes」（P2・`r3789543161`・採用）

**検証**: `results_f1a/`・`results_f1b/`・`results_f1c/` 配下の glue スクリプト
11 本すべてに、本セッションコンテナ固有の絶対パス（`/home/user/ugh-prompt-engine/
voice_genesis/...` の repo import・`/tmp/claude-0/.../scratchpad/foundry_*` の
入出力パス・`glue_sawsing.py`/`glue_golf_ismir.py` の `/workspace/...` 外部
依存クローンパス）がハードコードされていることを確認した。他コンテナでは
これらのパスが存在せず、`results_f1c/f1c_record_2026-08-15.md` 等が実験の
接着コードとして提示するスクリプトが再実行不能だった。

**修正**: 対象 11 本（`results_f1a/glue_control.py` /
`glue_golf_ismir.py` / `glue_pyworld.py` / `glue_sawsing.py`、
`results_f1b/glue_template.py` / `glue_template_nn.py` /
`glue_template_nn_v2.py` / `select_donor.py` / `analyze_h1.py`、
`results_f1c/glue_transplant.py` / `make_note_tracks.py`）を以下の方針で
パラメータ化した:

- **リポジトリ import**（`voice_genesis/singer` 等 3 モジュール・
  `glue_template_nn_v2.py`/`analyze_h1.py` の同ディレクトリ
  `glue_template_nn` import）は `Path(__file__).resolve().parents[N]`
  相対で解決するよう書き換えた（`_REPO_VOICE_GENESIS` 等の中間変数を
  module-level import の直前に置くと ruff E402 に抵触するため、
  `sys.path.insert` の引数へ式をインライン展開する形にした）。
- **入出力パス**（`OUT`/`DONOR_WAV`/`CONTROL_NPZ`/`ROOT` 等の定数）は
  すべて argparse の必須引数（既定値なし）へ置き換えた
  （`--out`/`--donor-wav`/`--control-npz`/`--root`）。
- **外部依存クローン**（`glue_sawsing.py` の
  yatingmusic/ddsp-singing-vocoders、`glue_golf_ismir.py` の
  yoyololicon/golf — いずれも本リポジトリの一部ではない）は
  `--ddsp-dir`/`--golf-dir` 必須引数化し、それらに依存する import
  （`ddsp.modules`/`ltng.ae` 等）を argparse 解析後の `main()` 内へ
  遅延させた（モジュールレベルで無条件 import すると `--help` 実行時にも
  存在しない外部リポジトリの import が走ってしまうため）。
- **歴史的記録の一次性**: `results_f1a/f1a_raw.md`・
  `results_f1b/f1b_raw.md`・`results_f1c/f1c_raw.md` および各
  `*_record_2026-08-15.md`（実行時の生ログ・数値・判定）は無改変。
  各スクリプト冒頭 docstring に「R11 で再現可能化のためパスをパラメータ化
  （元の実行は record 記載の環境で実施）」の 1 行を追記し、来歴を保った。

**動作確認**: 実データ（vocadito/GOLF checkpoint/ddsp-singing-vocoders
クローン等）は本セッション環境に存在しないため、指示どおりモジュール
import + `--help` 出力までを確認した。11 本すべてで `python <script>.py
--help` が exit code 0・想定の必須引数一覧（`--out` 等）を表示することを
確認済み（`glue_sawsing.py`/`glue_golf_ismir.py` は `--help` 経路が
argparse の early-exit で外部依存 import へ到達しないことも合わせて確認、
実データによる再実行は本ラウンドでは対象外）。

### 2. `scripts/measure_bands.py:187`「Emit standards-compliant JSON for unvoiced inputs」（P2・`r3789543163`・採用）

**検証**: 無声入力（有声フレームが 1 つも HNR 閾値を通過しない）で
`hnr_median_db()` は仕様どおり NaN を返し、`_round_floats()` はこれを
そのまま保持していた。`main()` の `json.dumps(results, ...)` は
`allow_nan`（既定 True）のため、この NaN が非標準トークン `NaN` として
JSON へ出力され、strict な JSON パーサでの readback が失敗していた
（実測: 全ゼロ WAV では `crest_db`（`peak=0` で `log10(0)` = `-inf`）も
同時に非標準 `-Infinity` を出力していたことを新規テストで確認）。

**修正**: `_round_floats()` を「NaN/Inf を含む非有限 float を `None`
（JSON `null`）へ正規化する」よう変更し（`np.isfinite` 判定）、
`main()` の `json.dumps` 呼び出しに `allow_nan=False` を追加した
（`_round_floats` を経由しない値が将来混入しても fail-fast する二重防御）。

**テスト**: `test_measure_bands.py` に 4 本追加
（`test_round_floats_normalizes_nan_to_none`・
`test_round_floats_normalizes_inf_to_none`（単体、NaN/Inf 双方を確認）・
`test_silent_input_report_json_is_standards_compliant_and_readback_parses`
（無声 WAV → `analyze_wav` → `_round_floats` → `json.dumps(allow_nan=False)`
→ `json.loads` の readback が例外を出さず `hnr_median_db is None` を確認）・
`test_main_e2e_silent_wav_writes_readback_parseable_json_with_null_hnr`
（CLI `--out` 経由の E2E。書き出した実ファイルを `json.loads` で readback
し `null` を確認））。既存 20 本と合わせて計 24 本 green。

**影響**: 音・bit には影響しない（`measure_bands.py` は計測専用 CLI で
音声を生成しない。有声入力では `hnr_median_db`/`crest_db` とも有限値の
ため `_round_floats` の丸め挙動は既存と同一——`np.isfinite(v)` が真の分岐
は従来の `round(v, ndigits)` のまま。既存 20 本のテストで非破壊を確認）。

### 3. `adapter/render.py:768`「Protect donor caches from output replacement」（P2・`r3789543157`・採用）

**検証**: `_reject_output_collision` の `protected_roots` に
`voicebank_root` は含まれるが `cache_dir` は含まれておらず、`--out` が
`--cache-dir` 配下の生成物（`donor_bank_*.npz`/`utau_bank_*.pkl`/
`lab_bank_*.pkl`）を指す場合、cache-hit render はそのキャッシュを読み込んだ
後に WAV バイト列で atomic 置換してしまう（次回同一 render はそのキャッシュ
をデコードしようとして失敗する）ことをコード読解で確認した。

**修正**: `render()` 内の `_reject_output_collision` 呼び出しの
`protected_roots` へ `cache_dir` を追加した（`[voicebank_root, cache_dir]`）。
`_reject_output_collision` 自体は既存の resolved containment 検査
（symlink 解決後の包含判定・`None`/未存在パスはスキップ）をそのまま再利用
するため、関数側の実装変更は不要だった。

**テスト**: `test_adapter_render_donor_wiring.py` に 4 本追加
（`test_reject_output_collision_out_inside_cache_dir_raises`:
`--out` が cache_dir 配下の未生成パスを指すケース。
`test_reject_output_collision_out_equals_existing_cache_file_raises`:
既存キャッシュファイルへの完全一致。
`test_reject_output_collision_cache_dir_none_is_skipped`:
`--cache-dir` 省略（`None`）時に誤検知しないことの対照ケース。
`test_render_reject_output_collision_call_includes_cache_dir_root`:
`render()` 本体が `_reject_output_collision` へ `cache_dir` を実際に
配線していることのソースレベル回帰ガード——完走 E2E は WORLD 分析込みで
重く配線 1 行の検証には不釣り合いなため、直接呼び出しテスト側で衝突判定
ロジック自体を検証したうえでの軽量な配線確認とした）。

### テスト・lint・回帰確認（R11）

- `ruff check .`（リポジトリ全体）: clean
- `python -m pytest voice_genesis/foundry/tests -q`: **331 件、全 green**
  （R10 時点の 323 本 + 新規 8 本。内訳: render donor_wiring +4 本（#3）/
  measure_bands +4 本（#2））
- `python -m pytest voice_genesis/singer/tests -q`: 38 本、非破壊確認済み
- `src/svp_rpe/**`・`pyproject.toml` は無変更（本体スイートへの影響なし）
- **音への影響**: #1 は committed 実験スクリプトのパラメータ化のみで
  音声生成ロジック自体は無改変（decibel/frame/WORLD 演算の式は一切
  変更していない）。#2 は計測専用 CLI で音声非生成。#3 は
  `protected_roots` へ要素を 1 つ追加しただけで既存の衝突判定ロジック
  （`_reject_output_collision`）自体は無改変のため、`--cache-dir` を
  指定しない既存呼び出し・`--out` がキャッシュと衝突しない既存呼び出しは
  従来どおり通過する（新規対照テストで確認）。
  3 件とも bit 不変と判定した。
- R11 で本 PR #262 のレビュースレッドは既知の境界宣言 1 件
  （R6「Bind provenance to the analyzed donor snapshot」・不採用・
  実装コスト対効果で見送り済み）を除き全数対応済み。

## Codex R12 対応（残 2 スレッド・coverage ファミリー終端宣言）

### 1. `donor_bank_utau.py:500`「Count only usable UTAU entries as covered」（P2・`r3789559165`・採用）

**検証**: `_select_wav_subset_for_contexts`（VCV 選択段）は候補 wav の
エイリアス文字列が要求文脈と一致した時点で即座に `remaining` から
その文脈を除去していた。区間の妥当性（`[offset, cutoff)` が
`MIN_UNIT_FRAMES_ABS` 未満に丸まらないか）は本体 `build_donor_bank_utau_vcv`
のメインループ（690–694 行）でしか検証されず、そこで棄却（`n_dropped_short`）
されても選択段の「発見済み」判定は訂正されなかった。同じ文脈を持つ
後続の有効な wav が存在しても、選択が終わった時点で `remaining` から
既に消えているため二度と候補にならず、`selection_stats_by_dir` は
covered のまま・`units.select_vcv_units` は near/global フォールバックへ
静かに縮退しうる、という実害を確認した。

**修正**: 個別パッチではなく設計判定どおりの一般解を実装した。

- `_probe_wav_duration_ms`（`soundfile.info` によるヘッダのみ probe。
  フルデコード不要）+ `_oto_entry_is_usable`（本体のフレーム区間クランプを
  実 WORLD 分析前の wav 実長だけで近似再現）を新設し、
  `_select_wav_subset_for_contexts` に `pdir` 引数（既定 `None`=後方互換）
  を追加した。`pdir` が渡された場合、候補エントリを「発見済み」として
  `remaining` から除去する前にこの前倒し検証を通す（不正な先行エントリが
  後続の有効な wav を遮らなくなる）。`pdir=None`（合成 fixture の単体
  テスト用）は従来どおり文字列一致のみで判定する。
- **終端の本体**: coverage は選択時の約束ではなく、実際に構築できた
  unit から導出し直す。`build_donor_bank_utau_vcv` の全 pitch dir 分析
  完了後（正規化/クリップの棄却が確定した後）に新設
  `enforce_realized_context_coverage` を呼び、`unit_contexts`（実際に
  構築された unit の文脈集合）を `required_contexts` と照合する。不足が
  あれば近傍/global フォールバックへ黙って縮退させず、不足文脈を列挙した
  `ValueError` で fail-closed する（cache-hit 経路でも同じ検証を通す）。
  `stats["realized_coverage"]` に選択時 coverage（`selection_stats_by_dir`）
  とは別に realized coverage を保持し、分母を隠さない。
- v1（`build_donor_bank_utau`）にも対称の `required_onsets`/`required_vowels`
  引数（既定 `None`）+ `enforce_realized_onset_vowel_coverage`（onset/vowel
  版・v1/PJS 共通ヘルパー）を追加し、3 銀行（UTAU v1/v2・PJS）で同一の
  終端方針を統一した（§ 下記「coverage ファミリー終端宣言」）。

**テスト**: `test_adapter_donor_bank_utau.py` に 6 本追加。

- `test_build_donor_bank_utau_vcv_frontload_skips_invalid_entry_selects_later_valid_wav`
  （指摘のシナリオそのもの: ファイル名昇順で先の wav が不正/短小区間の
  エントリしか持たず、後の wav が同じ文脈を有効な区間で持つ fixture。
  後続 wav のみが選択・分析され、実際に unit が構築されることを確認）。
- `test_build_donor_bank_utau_vcv_frontload_deterministic_repeat`（同一
  fixture の再構築で bit 一致）。
- `test_build_donor_bank_utau_vcv_realized_coverage_matches_selection_when_valid`
  （正常系: 選択時 coverage と realized coverage が完全一致）。
- `test_build_donor_bank_utau_vcv_fails_closed_when_required_context_unreachable`
  （voicebank 内のどこにも存在しない必須文脈で `ValueError` + 列挙）。
- `test_build_donor_bank_utau_required_onsets_vowels_pass_when_satisfied` /
  `test_build_donor_bank_utau_required_onsets_fails_closed_when_missing`
  （v1 builder でも同じ enforcement が機能することを確認）。

既存の `test_build_donor_bank_utau_vcv_required_contexts_change_cache_key`
は fixture が片方の `required_contexts`（`("a", "か")`）を実際には
提供していなかったため、新設の fail-closed 検証で正しく落ちるように
なった（旧実装はこの不整合を検出できていなかった）。fixture へ両文脈分の
oto エントリを追加し、テストの本来の意図（cache key の独立性）はそのまま
維持した。

**影響**: 音・bit には影響しない（前倒し検証は「選択の質」を上げる
フィルタであり、有効な区間しか選ばれない正常系の voicebank では旧実装と
同じ wav 集合・同じ unit が選ばれる。既存 71 本 + 新規 6 本 green で
非破壊を確認した）。

### 2. `donor_bank_lab.py:262`「Validate PJS coverage against the decoded WAV length」（P2・`r3789559167`・採用）

**検証**: R10 是正後の `_usable_onsets_and_vowels` は
`group_lab_to_morae` のグルーピング結果からフレーム長を判定するように
なっていたが、ペア `_song.wav` の実長は一度も参照していなかった。
切詰め/破損 `_song.wav`（EOF が .lab の宣言区間より前にある）に対しては、
EOF を越える区間がそのまま usable と報告され、貪欲選択が同じ音素を
実際に使用可能な形で持つ後続ファイルを skip しうる。`build_donor_bank_lab`
本体は実 WORLD 分析後の `n_donor_frames` でクランプするため、該当区間は
0 フレームへ潰れて unit/clip を作らないが、coverage 報告（`selection_stats`）
は成功のまま——という R6/R10 と同型の実害を確認した。

**修正**: `donor_bank_utau._probe_wav_duration_ms` と対称の
`_probe_wav_duration_s`（`soundfile.info` ヘッダのみ probe）を新設し、
`_usable_onsets_and_vowels` に `wav_duration_s`（既定 `None`=後方互換）
引数を追加した。渡された場合、境界フレームをその実長でクランプしてから
usable 判定する。`_select_lab_files` は候補ごとに対応する
`<stem>_song.wav` の実長を probe し、この引数へ渡すよう配線した。

**終端の本体**: `donor_bank_utau.py` §1 と同一の一般解。
`build_donor_bank_lab` に `required_onsets`/`required_vowels`（既定
`None`）を追加し、build 完了後に
`donor_bank_utau.enforce_realized_onset_vowel_coverage`（v1/PJS 共通
ヘルパー）で realized coverage（実際に構築できた `consonant_clips`/
`unit_vowels`）を要件と照合、不足があれば列挙付き `ValueError` で
fail-closed する（cache-hit 経路でも同じ検証を通す）。`render.py` の
PJS 分岐から `required_onsets=dbu.REQUIRED_ONSETS,
required_vowels=dbl.VOWELS_5`（従来から `_select_lab_files` が貪欲被覆の
target にしていた固定集合と同一）を渡すよう配線し、実運用でも
fail-closed が効くようにした。

**テスト**: `test_adapter_donor_bank_lab.py` に 4 本追加。

- `test_select_lab_files_truncated_wav_does_not_mask_later_valid_recording`
  （指摘のシナリオそのもの: pjs001 が宣言する母音区間が切詰め wav の EOF を
  越え、pjs002 が同じ母音を実際にフルレングスで収録している fixture。
  pjs002 が新規カバレッジとして選択されることを確認）。
- `test_build_donor_bank_lab_truncated_wav_fails_closed_with_missing_enumerated`
  （切詰め wav しか存在しないコーパスで `required_vowels=["o"]` を渡すと
  `ValueError` + `missing_vowels=['o']` の列挙で fail-closed）。
- `test_build_donor_bank_lab_realized_coverage_matches_selection_when_valid`
  （正常系: 必須要件と realized coverage が完全一致）。
- `test_build_donor_bank_lab_deterministic_repeat_with_required_coverage`
  （enforcement 込みの経路でも再構築で bit 一致）。

**影響**: 音・bit には影響しない（`wav_duration_s=None` の既存呼び出し
（合成 fixture の単体テストの大半）は従来どおり未クランプ判定。正常系
（宣言区間が実際の wav 長に収まる整形済み PJS コーパス）ではクランプが
発火せず判定結果は不変。既存 23 本 + 新規 4 本 green で非破壊を確認した）。

### coverage ファミリー終端宣言

R6（PJS `_song.wav` 欠落 fail-closed）・R9（UTAU 選択済み wav 欠落
fail-closed）・R10（PJS dangling トークンの coverage 誤認）は、いずれも
「選択時に立てた coverage の約束」と「実際に構築できた unit/クリップ」の
どこかの一段が食い違いうるという**同型の穴**を個別パッチで塞いだもの
だった。本 R12 は個別パッチの延長ではなく、**coverage を選択時の約束
からではなく実際に構築できた realized units から導出し直す**という
一般解へ 3 銀行（UTAU v1・UTAU v2/VCV・PJS）を統一した——正直会計として
明記すると、本対応は R6/R9/R10 が塞いだ個別の穴を包含する上位の構造
修正であり、それらの再発防止を構造的に保証する（selection 段の
前倒し検証は「有効な後続候補を見つけやすくする」ための最適化に過ぎず、
正しさの最終的な担保は build 完了後の realized coverage 再計算が
fail-closed backstop として一元的に持つ）。

この構造の下では、「選択時は covered と報告されたが、後段の正規化/
クリップで棄却されて realized には存在しない」という乖離パターン自体が
構造的に不可能になる——coverage の合否判定が実現された unit/クリップの
集合そのものから導出される以上、選択時の見立てが最終判定に影響しない
ため、同型の指摘は本構造の範囲では原理的に再発しない。

### テスト・lint・回帰確認（R12）

- `ruff check .`（リポジトリ全体）: clean
- `python -m pytest voice_genesis/foundry/tests -q`: **341 件、全 green**
  （R11 時点の 331 本 + 新規 10 本。内訳: `test_adapter_donor_bank_utau.py`
  +6 本 / `test_adapter_donor_bank_lab.py` +4 本）
- `python -m pytest voice_genesis/singer/tests -q`: **38 本、全 green**（非破壊）
- `src/svp_rpe/**`・`pyproject.toml` は無変更（本体スイートへの影響なし）
- **音への影響**: 3 銀行とも新設の前倒し検証・realized coverage
  再計算は「有効な区間しか選ばれない正常系」では発火しない（クランプ・
  fail-closed とも corner case 専用の分岐）。既存 fixture（合成 wav +
  oto.ini/.lab）はすべて正常系のため、決定論・bit-exact 再構築の
  既存テストは無改変のまま green（新規 determinism テストでも追加確認）。
- R12 で PR #262 のレビュースレッドは既知の境界宣言 1 件（R6「Bind
  provenance to the analyzed donor snapshot」・不採用・実装コスト対効果で
  見送り済み）を除き全数対応済み。coverage ファミリーは本ラウンドで
  終端とする（再着手条件: selection/realized 双方の検証を回避する新規の
  乖離経路が実測で示された場合）。

## Codex R13 対応（残 3 スレッド・全採用・F-C/F-D 拡張終端）

R11 でパラメータ化した実験 glue スクリプト 11 本には、`--out` の固定出力名が
`--donor-wav`/`--control-npz` と衝突しうる穴（F-C ファミリーの再発）が残って
いた。加えて PJS コーパス（`donor_bank_lab.py`）・UTAU voicebank
（`donor_bank_utau.py`）の 2 経路で、root 配下と誤認される非 basename/
symlink 参照が provenance の対象外のまま分析されうる穴（F-D: root 包含検査
ファミリー、UTAU 側は R8/R10 で個別対応済みだったが PJS 側は未対応・UTAU 側も
非 basename ネスト参照の死角が残っていた）を確認した。

### 1. `results_f1b/glue_template.py:243` 他「Reject glue outputs that alias experiment inputs」（P1・`r3789589662`・採用）

**検証**: `--donor-wav <out>/f1b_template_neutral.wav` のように `--out` 配下の
固定出力名を `--donor-wav`/`--control-npz` へ渡すと、donor 読込自体は先に
完了するため実行は成功するが、書き込みが実験の入力そのもの（ドナー原音・
共通 f0/amp npz）を生成音声/JSON で上書きし破壊する。R11 でパラメータ化
した 11 本のうち、固定出力名と CLI 経由の任意パス入力（`--donor-wav`/
`--control-npz`）を同時に持つスクリプトでこの実害が成立することを確認した。

**修正**: `render.py`/`measure_bands.py` の `_reject_output_collision` と
同じ resolved 比較（symlink 解決後の完全一致・存在しない入力はスキップ）の
軽量実装を、実害が成立する 5 本へ各所独立追加した（record スクリプトのため
共有モジュール新設ではなく各ファイル内 `OutputCollisionError`/
`_reject_output_collision`/`OUTPUT_NAMES` のコピペ実装。判断は Design Memo
で委任済み）:

- `results_f1b/glue_template.py`（`--donor-wav`/`--control-npz` vs 5 固定出力）
- `results_f1b/analyze_h1.py`（同上 vs 1 固定出力）
- `results_f1b/glue_template_nn.py`（同上 vs 4 固定出力）
- `results_f1b/glue_template_nn_v2.py`（同上 vs 3 固定出力）
- `results_f1c/glue_transplant.py`（`--donor-wav` vs 4 固定出力）

いずれも `main()` 冒頭・`out_dir` 確定直後（donor/control の読込より前）で
全固定出力 × 全保護入力の直積を検査し、1 件でも衝突すれば fail-closed で
拒否してから return する（読込前に拒否するため、衝突時は donor-wav が
「実在する任意のファイル」でありさえすればよく、有効な WAV/npz である
必要はない）。残り 6 本（`glue_control.py`/`glue_pyworld.py`/
`make_note_tracks.py`: `--out` 以外の外部パス引数を持たない。
`glue_golf_ismir.py`/`glue_sawsing.py`: `--golf-dir`/`--ddsp-dir` は
ディレクトリ引数で、読込対象は out_dir 配下の**固定相対名**
（`f1a_control.npz` 等）のみのため CLI 経由で任意パスを固定出力名へ
衝突させる経路が存在しない。`select_donor.py`: そもそも書き込みを行わない）
は F-C の脅威モデル該当なし——詳細は下記監査表。

**テスト**: 代表 1 本 `results_f1b/glue_template.py` を
`test_glue_template.py`（新規）で検証: `--help` smoke（exit 0・必須引数
一覧の確認）+ `_reject_output_collision` 単体（衝突/無関係/symlink 解決/
未存在入力スキップ）+ `main()` 経由の CLI 実害再現（`--donor-wav`/
`--control-npz` いずれかが固定出力へ衝突するケースでドナー/control の
バイト列が無傷のまま `OutputCollisionError` で停止することを確認）。
残り 4 本は同型実装の目視 + 下記監査表（全 11 本の判定根拠を明記）で
代替する（設計判断: record スクリプトは製品コードではなく、同一パターンの
コピペ実装 4 本分の重複テストは投資対効果が低い）。

### 2. `adapter/donor_bank_lab.py:340`「Reject PJS corpus symlinks that escape the root」（P2・`r3789589663`・採用）

**検証**: `pjsNNN` ディレクトリが `corpus_root` 外を指す symlink の場合、
`corpus_identity_hash`/`build_donor_bank_lab` の `root.glob("pjs*/pjs*.lab")`
は symlink を辿って外部の `.lab` を返し、対応する `_song.wav` も外部
ディレクトリから読まれる。`render.py` の出力ガード（`_reject_output_collision`）
は `voicebank_root` 配下のみを保護するため、脱出先ドナーの実体が分析後に
上書き/差し替えされても検知されない。`corpus_identity_hash`（provenance
pin）は `.lab`/`_song.wav` を `root` からの相対パスで列挙するのみで、
resolve 後の実体が root 内かどうかを検証していなかった（該当コード確認済み）。

**修正**: `donor_bank_utau._reject_wav_paths_outside_root` と対称の resolve
包含検査（symlink 解決後 `is_relative_to(root_resolved)`）を、共通ヘルパー
`donor_bank.reject_paths_outside_root(paths, root, source_label)` として
`adapter/donor_bank.py` へ切り出した（PJS 側の候補パスは `Path.glob` が
返す実ファイルパスであり、UTAU 側のように oto.ini の未検証テキスト
フィールド由来ではないため、UTAU 版が持つ絶対パス/`..` の語彙的検査は
不要——resolve 後包含検査のみで足りる）。`corpus_identity_hash`/
`build_donor_bank_lab` それぞれで `lab_paths` glob 直後・いずれの
`.lab`/`_song.wav` も read する前に、選択対象全件（`_select_lab_files` が
候補スコアリングで read する前の全 `lab_paths` を含む）へ適用する。

**テスト**: `test_adapter_donor_bank.py` に `reject_paths_outside_root` の
単体テスト 4 本（root 内許容・未作成パスの lexical 許容・symlink 脱出拒否・
エラーメッセージへの列挙確認）。`test_adapter_donor_bank_lab.py` に
統合テスト 3 本（`corpus_identity_hash`/`build_donor_bank_lab` それぞれの
symlink 脱出拒否 + 正規ディレクトリと脱出 symlink が混在するコーパスで
部分 skip せず呼び出し全体を拒否することの確認）。

**影響**: bit には影響しない（root 内に収まる既存 fixture・実データでは
検査は常に通過するため既存挙動は無変更。異常系（symlink 脱出）のみ挙動が
「無検査で外部ファイルを read」から「ValueError で fail-closed」へ変化）。

### 3. `adapter/donor_bank_utau.py:229`「Include nested UTAU WAV references in donor identity」（P2・`r3789589664`・採用）

**設計判定**: **非 basename 参照を拒否する**（identity 側をネスト対応へ
拡張する代替案は見送り）。`voicebank_identity_hash` は各 pitch dir
**直下**の `*.wav` のみを非再帰 glob (`pdir.glob("*.wav")`) で列挙する
ため、`samples/foo.wav` のようなネスト参照は絶対パスでも `..` 脱出でも
なく R8/R10 の検査（語彙的絶対パス/`..` 検査・resolve 後 root 包含検査)
をいずれも素通りしたまま、identity の対象外で WORLD 分析へ読み込まれる
（ネスト収録を差し替えても `voicebank_identity_hash`/
`spec.donor.voicebank_sha256` は不変で、両 provenance 検証が素通りした
まま render 済みバイト列だけが変わる）。UTAU の実運用慣行（oto.ini の
`filename=` は同一ディレクトリ内の wav ファイル名のみを指す。ベンダ配布
voicebank・主要エディタが生成する oto.ini はいずれも basename 参照のみ）
とも整合し、identity 側を「全 pitch dir 全 wav の全数インベントリ」に
保ったまま最小変更で閉じられるため、非 basename 参照の拒否を採る。

**実データ確認**: 波音リツ強連続音 Ver1.5.1（A3/F4 の 2 pitch dir、
`parse_oto_ini` でパースした oto.ini エントリ計 2474 件）に対し、実装した
`_wav_ref_is_basename` を全件へ適用したところ、**非 basename 参照は 0 件**
だった。設計判定の前提（実運用では basename 参照のみ）を実データで
確認済み。もし将来的に非 basename 参照を含む UTAU 音源が見つかった場合は、
本判定を撤回し identity 側のネスト対応拡張へ切り替える（再着手条件）。

**修正**: 語彙的検査本体を `_wav_ref_is_basename(wav_filename)`（新設）へ
集約し、`/`・`\`（pathlib は POSIX 上で `\` を区切りと解釈しないため文字列
検査で個別に拾う）を含む参照・`.`/`..` そのもの・絶対パスを拒否する。
selection 段の軽量フィルタ `_wav_ref_is_lexically_safe` はこれへ委譲し、
最終的な網羅的検査 `_reject_wav_paths_outside_root` の語彙的検査部分
（従来は独自にインライン実装していた絶対パス/`..` 判定）も同関数へ統一
した（v1/VCV 両 builder が共通で呼ぶため 1 箇所の修正で両経路に伝播）。

**テスト**: `_wav_ref_is_basename` の単体テスト 5 本（basename 許容・`/`
拒否・`\` 拒否・`.`/`..` 拒否・絶対パス拒否）+ `_wav_ref_is_lexically_safe`
の委譲確認 1 本 + `_reject_wav_paths_outside_root` がネスト参照を
（resolve 後も root 配下に収まるにもかかわらず）拒否することの確認 1 本 +
`build_donor_bank_utau` を実際に `samples/foo.wav` 参照の oto.ini で呼ぶ
end-to-end テスト 1 本（計 8 本）。

**影響**: bit には影響しない（実データで非 basename 参照が 0 件のため
既存挙動は無変更。異常系（ネスト参照）のみ挙動が「非再帰 glob の死角を
伴ったまま無検査で分析」から「ValueError で fail-closed」へ変化）。

### F-C ファミリー拡張 監査表（出力・入力衝突拒否）

R5 監査表時点では「`adapter/` + `scripts/` 内で argparse `main()` を持つのは
`render.py`/`measure_bands.py` の 2 ファイルのみ」だったが、R11 で
`results_f1a/`・`results_f1b/`・`results_f1c/` に argparse CLI 11 本が
新設されたため、F-C の対象範囲を拡張し全数判定する。

| スクリプト | CLI 引数（外部パス） | 固定出力名 | 判定 | 備考 |
|---|---|---|---|---|
| `adapter/render.py` | `--wav`/`--voice`/`--notes-csv`/`--voicebank-root` | `--out`（可変） | 準拠 | R2 で対応済み（`_reject_output_collision`） |
| `scripts/measure_bands.py` | 位置引数 `wav_paths` | `--out`（可変） | 準拠 | R4 で対応済み（`_reject_output_collision`） |
| `results_f1b/glue_template.py` | `--donor-wav`/`--control-npz` | 5 件（`f1b_template_*.wav` 等） | **修正**（本対応） | P1 指摘本体 |
| `results_f1b/analyze_h1.py` | `--donor-wav`/`--control-npz` | 1 件（`f1b_h1_diagnosis.json`） | **修正**（本対応） | glue_template と同型 |
| `results_f1b/glue_template_nn.py` | `--donor-wav`/`--control-npz` | 4 件（`f1b_nn_*`） | **修正**（本対応） | 同上 |
| `results_f1b/glue_template_nn_v2.py` | `--donor-wav`/`--control-npz` | 3 件（`f1b_nnv2_*`） | **修正**（本対応） | 同上 |
| `results_f1c/glue_transplant.py` | `--donor-wav` | 4 件（`f1c_roundtrip.wav` 等） | **修正**（本対応） | 同上（P1 指摘が「NN/F1c glue scripts」と明記した対象） |
| `results_f1a/glue_control.py` | なし（`--out` のみ） | 1 件（`f1a_control.npz`） | 対象外 | 外部パス引数を持たず衝突経路が存在しない |
| `results_f1a/glue_pyworld.py` | なし（`--out` のみ） | 1 件（`f1a_pyworld_direct.wav`） | 対象外 | 同上 |
| `results_f1c/make_note_tracks.py` | なし（`--out` のみ） | 2 件（`f1c_note_track*.npz`） | 対象外 | 同上 |
| `results_f1a/glue_golf_ismir.py` | `--golf-dir`（ディレクトリ） | 2 件（`f1a_golf_ismir23_*.wav`） | 対象外 | 読込対象は out_dir 配下の固定相対名（`f1a_control.npz`/`golf_ismir_input/sakura_001.wav`）のみで CLI 経由の任意パス入力を持たない。`--golf-dir` 配下の ckpt は読込専用で固定出力名と文字列上衝突し得ない |
| `results_f1a/glue_sawsing.py` | `--ddsp-dir`（ディレクトリ） | 2 件（`f1a_sawsingcore_*.wav`） | 対象外 | 同上 |
| `results_f1b/select_donor.py` | `--root`（ディレクトリ） | なし | 対象外 | 書き込みを一切行わない（stdout 出力のみ） |

**掃討結果**: R11 で新設された 11 本のうち、実害が成立する 5 本すべてに
`_reject_output_collision` 相当のガードを追加した。残り 6 本は CLI 引数と
固定出力の組み合わせが衝突経路を構造的に持たないため対象外と判定した
（表内根拠明記）。F-C ファミリーは `voice_genesis/foundry/` 全体
（`adapter/` 2 + `results_f1*/` 11 = 13 CLI エントリポイント）で残存ゼロ。

### F-D ファミリー新設・終端宣言（root 包含検査: symlink/非 basename ネスト脱出）

UTAU 側は R8（`../`・絶対パス・symlink）・R10（自 pitch dir 脱出の語彙的
検査）で個別対応済みだったが、PJS 側（`donor_bank_lab.py`）は未対応の
まま同型の穴が残っていた（本 R13 #2）。加えて UTAU 側自身にも「resolve 後は
root 内に収まるが非 basename ネスト参照」という R8/R10 未カバーの死角が
残っていた（本 R13 #3）。両者を「root 配下と誤認されて識別対象から漏れる
参照を fail-closed で拒否する」という同一ファミリーとして統一し、共通
ヘルパー `donor_bank.reject_paths_outside_root`（PJS の実ファイルパス向け・
resolve 後包含検査のみ）と `donor_bank_utau._wav_ref_is_basename`（UTAU の
テキストフィールド向け・語彙的検査 + resolve 後包含検査の両方を
`_reject_wav_paths_outside_root` が適用）の 2 実装へ集約した。

| ドナー種別 | root の実体 | 脱出経路 | 判定 | 対応ラウンド |
|---|---|---|---|---|
| UTAU v1/VCV（`donor_bank_utau.py`） | `voicebank_root` | `../` 相対脱出 | 準拠 | R8 |
| UTAU v1/VCV | `voicebank_root` | 絶対パス参照 | 準拠 | R8 |
| UTAU v1/VCV | `voicebank_root` | symlink（`filename=` が指す wav 自体が symlink） | 準拠 | R8 |
| UTAU v1/VCV | `voicebank_root` | 自 pitch dir 脱出（`A3/../F4/foo.wav`、resolve 後も root 内） | 準拠 | R10 |
| UTAU v1/VCV | `voicebank_root` | 非 basename ネスト参照（`samples/foo.wav`、resolve 後も root 内） | **修正**（本対応） | R13 |
| PJS（`donor_bank_lab.py`） | `corpus_root` | `pjsNNN/` 自体が root 外を指す symlink | **修正**（本対応） | R13 |
| PJS | `corpus_root` | `.lab`/`_song.wav` 個別ファイルの symlink 脱出 | 準拠 | R13（`reject_paths_outside_root` が両方を対象に含む） |
| vocadito（`donor_bank.py`） | 該当なし | — | 対象外 | root グロブを行わない設計（`--wav`/`--notes-csv` は個別ファイル引数。F-C で別途カバー） |

**終端結果**: 3 donor 種別（UTAU/PJS/vocadito）のうち root グロブを行う
UTAU・PJS の全脱出経路（`../`・絶対パス・symlink・自 dir 脱出・非
basename ネスト）を fail-closed で閉じた。vocadito は root 概念自体を
持たないため設計上対象外。本ラウンドで F-D ファミリーを終端とする
（再着手条件: root グロブを新設する donor 種別が追加された場合、または
本ラウンドで想定していない脱出経路が実測で示された場合）。

### テスト・lint・回帰確認（R13）

- `ruff check .`（リポジトリ全体）: clean
- `python -m pytest voice_genesis/foundry/tests -q`: **364 件、全 green**
  （R12 時点の 341 本 + 新規 23 本。内訳: `test_glue_template.py` 新規 8 本 /
  `test_adapter_donor_bank.py` +4 本 / `test_adapter_donor_bank_lab.py`
  +3 本 / `test_adapter_donor_bank_utau.py` +8 本）
- `python -m pytest voice_genesis/singer/tests -q`: **38 本、全 green**（非破壊）
- `src/svp_rpe/**`・`pyproject.toml` は無変更（本体スイートへの影響なし）
- 実データ確認: 波音リツ強連続音 Ver1.5.1（scratchpad 展開済み）の
  A3/F4 oto.ini 計 2474 エントリへ `_wav_ref_is_basename` を適用し、
  非 basename 参照 0 件を確認（上記 #3 参照）
- **音への影響**: なし。F-C 5 本のガードは衝突しない既存の正常経路
  （実験スクリプトの通常呼び出し）では発火せず、F-D の 2 修正も既存
  fixture・実データでは root 内に収まるため既存挙動は無変更（異常系のみ
  「無検査で外部/ネスト参照を read」から「fail-closed」へ変化）。
- R13 で PR #262 のレビュースレッドは既知の境界宣言 1 件（R6「Bind
  provenance to the analyzed donor snapshot」・不採用・実装コスト対効果で
  見送り済み・R13 でも再確認し不採否判断は不変）を除き全数対応済み。
  F-C（衝突拒否）・F-D（root 包含検査）の 2 ファミリーは本ラウンドで
  終端とする。

## Codex R14 対応（残 3 スレッド・全採用・音響 2 件 + 計器 1 件）

R13 push 後に新規発生した 3 スレッド（`render.py` フレーズ頭 preutterance
消音・`render.py` フレーム丸め累積ドリフト・`measure_bands.py` 短入力
welch クラッシュ）へ対応する。前 2 件は音に直結するため sakura/umi ritsu の
fixed5 WAV を再生成する。

### 1. `adapter/render.py:724`「Keep shifted VCV preutterance audible」（P2・`r3789636053`・採用）

**検証**: origin-shift 済みフレーズ頭ノートは `_note_frame_track` が
`seg.start_sample - shift` から amp_env をサンプリングするが、
`perf.build_amplitude_envelope` はフレーズ先頭より前（ブレス無音区間）を
常にゼロ初期化したままのため、preutterance に置いた録音済み子音/遷移
（典型 100ms）がゼロ乗算で消音されることを確認した（該当コード確認済み。
fixed4 実測でも `shift_per_note` が最大 60 frame(300ms) に達するノートが
複数あり、実害が成立する規模であることを確認）。

**修正**: `_extend_phrase_head_amp_for_preutterance`（新設）を追加し、
`donor="ritsu"` 経路の amp トラック抽出専用に、フレーズ先頭ノートの
amp_env を shift 分だけ前方（preutterance 区間）へ定数外挿する。外挿レベル
は「フェード完了直後の定常値」（= フェード適用前のアーチ境界値）を
`amp_env` から直接読み（`build_amplitude_envelope` の内部定数を再実装
しない）、外挿区間の先頭では 0 から短くフェードイン（クリック防止・
既存の attack フェード規約と対称）する。フレーズアーチ自体の形
（`amp[phrase_start:phrase_end]`）は無変更。f0 トラックは無関係（無変更）。
vocadito/pjs 経路は `shift_per_note` が全 0 のため no-op（完全不変）。

**テスト**: `test_adapter_render_donor_wiring.py` に 8 本追加
（`_extend_phrase_head_amp_for_preutterance`: 消音区間が可聴になる確認・
外挿区間先頭のフェードイン（クリック防止）・外挿区間より手前は無変更・
フレーズアーチ本体は無変更・mid-phrase ノートは対象外・shift=0 は
no-op・shift が start_sample を超える場合の 0 側クランプ・入力非破壊）。

### 2. `adapter/render.py:358`「Derive VCV positions from absolute sample timestamps」（P2・`r3789636058`・採用）

**検証**: `_build_vcv_placements` は名目開始フレームを `cursor`（前ノートの
`gap_frames`/`resolved.n_frames` の丸め済み値を逐次加算する累積変数）で
追跡していた。score が非整数フレーム長のビートを含む場合（umi の 1 拍 =
16,364 サンプル @24kHz・5ms hop = 136.36... frame）、各ノートの
`frames_for_samples()` 丸め誤差（最大 ±0.5 frame）が後続ノートへ伝播し、
umi 終盤で −3 frame(15ms) のドリフトに達することを確認した（該当コード
確認済み。レビュー指摘の実測例と一致）。

**修正**: 名目開始フレームを `cursor` 累積でなく、毎ノート独立に
`frames_for_samples(seg.start_sample, SR)` から絶対換算するよう変更した
（`cursor` 変数を撤廃）。`gap_frames`（フレーズ先頭ノートの preutterance
クリップ判定に使う breath 余地）は従来どおり実絶対サンプル差分から毎回
新規計算するため無変更（累積誤差を持たない）。各ノートが score グリッドへ
常に re-snap されるため、丸め誤差が後続ノートへ伝播しない。

**テスト**: `test_adapter_render_donor_wiring.py` に 2 本追加
（非整数フレーム長ビート 8 本連続で各ノートの名目開始フレームが
`frames_for_samples(seg.start_sample, SR)` と厳密一致することの確認・
20 ノート連続でも最終ノードの名目位置がドリフトしないことの確認）。
既存 4 本（`test_build_vcv_placements_*`）は全て clean な整数フレーム長の
synthetic fixture のため旧実装と新実装が数値的に一致し、無修正で green
のまま（回帰なし）。

### 3. `scripts/measure_bands.py:35`「Adapt Welch overlap for short WAV inputs」（P2・`r3789636063`・採用）

**検証**: `band_shares` は `nperseg=4096, noverlap=2048` を固定で
`sig.welch` へ渡していた。入力が 2,048 サンプル以下（24kHz で約85ms）だと
SciPy は内部で `nperseg` を信号長へ縮小する一方 `noverlap` は指定値の
2048 のまま渡されるため `noverlap >= nperseg` エラーで計測 CLI が例外
終了することを確認した（該当コード確認済み・実測 `sig.welch` 呼び出しで
再現）。

**修正**: `nperseg = min(4096, len(x))`・`noverlap = nperseg // 2` へ変更し、
`noverlap` が常に `nperseg` 未満になるようにした。`tilt_db_per_decade_1k_8k`
の回帰は 1k-8kHz 帯に 2 点未満しか入らない極端な短入力では `None` を返す
（周波数分解能不足で回帰不能）。空入力（0 サンプル）は `welch` を一切
呼ばず、`hnr_median_db` の「計測不能は NaN（JSON では null）」規約と対称に、
全帯域 `None` + `status` フィールド（`"ok"`/`"short_input"`/
`"empty_input"`）で明示的に報告する（fail-fast の例外ではなく計測不能の
記録）。

**テスト**: `test_measure_bands.py` に 5 本追加（境界値 2,048 サンプルで
クラッシュせず全帯域有限値・`status="short_input"`／2,048 未満の極端な
短入力でクラッシュせず `tilt` が `None`／空入力で全 `None` +
`status="empty_input"`／通常長入力で `status="ok"` かつ既存の帯域計算
結果が数値的に不変（回帰ガード）／`analyze_wav` 経由の E2E クラッシュ
再現）。

**影響**: bit には影響しない（`status="ok"` の通常長入力は `nperseg`/
`noverlap` とも既存の固定値 4096/2048 と一致するため既存挙動は無変更）。

### テスト・lint・回帰確認（R14）

- `ruff check .`（リポジトリ全体）: clean
- `python -m pytest voice_genesis/foundry -q`: **379 件、全 green**
  （R13 時点の 364 本 + 新規 15 本。内訳: `test_adapter_render_donor_wiring.py`
  +10 本（#1 の amp 外挿系 8 本 + #2 の絶対フレーム系 2 本）/
  `test_measure_bands.py` +5 本（#3 の短入力/空入力系））
- `python -m pytest voice_genesis/singer -q`: **38 本、全 green**（非破壊）
- `src/svp_rpe/**`・`pyproject.toml` は無変更（本体スイートへの影響なし）

### F1.4 fixed5 WAV（#1 preutterance 消音修正 + #2 フレーム丸めドリフト修正を反映・音に影響）

`presets/ritsu_neutral.json` を使い `--donor ritsu --voicebank-root <波音リツ
強連続音Ver1.5.1>` で再生成した（生成スクリプト: scratchpad
`foundry_f1_4/run_f1_4_fixed5_e2e.py`。出力先: scratchpad
`foundry_f1_4/wavs/*_fixed5.wav`・リポジトリへは非コミット・従来どおり）。

| ファイル | sha256(先頭12桁) | 長さ(s) | fixed4 版 長さ(s) |
|---|---|---|---|
| sakura_ritsu_f1_4_fixed5.wav | `c32153d87299` | 24.585 | 24.585（不変） |
| umi_ritsu_f1_4_fixed5.wav | `a2f12d0ca1c6` | 11.410 | 11.410（不変） |

完全 sha256:
- sakura: `c32153d87299b288c8331cd28574e9f870e3438751dc435700a9e7e66f9ab7bf`
- umi: `a2f12d0ca1c6c112bee8f4cba75e130699a91a0851f995bfaf21890f0eae69ec`

総尺は fixed4 から不変（#1/#2 とも配置の絶対フレーム位置・総フレーム数
（`score_total_frames` 基準）を変えない——#1 は amp サンプリング内容のみ
補正、#2 は個々のノート位置の丸め誤差を除去するのみで累積量自体は
score 総尺の丸めに収束する）。sha256 は fixed4 から変化している（amp
トラックの内容・note 位置の frame 単位微小シフトが変わるため、音自体は
影響を受ける——想定どおり）。

**決定論確認**: sakura/umi とも (a) 初回（`cache_fixed5`・新規 cache_dir）、
(b) 別 cache_dir でのフル再計算（`cache_fixed5_nocache`・no-cache 相当）の
2 経路で sha256 完全一致を確認済み。

**donor_provenance**: 両ファイルとも `declared_sha256 == actual_sha256`
（`presets/ritsu_neutral.json` の pin が実データと一致）。

**shift 実測**: `vcv_placement_stats.n_preutterance_applied` = sakura 19件・
umi 10件、`shift_per_note` は最大 60 frame(300ms) に達するノートを含み、
#1 の修正が実際に発火する規模であることを確認した。

**再耳判定はスコープ外**（User 判断待ち。F1.x 梯子凍結の裁定・S0–S4 実行順は
本対応で変更しない）。

### 未対応スレッド（R14 時点）

R14 時点で PR #262 の Codex レビュー由来の未解決スレッドは、既知の境界宣言
1 件（R6「Bind provenance to the analyzed donor snapshot」・不採用・実装
コスト対効果で見送り済み・R8/R13 で再確認し不採否判断は不変）を除き
残っていない。

## Codex R19 対応（新規 3 スレッド・音響 2 件採用 + 計器 1 件 follow-up 移管）

R18 push 後（2026-08-15T16:24Z 頃）に Codex が新規発生させた 3 スレッド
（`render.py` フレーズ頭 attack フェード二重化・`render.py` origin-shift
preutterance の F0 無声化・`donor_bank_utau.py` 空母音核の VCV 被覆計上）へ
対応する。指摘 1/3 は同一分岐（origin-shift 済みフレーズ先頭ノート）に
起因する対の欠陥のため 1 コミットで同時修正し、sakura/umi ritsu の fixed6
WAV を再生成する。指摘 2 はコード上妥当だが実 oto.ini 全数走査で現行成果物
への実害ゼロを実測確認したため、本 PR では対応せず follow-up PR キューへ
登録する（登録のみ）。

### 1. `adapter/render.py:480`「フレーズ頭 attack フェードの二重化」（P2・採用・音影響あり）

**検証**: R14 で追加した `_extend_phrase_head_amp_for_preutterance` は
拡張区間 `[ext_start, seg.start_sample)` のみを埋めており、
`perf.build_amplitude_envelope` が既に書き込んだ元の attack フェード
`[seg.start_sample, seg.start_sample+ar)`（0→フルレベルへ `NOTE_ATTACK_RELEASE_MS`
かけて立ち上がる）には一切触れていなかった。結果として、外挿区間で
0→定常値へ立ち上がった振幅が `seg.start_sample` の直前でほぼ定常値へ
達した直後、`seg.start_sample` の瞬間に元フェードの先頭値（ほぼ0）へ
落ち、そこから再び 15ms かけて立ち上がる——「立ち上がり→公称拍でゼロ
落ち→再立ち上がり」のノッチが生じることを確認した（該当コード確認済み。
発火条件は指摘3と同一 = `donor="ritsu"` かつ `seg.is_phrase_first` かつ
`shift_frames>0`。fixed5 実測で `shift_per_note` が最大 60 frame(300ms) に
達するノートを複数含むため、現行 fixed5 WAV の該当ノートで実際に発火して
いる規模であることを確認）。

**修正**: フェードそのものを延長端へ移設し、単一の立ち上がりにした。
`[ext_start, ext_start+ar)`（`ar` = 元のアタック時間そのまま）を
0→`connect_level` へフェードインし、続く `[ext_start+ar,
seg.start_sample+ar)` を `connect_level` で定数充填する（旧フェード区間
`[seg.start_sample, seg.start_sample+ar)` もこの充填で上書きされ、二重
フェードが解消する）。`connect_level` は既存 `amp_env` の
`seg.start_sample+ar` 時点の値（フェード完了直後の定常値）をそのまま
読み、`seg.start_sample+ar` 以降（不変域）との段差をゼロにする。対象
ノート判定（`is_phrase_first` かつ `shift_frames>0`）は指摘3の F0 修正と
`_phrase_head_preutterance_shifts`（新設）で共有し、二重管理を避けた。
シフトなし経路（`origin_shift_frames==0`・vocadito/pjs）は無変更
（bit-exact no-op、回帰テストで確認）。

**テスト**: `test_adapter_render_donor_wiring.py` に追加・更新。
既存の `test_extend_phrase_head_amp_leaves_phrase_body_unchanged` は
「フレーズ本体 `[start, end)` 全体不変」という旧実装の副作用（二重フェード
込み）を誤って「不変」と検証していたため、`[start+ar, end)` のみを不変域
とする `test_extend_phrase_head_amp_leaves_phrase_body_past_attack_unchanged`
へ更新。新規
`test_extend_phrase_head_amp_no_double_fade_notch_and_monotonic`
（`[ext_start, start+ar]` が単調非減少 = ノッチ不在、`start+ar` で既存
エンベロープ値と連続に接続）を追加。

### 2. `adapter/donor_bank_utau.py:905`「空母音コアが VCV 被覆へ計上される」（P2・follow-up 移管）

**検証**: `consonant_end_ms`（cutoff 由来）が `cutoff_ms` 以遠になる oto
エントリでは `vcore_rel` が `seg_end - seg_start`（セグメント全長）へ
クランプされ、母音核が空の unit が構築されうる。棄却条件
（`seg_end - seg_start < MIN_UNIT_FRAMES_ABS`）は母音核の非空性を検査
しないため、`units.py` の `resolve_vcv_unit_to_note` が
`degenerate_empty_core` 分岐（直前フレームの `sp/ap` 繰り返し充填）へ
落ち、かつ `enforce_realized_context_coverage` / `_select_wav_subset_for_contexts`
はこの空核 unit も「被覆済み」として計上し、後続の有効候補の探索を
打ち切りうる——指摘はコード上妥当と評価した。

**実測（不採否判定の根拠）**: 現行 fixed5/fixed6 生成に使用している波音
リツ実 oto.ini（`A3`/`F4` 各 1,237 エントリ・計 2,474 エントリ全数）を、
実 wav ファイルのヘッダから実尺を取得して cutoff/vcore を再計算する形で
走査した。consonant point が cutoff 以遠となるエントリは **0/2,474**
（A3/F4 とも 0 件）。母音核の残余マージン（`seg_len - vcore_rel`、
1 フレーム=5ms）の最小値は **A3=30 frame(150ms)・F4=54 frame(270ms)**
で、閾値0への近接例（margin≤3）も皆無だった。したがって現行成果物
（sakura/umi fixed6 WAV）への音影響はない。

**判定**: 実害なし・バグはコード上確実に存在するため、被覆正直会計の
強化として follow-up PR キューへ登録する（本 PR では実装しない。
Follow-up キュー item 10 として追記）。

### 3. `adapter/render.py:434`「origin-shift preutterance の F0 無声化」（P2・採用・音影響あり）

**検証**: `_note_frame_track` は origin-shift 分だけサンプリング原点を
`seg.start_sample` より前へずらすが、`base_f0_persample`
（`perf.build_f0_contour` 出力）はフレーズ先頭より前（ブレス無音区間）を
常にゼロ初期化したまま残す。指摘1の amp 修正により当該区間は既に
無音でなくなっているため、ずらしたサンプリング原点が拾う F0=0 は無声
ノイズとして可聴になりうることを確認した（該当コード確認済み。
`amp_env_for_extraction` には対応する f0 側の姉妹処理が存在しないことも
確認）。発火条件は指摘1と同一。

**修正**: 振幅側と対になる `_extend_phrase_head_f0_for_preutterance`
（新設）を追加した。対象ノート判定は指摘1と `_phrase_head_preutterance_shifts`
を共有し、延長区間 `[ext_start, seg.start_sample)` を「ノート開始時点の
F0（`seg.start_sample` の輪郭値。それ自体が 0 の場合は同ノート内で
最初に現れる有声値へフォールバック）」の定数外挿で埋める。ビブラート等の
時間変化は延長区間には適用しない（定数で足りる）。シフトなし経路
（vocadito/pjs）は無変更（bit-exact no-op）。

**テスト**: `test_adapter_render_donor_wiring.py` に 5 本追加
（延長区間がノート開始 F0 で埋まり全フレーム有声・ノート開始自体が
無声な場合の最初の有声値へのフォールバック・mid-phrase ノートは対象外・
shift=0 は no-op・入力非破壊）+ シフトなし経路の amp/f0 双方が bit-exact
no-op であることを確認する回帰テスト 1 本
（`test_extend_phrase_head_amp_and_f0_shiftless_path_is_bit_exact_noop`）。

### テスト・lint・回帰確認（R19）

- `ruff check .`（リポジトリ全体）: clean
- `python -m pytest voice_genesis/foundry -q --tb=short`: **386 件、全 green**
  （R14 時点の 379 本 + 新規 7 本。既存 1 本は旧実装の副作用を「不変」と
  誤検証していたため意味論を修正して更新・件数には含めない）
- `python -m pytest voice_genesis/singer -q --tb=short`: **38 本、全 green**
  （非破壊）

### F1.4 fixed6 WAV（#1 attack フェード二重化解消 + #3 preutterance F0 無声化解消を反映・音に影響）

`presets/ritsu_neutral.json` を使い `--donor ritsu --voicebank-root <波音リツ
強連続音Ver1.5.1>` で再生成した（生成スクリプト: scratchpad
`foundry_f1_4/run_f1_4_fixed6_e2e.py`。出力先: scratchpad
`foundry_f1_4/wavs/*_fixed6.wav`・リポジトリへは非コミット・従来どおり）。

| ファイル | sha256(先頭12桁) | 長さ(s) | fixed5 版 長さ(s) |
|---|---|---|---|
| sakura_ritsu_f1_4_fixed6.wav | `833d65d8b139` | 24.585 | 24.585（不変） |
| umi_ritsu_f1_4_fixed6.wav | `9f561fa88cad` | 11.410 | 11.410（不変） |

完全 sha256:
- sakura: `833d65d8b1394d3e7fc655d0a41acc212e94a83adcc3ac7686b560b17469ec0c`
- umi: `9f561fa88cadaf4d395b6ed16dbf2d6e14326d3062668c4097acd8f398ea3a3a`

総尺は fixed5 から不変（本ラウンドの修正は amp/f0 トラックの内容のみを
補正し、配置の絶対フレーム位置・総フレーム数を変えない）。sha256 は
fixed5 から変化している（amp/f0 トラックの内容が変わるため、音自体は
影響を受ける——想定どおり）。

**決定論確認**: sakura/umi とも (a) 初回（`cache_fixed6`・新規 cache_dir）、
(b) 別 cache_dir でのフル再計算（`cache_fixed6_nocache`）の 2 経路で
sha256 完全一致を確認済み。

**再耳判定はスコープ外**（User 判断待ち。F1.x 梯子凍結の裁定・S0–S4 実行順は
本対応で変更しない）。

### 未対応スレッド（R19 時点）

R19 対応後、PR #262 の Codex レビュー由来の未解決スレッドは、既知の境界
宣言 1 件（R6「Bind provenance to the analyzed donor snapshot」・不採用・
R8/R13/R14 で再確認し不採否判断は不変）を除き残っていない
（指摘2は follow-up PR キューへ登録済み・本 PR のスコープ外）。

## Follow-up PR キュー（R15 以降・本 PR では対応しない）

R14 push 後に新規発生した Codex 未解決スレッド 5 件 + PR #262 クローズアウト
追作業（R17）で新規発生した PR レビュースレッド 2 件 + 同追作業（R18）で新規
発生した PR レビュースレッド 2 件 + R19 で不採否判定・follow-up 移管とした
1 件の計 10 件。採否判定は妥当と評価するが、本 PR のレビュー対応は R14 で
線引きしたためマージ後の follow-up PR で対応する（登録のみ・本コミットでの
実装は行わない）。**先頭 2 件（R16）は音への潜在影響、3–4 件目（R17）は
安全性/計器区分、5–6 件目（R18）は F-D ファミリー新証拠/fail-closed 強化、
10 件目（R19-2）は被覆正直会計の強化を理由に優先項目として追記**
（R19 分は PR #262 R19 対応・実測で現行成果物への実害ゼロを確認済み）。

1. **UTAU 重複 oto 区間の共有フレームへの正規化二重適用**（P2・
   `adapter/donor_bank.py:463`・follow-up 最優先）: `normalize_unit_energy` の
   ゲイン適用（`sp2[u.start_frame:u.end_frame] *= gain`）は「unit 間で frame
   範囲が重複しない」前提に立つが、UTAU oto.ini に重複エイリアス（同一録音
   区間を指す複数 oto エントリ）が存在する音源では前提が崩れ、共有フレームへ
   正規化ゲインが複数回重ねて適用されうる。**リツ経路の unit 間レベル段差に
   影響しうるため follow-up 最優先**。
2. **PJS 録音子音クリップが正規化対象外で母音との境界に段差**（P2・
   `adapter/render.py:647`）: `n_recorded_consonants_used`/
   `prepend_recorded_consonant` 経路で前置される録音子音クリップは
   `normalize_unit_energy`（母音核基準の正規化）の対象外のまま母音 unit の
   前へ連結されるため、子音クリップの収録時レベルと正規化後の母音レベルが
   食い違う場合、子音/母音境界に聴感上のレベル段差が生じうる。
3. **R17-1: donor キャッシュの pickle.load を非実行形式へ置換**（P1・安全性・
   `donor_bank_utau.py:822` / `donor_bank_utau.py:1090` / `donor_bank_lab.py:503`、
   共通 helper = `donor_bank.py:152` `atomic_pickle_dump`）: 3 箇所の
   donor キャッシュ復元が `pickle.load` を使用しており、AGENTS.md Persistent
   Artifact Safety Gate の観点で妥当な指摘。cache-dir が他者書込可能な環境では
   任意コード実行の脅威が成立しうる。ローカルツールのため脅威成立条件は限定的
   でマージブロッカーではないが、安全性区分として優先扱いとする。対応方針:
   非実行形式（npz + JSON メタデータ）+ 読戻し時のスキーマ厳格検証へ置換。
4. **R17-2: measure_bands.py の `hnr_median_db` 窓ループが最終完全窓を脱落**
   （P2・計器・`measure_bands.py:94`）: `range(0, len(x) - win, hop)` が排他境界
   のため、単一窓のみ収まる短入力では窓が 1 つも生成されず null 化し、
   任意の入力長でも `len(x) - win` を開始点とする最終完全窓が恒常的に脱落する。
   `len(x) - win` 開始フレームを含むよう修正（例: 終端を `+ 1`）。**修正時は
   計器挙動変更として本 record に注記する**（過去計測値との微差が生じうる）。
5. **R18-1: donor_bank_utau.py の symlink 検査が pdir（pitch ディレクトリ）
   包含を検査しない**（P2・F-D ファミリー新証拠・`donor_bank_utau.py:539`
   `_reject_wav_paths_outside_root`）: root 包含のみ検査し pdir 包含を検査し
   ないため、`A3/foo.wav -> ../F4/bar.wav` のような basename symlink が通過し、
   A3 のアノテーションで F4 音声を解析しうる。resolve 後の候補が resolved
   pdir 配下にあることも要求する修正が必要。緩和要因: preset の voicebank
   identity pin（WAV バイト込み sha256）が改変混入を検出する。
6. **R18-2: voice_spec.py の数値検証が有限性のみで値域を制限せず inf 化
   しうる**（P2・fail-closed 強化・`voice_spec.py:107`）: `tilt_db_oct: 1e308`
   等の極端値が `spectral_tilt()` / `2**(cents/1200)` で inf 化し、WORLD に
   非有限配列が渡りうる。フィールド別安全域の導入または変換結果の有限性検査
   を追加する。
7. **glue スクリプトの結果セット一括公開**（P1・`results_f1b/glue_template.py:285`
   ほか NN/F1c 同型スクリプト）: `discussion_r3789670080`「Stage the complete
   glue result set before publishing」。再実行時に WAV 合成/書き込みや最終 JSON
   dump が失敗すると、先行して直接書き込み済みの成果物が既存の有効な出力を
   破壊しうる（AGENTS.md Persistent Artifact Safety Gate「全構築後公開」の
   glue スクリプト群（NN/F1c 同型含む）への未展開）。
8. **notes CSV digest の DonorBank/provenance 露出**（P2・
   `adapter/donor_bank.py:591`）: `discussion_r3789670082`「Expose the notes
   annotation digest in render provenance」。同一 vocadito WAV を異なる/編集済み
   `--notes-csv` で render すると unit 境界・生成音声が変わるが、render
   provenance にはこの digest が露出しておらず、出力の追跡可能性に欠ける。
9. **glue_transplant のドナー hash と分析バイトの単一 read 化**（P2・
   `results_f1c/glue_transplant.py:228`）: `discussion_r3789670081`「Bind the
   logged donor hash to the analyzed bytes」。`load_donor_24k()` とログ用の
   独立 hash read の間でドナー WAV が置き換わると、ログに記録される digest と
   WORLD 分析が実際に読んだバイト列が食い違う TOCTOU 窓がある
   （measure_bands.py R1 / donor_bank.py 同種修正と同型の未展開）。
10. **R19-2: UTAU 空母音コアが VCV 被覆へ計上される**（P2・被覆正直会計・
    `adapter/donor_bank_utau.py:905`）: consonant point が cutoff 以遠となる
    oto エントリでは `vcore_rel` がセグメント全長へクランプされ、母音核が
    空の unit が構築されうる。棄却条件は母音核の非空性を検査しないため、
    `resolve_vcv_unit_to_note` が直前フレーム繰り返し充填の
    `degenerate_empty_core` 分岐へ落ち、かつ被覆計上・wav 部分集合選択の
    両方がこの空核 unit を「被覆済み/発見済み」として扱い、後続の有効候補の
    探索を打ち切りうる。**R19 実測**: 波音リツ実 oto.ini 全 2,474 エントリ
    走査で該当ゼロ（母音核の残余マージン最小 = A3 150ms・F4 270ms）のため
    現行成果物（fixed6 含む）への実害はない。母音核の非空性検査を棄却条件へ
    追加する修正が必要。

**総括**: R1–R19 = 指摘 56 件処理（採用 52・境界宣言 2・撤回 1・follow-up
移管 1）・音関連修正 7 件・テスト 180→386。R15 以降の指摘（R19-2 含む）は
マージ後の follow-up PR で受ける（本 PR は R14 で線引き・R19 は音響 2 件の
み例外的に本 PR 内で対応）。
