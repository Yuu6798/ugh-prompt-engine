# RunPod 実行環境の依存 lock（2026-08-19 外部レビュー P3）
#
# 目的: 実行時解決（pip install -e ".[dev]" / DiffSinger requirements.txt /
# praat-parselmouth）で版が動く露出を、成功 run 基準の固定へ寄せる。
# runtime gate（run5_bootstrap.py の 4 ゲート = 版 pin 検査）は維持し、
# lock + runtime gate の二重保証とする。
#
# 会計（正直会計）: run 5（2026-08-19 完走・pin 9899f3c）は pip freeze を
# 捕獲しておらず、学習側 venv の完全な実測版一覧は存在しない。本ファイルの
# 確定 pin は run 5 で実測検証済みの範囲のみだった。**run 6（2026-08-19
# 完走）で gates/binarize 両段の pip freeze を実測捕獲し、PENDING は解消
# 済み**（下部の解消節）。run 7 以降も同経路で毎走行の実測が残る。
#
# [render / gates 環境 — run 5 実測・gate1 で毎回検証される]
# run5_bootstrap.py の NUMERIC_STACK_PIN と同期必須
# （tests/test_run5_bootstrap.py の同期テストが drift を検出する）。
numpy==2.4.6
scipy==1.17.1
pyworld==0.3.5
soundfile==0.14.0
#
# [学習側 venv — run 6 実測 freeze で確定（§下部の PENDING 解消節）]
#
# [f0 解析系 — 2026-08-19 実測で確定（run 6 pin 生成時）]
# numba 0.67.0 は numpy 2.4.6 と組むと librosa.pyin（guvectorize）が SIGSEGV
# する（実測・NUMBA_DISABLE_JIT でも回避不可）。0.66.0 で解消を実測確認。
# run 5 Pod は 0.66 系を解決していたと推定（freeze 未捕獲のため間接）—
# 未 pin の numba が数日で 0.66→0.67 へドリフトして壊れた実例であり、
# 本 lock の存在理由そのもの。bootstrap gates 段が本 pin を強制する。
numba==0.66.0
librosa==0.11.0
# pyloudnorm = run 6 正規化の数値を決める依存（run6 pin は 0.2.0 で生成）
pyloudnorm==0.2.0
praat-parselmouth==0.4.7
#
# ===========================================================================
# [PENDING 解消 — run 6（2026-08-19 完走・pin 038165b）の実測 freeze]
# ===========================================================================
#
# 捕獲元 = bootstrap が salvage した 2 段の cmdlog（Drive run6/cmdlogs/）:
#   gates_pip-freeze.log     sha256 b2be3ff4fec3902aed3767528007759c42518f7deef10dcddead66b42d7fdc6f（180 パッケージ）
#   binarize_pip-freeze.log  sha256 8170eab7ae4f9042e5317ece262c89ac561e8c78b8f2a1a9534a35695f276223（215 パッケージ）
#
# **重要な実測（正直会計）**: Pod 内には**版の異なる 2 つの環境が共存する**。
#   (A) gates/変換系  = 本ファイル上部の pin 群が支配（numpy 2.x 系）
#   (B) binarize/学習 = DiffSinger requirements.txt が支配（numpy 1.x 系）
# 両者は同一マシン上の別解決であり、揃える設計にはなっていない（D3 render と
# user 正規化の決定論は (A) 側だけで担保され、その出力は pin 照合で機械検証
# されるため、(B) の版差は成果物のバイト同一性に干渉しない）。この 2 環境
# 構造そのものを lock に明記しておくことが、次回以降の「どちらの numpy の
# 話か」という取り違えを防ぐ。
#
# --- (A) gates / 変換系 venv の実測（上部 pin と一致することを確認済み）---
# numpy==2.4.6 / scipy==1.17.1 / pyworld==0.3.5 / soundfile==0.14.0
# numba==0.66.0 / librosa==0.11.0 / pyloudnorm==0.2.0 / praat-parselmouth==0.4.7
#
# --- (B) binarize / 学習側の実測（DiffSinger e2307b1 requirements の解決結果）---
# 学習側は runtime gate の対象外（bootstrap binarize 段の注記）。以下は
# 「この版で 40K 完走・NaN ゼロ・ONNX export 成功」の実測記録であり、
# 強制ではなく**再現の基準線**として記帳する:
#   numpy==1.26.4          scipy==1.17.1        librosa==0.9.2
#   numba==0.66.0          praat-parselmouth==0.4.3   pyworld==0.3.4
#   soundfile==0.14.0      h5py==3.16.0         einops==0.8.2
#   torch==2.4.1+cu124     torchaudio==2.4.1+cu124    torchvision==0.19.1+cu124
#   lightning==2.3.3       pytorch-lightning==2.6.5   torchmetrics==1.9.0
#   onnx==1.22.0           onnxsim==0.7.3       matplotlib==3.11.1
#   tensorboard==2.21.0    tensorboardX==2.6.5  PyYAML==6.0.2
#   click==8.4.2           tqdm==4.70.0         protobuf==7.35.1
#   packaging==24.1
# 全 215 パッケージの一覧は上記 cmdlog（sha256 で pin 済み）が正本。
