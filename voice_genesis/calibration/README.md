# voice_genesis/calibration — RUN10-CAL

campaign_id: `RUN10-CAL`

## 目的

VoiceGenesis の meter（M2 spectral tilt / M2 aperiodicity / M3 formants / M4
resonance / M5 transition / M6 identity / F0 control）を、正解付き合成 fixture
上で校正する技術負債返済キャンペーンの基盤コード。設計正本の核心:

> 正解付き世界で測定器自身を試験し、使える範囲・誤差・失敗条件を先に固定して
> から、VoiceGenesis の個体や遺伝を測る。

正本ドキュメント:

- [`DESIGN_VG_METER_CAL_DEBT_v1.1.md`](DESIGN_VG_METER_CAL_DEBT_v1.1.md) —
  VG-METER-CAL-DEBT-DESIGN-v1.1。統治 revision（現行の技術内容の正本）。
- [`DESIGN_VG_METER_CAL_DEBT_v1.0.md`](DESIGN_VG_METER_CAL_DEBT_v1.0.md) —
  VG-METER-CAL-DEBT-DESIGN-v1.0。**read-only 基底**（以後改変不可。v1.1 の
  `base_document_sha256` front matter が事後改変を pin 検証で検出する）。
- [`IMPLEMENTATION_MAP_v1.md`](IMPLEMENTATION_MAP_v1.md) — 実装マップ
  (Design Memo)。モジュール → 設計正本 §番号の対応表。

## 授権境界（§0）

設計正本は `execution_authorized: false` / `meter_changes_authorized: false` /
`run11_measurement_entry_authorized: false`。本パッケージ（Phase A: framework
core）は **インフラ実装のみ** であり、以下は一切含まない:

- C0 freeze の実行（manifest/registry の凍結・freeze event 記録）
- secret（`split_secret` / `render_root_secret`）の生成・保存
  （全 API は secret を呼び出し側の引数として受け取るのみ。テストはテスト内
  生成のダミー secret を使う）
- 456×5 campaign の実測走行・selection/holdout の実行
- 既存 meter（`voice_genesis/harness/*` 等）の変更
- RUN11 関連の一切

`c0_validate.py`（Phase C）は dry-run 検証のみを行う（書込なし・secret 生成
なし）。武装版 freeze スクリプトは §18 Gate 2 承認後の別 PR。

## モジュール一覧（Phase A: framework core — 実装済み）

| module | 内容 | 正本 § |
|---|---|---|
| `vocab.py` | 全閉語彙（終端 status・手続 Gate・BLOCKED code・missing 理由・independence tier/claim ceiling・evidence class・Domain/Split/MeterId）、`CLAIM_CRITICAL_SET`、`debt_discharged()` / `campaign_closed()` 純導出関数 | §1, §3.3, §4.1, §10.2, §11 |
| `canonical.py` | 独自正規形 `vgcal-canon/1`（sorted keys・最小区切り・NaN/Inf 拒否・float 最短往復・`-0.0`→`0.0` 正規化）+ `row_id` / `manifest_sha` | §7, §3.3 |
| `streams.py` | HKDF-SHA256 (RFC 5869) による RNG stream 分離、`derive_seed`/`derive_generator`、`RngLedger`（secret を記録しない） | §3.3, §7 |
| `splitter.py` | HMAC-SHA256 昇順 → stratum 内 largest-remainder 50/25/25 → family 合計補正 → coverage 制約検査 → 決定的最小 swap 修復 → `RealizedSplitMap` + `verify_split` | §7 |
| `tolerance.py` | pooled dispersion（family×condition class）・`tolerance=max(k·SD,floor)`・floor 機械導出・`UNSTABLE_CELL`・`TOLERANCE_FLOOR_LIMITED` | §6 |
| `observables.py` | 二段 median・`e/AE/RE`・BIAS/MAE/q95(linear)・`U_rep`/`U_proc`（singleton 除外込み）・nuisance `dS`・FDR0/FNR1・failure boundary | §10.1 |
| `gates.py` | threshold budget `M[i]`・ABSOLUTE gate 1/2'/max'/3/4'/5・DIRECTIONAL 単位健全二連言 (+可換時合算式) resolvability。`EUseEvidenceRow.e_use_mode`（`"absolute"`\|`"relative"`、既定 `absolute`。`[UNDERSPEC-CAL-D11]`）: construct 単位の 1 スカラー `e_use_value` が truth 値への相対誤差（例: formant 5%、F0 20 cents）を宣言する場合に使う——per-instance 絶対値展開は `e_use_table.py`/campaign 側の責務、`InstanceMargin.e_use`/`threshold_margin()` は引き続き絶対量のみを消費する | §10.2–10.4 |
| `selection.py` | 族別 lexicographic 選択（丸め後比較）・`select_across_ceilings` ceiling 階級間裁定・`SELECTION_FAILED_CLOSED`。ranking vector は criteria payload が揃っている候補のみに構築し、`eligible=False` 明示 or criteria payload absent のいずれかで ineligible と判定した候補は `(candidate_id, reason)` として `SelectionOutcome.ineligible_candidates` に記録（Codex レビュー 2026-09-01 P1: 従来は全候補に無条件で vector 構築を試み、criteria payload が丸ごと欠けた候補で `ValueError` を送出し fail-closed/ranking パスへ到達できなかった） | §9, §2.6 |
| `status.py` | 終端 status first-match cascade・missing→(status,reason) 一意写像・手続 Gate 単調性検査 | §11, §1 |
| `m6_identity.py` | `u_X[j]`・sum-of-norms pairwise uncertainty・`T_null`・`distinct()`・CLAIM_CRITICAL_SET 全 member ABSOLUTE 必須（部分構成での distance 出力禁止） | §12 |
| `provenance.py` | §13 provenance schema（nested frozen dataclass）+ append-only JSONL ledger（fcntl 排他 + fsync、entry_sha 連鎖、truncated tail / tamper 区別）+ leakage 検査 | §7, §13 |

## fixtures（Phase B — 実装済み）

456 logical cell の明示列挙（IMPLEMENTATION_MAP §2.7 FROZEN spec の機械転記。
行選択の裁量なし）。

| module | 内容 |
|---|---|
| `fixtures/axes.py` | §5.1 primary/boundary 軸水準・§2.7 anchor 水準・family 別 truth 値の凍結定数 |
| `fixtures/matrix.py` | `FixtureRow`（canonical dict → `row_id`）+ `MatrixRow`（+ domain）。family 別 truth core 因子分解・正準 nuisance/boundary/negative 系列の決定的レシピ・per-family targeted interaction 列挙・D2 + §3.3 F0 帯域整合検査による domain 導出・`validate_matrix()` / `assert_no_duplicate_row_ids()` |
| `fixtures/controls.py` | `ControlClass` 閉語彙 + negative control row_id 抽出（`provenance.check_leakage` の `control_row_ids` 用。positive control は truth core 行のため leakage 除外集合には含めない）+ `positive_detection_instances()`（split 内 truth 行から N_pos を instance 数で数える） |
| `fixtures/generators/common.py` | PCM 16-bit 量子化・dBFS gain・20ms cosine ramp・100ms voiced prefix/suffix・transition-adjacent context・declared SNR noise mixing。`finalize()` の適用順序は context→gain→noise: declared gain は context 付加後の完全な assembled waveform 全体へ単一スカラーとして適用する（`make_voiced_tone` も unit peak。Codex レビュー 2026-09-01 P1: 従来は core 単体にのみ gain を適用してから固定振幅 `*0.5` の context トーンを追加しており、voiced-prefix/suffix・transition-adjacent 区間が declared gain と無関係な -6dBFS 相当に固定されていた） |
| `fixtures/generators/{f0_control,formant,tilt,aperiodicity,resonance,transition,identity_sweep}.py` | family 別決定論 generator。FORMANT_GT は resonator code path を共有しない 2 実装（cascade: `scipy.signal.lfilter` 時間領域 / additive: 閉形式 `\|H(f)\|` 周波数領域） |
| `fixtures/determinism.py` | fresh-process byte-identity 検査 → 違反 `BLOCKED_C1_GENERATOR_NONDETERMINISTIC` |

検証済み: 456 行 (48/96/48/72/48/48/96 per family, truth/confound/boundary
sub-count 全一致) / row_id 全 456 一意 / Phase A `splitter.realize_split` を
matrix 全体へ適用すると 228/114/114（family 別も §5.2 の 50/25/25 目標に
厳密一致）。

## candidates（Phase C — 実装済み）

99 候補の宣言的定義（設計正本 §8 + `IMPLEMENTATION_MAP_v1.md` §2.6 が凍結した
パラメタグリッド）+ dry-run C0 manifest 検証器。実測 campaign・selection・
C0 freeze の実行は一切含まない（授権境界は本ファイル冒頭の節を参照）。

| module | 内容 | 正本 § |
|---|---|---|
| `candidates/registry.py` | `ALL_CANDIDATES`（99 件固定）: candidate_id / meter / construct / unit / algorithm_family / parameters / domain / missing_rule / independence_tier / claim_ceiling / complexity_rank / implementation_ref。F0 5・M3 43・M2T 13・M2A 24・M4 5・M5 7・M6 2 | §8, memo §2.6 |
| `candidates/adapter.py` | `MeterAdapter` Protocol・`MeterOutput`（typed missing/ineligible）・共通 fail filter（schema 違反・非有限値の無説明返却・within/fresh-process 不一致・negative control 偽検出・positive control 不発火） | §8 |
| `candidates/impl/f0_pyin.py` | `librosa.pyin` (frame/hop グリッド、fmin=80/fmax=600 固定) | §8 |
| `candidates/impl/formant_cepstral.py` | ケプストラム low-time liftering → 包絡ローカルピーク（M3-CEPSTRAL-POLES、baseline と同族の独立実装） | §8 |
| `candidates/impl/formant_burg.py` | Burg 法 LPC（自前再帰）→ 極抽出。`fs'=2*max_formant_hz` への決定的リサンプル必須（M3-BURG-LPC、唯一の独立 family） | §8 |
| `candidates/impl/tilt_harmonic.py` | 倍音振幅取得を単一方式（最近傍 rFFT ビン + 放物線補間）に凍結。OLS / Theil-Sen 回帰（M2T-HARMONIC-OLS/THEILSEN、K 本未満は縮退せず missing） | §8 |
| `candidates/impl/aperiodicity.py` | HNR-ACF（正規化自己相関ピーク）・harmonic-residual（comb-remove 残差比）・D4C（`pyworld` guarded import、`ModuleNotFoundError` かつ `e.name=="pyworld"` の場合のみ ineligible。ABI/共有ライブラリ破損等の他の import 失敗は re-raise = Codex レビュー 2026-09-01 P2） | §8, §3.3 |
| `candidates/impl/resonance_prominence.py` | 平滑化スペクトル包絡上の `scipy.signal.find_peaks(prominence=...)`（M4-LOCAL-PROMINENCE） | §8 |
| `candidates/impl/transition.py` | wave-discontinuity（短窓 RMS jump）・spectral-flux（frame-to-frame L1/L2 magnitude flux） | §8 |
| `candidates/impl/b0_wrappers.py` | `voice_genesis/harness/measure.py` / `measure_v3.py` の**無改変 import**（既存 `voice_genesis/singer/gate_checks.py` 等と同じ `sys.path` sibling-import パターンを踏襲）。5 つの B0 candidate をここでのみ harness へ配線する | §8 |
| `c0_validate.py` | C0 manifest dry-run 検証: REQUIRED_BLOCKING（§3.1）欠落・hollow（空文字列/空コンテナ）→ `BLOCKED_C0_MANIFEST_INCOMPLETE`（キー存在チェックに加え、path+hash マップの形状・`frozen_design.meter_specs`/`fixture_spec` の全 meter family / 全 7 fixture family 網羅とそれぞれのエントリの完全なネスト鍵集合（`METER_SPEC_REQUIRED_KEYS`/`FIXTURE_SPEC_REQUIRED_KEYS`）・campaign-level セクション（`split_spec`/`selection_spec`/`provenance_spec`/`cost_caps`）のネスト鍵集合・`independence_ledger`/`rng_ledger` のエントリ形状も検証 = Codex レビュー 2026-09-01 P1、`UNDERSPEC-CAL-C17`。`independence_ledger` はさらにキー集合が `candidates.registry.ALL_CANDIDATES` の凍結 99 candidate_id 全集合と完全一致すること、各 entry の tier が registry 宣言 tier と一致することも検証 = Codex レビュー 2026-09-01 追加 P1）。加えて、上記ネスト鍵とトップレベル `frozen_design.stop_rules` は BOUNDED shape validation（`*_hash`/`*_sha256` → 64桁hex sha256、`confound_axes`/`boundary_probes`/`negative_controls`/`stop_rules` → 非空 list、`parameter_grid` → 非空 mapping、`generator_version`/`schema_version` → 非空白 str。値の意味論的相互検証は範囲外・armed producer 実装時に別 PR で対応 = Codex レビュー 2026-09-01 P1、`UNDERSPEC-CAL-C18`）、RECORDED_OR_ABSENT（§3.2）は値または `ABSENT:<理由>` を許容し `WEAK_ENV_LOCK` 降格 annotation、pyworld 特則（§3.3、D4C のみ ineligible・campaign は BLOCK しない）、RNG 台帳の unseeded stream 検出 → `BLOCKED_C0_UNSEEDED_RNG`。**書込・secret 生成・freeze event 記録なし**。§18 Gate 2 承認後の武装版 freeze スクリプトは別 PR（未着手） | §3 |

依存の例外: `candidates/impl/b0_wrappers.py` のみ `voice_genesis/harness/*` を
read-only import する（本ファイル冒頭「テスト」節の「`voice_genesis.harness`
を import しない」は Phase A framework core 各モジュールの依存方針であり、
Phase C の B0 wrapper には適用されない。タスク境界で明示的に許可された唯一の
例外）。`candidates/impl/f0_pyin.py` は `librosa` に依存する
（`IMPLEMENTATION_MAP_v1.md` §1 が宣言する campaign 依存の一部）。

## Phase D — 未武装の実行基盤（D1: freeze producer + 承認 Gate — 実装済み）

§18 の 3 承認 Gate が通ればコード変更なしで C0 freeze と campaign 実行に入れる
状態まで基盤を用意する。本 Phase 自身は freeze も実測も行わない（既定 =
dry-run。武装経路はテストが `tmp_path` 配下の test-local な approval/secret/
campaigns dir に対してのみ実行し、本リポジトリへの実 freeze は行わない =
IMPLEMENTATION_MAP §0）。

| module | 内容 | 正本 § |
|---|---|---|
| `approvals.py` | Gate 1–3 承認ファイル（`VG_CAL_APPROVAL_DIR`、既定 `~/.vg_cal/approvals/`、checkout 外）の loader + shape 検証 + `design_doc_sha256`/`memo_sha256` の実ファイル hash 照合 + 三要素武装判定 `check_armed()`（`--armed` AND 環境変数（gate1/gate3=`VG_CAL_CAMPAIGN_AUTHORIZED=1`、gate2=`VG_CAL_C0_FREEZE_AUTHORIZED=1`）AND 有効な承認ファイル。1 つでも欠ければ `AUTHORIZATION_REQUIRED`）。承認ファイルは `campaign_id` を含まない（PR レビュー第 2 巡: ハッシュ循環回避）。gate1/gate2 は共通 `authorization_nonce` を必須とし、`check_armed(GATE2)` は gate1 が承認済みなら両者の nonce 一致を検証する（不一致 → `AUTHORIZATION_REQUIRED`、理由 `nonce_mismatch`。PR レビュー第 5 巡: 承認の一回性）。gate3 record 型・loader はここに用意するが `c0_freeze.py` はこれを manifest / freeze event のいずれにも埋め込まない（seal 受容は C0 freeze 後に成立する概念のため。D2 runner が別途 `GATE3_ACCEPTED` ledger event で束縛する設計、本 Phase の範囲外）。`check_armed()` は `preloaded=`（`load_all_approvals()` のスナップショット）を受け取れば承認ファイルを再読込しない（PR レビュー第 6 巡 #5: 二重読み排除、`c0_freeze.py` が利用）。**`refresh_document_hashes(approval_path, repo_root)`**（Part A/D1b）: 既存の承認ファイルを再読込し `design_doc_sha256`/`memo_sha256` のみを現在の実ファイルハッシュへ atomic に書き戻す（他フィールド不変、旧/新ハッシュを返す）——メモ編集の都度ハッシュ束縛が無効化されるため、承認者の再承認を代替しない機械的な再スタンプ手段として提供する。CLI: `python -m voice_genesis.calibration.approvals refresh --gate {gate1,gate2,gate3} [--approval-dir] [--repo-root]` | §18, memo §6.1 |
| `e_use_table.py` | `gates.EUseEvidenceRow`（14 列: 必須 13 列 + `e_use_mode`。`[UNDERSPEC-CAL-D11]`）の JSON loader/validator + `generate_template()`（`registry.ALL_CANDIDATES` の一意な `(construct, unit, domain)` タプルごとに 1 行、全て `evidence_class=UNJUSTIFIED` かつ `e_use_value=null`。数値 placeholder なし）+ `auto_ceiling()`（`gates.auto_ceiling_for_unjustified` 委譲）+ `USER_ACCEPTED_USE_BOUND` 行に Gate 1 `e_use_bound_accepted` 承諾を要求する横断検証。実データ = `config/e_use_table_v1.json`（Gate 1 委任決定、`approvals/records/GATE1_DECISION_RECORD.md` 参照） | §10.2, memo §6.3 |
| `cost_caps.py` | `CostCaps`（`compute`/`storage`/`budget` の 3 キー。`c0_validate.COST_CAPS_REQUIRED_KEYS` と一致。単位: 秒/bytes/課金単位）+ `budget_accounting_mode`（`"local_zero_cost"`\|`"per_unit_fixed"` + 後者専用 `budget_unit_cost`。`[UNDERSPEC-CAL-D27]`）+ `CapCounters` 累積器 + `check()` 超過判定（stop event payload 生成） | §14, §18 |
| `c0_freeze.py` | manifest producer: git HEAD/dirty-tree・path+hash 実測・依存 exact version（pyworld guarded）・sample format policy・`frozen_design`（registry/matrix から機械導出）・independence ledger・RNG 宣言台帳・RECORDED_OR_ABSENT env、を `build_manifest()` でコード生成（"core" manifest。`approvals`/`commitments`/`realized_split`/`realized_split_sha`/`campaign_id`/`authorization_nonce` の 6 節を含まない）。`core_payload()`/`manifest_core_sha()` はこの 6 節を（frozen/full manifest から渡されても）除いてから hash する（PR レビュー第 4/5 巡: 定義の精緻化。dry-run の `manifest_core_sha` は armed 後の frozen manifest から同じ 6 節を除いて再計算した値と一致する）。`campaign_id_for()` = `RUN10-CAL-<YYYYMMDD>-<core_sha[:8]>`。`dry_run()` は書込・secret なしで、呼び出しごとに `authorization_nonce`（`secrets.token_hex(16)`。承認の一回性、PR レビュー第 5 巡）も新規発行して報告する。`armed_freeze()` は `check_armed(GATE2)`（Gate 1/Gate 2 の nonce 一致も検証済み）→ Gate 2 承認の `manifest_core_sha` 一致検証（不一致は "承認は別 manifest 用" として拒否）→ `campaigns_dir` 配下の既公開 manifest を同じ `authorization_nonce` で走査し、ヒットすれば `NONCE_ALREADY_USED` で副作用なく拒否（同一承認ファイルによる再 freeze を防ぐ）→ secret 生成（`secrets.token_bytes(32)` ×2）→ `splitter.realize_split()` → 設計正本 §7「正本は C0 manifest に列挙した実現済み row→split 表」に従い `realized_split` を frozen manifest 本体へインライン（`realized_split.json` は同内容の便宜コピー）→ validation → staging（`campaigns/.staging-<id>/` + `secret_dir/.staging-<id>/`、secret dir 0700・secret file 0600）→ read-back（`validate_c0_manifest`/`verify_split`/`Ledger.verify_chain`）→ **`os.replace` を secret dir → campaign dir の順に固定**（PR レビュー第 2 巡）で atomic 公開。二根公開と `detect_orphans()` は同一 `<secret_dir>/.publish.lock` 排他ロックを共有する: `armed_freeze()` は blocking 取得（公開区間全体を保持）、`detect_orphans()` は **non-blocking** 取得（競合時は何もせず即 return。PR レビュー第 4 巡）。ロックを取得できたこと自体が「生きた公開処理は無い」ことの証明になるため、secret のみ（campaign 対応なし）は取得できた時点で常に stale と判定して削除、secret+campaign が揃うのに `.publishing` マーカーが残る（公開完了・マーカー削除だけ未了）場合はマーカーのみ除去する。campaign 側 rename 失敗時は公開済み secret dir を削除してロールバックし、いずれの失敗でも staging を全削除して secret を残さない。**git commit はしない**（ユーザー操作）。**E_use evidence table**（Part A/D1b, `[UNDERSPEC-CAL-D10]`）: `dry_run()`/`armed_freeze()` は既定 `config/e_use_table_v1.json`（`e_use_table_path` で上書き可）を `e_use_table.load_e_use_table()`/`validate_e_use_table()` で load+validate し、違反は `"e_use_table: ..."` detail 付きで `BLOCKED_C0_MANIFEST_INCOMPLETE` へ合流させる（`gate1_e_use_bound_accepted` は Gate 1 承認の `e_use_bound_accepted` から導出）。armed freeze はこのファイルを `campaigns/<id>/e_use_table.json` へコピーし、sha256 を manifest 非-core 節 `frozen_inputs.e_use_table_sha256` と freeze event の双方へ記録する。**PR レビュー第 6 巡（commit 6494395 対象）で 6 件採用**: (1) CLI dry-run は `authorization_nonce` も表示、(2) `detect_orphans()` は既定 dry-run では呼ばない（`--armed` 経路の認可成立後、または明示 `--maintenance-orphans` のみ。副作用ゼロを維持）、(3) 上記 E_use table 統合、(4) 承認の一回性 nonce 走査は `_publish_lock` 取得前の早期チェックに加え、ロック保持中に権威的再チェックを行う（TOCTOU window を閉じる）、(5) 承認ファイルは `load_all_approvals()` を 1 回だけ呼び、`check_armed(preloaded=...)` へそのスナップショットを渡して二重読みを排除、(6) read-back で `render_root_secret.bin` も長さ・生成値・commitment ハッシュの 3 点を検証してから公開し、`_write_secret_file()` は `os.write()` の短い書込みを検出して例外にする。CLI: `python -m voice_genesis.calibration.c0_freeze [--armed] [--repo-root] [--approval-dir] [--secret-dir] [--campaigns-dir] [--e-use-table-path] [--maintenance-orphans]` | §3, §7, §10.2, §18, memo §6.3 |
| `approvals/README.md` | 承認ファイルの外部配置先（`VG_CAL_APPROVAL_DIR`、既定 `~/.vg_cal/approvals/`）・3 ファイル名・スキーマの説明のみ。承認 json 実体はリポジトリに一切格納しない | memo §6.1 |

`.gitignore` に `**/.vg_cal/`・`voice_genesis/calibration/campaigns/*/renders/`・
`voice_genesis/calibration/campaigns/*/measurements/`・
`voice_genesis/calibration/campaigns/.staging-*/` を追加した。

## campaign（Phase D2 — 未武装の campaign runner、実装済み）

`c0_freeze.armed_freeze()` が公開した凍結 campaign dir を読み込み、手続 Gate
単位（C1 fixtures → C2 baseline → C3a F0 selection → C3b selection → unseal →
C4 holdout → close）でキャンペーンを進行させる runner。本 Phase 自身は C0
freeze・secret 生成・RUN11 関連を一切行わない。既定はサブコマンドごとの
dry-run（`plan`、または `--armed` を渡さない全サブコマンド）であり、
`--armed` + 環境変数 `VG_CAL_CAMPAIGN_AUTHORIZED=1` + 有効な Gate 1 承認
ファイルが揃わなければ `AUTHORIZATION_REQUIRED` で副作用ゼロのまま拒否する。

| module | 内容 | 正本 § |
|---|---|---|
| `campaign/state.py` | 凍結 campaign dir（`c0_manifest.json`/`realized_split`/`ledger.jsonl`）+ `<secret_dir>/<campaign_id>/` の secret 2 ファイルを読み込み検証する `load_frozen_campaign()`（ledger chain 検証・freeze event 先頭確認・commitment sha256 照合・`realized_split_sha` 照合。1 つでも失敗すれば `CampaignStateError`）。`detect_orphans()` と同じ fail-closed 意味論（campaign dir はあるが対応する secret dir が無ければ拒否）をここでも単体適用する。D2 runner 固有の拡張手続フェーズ `CampaignPhase`（`PREPARATION_VALID`/`FIXTURE_VALID`/`BASELINE_AUDITED`/`F0_SELECTION_FROZEN`/`SELECTION_FROZEN`/`UNSEALED`/`HOLDOUT_EXECUTED_VALID`/`CAMPAIGN_CLOSED`）を ledger event の `kind` から導出し、前半 5 値は `vocab.ProcedureGate`/`vocab.procedure_gates_monotonic()` へ委譲して単調性を検査する（`[UNDERSPEC-CAL-D19]`） | §1, §6.4 |
| `campaign/workunits.py` | C1/C4 render work unit（instance = row×probe_index、2 fresh-process render）・C2/C3a/C3b/C4 の測定 instance 集合・meter call work unit（instance×candidate×{within 3, fresh 3}）の決定論的列挙。`plan_counts()` は §6/§14 の設計値（instances 2,280 / renders 4,560 / meter calls 13,680 per implementation）を `axes.TOTAL_LOGICAL_CELLS`/`controls.PROBE_REPEATS` から再導出し、`realized_plan()` は実 realized split 上の内訳を返す | §6, §6.4, §14 |
| `campaign/render_stage.py` + `campaign/_render_worker.py` | C1/C4 render: `render_root_secret` から streams 派生 RNG で 2 回 fresh-process render（`_render_worker.py` を subprocess 起動。`fixtures.determinism.render_row_pcm_hex` の薄いラッパー）し byte 一致を要求、不一致は `BLOCKED_C1_GENERATOR_NONDETERMINISTIC` stop event + fail-closed。一致すれば `renders/<row_id>/<probe>.pcm` + sha256 を書き `render` ledger event を記帳。resume は ledger 記録済み sha と現ファイル sha の一致時のみスキップ、不一致/欠損は stale として fail-closed。C4 は render 前に `provenance.Ledger.check_leakage()` を呼び unseal 前なら `BLOCKED_LEAKAGE` で拒否する | §3.3, §6, §7, memo §6.4 |
| `campaign/measure_stage.py` + `campaign/_measure_worker.py` | meter call: within-process 3 call（プロセス内直接呼び出し）+ fresh-process 3 call（`_measure_worker.py` を subprocess 起動、PCM ファイルを再読込）。`implementation_ref` は `importlib` で解決。単一 writer 境界: worker は stdout 経由で結果を返すのみで ledger には触れず、呼び出し元が直列に `meter_call` event を append する（`[UNDERSPEC-CAL-D14]`）。`cost_caps.check()` の超過で `stop_event` 記帳 + `CostCapExceededError`。`PRIMARY_OUTPUT_FIELD_BY_ALGORITHM_FAMILY`（`[UNDERSPEC-CAL-D12]`）が `MeterOutput.values` の主要スカラーフィールドを候補の algorithm_family から機械的に定める | §6, §8, §14 |
| `campaign/baseline_stage.py` | C2: B0 候補（`candidate_id` に `-B0-` を含む全候補）× CALIBRATION split を実測 → `tolerance.pooled_dispersion()`（プール粒度 = candidate_id 単位、`[UNDERSPEC-CAL-D15]`）→ `tolerance = max(k·pooled_SD, floor)` を候補ごとに導出 → `baseline_audit`（artifact_sha）+ `baseline_audited` event | §6, memo §6.4 |
| `campaign/selection_stage.py` | C3a（F0_CONTROL candidates）→ `f0_selection_frozen` event（unseal chain には参加しない）。C3b（F0_CONTROL を除く各 family、`select_across_ceilings()`）→ `candidate_space`/`selection_rule`/`selected_candidate`（全 non-F0 family の選択結果を 1 event へ集約、`[UNDERSPEC-CAL-D20]`）の 3 前提 event + `baseline_audit`（呼び出し側が C2 の event entry_sha を渡す）を合わせた 4 前提 **entry_sha** 参照を持つ `selection_frozen` event。`build_candidate_criteria()`（`[UNDERSPEC-CAL-D16]`）は実測 record から normalized MAE/bias/q95(AE)（ABSOLUTE）・Kendall tau/隣接反転率（DIRECTIONAL）を集計する。`truth_value_for_row()`（`[UNDERSPEC-CAL-D13]`）は family の主要 truth スカラーを返す（FORMANT_GT は F1 代表）。`candidate_fail_filter_report()` の positive control 母集団は `cli.py` 側で `fixtures.controls.positive_detection_instances()` から導出する（`[UNDERSPEC-CAL-D25]`。designated 2-anchor ではなく評価対象 split の全 TRUTH_CORE 行）。同関数は `positive_rows_absent` filter を持つ（宣言された正母集団が非空にもかかわらず record が 1 件も一致しない場合を ineligible とする） | §7, §9, memo §2.6 |
| `campaign/unseal.py` | §7 の 5 sha 相互参照検査: `selection_frozen` event を見つけ、その 4 前提 sha（entry_sha 参照）が実在し正しい kind を指すことを検証 → Gate 3（`approvals.Gate.GATE3_SEAL_ACCEPTANCE`、`seal_protection_level_accepted`）承認を検証 → `gate3_accepted` event（承認ファイル content sha256 記録）→ `holdout_unseal` event（`gate3_accepted_sha` で 2 の event を参照。`provenance.Ledger.check_leakage`/`_verified_holdout_unseal_seq` が権威的に認識する形——検証側は `gate3_accepted_sha` が prior chain-valid な `gate3_accepted` event を指すことも必須とする、`[UNDERSPEC-CAL-D50]`）。いずれか欠ければ `UnsealError`、ledger には一切追記しない | §7, §18, memo §6.4 |
| `campaign/holdout_stage.py` | C4 gate 判定 + 終端 status cascade。building blocks（`build_instance_margins`/`build_directional_pairs`/`evaluate_absolute_meter`/`evaluate_directional_meter`）は `observables.py`/`gates.py`/`status.py` を呼ぶ薄いラッパーで、合成観測値のみで単体テスト可能。`declared_axes_for_family()`（`[UNDERSPEC-CAL-D18]`）は凍結 `frozen_design.fixture_spec.<FAMILY>.confound_axes` を gate4' invariance 軸/DIRECTIONAL sweep_id 宣言として再利用する。`load_e_use_rows()`/`split_e_use_rows_by_mode()` は `gates.EUseEvidenceRow.e_use_mode`（absolute/relative）で E_use 行を分割する。`render_and_measure_holdout()`/`run_holdout_stage()` は実 audio 経路のオーケストレーション。`HOLDOUT_EXECUTED_VALID` は全 meter の終端 status + reason code をまとめた単一 event | §10, §11, memo §6.4 |
| `campaign/close.py` | `CAMPAIGN_CLOSED` event: 全 `vocab.MeterId` が終端 status を持つことを要求（`vocab.campaign_closed()`）し、`derived.debt_discharged`（`vocab.debt_discharged()` の派生値の写しのみ、宣言フィールド化はしない = D1）を記録。CLAIM_CRITICAL_SET 全 member が CALIBRATED_ABSOLUTE かつ M6 component/E_use が明示供給された場合のみ `m6_identity.m6_distance()` を計算。`reveal_split_secret()`（`[UNDERSPEC-CAL-D09]`、`--reveal-split-secret` 経由のみ）は CAMPAIGN_CLOSED **後**にのみ `split_secret` の commit-reveal event を許可する | §1 D1, §7, §12, memo §6.4 |
| `campaign/cli.py` + `campaign/__main__.py` | `python -m voice_genesis.calibration.campaign <plan\|c1-fixtures\|c2-baseline\|c3a-f0-selection\|c3b-selection\|unseal\|c4-holdout\|close> --campaign-dir ... --secret-dir ... [--armed] [--workers N] [--reveal-split-secret]`。`plan` は常に副作用ゼロで設計値 vs realized split の work unit 件数を報告。他サブコマンドは `--armed` 無しなら当該 stage の計画のみ表示、`--armed` ありなら三要素武装判定（`approvals.check_armed(GATE1)`）→ 未武装なら `AUTHORIZATION_REQUIRED`（副作用ゼロ）→ 武装済みなら該当 stage 関数を実行。`c4-holdout` の E_use 拘束 absolute/directional gate 組立の完全な CLI 配線は本 D2 infra の範囲外とした（`[UNDERSPEC-CAL-D17]`。`holdout_stage.evaluate_absolute_meter`/`evaluate_directional_meter` は実 gate 配線込みでテストで直接検証する）。`main()` は `campaign.caps.load_cap_counters()` 直後・stage dispatch 前に frozen cap 超過を再チェックし、breach していれば dispatch 前に `COST_CAP_EXCEEDED` で拒否する（idempotent stop_event。`[UNDERSPEC-CAL-D26]`）。同じ箇所で `campaign.caps.cost_caps_from_manifest()` の `cost_caps.BudgetAccountingUndeclaredError` を捕捉し `BUDGET_ACCOUNTING_UNDECLARED` で dispatch を拒否する（`[UNDERSPEC-CAL-D27]`） | §18, memo §6.1, §6.4 |
| `tools/archive_aborted_ledger.py` | 破棄（abort）裁定済み campaign の `ledger.jsonl` を原子的に gzip 保全する運用ツール（同一ディレクトリ内固定名 staging → 伸長+chain 検証 → `os.fsync`+`os.rename` で公開 → 原本削除。中断回復込みの単一入口 `ensure_archived()`）。詳細は下記「運用」節と `tools/archive_aborted_ledger.py` の docstring を参照 | v1.1 §V4 |

## 運用

### 破棄 campaign ledger の圧縮保全（v1.1 §V4）

破棄（abort）裁定済み campaign の `ledger.jsonl` は
`voice_genesis/calibration/tools/archive_aborted_ledger.py` で原子的に
gzip 保全する:

    python -m voice_genesis.calibration.tools.archive_aborted_ledger \
        voice_genesis/calibration/campaigns/<discarded-campaign-id>

対象ディレクトリに `ledger.jsonl.gz` + `ledger.jsonl.sha256`
（`sha256sum -c` 互換）が生成され、原本 `ledger.jsonl` は検証成功後にのみ
削除される。原子的置換・中断回復の詳細仕様は設計正本
`DESIGN_VG_METER_CAL_DEBT_v1.1.md` §V4、実装/回復ロジックは
`tools/archive_aborted_ledger.py` の docstring を参照。

**閉鎖（CAMPAIGN_CLOSED）campaign には絶対に適用しないこと** —
このスクリプトは対象を選ばないため、呼び出し側が対象を破棄 campaign に
限定する責務を負う。

適用済み: `RUN10-CAL-20260903-9bcbbf86`
（sha256=`2be10706717efd51bc08069d73143fb4d99bfc50bc7f078785a75ccdbb184221`）、
`RUN10-CAL-20260903-591cadcd`
（sha256=`c02f513b086ade0b2e23e2153adb009bf51204dfb3bfb9d61ff8a3828e969580`）。

## UNDERSPEC 台帳

設計正本が数値・グリッド・エンコーディングを確定していない箇所について、
実装マップ §3 の規約（最も単純で安全な選択を採り docstring にタグ記録）に
従って導入した判断の一覧。C0 freeze 承認時のユーザーレビュー対象であり、
コードが設計正本を上書きするものではない。

| tag | module | 内容 |
|---|---|---|
| `UNDERSPEC-CAL-01` | `vocab.py` | `IndependenceTier.CROSS_IMPLEMENTATION` の claim ceiling 写像を `ABSOLUTE`（tier が許す上限。実際の到達は gate 判定で下方に絞られる）とした |
| `UNDERSPEC-CAL-02` | `streams.py` | HKDF `info` のフィールド連結を、区切り文字連結ではなく衝突耐性のある長さ接頭辞（4-byte big-endian）方式で実装した |
| `UNDERSPEC-CAL-03` | `splitter.py` | stratum 内 largest-remainder の closed-form 導出（`n mod 4` で場合分け）と、SEL/HOLD 端数 tie（構造的に常に同値）を stratum 内 HMAC 順位最大行の末尾ニブル偶奇で決める規則 |
| `UNDERSPEC-CAL-04` | `splitter.py` | family 合計の厳密一致は pairwise swap だけでは原理的に不可能なため、`reason="family_total"` の片道移動を導入し `reason="coverage"` の真の 2 行交換と区別した |
| `UNDERSPEC-CAL-05` | `tolerance.py` | floor 導出式を「PCM 量子化半ステップ・float eps bound・meter 宣言分解能の `max()`」として機械導出した |
| `UNDERSPEC-CAL-06` | `splitter.py` | coverage repair の donor 選択に、victim と対称な `_safe_to_remove` 安全性検査を適用した（同一行が違反間を往復し repair が収束しない振動を防ぐ） |
| `UNDERSPEC-CAL-07` | `provenance.py` | ledger `entry_sha` の digest 対象に `payload`+`prev_sha` に加えて `seq` も含めた（chain 上の位置を署名に取り込む保守的選択） |
| `UNDERSPEC-CAL-08` | `m6_identity.py` | M6 component の識別子型を `vocab.MeterId` に固定した（他 meter の校正 status と直接突合できる一貫性を優先） |
| `UNDERSPEC-CAL-B01` | `fixtures/axes.py` | negative control の `TOO_SHORT` (0.02s) / `INVALID_SR` (8000Hz) 具体数値。boundary probe（0.10s/16000Hz）より外側の値とし、boundary 探査点と negative 探査点を区別した |
| `UNDERSPEC-CAL-B02` | `fixtures/axes.py` | targeted interaction の "low-SR/high-SR" を boundary SR 極値 (16000/96000) と定義した。primary SR 極値 (24000/48000) のみで表現すると、anchor F0 が既に primary 低域にある family（FORMANT_GT 等）で interaction 行が truth core anchor 行と完全一致する退化（row_id 衝突）が生じるため |
| `UNDERSPEC-CAL-B03` | `fixtures/axes.py` | FORMANT_GT の confound/boundary/negative 行（truth core 以外）の `generator_impl` を `cascade` に固定した（implementation は truth core でのみ因子として直積される） |
| `UNDERSPEC-CAL-B04` | `fixtures/axes.py` | RESONANCE_GT / TRANSITION_GT（truth core に F0 を因子として持たない family）の励起/context 用 F0 を primary domain 中央値 C4 (261.626Hz) に固定した |
| `UNDERSPEC-CAL-B05` | `fixtures/axes.py` | TRANSITION_GT の 3 severity（設計正本が「C0 表で固定」として据え置く discontinuity magnitude）を low=0.15/medium=0.35/high=0.65（無次元、join_type ごとに解釈）とした |
| `UNDERSPEC-CAL-B06` | `fixtures/axes.py` | TRANSITION_GT の 2 duration class（join 遷移窓長。primary "duration" 軸のクリップ全長とは別概念）を short=5ms/long=50ms とした |
| `UNDERSPEC-CAL-B07` | `fixtures/axes.py` | IDENTITY_CAUSAL_SWEEP の 4 founder（distinct F0/formant-set/tilt parameter bundle）の具体値。primary F0 4 水準 + FORMANT_GT pole set の一部を再利用し内部一貫性のある bundle を凍結した |
| `UNDERSPEC-CAL-B08` | `fixtures/axes.py` | IDENTITY_CAUSAL_SWEEP の 3 trait の generator-unit→物理量換算則（F0: 1 unit=5 cents、FORMANT_SHIFT: 1 unit=pole周波数 2% scale、TILT_SLOPE: 1 unit=1 dB/oct）を凍結した |
| `UNDERSPEC-CAL-B09` | `fixtures/axes.py` | F0_CONTROL の第 2 confound anchor（設計正本 §2.7 の anchor 一覧は単一 anchor C4@48k のみ明記するが、confound 件数検算「F0 24=11+6+7」は FORMANT_GT/IDENTITY と同型の 2-anchor 構造を要求する）を G4@48k（F0 のみ変更）とした |
| `UNDERSPEC-CAL-B10` | `fixtures/axes.py` | single-anchor family（TILT/APERIODICITY/RESONANCE/TRANSITION）の positive control 用第 2 anchor（§2.7 control 共有契約は「2 anchor truth rows per family」を要求するが、これら 4 family の anchor 一覧は単一 anchor のみ明記）を、truth core grid 上で A1 と最も対照的な点（primary sweep 軸の反対端）とした |
| `UNDERSPEC-CAL-B11` | `fixtures/generators/tilt.py` | TILT_GT の dB/oct slope 定義を `A_k[dB]=slope*log2(k)`（高調波次数 k の log2、`k=1` を 0dB 基準）の 1 定義に凍結した（回帰ルーチンは使わず、meter 側の tilt 推定と非共有） |
| `UNDERSPEC-CAL-B12` | `fixtures/generators/transition.py` | `duration_class`（short=5ms/long=50ms, `UNDERSPEC-CAL-B06`）を 4 join type（amplitude-step/phase-jump/spectral-envelope-switch/crossfade）全てで「join time を中心とした raised-cosine 遷移窓の物理的な長さ」として具現化した（Codex レビュー 2026-09-01 P1: 従来 crossfade 以外の 3 join type は瞬時切り替えで `duration_class` を無視しており、同一 severity の short/long 行が byte-identical に render されていた） |
| `UNDERSPEC-CAL-C01` | `candidates/impl/b0_wrappers.py` | 5 つの B0 candidate と harness 関数の配線対応（`F0-B0-CURRENT`→`estimate_f0_hps`、`M3-B0-CURRENT-CENTROID`/`M4-B0-CURRENT-CENTROID`→ともに `formant_centroid_and_f1`（M3/M4 で同一の現行 centroid 実装を診断的に再利用）、`M2T-B0-CURRENT-HYBRID`→`source_tilt_v2`（regression/h1h2 の unit 混在そのものが「HYBRID」の実体。`spectral_tilt_db_per_oct` は legacy 参照値として diagnostics に同梱）、`M2A-B0-AUTOCORR-PERIODICITY`→`hnr_db_approx`）を設計正本の候補名一致から導いた |
| `UNDERSPEC-CAL-C02` | `candidates/impl/formant_cepstral.py` | M3 formant 系のピーク missing 閾値を「帯域内ピーク 0 個で OUTPUT_MISSING」とした（M2T の「K 本未満は missing」に相当する明記が M3 にはないため、最も単純な閾値を採用） |
| `UNDERSPEC-CAL-C03` | `candidates/impl/formant_burg.py` | Burg LPC 実装詳細（リサンプラ=`scipy.signal.resample_poly`（有理比を `Fraction` で厳密化）、preemphasis=1 次ハイパス時定数として `preemph_hz` を解釈、窓関数=Hamming、極選択=単位円内・虚部正・周波数昇順）を機械的に選んだ |
| `UNDERSPEC-CAL-C04` | `candidates/impl/resonance_prominence.py` | 包絡平滑化を移動平均（box filter、帯域幅 Hz→ビン数換算）とした（設計正本は平滑化アルゴリズムそのものは規定しない） |
| `UNDERSPEC-CAL-C05` | `candidates/registry.py` | `complexity_rank` を「本モジュール内の宣言順（B0→§8 記載順の family→grid 軸宣言順）の 0-based 連番」とした（meter family 内で一意な全順序を与える最も単純な規則。実計算コストの実測値ではない） |
| `UNDERSPEC-CAL-C06` | `candidates/registry.py` | `M2T-B0-CURRENT-HYBRID`（「そのままでは INVALID」）を vocab 4-tier のうち意味が最も近い `INVALID_CIRCULAR`/`ClaimCeiling.NONE` へ割り当てた（真の circular ではなく unit/construct 不一致が実体。専用 tier が閉語彙に存在しないための代用） |
| `UNDERSPEC-CAL-C07` | `c0_validate.py` | RECORDED_OR_ABSENT（§3.2）キーが manifest に全く存在しない場合を REQUIRED_BLOCKING と同様の missing 扱いとした（「値または `ABSENT:<理由>` を必須記録」の「必須記録」を文字通り読んだ）。`WEAK_ENV_LOCK` 降格 annotation は §3.2 の 5 項目全てに一律適用する（設計正本の明示例は container/image digest の 1 件のみ） |
| `UNDERSPEC-CAL-C08` | `c0_validate.py` | RNG 台帳 entry のフィールド名を `{"stream_name": str, "seeded": bool}` に固定した（設計正本は entry の具体的なフィールド名までは規定しない） |
| `UNDERSPEC-CAL-C09` | `candidates/registry.py`, `candidates/impl/aperiodicity.py` | M2A-HARMONIC-RESIDUAL の残差帯域グリッド「0–Nyquist」を実装トークン `broadband`（D4C 側の帯域トークンと統一）へ写像した |
| `UNDERSPEC-CAL-C10` | `c0_validate.py` | path+hash 系マップ（`candidates.*_paths_sha256`）の各エントリを `path(非空文字列) -> sha256(64 桁小文字 16 進)` 形状として検証した（Codex レビュー 2026-09-01 P1） |
| `UNDERSPEC-CAL-C11` | `c0_validate.py` | `frozen_design.meter_specs` が `candidates.registry.ALL_CANDIDATES` の全 meter family（vocab.MeterId 全件）をカバーすることを要求し、欠落 meter を個別キーとして列挙する規則にした（Codex レビュー 2026-09-01 P1） |
| `UNDERSPEC-CAL-C12` | `c0_validate.py` | `independence_ledger` の各エントリ値を `vocab.IndependenceTier` の閉語彙メンバーであることまで検証した（Codex レビュー 2026-09-01 P1）。加えて、ledger のキー集合は `candidates.registry.ALL_CANDIDATES` の凍結 99 candidate_id 全集合と完全一致（欠落・unknown/extra を個別列挙）、各 entry の tier は registry 宣言 tier と一致（不一致を個別列挙）することを要求する（Codex レビュー 2026-09-01 追加 P1: 従来はキー集合の網羅性を一切検査していなかった） |
| `UNDERSPEC-CAL-C13` | `c0_validate.py` | `rng_ledger` エントリの形状を `{"stream_name": str(非空), "seeded": bool}` に加え、`seeded=true` の場合は非空 `public_seed_id`（`streams.RngLedgerEntry.public_seed_id` と命名を揃えた seed 参照）を必須とした（Codex レビュー 2026-09-01 P1: §3.3「stream 列挙 + seed 参照」の反映） |
| `UNDERSPEC-CAL-C14` | `c0_validate.py` | path+hash 系マップ（`candidates.*_paths_sha256`）4 カテゴリの合併集合が要求すべき inventory を、版管理されコミットされた閉じた inventory ファイル `c0_path_inventory.json`（`calibration_path_inventory()` が parse-strict に読む。public 関数として freeze script 再利用可）として機械定義した（Codex レビュー 2026-09-01 P1: 従来は supplied entries のみを検証しファイル省略・phantom path 混入を検出できなかった。P1 #2 改訂: inventory 自体を検証対象 checkout への `rglob` から算出していたのは circular だったため、版管理済み JSON へ切替。実ツリーとのドリフト検知は `scan_calibration_tree_inventory()` + `tests/test_c0_path_inventory_sync.py` が別途担う） |
| `UNDERSPEC-CAL-C15` | `fixtures/generators/resonance.py` | declared `noise_snr_db` の nuisance noise を prominence 較正の floor 測定より前に解析的に折り込む式（pre-gain スケールでの SNR 式適用。gain は mixed signal 全体への単一スカラー倍のため相対 dB 比を保存する）を採用した（Codex レビュー 2026-09-01 P1: 従来 `common.finalize()` が較正後にこの noise を加えており、noise 軸 confound 行で実現 prominence が declared 値を下回っていた）。同様に `context`（cosine-ramp/voiced-prefix-suffix/transition-adjacent）も較正パスへ折り込み、連結型 context は "steady" core 区間に測定窓を限定した（Codex レビュー 2026-09-01 P1） |
| `UNDERSPEC-CAL-C16` | `streams.py`, `c0_validate.py` | C0 の `rng_ledger` 記録粒度を「family ごとの generator render stream 1 個 (`streams.stream_name()` が purpose を `"<family>/render"` へ折り畳む) ∪ `"split/hmac"` ∪ `"split/tiebreak"`」の 9 stream closed set に固定した（`streams.expected_rng_stream_names()` が code-derived に導出。row/probe 単位の実 HKDF 導出はこの per-family stream の sub-derivation として個別列挙しない）。`c0_validate._check_rng_ledger_closed_set` がこの closed set との厳密一致（欠落・unknown/extra・重複を個別列挙）を要求する（Codex レビュー 2026-09-01 P1 finding #2: 従来は entry 形状のみを検証し stream 集合が閉じているかを一切見ていなかった） |
| `UNDERSPEC-CAL-C17` | `c0_validate.py` | `frozen_design` の各セクションが持つべき完全なネスト鍵集合を module-level frozen 定数として定義した: 「meter 別 construct/unit/domain/algorithm family/有限 parameter grid/baseline/fallback/missing・failure rule」を `METER_SPEC_REQUIRED_KEYS`、「fixture family・generator version/hash・known-truth field・confound 軸・boundary probes・negative controls」を `FIXTURE_SPEC_REQUIRED_KEYS`（`fixtures.axes.FixtureFamily` の全 7 family をキーとする mapping、`meter_specs` と対称な網羅性検査を追加）、「split・seed・seal」を `SPLIT_SPEC_REQUIRED_KEYS`、「selection rule・tie rule・candidate exhaustion rule・holdout FAIL 後の固定 outcome」を `SELECTION_SPEC_REQUIRED_KEYS`（旧 `frozen_design.selection_rule` を `selection_spec` へ改名・4 項目に拡張）、「provenance schema・artifact layout」を `PROVENANCE_SPEC_REQUIRED_KEYS`、「cost cap」を `COST_CAPS_REQUIRED_KEYS`（compute/storage/budget の 3 次元に固定）とした。`stop_rules` は `frozen_design` の新規 REQUIRED_BLOCKING キーとして追加したが、設計正本がネスト構造を規定しないため非空チェックのみとした（Codex レビュー 2026-09-01 P1: `fixture_spec={"family": "F0_CONTROL"}` のような hollow な placeholder manifest が、メータ ID 網羅性チェックのみでは通過してしまっていた） |
| `UNDERSPEC-CAL-C18` | `c0_validate.py` | `frozen_design` ネスト鍵（`meter_specs`/`fixture_spec`/`split_spec`/`selection_spec`/`provenance_spec`/`cost_caps` 配下）と `stop_rules` に BOUNDED shape validation を追加した: フィールド名が `*_hash`/`*_sha256` で終わるものは bare 64 桁小文字 16 進 sha256 文字列、`confound_axes`/`boundary_probes`/`negative_controls`/`stop_rules` は非空 list、`parameter_grid` は非空 mapping、`generator_version`/`schema_version` は非空白 str であることを機械的に要求する規則をフィールド名の命名規則から導出した（設計正本 §3.1 はネストしたリーフ値の型までは規定しないため）。**値の意味論的相互検証（registry/matrix との突合）は本 validator の範囲外とし、armed C0 freeze producer 実装時（§18 Gate 2 承認後の別 PR）の責務とする設計判断**（Codex レビュー 2026-09-01 P1: `generator_hash="not-a-hash"`・`confound_axes="x"`・`parameter_grid=1` のような、非 hollow だが型として明らかに壊れた scalar 値が従来の存在/hollow チェックのみでは通過してしまっていた） |

| `UNDERSPEC-CAL-D01` | `c0_freeze.py` | `measurement_directory_status` を `tests/test_c0_validate.py` の慣例と同一の固定文字列 `"ABSENT:legacy_path=voice_genesis/harness/measure_v3.py"` として producer 側にも転記した |
| `UNDERSPEC-CAL-D02` | `c0_freeze.py` | `repo.url` は `git remote get-url origin` を実測し、取得失敗時のみ CLAUDE.md 記載の canonical URL へ fallback する |
| `UNDERSPEC-CAL-D03` | `c0_freeze.py` | path+hash 系マップの producer 側カテゴリ分類規則を `tests/test_c0_validate.py::_classify_path` と同一（`candidates/`→meter、`fixtures/generators/`→generator、`tests/`→test、他→schema）に揃えた（`c0_validate.py` はカテゴリ単位の完全性を要求しないため、テスト fixture との整合性のみを目的とする選択） |
| `UNDERSPEC-CAL-D04` | `c0_freeze.py` | pyworld wheel hash は本環境から安価に取得できないため常に `ABSENT:wheel_hash_not_recorded`（pyworld version は `importlib.metadata` で実測、未インストールなら `ABSENT:not_installed`） |
| `UNDERSPEC-CAL-D05` | `c0_freeze.py` | `sample_format` policy を `fixtures/generators/common.py` の実装事実（`quantize_pcm16` の最終 PCM int16 出力・全 generator が 1 次元=mono）と `candidates/impl/formant_burg.py` の唯一のリサンプラ実装から機械転記した |
| `UNDERSPEC-CAL-D06` | `c0_freeze.py` | `frozen_design.fixture_spec.<FAMILY>.confound_axes`/`.boundary_probes` を、matrix.py が既に厳密に持つ per-family targeted interaction 実列挙（memo §2.7）を二重管理しない、より粗い「変動しうる primary/boundary 軸名」の宣言に留めた（非空 list 要求は満たす。値の意味論的相互検証は `c0_validate.py` の範囲外） |
| `UNDERSPEC-CAL-D07` | `c0_freeze.py` | C0 manifest の `rng_ledger` を "declaration form" とし、`public_seed_id` を `sha256("declared:"+stream_name)`（公開情報のみから導出する placeholder）とした。`build_manifest()` は dry-run/armed 双方から同一シグネチャで呼ばれ secret を一切受け取らないため（secret から実導出した OKM の digest ではない） |
| `UNDERSPEC-CAL-D08` | `c0_freeze.py` | `splitter.realize_split` の stratum 化因子を `("truth_level", "boundary_class")` に固定した（`splitter._COVERAGE_AXES` のうち row 単位で常に定義される 2 軸。`generator_impl` は FORMANT_GT 以外で常に `None` のため除外） |
| `UNDERSPEC-CAL-D09` | `armed_freeze()`（設計正本 §7） | campaign close 後に `split_secret` を ledger へ reveal（commit-reveal）し、検証器（第三者）が split を再計算・再現できるようにする運用を新規提案した。設計正本 §7 は secret の生成時保持と sha256 commitment の記録は明記するが、CAMPAIGN_CLOSED 後の reveal 手順を明記しない（`approvals/records/GATE1_DECISION_RECORD.md` §5.1）。C0 レビュー対象、本 Phase では未実装 |
| `UNDERSPEC-CAL-D10` | `c0_freeze.py` | E_use evidence table（設計正本 §10.2）の既定 path を `voice_genesis/calibration/config/e_use_table_v1.json`（repo-relative、追跡下）に固定した。承認ファイルとは異なり、これはユーザーが事前記入・コミットするデータであり dirty-tree 判定の対象内で構わない。dry-run は load+validate のみ（違反は `BLOCKED_C0_MANIFEST_INCOMPLETE` に `"e_use_table: ..."` detail で合流）、armed freeze は `campaigns/<id>/e_use_table.json` へコピーし sha256 を非-core 節 `frozen_inputs.e_use_table_sha256` と freeze event へ記録する |
| `UNDERSPEC-CAL-D11` | `gates.py`, `e_use_table.py` | `EUseEvidenceRow` に 14 列目 `e_use_mode: "absolute"\|"relative"`（既定 `"absolute"`、旧 13 列のみの行と後方互換）を追加した。設計正本 §10.2 は E_use を「用途許容誤差」とのみ述べ、construct 単位の 1 スカラーが truth 値に対する相対誤差（例: formant の 5%、F0 の 20 cents）を宣言する手段までは規定しない。`"relative"` 行の per-instance 絶対値展開（`e_use_value × declared_truth`）は本拡張の範囲外（campaign 側の責務） |
| `UNDERSPEC-CAL-D25` | `campaign/cli.py`, `campaign/selection_stage.py` | round 13 finding #1: positive 証拠母集団を designated 2-anchor（`fixtures.controls.positive_control_row_ids()`）から評価対象 SELECTION split の当該 family 全 TRUTH_CORE 行（`positive_detection_instances()`。`fixtures/controls.py` 自身の DESIGN RULING と整合させた）へ拡張し、C3a/C3b の fail filter 入力をそこから導出するよう配線し直した。加えて `candidate_fail_filter_report()` に `positive_rows_absent` filter を新設: 宣言された正母集団が**非空**にもかかわらず record が 1 件も一致しない場合を「判定材料なし=non-failure」ではなく明示的な ineligible 理由として扱う（引数自体が空＝family に正母集団が無い、という既存の正当なケースとは区別する） |
| `UNDERSPEC-CAL-D26` | `campaign/cli.py` | round 13 finding #2: `--armed` dispatch は `counters.json` 読み戻し直後（stage 呼び出しの前）に凍結 cap 超過を再チェックする規則を新設した。設計正本/IMPLEMENTATION_MAP は「cap 超過時に stop event + fail-closed」とのみ述べ、`counters.json` を re-load しただけの新規プロセス起動時点でも同じ判定を行うことまでは明記しないため、最も安全側の解釈（起動直後に再チェックしない限り、既に breach 済みの永続化 counter を持つ campaign へ retry するたびに 1 work unit 分課金が進んでしまう）を採用した。同一 breach を指す `stop_event`（reason/counters/caps が一致）が既に ledger 末尾にあれば重複記帳しない |
| `UNDERSPEC-CAL-D27` | `cost_caps.py`, `campaign/render_stage.py`, `campaign/measure_stage.py`, `approvals.py` | round 13 finding #3: `CostCaps` に `budget_accounting_mode`（closed vocabulary `"local_zero_cost"`\|`"per_unit_fixed"` + 後者専用の `budget_unit_cost`）を追加した。設計正本/IMPLEMENTATION_MAP は `budget` cap の値と超過判定のみを規定し、render/measurement の各 work unit が `budget_used` へ何をどれだけ加算するかの会計規則を規定しないため（結果として実装が存在せず `budget` cap が常に非発火の死んだ次元だった）、最も単純な 2 モード（ローカル計算資源のみなら 0、外部課金資源ありなら固定単価）を凍結した。mode 欠落/閉語彙外は `BudgetAccountingUndeclaredError`（`BUDGET_ACCOUNTING_UNDECLARED`）で dispatch を fail-closed に拒否する。Gate 1 承認 payload の一部として `approvals.py` が検証し、凍結 manifest の `frozen_design.cost_caps` へ含まれるため `manifest_core_sha` の対象になる |
| `UNDERSPEC-CAL-D28` | `c0_freeze.py`, `tests/_campaign_fixture.py` | round 14 finding #1: `armed_freeze()` は `c0_freeze` event の直後（同一 publish sequence 内、campaign dir 公開前）に `kind="split_frozen"` event（`realized_split_map_hash`/`seal_commitment`）を新規に記帳するようにした。設計正本/IMPLEMENTATION_MAP は `provenance.Ledger.check_leakage()`（`_verified_split_freeze_commitment()`）がこの event を要求することは既に規定していたが、それを実際に emit する producer 側の実装は存在せず、結果として実際の C0→C1→...→C4 flow は常に `BLOCKED_LEAKAGE` になっていた（`check_leakage` 側は緩めない）。payload の正本は新規公開関数 `c0_freeze.split_frozen_event_payload()` とし、`tests/_campaign_fixture.build_tiny_campaign()` もこれを呼ぶよう改めた（fixture が独立に手組みした同型 dict を持つと本番実装と drift しうるため） |
| `UNDERSPEC-CAL-D29` | `campaign/caps.py`, `campaign/_render_worker.py`, `campaign/_measure_worker.py`, `campaign/render_stage.py`, `campaign/measure_stage.py` | round 14 finding #2: `--workers > 1` 下で fresh-process 呼び出しが並行実行されると、親プロセスの wall-clock 経過時間は compute cap が定義する CPU 秒数を過小計上する（並行分の CPU 時間が合算されない）。各 fresh-process worker（`_render_worker.py`/`_measure_worker.py`）が自身の `resource.getrusage`（RUSAGE_SELF+RUSAGE_CHILDREN、user+sys）由来の `cpu_seconds` を JSON 結果へ含め、親（`render_instance`/`run_measurement_for_instance`）はそれを compute counter へ課金する（within-process 側は親プロセス自身の `resource.getrusage` 差分を使う — 並行化されないため wall time ではなく厳密値）。wall time は `wall_seconds` として ledger `render`/`meter_call` event へ informational にのみ記録する。worker が報告する `cpu_seconds` の欠落/非有限/負値は `campaign/caps.py` の `validate_worker_cpu_seconds()`（`WorkerCpuSecondsInvalidError`）で fail-closed（stale/invalid unit 扱い、stop event 記帳、compute 課金なし）とした |
| `UNDERSPEC-CAL-D30` | `campaign/cli.py` | round 14 finding #3: C3b/C4 の F0 repeat-record 再利用（`cli._reusable_f0_values_by_process()`）が独立の subset 比較（`expected.issubset(seen)`）と無条件 append による自前 ledger 走査を行っており、`measure_stage._completed_meter_call_records()` の duplicate-key 拒否を素通りして同一 `(repeat_kind, repeat_index)` への重複 `meter_call` event が `two_stage_median()` へそのまま平均入力されていた。同関数を `_completed_meter_call_records()`（`(repeat_kind, repeat_index)` の厳密な一意キー集合を要求する唯一の正本）へ一本化し、duplicate/partial coverage の両方を `StaleMeasurementError` として fail-closed にした |
| `UNDERSPEC-CAL-D31` | `campaign/caps.py`, `campaign/cli.py`, `campaign/render_stage.py`, `campaign/measure_stage.py` | round 15 findings #1/#3/#5（採用。1 つの coherent change として実装）: **#1** `load_cap_counters()` は `counters.json` の `compute_used`/`budget_used` が finite かつ非負の非 bool 数値であること、`storage_used` が非 bool `int`（暗黙変換なし）であることを要求し、違反を `CountersCorruptError`（`CODE="COUNTERS_CORRUPT"`。`CapStateError` サブクラス）で fail-closed する——`cli.main()` はこれを捕捉し ledger `stop_event`（`reason="COUNTERS_CORRUPT"`）を記帳して dispatch を一切行わない。**#3** `counters.json` はキャッシュに過ぎず ledger が正本という前提に立ち、`cap_counters_from_ledger()` が `render`/`meter_call`/`stage_summary` event から compute/storage/budget を再導出する（`meter_call` は work unit あたり 6 record 記帳されるため `cpu_seconds`＝集約値の重複計上を `(row_id, probe_index, candidate_id)` 一意キーで防止、per-record 個別値の `storage_bytes` は非重複合算）。`reconcile_cap_counters()` が次元ごと `max(persisted, ledger 由来)` を実効値とし（`counters.json` 欠落かつ ledger に実績があれば ledger 由来を採用し `counters_reconstructed` event を 1 回記帳、persisted が ledger 由来を上回れば エラーにせず persisted を保持——fail-closed 方向）、`cli.main()` は dispatch 直前にこの実効値を `counters.json` へ再永続化してから round13 finding #2 の事前 breach チェックへ渡す。**#5** `cli.main()` は各 stage dispatch の直前に自プロセス自身の `resource.getrusage(RUSAGE_SELF)` を記録し、dispatch 終了時（正常終了・cap 超過・例外のいずれでも `finally` で）その差分（CLI 側の matrix 構築・ledger/JSON I/O・hashing 等、worker 自己申告にも `measure_stage` の within-process 窓にも含まれない parent 側 CPU）を compute counter へ課金・永続化し、ledger `stage_summary` event（`stage`/`parent_cpu_seconds`）として記帳する（worker 自己申告の `cpu_seconds` 会計＝34476ab は無変更）。この `stage_summary` が既存 `stop_event` の後に付くことがあるため、round13 finding #2 の idempotent 重複防止チェックは「直前の entry」ではなく「最新の `stop_event`」を参照するよう合わせて修正した |
| `UNDERSPEC-CAL-D32` | （コード変更なし。境界宣言） | round 15 finding #2 見送り（境界宣言）: campaign runner はキャンペーン単位で単一 armed プロセスの逐次実行を運用契約とする（1 campaign = 1 operator の同時実行なし。IMPLEMENTATION_MAP_v1.md §6.4 に運用契約を明記済み）。同時実行の重複プロセスは round 13/14 の duplicate-key 再構成（`_completed_meter_call_records`/`StaleMeasurementError`）で既に fail-closed に拒否されており、`UNDERSPEC-CAL-D31`（本 round finding #3）が counters を ledger 由来に束縛したことで両プロセスの実績がいずれ ledger から再導出される（過小計上の温床が解消）。再着手条件: 複数 operator が同一 campaign を実際に並行運用する要件が生じた場合、またはロックなしで実害（二重 render/測定の並行実行によるデータ破壊）が実測された場合 |
| `UNDERSPEC-CAL-D33` | （コード変更なし。境界宣言） | round 15 finding #4 見送り（境界宣言）: `c0_freeze.py` の staging dir（`.staging-<id>-*`）は 0700 の secrets root/campaigns dir 配下にのみ存在し、marker 作成失敗時に残る staging dir は一切 publish されず secret 相当の情報を外部へ晒さない。cleanup は運用者の手動手順とし、`approvals/README.md`（または freeze runbook 節）に「失敗した freeze 後は `.staging-<id>-*` を `rm -rf` する」を追記する。再着手条件: 自動 cleanup を怠った場合に disk 圧迫等の実害が実測された場合、または staging dir が secret 相当情報を含むよう仕様変更された場合 |
| `UNDERSPEC-CAL-D34` | `campaign/cli.py` | round 16 finding #2（採用）: `cli.main()` は round 15 finding #5（`UNDERSPEC-CAL-D31`, 1351736）が新設した `finally` ブロックの parent RUSAGE_SELF 課金の**後**、cap 超過を再チェックせずそのまま成功を報告し得た（stage 本体は cap 内でも、CLI 側 parent CPU 課金の一撃で breach していても success を返す穴）。`finally` の残余課金後に `cost_caps.check()` を再実行し、breach していれば `COST_CAP_EXCEEDED` stop event（`_refuse_if_caps_already_breached()` の idempotent dedup を再利用）を記帳し非 0 終了するよう改めた。設計正本/IMPLEMENTATION_MAP は「stage dispatch 前後の cap 超過検出」の**順序**（phase-transition event の記帳より前か後か）までは規定しないため、最も安全側の裁定（本 round のルーリング）を採用: `c3a-f0-selection`/`c3b-selection`/`c4-holdout`/`close` は各 `_run_*` wrapper 内で、その stage 自身が `f0_selection_frozen`/`selection_frozen`/`holdout_executed_valid`/`campaign_closed` を記帳する呼び出しの**直前**に同種の再チェック（`_checkpoint_parent_cpu_before_transition()`。可変 1 要素 box `parent_cpu_checkpoint` でチェックポイントを更新し、`main()` の `finally` はそれ以降の残余 CPU のみを課金する）を行い、breach していれば phase transition 自体を呼ばずブロックする。`c1-fixtures`/`c2-baseline` は phase-transition event 自体を `render_stage`/`baseline_stage` に一括委譲しており `cli.py` 側に介在する追加 CPU が実質無い（pre-transition checkpoint を挟んでも dispatch-entry guard の同値再チェックになるだけの no-op）ため、意図的に配線せず `finally` の残余チェックのみに委ねた（`_checkpoint_parent_cpu_before_transition()` docstring 参照）。`close` は transition 後（`campaign_closed` 記帳後）にのみ判明する残余breachを「post-close breach」として区別する: `campaign_closed` 自体は append-only ledger 上に記録されたまま残るが（close は取り消さない）、当該 dispatch の呼び出し結果は `COST_CAP_EXCEEDED`／非 0 終了として報告し、stop event payload に `post_close_breach: true` を付与して ledger 上でも区別できるようにした |
| `UNDERSPEC-CAL-D35` | `campaign/measure_stage.py`, `campaign/caps.py` | round 16 finding #3（採用）: `measure_stage.run_measurement_for_instance()` の compute 課金が within-process 3 call の CPU（`within_cpu_seconds`）と fresh-process 3 call の CPU（`fresh_cpu_seconds`）の**和**だったが、within-process CPU は 1351736（round 15 finding #5, `UNDERSPEC-CAL-D31`）以降 `cli.py main()` の parent RUSAGE_SELF `stage_summary` 課金に既に含まれており（within は並行化されず本プロセス内で直列実行されるため、`cli.py` 側の parent CPU delta measurement がこれを丸ごと含む）、二重計上になっていた。`cap_counters.add(compute=...)` を fresh-process 側 `fresh_cpu_seconds` のみに限定し、`within_cpu_seconds` は ledger `meter_call` event へ informational field として残す（既存の `cpu_seconds` field は within+fresh 合計のまま — 課金額そのものではなく informational な全体像として維持）よう改めた。設計正本/IMPLEMENTATION_MAP は work unit 単位の compute 課金の**内訳**（parent 課金との重複排除）までは規定しないため、fail-closed 方向（過小計上より過大計上を許容する）の裁定を採用: `campaign/caps.py` の `cap_counters_from_ledger()` は `meter_call` event の再構成でも `cpu_seconds - within_cpu_seconds`（下限 0）を compute へ計上するよう改めたが、`within_cpu_seconds` field を持たない（round 16 以前の）過去 event は差し引きゼロ（＝旧来どおり `cpu_seconds` 全体を計上）となり、persisted と derived の乖離は過大計上側にのみ発生し得る形にした |
| `UNDERSPEC-CAL-D36` | （コード変更なし。境界宣言） | round 16 finding #1 見送り（境界宣言）: `campaign/caps.py` の `cap_counters_from_ledger()` は `render`/`meter_call`/`stage_summary` event のみから compute/storage/budget を再導出し、`COST_CAP_EXCEEDED` stop event payload に記帳されている `counters` snapshot（breach 時点の完全な累積値）自体は再構成の入力に使わない。そのため、breach を記録した stop event の**後**に運用者が `counters.json` を（breach 前の状態などへ）ロールバックすると、breach を引き起こした最後の 1 work unit の charge がその work unit 自身の `render`/`meter_call` event （それらは削除していない前提）から通常どおり再構成される一方、`reconcile_cap_counters()` の `max(persisted, ledger 由来)` が「ロールバック後の persisted」と「その 1 unit を含む ledger 由来」の**より高い方**を採る設計そのものにより実害は生じない——ただし stop event 自体の `counters` snapshot を独立の下限として使っていないため、理論上は「その 1 unit の `render`/`meter_call` event 自体も欠落・改変された」極端なケースでのみ、stop event の snapshot が持つはずの追加情報（breach 時点の higher-fidelity な値）を活かせない。ルーリング: 見送り — (1) 運用者が breach 記録**後**にわざわざ `counters.json` を改変するという意図的な運用逸脱を前提とし、(2) 影響は最大 1 work unit 分の charge に限定され、(3) `stop_event` 自体は append-only ledger 上に breach の証拠として残り続ける（監査可能性は失われない）。再着手条件: 実運用で `counters.json` 改変を伴う breach 後のロールバックが実際に発生し、その影響が 1 work unit を超えて拡大する実害が観測された場合、または stop event の `counters` snapshot を再構成の独立入力として使う設計変更が別の目的で必要になった場合 |
| `UNDERSPEC-CAL-D37` | `campaign/workunits.py`, `campaign/cli.py`, `campaign/selection_stage.py` | round 17 finding #1（採用）: `workunits.c3a_f0_selection_instances()`/`c3b_family_selection_instances()` は SELECTION split の行のみを測定対象 instance 集合としており、HMAC home split が CALIBRATION/HOLDOUT の negative control 行は C3a/C3b で一度も測定されなかった。この結果 `candidate_fail_filter_report()` は当該 control 行に対する `neg_detections` が単に空になるだけで `negative_control_false_fire` が発火せず、「偽検出なし」と誤判定していた（fail-open）。ルーリング（§2.7 control 共有契約に基づく採用）: **ALL family negative-control instances は home split に依らず C3a/C3b の測定対象集合の一部**とする（これらの行は C1 で「全 control」として既に render 済みのため追加 render は発生しない）——`fixtures/controls.py` に `negative_control_instances()` を新設し、`workunits.c3a_f0_selection_instances()`/`c3b_family_selection_instances()` を「SELECTION split instance ∪ 当該 family の全 negative control instance」の union に改めた。あわせて `candidate_fail_filter_report()` に `positive_rows_absent` の鏡像として `negative_controls_incomplete` fail filter（宣言された negative control 行のうち 1 件でも record を欠けば ineligible）を新設した（`FAIL_FILTER_NAMES` は 6→7 種）。副次的に、`cli.py` の `_run_c3a` が `negative_control_row_ids(matrix_rows)`（family 非スコープ）を使っていた既存の不整合（`_run_c3b` は family スコープ済みで整合していた）を、この完全性検査が新設されたことで顕在化するため F0_CONTROL family スコープへ修正した |
| `UNDERSPEC-CAL-D38` | `campaign/cli.py` | round 17 finding #2（採用）: `cli.main()` は凍結 manifest の `dependencies` セクション（`c0_freeze._dependencies_section()` が記録した Python/numpy/scipy/librosa/soundfile/pyworld の各バージョン）と、dispatch 直前の実行環境を一度も照合していなかった。C0 freeze 後に環境が drift（依存パッケージの再インストール・Python バージョン変更等）していても、meter 出力が変わり得るのに何の痕跡も残らず dispatch が進んでしまう穴があった。`_environment_drift_violations()` を新設し、`importlib.metadata.version()`/`platform.python_version()` で再取得した現在値と manifest 記載値を照合、1 件でも不一致があれば `BLOCKED_ENVIRONMENT_DRIFT`（差分パッケージ一覧付き）stop event を記帳し非 0 終了で dispatch を拒否する。`plan`（unarmed）は `environment_drift` キーで報告のみ行い block しない。設計判断: (1) 比較対象は `manifest["dependencies"]` に記録された全キーのうち実行時に安価に再取得できるもの（python_version + numpy/scipy/librosa/soundfile/pyworld の `*_version`）に限り、`pyworld_wheel_hash` はこの環境から再取得できない wheel バイト列ハッシュのため対象外とする。(2) pyworld 未インストール時は `c0_freeze` と同じ `"ABSENT:not_installed"` 記法で比較し、凍結時から変わらず未インストールなら drift 扱いしない。(3) `BLOCKED_ENVIRONMENT_DRIFT` は `vocab.BlockedCode`（設計正本 §3.3 の閉語彙。`tests/test_vocab.py::test_blocked_code_closed_vocab` が `len(BlockedCode) == 6` で enforce）には追加せず、`AUTHORIZATION_REQUIRED`/`PHASE_ORDER_VIOLATION` と同様に閉語彙とは別軸の pre-dispatch 拒否コード（`cli.ENVIRONMENT_DRIFT_CODE`）として定義した |
| `UNDERSPEC-CAL-D39` | `campaign/cli.py`, `campaign/caps.py` | round 17 finding #3（採用）: `cli.main()` の `finally` ブロックが記帳する `stage_summary` ledger event の `parent_cpu_seconds` は、round 16 finding #2（`UNDERSPEC-CAL-D34`）が新設した mid-stage checkpoint（`_checkpoint_parent_cpu_before_transition()`）以降の**残余** CPU のみだった。checkpoint 自体が課金する pre-transition の delta は `cap_counters`（`counters.json`）へは正しく加算されるが、対応する ledger event を一切記帳しないため、`cap_counters_from_ledger()`（`caps.py`。round 15 finding #5, `UNDERSPEC-CAL-D31`）による ledger からの再構成では checkpoint 分が丸ごと欠落し、mid-stage checkpoint を持つ stage（`c3a`/`c3b`/`c4`/`close`）で persisted counters と ledger-derived counters が恒常的に乖離していた。修正: `finally` は `cap_counters` への加算（`counters.json` 側）は従来どおり残余のみに保つが、`stage_summary` event の `parent_cpu_seconds` には dispatch 開始（`parent_cpu_t0`）からの**全 delta**（pre-transition checkpoint delta 合計 + 残余）を記録するよう改めた。`cap_counters_from_ledger()` は `stage_summary.parent_cpu_seconds` を無修正で 1:1 合算しているため、この書き込み側の修正のみで再構成が persisted と一致するようになる（`caps.py` 側のロジック変更は不要、docstring のみ更新）。テスト: mid-stage checkpoint を持つ stage（c3a）に対する round 15/16 の persisted-vs-ledger-derived 等価性テストを拡張して確認する |
| `UNDERSPEC-CAL-D40` | （コード変更なし。境界宣言） | round 17 finding #4 見送り（境界宣言）: `cli.py` の canonical path 照合（`_canonical_path_violations`, finding #7）が保証する「照合通過後は凍結済みモジュールのみを使う」という性質は、「stage 呼び出しごとに新規 `python -m voice_genesis.calibration.campaign` プロセスを起動し、各プロセスが自身の起動時に照合を実行してからモジュールを使う」という運用契約を前提とする——in-process で `main()` を長時間・繰り返し呼ぶ、またはモジュールをプロセスをまたいで再利用するような呼び出し方はこの契約の対象外（照合後にファイルが書き換わっても、プロセスが再 import しない限り検出できない）。ルーリング: 見送り — 現行の runner はそもそも「1 stage = 1 fresh process」の CLI 起動を前提として設計されている（`IMPLEMENTATION_MAP_v1.md` §6.4 の subcommand 列挙がそのまま CLI 起動単位に対応）。in-process long-lived caller という呼び出し方自体が現行設計の想定外であり、コード変更ではなく運用契約の明文化（`IMPLEMENTATION_MAP_v1.md` §6.4 + 本エントリ）で足りる。再着手条件: in-process long-lived caller（例: テストハーネスや別ツールが `cli.main()` を同一プロセス内で繰り返し呼ぶ運用）が実際に採用される場合、そのときは path-hash 照合をプロセス起動時ではなく各 stage 呼び出し直前に再実行する設計へ改める |
| `UNDERSPEC-CAL-D41` | （コード変更なし。境界宣言） | round 18 finding「Verify frozen source hashes inside each worker」（`campaign/_render_worker.py` ~26）見送り（境界宣言）: render/measure worker はいずれも fresh process として checkout を改めて import するため、親プロセスの one-time frozen-path hash 照合の**後**に pin 済み generator/candidate ファイルが改変された場合、worker 側はその改変を未検証のまま実行する。ルーリング: 見送り — runner 運用契約は armed stage 実行中の checkout 変更をそもそも禁止する（single operator・C0 後のコード凍結・canonical な変更は新規 campaign を要する）。親プロセスの照合と「1 stage = 1 fresh process」契約（`UNDERSPEC-CAL-D40`）を合わせた境界を正本とし、worker 側の再検証は採用しない。再着手条件: armed stage 実行中の checkout 変更を許容する運用（複数 operator の並行編集、hot-patch 運用等）が実際に要求される場合 |
| `UNDERSPEC-CAL-D42` | （コード変更なし。境界宣言） | round 18 finding「Bind fresh measurements to the verified PCM bytes」（`campaign/_measure_worker.py` ~49）見送り（境界宣言）: fresh-process measure worker は render 済み PCM ファイルを path のみで再読込し、期待 digest との照合を行わない。親プロセスの検証と worker の読み込みの間に render ファイルが差し替えられた場合、worker は未 pin のバイト列を測定し得る。ルーリング: 見送り — 同一の runner 運用契約により、armed stage 実行中は campaign dir 配下の artifact も変更しない前提とする。親プロセスは dispatch 前に PCM を ledger pin と照合しており、結果はその pin に束縛される。worker 側の digest 束縛は採用しない。再着手条件: armed stage 実行中の campaign artifact 変更を許容する運用が実際に要求される場合、または pin 検証と worker 読み込みの間に実害（差し替えによる未 pin 測定）が実測された場合 |
| `UNDERSPEC-CAL-D43` | `campaign/selection_stage.py` | round 19 finding #1（採用）: `build_candidate_criteria()` の per-instance 集計が単純平均（`np.mean`）で、設計正本 §10.1 の凍結二段 median `m[i] = median_p( median_r( x_hat[i,p,r] ) )`（「process 間で repeat 数が不均等な場合に、repeat 数の多い process が支配的にならないよう」）に違反していた。holdout 側（`holdout_stage.build_instance_margins`）は既に `observables.two_stage_median()` を使っており、selection と holdout で同一 instance の観測集計が乖離し得た。修正: `MeasurementRecord.process_id`（within-process 3 repeat = 単一 group、fresh-process 3 repeat = 各 1 repeat の独立 group。holdout 側と同一の入力形）で group 化し、`two_stage_median()`/`error_terms()` を holdout と共有する。ルーリング: 曖昧さなし（設計正本 §10.1 の式そのもの） |
| `UNDERSPEC-CAL-D44` | `campaign/selection_stage.py` | round 19 finding #2（採用）: `build_candidate_criteria()` は正規化誤差 `RE[i] = (measured-truth)/max(\|truth\|, zero_guard)` の配列を `normalized_mae`/`signed_bias`/`primary_q95_ae` の 3 統計すべてに使い回していた。設計正本 §10.1 は `e[i] = m[i] - x[i]`（raw signed error）・`AE[i] = |e[i]|`・`RE[i] = AE[i]/max(\|x[i]\|, d[i])` を別々に定義したうえで `BIAS = mean_i(e[i])`（raw `e[i]` 明記）・`q95(method=linear 固定)`（`AE[i]` の q95、normalized ではない）と続けており、§9 の「primary-domain normalized MAE」だけが正規化誤差ベースである。ルーリング（本タスクの裁定に従う。正本の文言は `BIAS`/`q95` の入力が `e[i]`/`AE[i]`（raw）であることを式として明示するが、§9 の "normalized MAE" と地続きに読めてしまう曖昧さがあったため本エントリで記録する）: **normalized MAE のみ相対誤差 `RE[i]` ベース、BIAS（`mean_i(e[i])`）と q95(AE)（`q95_i(\|e[i]\|)`）は raw 誤差 `e[i]`/`AE[i]` ベース**に分離した。q95 の quantile 方式（`method="linear"` 固定）は既存の `observables.q95()` をそのまま流用し変更しない |
| `UNDERSPEC-CAL-D45` | `campaign/cli.py` | round 19 finding #3（採用）: 完了済み段（`c1-fixtures` after `FIXTURE_VALID`、`c4-holdout` after `HOLDOUT_EXECUTED_VALID`）の再実行が、`_RESUMABLE_SUBCOMMANDS` により `_phase_order_violation` を素通りしたのち、render/measure の resume 機構により実質 no-op ではあるものの、phase 遷移 event（`fixture_valid`/`holdout_executed_valid`）と `stage_summary` event を無条件に re-append していた。設計正本/IMPLEMENTATION_MAP は「完了済み段の再実行」の扱いを規定しないため（`cli.py` 内 `[UNDERSPEC-CAL-D22]` も同旨で当時は byte-identical 判定を範囲外とし一律拒否寄りの近似に留めていた）、本ラウンドで裁定: **produces phase 到達済みの resumable subcommand 再実行は、dispatch 自体を呼ばない真の no-op**（render/measure なし・phase 遷移 event なし・`stage_summary` なし・cap accounting の追記なし）とし、`NOOP_ALREADY_COMPLETE` を exit 0 で報告する（`_stage_already_complete()`、`phase_violation` チェック直後・matrix 構築/cap 精算より前で判定するため副作用ゼロ）。あわせて `CampaignPhase.CAMPAIGN_CLOSED` 到達後は resumable/non-resumable を問わず（`close` 自身を含む）常に `PHASE_ORDER_VIOLATION` とする分岐を `_phase_order_violation` の先頭に追加した（plan は unarmed 経路で別途 exempt 済み）。厳密な byte-identical 判定（`[UNDERSPEC-CAL-D22]`）は引き続き範囲外——本 no-op 判定は phase 到達済みという粗い basis のみに基づく |
| `UNDERSPEC-CAL-D46` | `e_use_table.py`, `c0_freeze.py` | round 20 finding「Replace the stale Gate 1 source digest」（採用）: `config/e_use_table_v1.json` の全 9 `USER_ACCEPTED_USE_BOUND` 行が `source_hash_or_version` に引用する `GATE1_DECISION_RECORD.md` の sha256 が、決定記録の確定稿とすでに乖離していた（引用行は §4 で「本ファイル確定後の最終内容の sha256」と明記するが、これを検証・再刻印する仕組みが存在しなかった）。機械的再刻印（(a)）: 対象 9 行を現在の `GATE1_DECISION_RECORD.md` sha256 へ書き換え、決定記録自体は本コミットで変更しない。検証（(b)）: `e_use_table.validate_source_digests()` を新設し、`source_id_or_url` が `GATE1_DELEGATION_SOURCE_ID_PREFIX`（`"GATE1-DELEGATION-"`）で始まる行について引用元ファイルの実 sha256 と `source_hash_or_version` の一致を検証、不一致・出典欠落は `E_USE_SOURCE_DIGEST_MISMATCH: row[i] (...)` 形式で行を名指しして violation を返す。`c0_freeze._check_e_use_table()` から `validate_e_use_table()` と合わせて呼び、`BLOCKED_C0_MANIFEST_INCOMPLETE` へ合流させ dry-run/armed 双方で fail-closed する。運用: `python -m voice_genesis.calibration.e_use_table restamp`（`--source`/`--table-path`/`--repo-root` で上書き可、既定は `voice_genesis/calibration/{config/e_use_table_v1.json, approvals/records/GATE1_DECISION_RECORD.md}`）が機械的再刻印を行う——決定記録自体は編集しない。freeze runbook 手順: 決定記録を確定 → `restamp` → commit → dry-run（`approvals/README.md`） |
| `UNDERSPEC-CAL-D47` | `campaign/holdout_stage.py` | round 20 finding「Verify frozen E-use bytes before parsing them」（採用）: `load_e_use_rows()` は凍結 campaign dir の `e_use_table.json` の有無のみを見て、無ければ空タプルへ fail-soft していた（「REQUIRED_BLOCKING は C0 freeze 側の責務」という理由付け）。これは freeze 後にファイルが差し替え/削除されても holdout gate が「E_use evidence 無し」を静かに受理する穴だった。修正: `e_use_table.json` を 1 回だけ `read_bytes()` し、その sha256 を凍結 manifest の `frozen_inputs.e_use_table_sha256` pin（`[UNDERSPEC-CAL-D12]` が導入した pin）と照合してから同一バッファをパースする（`c0_freeze._check_e_use_table()`/`measure_stage._verify_and_load_rendered_pcm()` と同じ「1 read → 検証 → 同一バッファをパース」規約で TOCTOU を排除）。ファイル欠落・pin 欠落・sha256 不一致はいずれも新設 `StaleEUseTableError` を送出しつつ ledger `stop_event`（`reason: "E_USE_TABLE_STALE_OR_MUTATED"`）を記帳して fail-closed する（`measure_stage.run_measurement_for_instance` の `StaleMeasurementError`/`StaleRenderError` と同じ「catch → stop_event 記帳 → re-raise」規約）。空タプルを返す経路は撤去した |
| `UNDERSPEC-CAL-D48` | `campaign/cli.py` | round 20 finding「Check frozen caps before returning a completed-stage no-op」（採用）: `[UNDERSPEC-CAL-D45]`（round 19）が新設した `NOOP_ALREADY_COMPLETE` 早期リターンは、frozen cost caps の reconciliation/breach 判定（round 13 finding #2, `[UNDERSPEC-CAL-D26]`）より**前**に評価されていた——`c1-fixtures`/`c4-holdout` の produces phase に到達済みのキャンペーンが既に cap breach 状態でも、retry は breach を再表面化させずに `NOOP_ALREADY_COMPLETE`/exit 0 を返してしまい、breach 状態を実質的に再正当化していた。修正: `cost_caps_from_manifest()`/`reconcile_cap_counters()` を no-op 早期リターンより前に移動し、`_stage_already_complete()` が真の場合はまず `_refuse_if_caps_already_breached()` を評価する——breach あり → `COST_CAP_EXCEEDED` stop event（`_refuse_if_caps_already_breached()` 自体が idempotent なので retry のたびに重複記帳しない）を伴い非 0 終了、breach なし → 従来どおり `NOOP_ALREADY_COMPLETE`/exit 0（`counters_reconstructed` event 追記・`counters.json` 永続化のいずれも no-op 経路では行わない——`[UNDERSPEC-CAL-D45]` の「真の no-op は他に痕跡を残さない」を維持） |
| `UNDERSPEC-CAL-D49` | `c0_validate.py`, `c0_freeze.py`, `campaign/cli.py`, `tests/_campaign_fixture.py`, `tests/test_c0_path_inventory_sync.py`, `tests/test_c0_validate.py` | round 21 finding「Include harness meters in the frozen source inventory」（採用）: `candidates/impl/b0_wrappers.py` は `voice_genesis/harness/measure.py`/`measure_v3.py` を無改変 import で実行し（`measure_v3.py` はさらに `measure_v2.py` を import）、B0 baseline 候補の実体を harness 側実装へ委譲しているが、`calibration_path_inventory()` はこれら 3 ファイルのいずれも含んでいなかった。結果として C0 freeze 後に harness meter 実装を改変しても `campaign/cli.py::_canonical_path_violations`（finding #7, round 9）の canonical-path 照合を素通りしていた。修正: `HASH_MAP_KEYS`/`REQUIRED_BLOCKING_KEYS`/`_CONTAINER_TYPE_KEYS`（`c0_validate.py`）・`_classify_path`/`_path_hash_maps`（`c0_freeze.py`）・`_CANONICAL_PATH_CATEGORIES`（`campaign/cli.py`）へ新カテゴリ `meter_implementation_paths_sha256` を追加し、`voice_genesis/harness/` 配下のファイルを `meter_paths_sha256`（calibration 側候補実装）とは別カテゴリで記録するようにした。対象ファイルの transitive closure は `c0_validate.py` に新設した `resolve_b0_wrapper_harness_paths()`（`candidates/impl/b0_wrappers.py` の import 文を AST で静的解析。third-party import は `harness/` に同名 `.py` が無いため自動的に除外される）で機械的に求め、`c0_path_inventory.json` をこの 3 ファイル込みで再生成した。`tests/test_c0_path_inventory_sync.py` に `test_b0_wrapper_harness_imports_are_inventoried`（B0 wrapper の import が inventory 未収載なら fail する静的検査）を新設し、既存の drift 検査も `scan_calibration_tree_inventory() | resolve_b0_wrapper_harness_paths()` の union と厳密一致するよう拡張した。`tests/test_c0_validate.py`/`tests/_campaign_fixture.py` の mirror fixture（`_classify_path`/path+hash マップ/canonical path サンプル集合）も新カテゴリへ同期した |
| `UNDERSPEC-CAL-D50` | `provenance.py` | round 22 finding「Validate Gate 3 before accepting unseal」（採用）: `campaign/unseal.py` は `holdout_unseal` event に `gate3_accepted_sha` を記帳するが、`provenance._verified_holdout_unseal_seq()` は `selection_frozen` event とその 4 前提 sha のみを検証し `gate3_accepted_sha` を一切検査していなかった——結果として、選択チェーンの参照が正しく Gate 3 参照が欠落/任意の crafted・legacy `holdout_unseal` 行が `Ledger.check_leakage()` に権威ある unseal 境界として受理され得た。修正: `_verified_holdout_unseal_seq()`（内部で `_verified_holdout_unseal_detail()` に委譲）が `holdout_unseal.gate3_accepted_sha` の検証を追加——prior chain-valid な `kind == "gate3_accepted"` event を参照し、そのペイロードが `seal_protection_level_accepted: True`（`approvals._parse_gate3_payload`/`unseal.py` の emit 契約と同じスキーマ）を持つことを要求する。満たさなければ unseal boundary は返らず、`check_leakage` は `BlockedCode.BLOCKED_LEAKAGE`（閉語彙は不変）に加え、新設の `LeakageCheckResult.reason == "UNSEAL_GATE3_UNVERIFIED"` で fail-closed する。emitter（`unseal.py`）はすでに `gate3_accepted_sha` を供給しているため無改変 |
| `UNDERSPEC-CAL-D51` | `approvals/records/gate1_campaign_execution.2026-09-02.json`, `approvals/records/GATE1_DECISION_RECORD.md`, `config/e_use_table_v1.json`, `tests/test_approvals.py` | round 23 ADOPT (1) finding「Refresh the Gate 1 memo digest」（採用）: `IMPLEMENTATION_MAP_v1.md` の編集後、checkout 内参照用コピー（`approvals/records/gate1_campaign_execution.2026-09-02.json`）と checkout 外正本（`~/.vg_cal/approvals/gate1_campaign_execution.json`）の `memo_sha256` が stale のまま残っていた（`design_doc_sha256` は不変で一致していた）。修正: `approvals.refresh_document_hashes()` を両ファイルへ適用し `memo_sha256` のみ再スタンプ（他フィールド無変更、両ファイルは再スタンプ後も内容一致）。`GATE1_DECISION_RECORD.md` §9.1 へ再刻印記録を追記したため、そのファイルを `source_hash_or_version` として引用する `config/e_use_table_v1.json` の `USER_ACCEPTED_USE_BOUND` 9 行も `e_use_table restamp` で追随させた（round 20 ADOPT(1)(a) の運用手順どおり）。CI regression guard として `tests/test_approvals.py::test_repo_gate1_record_copy_document_hashes_match_tree_at_head` を新設: checkout 内コピーの `design_doc_sha256`/`memo_sha256` を現在の DESIGN/メモ実測ハッシュと突き合わせる（期待値はハードコードせず working tree から都度実測するため、正当な同時更新では失敗しない） |
| `UNDERSPEC-CAL-D52` | `campaign/render_stage.py`, `campaign/caps.py` | round 23 ADOPT (2) finding「Charge nondeterministic render attempts before raising」（採用）: `render_instance()` は 2 回の fresh-process render worker が既に消費した compute（各自報告する `cpu_seconds`）を、出力不一致検出時に一切課金しないまま `RenderNondeterministicError` を送出していた——cap を全く消費しないまま同一の非決定的 instance へ何度でも retry できる抜け穴だった。修正: 出力不一致を検出したら、raise の**前**に (a) 両 worker の `cpu_seconds` 合計を compute へ、budget を凍結 `budget_accounting_mode` に従い 1 work unit 分（storage は PCM を書かないため常に 0）を `cap_counters` へ課金・`counters.json` へ persist、(b) `cost_caps_check()` で cap 超過を再検査（超過なら `CostCapExceededError` を優先して送出——完了 render の既存優先順位と同じ）、(c) 課金内容を carry する `render_nondeterministic` ledger event を記帳（`cap_counters` の有無に関わらず常に記帳し、`campaign.caps.cap_counters_from_ledger()` がこの新 event kind を `render` と同じ 1 work unit として `render_units`/`compute`/`storage` へ合算するようリデューサを拡張）。テスト: `tests/test_campaign_render.py::test_c1_render_nondeterministic_charges_before_raising`（2 worker の cpu_seconds 合計が課金される・`render_nondeterministic` event が記帳される・ledger 単独からの再構成が persisted counters と一致する・`RenderNondeterministicError` は依然として送出される、を検証） |
| `UNDERSPEC-CAL-D53` | `provenance.py`, `tests/test_provenance_unseal_prerequisites.py` | round 23 ADOPT (3) finding「Require evidence fields on Gate 3 acceptance events」（採用）: `[UNDERSPEC-CAL-D50]`（round 22）が新設した `_valid_gate3_accepted_payload()` は `kind`/`seal_protection_level_accepted: True` の 2 フィールドのみを検証しており、`approval_content_sha256`/`approver`/`approved_at_utc` を欠く `gate3_accepted` event（kind と bool だけの最小行）でも `_references_prior_gate3_acceptance()` を通過し unseal boundary を権威付けてしまっていた。修正: `_valid_gate3_accepted_payload()` を拡張し、`unseal_campaign()` が実際に emit する envelope（`campaign/unseal.py`）と同じ 4 フィールドを要求する——`approval_content_sha256` は 64 桁小文字 16 進 sha256（`_is_sha256_hex()` を再利用）、`approver` は非空文字列、`approved_at_utc` は UTC オフセット明示（`Z`/`+00:00`）の ISO 8601（新設 `_is_iso8601_utc_timestamp()`）、`seal_protection_level_accepted is True`。いずれか欠ければ `check_leakage` は既存の `BlockedCode.BLOCKED_LEAKAGE` + `reason == "UNSEAL_GATE3_UNVERIFIED"` で fail-closed する（新設の `UNSEAL_GATE3_UNVERIFIED` sub-reason は追加しない——既存の単一 reason で十分区別できるため）。テスト: 最小 envelope（kind + bool のみ）→ blocked、4 フィールドいずれか 1 つのみ malformed（sha 短い/大文字・approver 空/空白のみ・timestamp が UTC オフセット無し/非 UTC/不正形式の 7 パターン）→ blocked、`unseal_campaign()` が実際に emit する完全な production envelope → 従来どおり通過、`test_campaign_unseal.py`/`test_provenance.py` の既存 E2E production path は無改変のまま全パス |
| `UNDERSPEC-CAL-D54` | （コード変更なし。境界宣言。`campaign/measure_stage.py` ~654） | round 23 BOUNDARY (4)「Count the complete persisted ledger record toward storage」（見送り・境界宣言）: `run_measurement_for_instance()` の `record_bytes`（storage 課金対象）は `meter_call` payload 自体の serialized size のみを測り、`Ledger.append()` が実際に書き込む canonical envelope（`seq`/`prev_sha`（64 hex）/`entry_sha`（64 hex）/末尾改行）のオーバーヘッドを含まない。**判定**: 本 storage 会計境界は「render bytes + measurement payload bytes」までと定義し、envelope オーバーヘッドは対象外とする——代表的な `meter_call` payload（`kind`/`row_id`/`probe_index`/`candidate_id`/`repeat_kind`/`repeat_index`/`wall_seconds`/`cpu_seconds`/`within_cpu_seconds`/1 construct 分の meter 出力/`storage_bytes` 自身）で `canonical_json()` 実測すると envelope オーバーヘッド（seq+prev_sha+entry_sha+括弧類+改行、"payload" の入れ子は含むが payload 本体は除く）は約 182 bytes/record——設計正本 §14 の全設計規模（約 14k meter calls）に対して概算 14,000 × 182 ≈ 2.6 MB、承認済み storage cap 4 GiB（`gate1_campaign_execution.json` `cost_caps.storage`）の約 0.006%、1% を大きく下回る。数百万レコード規模へスケールしない限り再訪不要 |
| `UNDERSPEC-CAL-D55` | `campaign/caps.py`, `campaign/measure_stage.py`, `campaign/render_stage.py` | round 24 ADOPT (1) finding「Charge failed fresh-worker attempts」（採用）: fresh-process worker（measure 側 `_measure_worker.py`／render 側 `_render_worker.py`）が起動後に timeout / nonzero exit / malformed JSON で失敗すると、旧実装は `cap_counters.add()`/ledger append の**前**に例外を送出しており、クラッシュする candidate/instance への retry が worker CPU を無制限・無課金で消費できてしまっていた。修正: post-spawn の全失敗経路（measure/render 双方）で、raise の**前**に (a) compute を worker 自身の報告 `cpu_seconds`（well-formed な報告が回収できる場合——nonzero exit の captured stdout から `caps.reported_cpu_seconds_or_none()` で試みる）、無ければ親プロセスの `resource.getrusage(RUSAGE_CHILDREN)` 差分（timeout で kill された子は reap 後の差分。`caps._children_cpu_seconds()`／`measure_stage._children_cpu_seconds()`／`render_stage._children_cpu_seconds()`）で課金、(b) budget を凍結 `budget_accounting_mode` に従い 1 work unit 分、(c) storage は常に 0（失敗した attempt は出力を一切残さない）——`caps.charge_worker_failure()` へ集約し、`worker_failed` ledger event（`failure_kind` ∈ `timeout`/`nonzero_exit`/`malformed_output` を含む）を無条件に記帳・（`cap_counters` があれば）`counters.json` へ persist・cap 再検査してから元の例外を再送出する（cap 超過を検出すれば `CostCapExceededError` を優先——round 23 ADOPT (2) `render_nondeterministic` と同じ優先順位）。既存の「worker が exit 0 で JSON は parse できるが `cpu_seconds` フィールド自体が無効」経路（`WorkerCpuSecondsInvalidError`、round 14 finding #2）は本 finding の対象外のため無改変のまま——引き続き無課金・`stop_event` のみ。`campaign.caps.cap_counters_from_ledger()` を拡張し `worker_failed` event（`meter_call` と異なり record 単位の dedup 無し——failure ごとに独立した 1 attempt として全て合算）を compute/storage/budget（work unit）へ算入する。テスト: measure/render 双方で failure kind ごとに（counters 増加・`worker_failed` event 記帳・ledger 単独からの再構成が persisted counters と一致・元の例外が依然として送出される）を検証、および課金自体が cap を超過するケースで `CostCapExceededError` が優先されることを検証 |
| `UNDERSPEC-CAL-D56` | `approvals.py` | round 24 ADOPT (2) P2 finding「Validate approval timestamps before accepting them」（採用）: `load_approval()` は `approved_at_utc` を `_require_nonblank_str()`（非空文字列であること）としか検証しておらず、`"tomorrow"` のような不正な値や、UTC オフセットを持たないナイーブなタイムスタンプ、UTC 以外のオフセット（例 `+09:00`）を持つタイムスタンプでも 3 Gate いずれも `approved=True` に到達し得た。修正: `_require_nonblank_str()` の代わりに新設 `_require_iso8601_utc_timestamp()`（内部で `_is_iso8601_utc_timestamp()` に委譲——`provenance._is_iso8601_utc_timestamp()`（round 23 ADOPT (3), `[UNDERSPEC-CAL-D53]`）と同一のロジックだが、`provenance.py` のledger 固有内部実装への依存を避けるため本モジュールへ複製）を用いる——明示的な UTC オフセット（`Z` または `+00:00`、秒未満の小数部は任意）を要求し、ナイーブ・非 UTC オフセット・パース不能な値は「非空文字列」とは別の理由文字列で拒否する。`load_approval()` は 3 Gate 共通の loader を経由するため本修正は全 Gate に自動適用される。LIVE 承認 (`~/.vg_cal/approvals/gate1_campaign_execution.json`) と checkout 内参照用コピー (`approvals/records/gate1_campaign_execution.2026-09-02.json`) はいずれも `approved_at_utc: "...Z"` 形式のため、より厳格化された規則の下でも従来どおり通過することを実測確認した（内容は非開示）。テスト: `"tomorrow"` 拒否、ナイーブなタイムスタンプ（オフセット無し）拒否、非 UTC オフセット拒否、`Z`/`+00:00`/小数秒付き `Z` の 3 形式が accepted であることを確認 |
| `UNDERSPEC-CAL-D57` | `campaign/caps.py`, `campaign/measure_stage.py`, `campaign/render_stage.py` | round 25 ADOPT (1)(2) finding「Charge every worker attempt before propagating a repeat failure」+「Charge parseable but invalid worker results」（両採用・1 つの統一会計規則として実装）: **統一規則** — 「spawn された worker attempt は結果に関わらず必ず 1 回課金される。post-spawn の検証/変換失敗はすべて課金対象の malformed_output worker failure として扱われる」。round 24 ADOPT (1)（`[UNDERSPEC-CAL-D55]`）は失敗した 1 attempt のみを課金し、同一 batch 内で既に完了していた兄弟 attempt（sequential では先行する成功 repeat、`--workers>1` では executor が既に起動していた他の future）を無課金のまま破棄していた——修正 (1): measure 側 `run_fresh_process_calls()`／render 側 `render_instance()` の 2-worker loop はいずれも、1 attempt が失敗しても残りの attempt を打ち切らず batch 全体（measure は `FRESH_PROCESS_REPEATS` 回全て、render は必ず 2 worker 両方）を完走させてから、新設 `caps.charge_worker_attempts_before_raising()` で batch 全体を一括課金する：失敗した各 attempt は従来どおり `worker_failed` event、同一 batch 内で成功したが結果を破棄する attempt は新設 `worker_attempts_discarded` event（`discarded_success_attempts` に各自の `cpu_seconds` を列挙）として記帳し、budget は「1 attempt = 1 work unit」で成功・失敗を問わず全 attempt に課金したのち、cap 再検査を 1 回だけ行って batch 内で最初に失敗した attempt の元の例外を再送出する。`caps.charge_worker_failure()` は後方互換のため存置するが、内部実装は `charge_worker_attempts_before_raising()`（`successes=[]`・1 件の `failures`）へ委譲する薄いラッパへ改めた。修正 (2): round 14 finding #2 以来「worker が exit 0 で JSON は parse できるが `cpu_seconds` フィールド自体が無効」経路は round 24 finding #1 の対象外として無課金のまま残っていたが、round 25 でこの区別を廃止した——`cpu_seconds` 検証失敗（非数値/非有限/負値）と `MeterOutput`/`pcm_hex` の結果形状不正（`meter_output_from_dict()` の `ValueError`、render の `pcm_hex` 欠落/非文字列/`bytes.fromhex()` 失敗）はいずれも `malformed_output` worker failure として上記の統一規則に合流する。課金する compute は「`cpu_seconds` 自体が無効な field ならその worker 自身の値は使えないため `RUSAGE_CHILDREN` フォールバック、`cpu_seconds` は有効だが結果形状の方が不正なら（`cpu_seconds` 自体は信頼できるため）その値をそのまま充てる」の 2 分岐。`cap_counters_from_ledger()` を拡張し `worker_attempts_discarded` event（`discarded_success_attempts` の各エントリを `worker_failed` と同じ粒度で compute/budget へ算入、dedup 無し）を再構成できるようにした。テスト: measure 側 repeat 2/3 失敗（workers=1/workers=3 双方でシーケンシャル/concurrent を検証）で残り 2 attempt が discarded として課金されること、render 側 worker1 成功・worker2 失敗で両方課金されること、`cpu_seconds` が `"abc"`/NaN/負値の 3 パターンおよび `MeterOutput`/`pcm_hex` 形状不正がいずれも `malformed_output` として課金されること、ledger 単独からの再構成が persisted counters と一致すること、を `campaign/caps.py`・`measure_stage.py`・`render_stage.py` の各テストファイルに追加した。**family 終端宣言**: 「worker attempt accounting」ファミリー（`[UNDERSPEC-CAL-D52]` 非決定 render 課金・`[UNDERSPEC-CAL-D55]` failed fresh-worker 課金・本 `[UNDERSPEC-CAL-D57]` 統一規則）は D52/D55/D57 の 3 巡で spawn された worker attempt が課金を逃れる経路を体系的に掃討し終えたため、本 finding をもって**終端**とする。以降このファミリーに属する新規指摘は、新しい無課金 spawn 経路（例: 未知の post-spawn 失敗モード）を具体的に示すもの以外は境界宣言で見送る |
| `UNDERSPEC-CAL-D58` | `campaign/measure_stage.py`, `tests/test_campaign_measure.py` | round 26 ADOPT (1) finding「Classify non-finite values before ledger serialization」（採用）: 候補が `missing_reason`/`ineligible` による説明なしに NaN/±Inf を返した場合（`candidates/adapter.py` の契約違反 — 値が見つからないフィールドはキー自体を省略すべきだが、本来 `unexplained_nonfinite` fail filter が拾うはずのこの違反を、値変換自体が生き延びてしまっていた）、`meter_output_to_dict()` は非有限値をそのまま `values` へ詰めて `meter_call` payload を組み立て、`provenance.Ledger.append()` が `entry_sha` 計算に使う `canonical.canonical_json()`（`vgcal-canon/1`: NaN/Infinity は `ValueError`）がそれを拒否していた——この拒否は `Ledger.append()` の排他ロック書込みパス内（1 byte も書かずに raise）かつ `run_measurement_for_instance()` が `for record in records: ... ledger.append(...)` ループを**全て終えたあとに**課金する構造の**前**で起きるため、within-process 3 call + fresh-process 3 call（fresh は subprocess worker の実 CPU を含む）が既に消費した compute が一切課金されず、`meter_call` も 1 件も記帳されず、retry のたびに同じ work unit を無課金で再実行できた。修正: `meter_output_to_dict()` で `values` の各エントリを変換時に検査し、非有限値は `None`（valid JSON）へ置換したうえで、値の種別（`"nan"`/`"inf"`/`"-inf"`）を新設の兄弟フィールド `nonfinite_kind`（`{field_name: kind}`、非有限エントリが無ければ `None`）へ記録する（スキーマはそれ以外無改変。`values`/`missing_reason`/`ineligible`/`ineligible_reason` の意味は不変）。`meter_output_from_dict()` は `nonfinite_kind` を使って `None` エントリを元の `nan`/`inf`/`-inf` へ厳密に復元する（`None` 単体では 3 種を判別できないため `nonfinite_kind` が必須）ため、フレッシュ測定・resume（`_completed_meter_call_records()`）いずれの経路でも `candidates.adapter.unexplained_nonfinite()`（本契約が既に指定する disqualification 経路）を含む全下流消費者（`is_finite_and_explained()`/`within_fresh_process_mismatch()`/`observables.two_stage_median()`/`u_rep()`）が in-memory の生 NaN/Inf を見たときと同一の判定を行う——`two_stage_median()`/`u_rep()` は非有限を 0 へ丸めず absent-with-reason（前者は該当 process の repeat を median 計算前に落とさない設計のため非有限が混入すれば結果自体が非有限化し、後者は既存の `U_rep = None` fail-closed 経路）として伝播する既存の挙動をそのまま保つ（本 fix は変換層のみでこれらの集計式自体は無改変）。`_measure_worker.py` は同じ `meter_output_to_dict()` を使って stdout JSON を組み立てるため、fresh-process worker からの NaN/Inf も同一境界で同一に扱われる。テスト: within call の NaN → `meter_call` に `nonfinite_kind` 付きで記帳・課金される（クラッシュしない）、fresh worker の NaN（sanitize 済み stdout を模擬）→ 同様、resume が該当 record を再測定せず再利用しつつ `math.isnan`/`unexplained_nonfinite` が正しく成立、±Inf 変種、`values`/`nonfinite_kind` の round-trip 単体テスト、`nonfinite_kind` 欠落のまま `values` に `None` がある malformed payload の `ValueError` |
| `UNDERSPEC-CAL-D59` | `campaign/caps.py`, `tests/test_campaign_caps.py`, `tests/test_campaign_measure.py`, `tests/test_campaign_render.py` | round 26 ADOPT (3) finding「Charge failed batches as one outer work unit」（採用）: `budget_accounting_mode="per_unit_fixed"` の下で、`caps.charge_worker_attempts_before_raising()`（round 25, `[UNDERSPEC-CAL-D57]`）は budget を `budget_per_unit * (len(successes) + len(failures))`（attempt 数ぶん）で課金していた——failed 3-attempt batch（例: measure の `FRESH_PROCESS_REPEATS=3` 全滅）は budget を 3x 課金する一方、同形の**成功**した batch は既存の通常経路（`cap_counters.add(budget=budget_per_unit)`、1 work unit）で 1x しか課金されない。これは work unit の凍結定義（`cost_caps.CostCaps.budget_charge_per_work_unit()` docstring: 「render 呼び出し 1 回、または measurement 呼び出し 1 回」= `IMPLEMENTATION_MAP_v1.md` §6.4 ~438-444）に矛盾する——1 回の呼び出し（1 batch）は成功しても失敗しても budget 上は 1 unit のはずだが、失敗時のみ attempt 数ぶん水増しされていた。ルーリング: **CPU は attempt ごとの課金を維持**（`total_compute = sum(successes) + sum(failures の compute)` は無改変）、**budget のみ batch あたり 1 unit** に修正する（`cap_counters.add(budget=budget_per_unit)`、`attempt_count` 倍率を除去）。ledger 再構成（`cap_counters_from_ledger()`）も同じ粒度に揃える必要があるが、batch を構成する `worker_failed`/`worker_attempts_discarded` の各 event は `(row_id, probe_index, candidate_id, stage)` キーで dedup できない（`meter_call` と異なり、完全失敗した batch は `meter_call`/`render` を 1 件も残さないため resume が「未着手」と誤認し、同一キーで正当な別 batch（別の attempted work unit）を再試行し得る——キー dedup は真の retry を過少カウントする）。そこで batch の 1 unit 課金を**batch-level の 1 event にのみ載せる**方式を採る: `charge_worker_attempts_before_raising()` は新設フィールド `budget_units`（0 または 1）を batch 内の全 event（`worker_attempts_discarded`（存在すれば）+ 各 `worker_failed`）に持たせ、**先頭の 1 件のみ `budget_units: 1`、残りは `0`** とする。`cap_counters_from_ledger()` は `worker_failed`/`worker_attempts_discarded` それぞれで `payload.get("budget_units", 1)` を合算（フィールド欠落＝本 round 以前の legacy event は `1` へフォールバック——`meter_call` の `within_cpu_seconds` 欠落フォールバックと同じ、legacy event に対する overcount 方向の fail-closed）して budget unit 数を得る（`discarded_success_attempts` の**エントリ数**はもう使わない——round 25 時点はここが per-attempt 課金の入口だった）。`charge_worker_failure()`（1 attempt のみの薄いラッパ）は単一 attempt = 単一 batch のままなので退行しない。render 側 `render_instance()` の 2-worker batch も同じ関数を経由するため同時に是正される。テスト: measure 側の failed 3-attempt batch（sequential/concurrent 双方）が budget を 1 unit のみ課金・再構成が persisted と一致、render 側 2-attempt batch も同様、`local_zero_cost` モードは無影響、`cap_counters_from_ledger()` の legacy（`budget_units` 欠落）event が 1 unit へフォールバックする単体テスト |
| `UNDERSPEC-CAL-D60` | （コード変更なし。境界宣言。`[UNDERSPEC-CAL-D57]` を引用） | round 26 BOUNDARY (2) finding「Stop launching sequential attempts after a failure」（`campaign/measure_stage.py` ~608）見送り（境界宣言）: `run_fresh_process_calls()` の `max_workers<=1` 分岐は、1 attempt が失敗しても残りの repeat を打ち切らず開始する（round 25, `[UNDERSPEC-CAL-D57]` が「batch 全体を完走させてから一括課金する」設計として意図的に導入した挙動そのもの）。ルーリング: 見送り — `[UNDERSPEC-CAL-D57]` の「worker attempt accounting」family 終端宣言が既に定める境界どおり、開始された全 attempt は例外なく `charge_worker_attempts_before_raising()` で課金・cap 再検査される（本ラウンドの `[UNDERSPEC-CAL-D59]` で budget 粒度がさらに batch 単位へ是正済み）ため、無駄になり得る CPU は「1 work unit あたりの repeat 回数」で上限が決まり、承認済み cap を超えることはない——新しい無課金 spawn 経路ではないため、`[UNDERSPEC-CAL-D57]` の終端宣言が定める見送り条件（「新しい無課金 spawn 経路を具体的に示すもの以外は境界宣言で見送る」）にそのまま該当する。再着手条件: `[UNDERSPEC-CAL-D57]`/本エントリの終端宣言を覆す新しい無課金 spawn 経路が具体的に示された場合、または「1 attempt 失敗で即座に残りをスキップする」ことが cap 消費の観点以外の理由（例: 実運用での wall-clock 短縮）で要求される場合 |
| `UNDERSPEC-CAL-D61` | `campaign/cli.py`, `campaign/measure_stage.py`, `campaign/holdout_stage.py`, `tests/test_campaign_cli.py`, `tests/test_campaign_measure.py` | round 27 ADOPT (1) finding「Reject unusable F0 values before downstream injection」（`campaign/cli.py` ~508、採用）: `_build_f0_by_instance()` の F0 注入分岐は `f0_hz` 値を `None` かどうかでしか弾いておらず、round 26（`[UNDERSPEC-CAL-D58]`）が非有限 `f0_hz` repeat をレガー上で失敗させず durably round-trip させるようになった結果、NaN/±Inf の repeat が `observables.two_stage_median()` を素通りして `f0_by_instance` へ入り、`_params_with_f0()` 経由で F0 依存候補（`aperiodicity.py`/`tilt_harmonic.py`/`formant_cepstral.py` の `algorithm_family` ∈ `HARMONIC_OLS`/`HARMONIC_THEILSEN`/`HARMONIC_RESIDUAL`/`D4C_WORLD`/`CEPSTRAL_POLES`）の `params["f0_hz"]` へ注入されていた。`aperiodicity.py`/`tilt_harmonic.py` は非有限/非正の `f0_hz` を自前で missing 扱いするが、`formant_cepstral.cepstral_envelope_db()` のみ既定 lifter cutoff（64）へ縮退代入し、無効な F0 が使われたにもかかわらず有限な formant 出力を返してしまう（メータ実装は凍結のため `formant_cepstral.py` 自体は無改変）。ルーリング: 注入される F0 は repeat 単位・two-stage-median 集約値の両方が有限かつ厳密に正でなければならない——1 件でも非有限/非正なら当該 instance は F0-dependent candidate に対して評価不能（never injected、never 縮退代入）として扱う。実装: `cli._build_f0_by_instance()` が repeat/aggregate の両方を検査し、不合格 instance を戻り値から除外して `f0_unusable_instances`（新設の第 2 戻り値）へ積み、`f0_injection_rejected`（`reason: "F0_UNUSABLE"`、`stage`、`instances` 一覧）ledger event を記帳する。新設 `measure_stage.F0_DEPENDENT_ALGORITHM_FAMILIES` を `run_measure_stage()`（新設 kwonly 引数 `f0_unusable_instances`、既定 `frozenset()`）が参照し、該当 family の candidate を該当 instance 上で一切呼ばない（`measure()` 未呼出——ledger `meter_call` も一切記帳されない。単に該当 instance に record が無い、`build_candidate_criteria()`/`candidate_fail_filter_report()` が既に unmeasured instance へ与える「N_pos/coverage の分母から除外」と同じ扱いに自然に合流する）。`holdout_stage.render_and_measure_holdout()` は同じ kwarg を素通しする。`_run_c3b`/`_run_c4` の両呼び出し元を更新（`stage="c3b"`/`"c4"`）。テスト: NaN/+Inf/0/負値の 4 パターンで repeat 混在（他は valid）→ instance 全体が unusable・`f0_injection_rejected` event 記帳・再測定なし、全 repeat valid → 従来どおり通過し unusable に入らない、`run_measure_stage()` レベルで F0-dependent candidate（CEPSTRAL_POLES）が unusable instance 上で record 0 件・`meter_call` 0 件・非依存 candidate は無影響、usable instance では従来どおり測定されることを確認 |
| `UNDERSPEC-CAL-D62` | （コード変更なし。境界宣言。`[UNDERSPEC-CAL-D57]` を引用） | round 27 BOUNDARY (2) finding「Avoid process-wide CPU deltas for parallel workers」（`campaign/measure_stage.py` ~603）見送り（境界宣言）: `--workers>1` で `RUSAGE_CHILDREN` フォールバック（自身の CPU report が使えない失敗 attempt 用）は executor スレッド間で重複し得るため、1 子プロセスの CPU が複数の `_FreshWorkerFailure.compute` へ二重計上され得る。ルーリング: 見送り — このフォールバックは「自身の CPU report が使えない失敗 attempt」にのみ使われ、誤差は常に過大方向（cap への over-charge。過小課金にはならない）であり、`[UNDERSPEC-CAL-D57]` の統一規則どおり `workers × 失敗 attempt 数` で上限が決まる（新しい無課金 spawn 経路ではない）ため、`[UNDERSPEC-CAL-D57]`/`[UNDERSPEC-CAL-D60]` が既に定める見送り条件にそのまま該当する。再着手条件: `[UNDERSPEC-CAL-D57]` の終端宣言を覆す新しい無課金 spawn 経路が具体的に示された場合、または over-charge が実運用の cap 消費に無視できない影響を与えることが実測で示された場合 |
| `UNDERSPEC-CAL-D63` | `campaign/cli.py`, `tests/test_campaign_cli.py` | round 28 ADOPT (1) finding「Mark empty F0 measurements unusable」（`campaign/cli.py` ~508、D61 の未網羅ケース 2 件のうち 1 件、採用）: `[UNDERSPEC-CAL-D61]` が固定した guard は「1件以上の repeat が存在し、その中に非有限/非正な値がある」場合のみ `unusable` へ積んだ——選択済み F0 meter が **全 repeat** で `OUTPUT_MISSING`（`f0_hz is None`）を返すと `by_process` が空 dict のまま `if not by_process: continue` で **`result` にも `unusable` にも入らず素通り**し、`_params_with_f0()` は `params.get("f0_hz")` の呼び出し側既定値 `None` を渡すことになる。`formant_cepstral.cepstral_envelope_db()` は D61 が扱った非有限/非正 F0 と同じ経路で、この `None`（cutoff 未指定）も既定 lifter cutoff（64）へ縮退代入し、無効な F0 が使われたにもかかわらず有限な formant 出力を返す。ルーリング: 「有限かつ厳密に正の repeat/aggregate」が 1 件も無い——空、または部分的に空（一部 repeat のみ `OUTPUT_MISSING`）——な F0 観測集合は、非有限/非正 repeat とまったく同じ `F0_UNUSABLE` として扱う（第三の結果は無い）。実装: `_build_f0_by_instance()` に `missing_repeat` フラグを追加し、`_reusable_f0_values_by_process()` が `None` を返した後の手動 `by_process` 再構築ループ（`f0 is None` で `continue` する唯一の箇所——`_reusable_f0_values_by_process()` 自体は all-or-nothing で、1 repeat でも非数値なら `None` を返しここへフォールスルーする）で `f0 is None` を検出するたびに立てる。`if not by_process or missing_repeat:` で `unusable` へ積み、`f0_injection_rejected`（`reason: "F0_UNUSABLE"`）を D61 と同じ形で記帳する。テスト: 全 6 repeat が `OUTPUT_MISSING`（`_fake_missing_meter_call` で `MeterOutput(missing_reason=OUTPUT_MISSING)`、`f0_hz` キー無し）→ `result == {}`・`unusable == {("r1", 0)}`・`f0_injection_rejected` 記帳、within 1 repeat のみ `OUTPUT_MISSING`（他 5 repeat は有効値）→ 同様に instance 全体が unusable（部分欠損も「一部の repeat が投票から消える」だけの黙認ではなく明示的拒否になることを確認） |
| `UNDERSPEC-CAL-D64` | `campaign/measure_stage.py`, `campaign/selection_stage.py`, `tests/test_campaign_measure.py`, `tests/test_campaign_selection.py`, `tests/test_campaign_holdout.py` | round 28 ADOPT (2) finding「Count rejected F0 instances as missing coverage」（`campaign/measure_stage.py` ~1010、D61 の未網羅ケース 2 件のうち 1 件、採用。design 引用: 正本 §10.1「control 出力の missing/invalid は分子に算入（分母から除外しない。eligibility は C0 入力側条件のみで判定）」+ §11「score 計算可能だが PRIMARY 一部 output missing で gate 不通過 → DIAGNOSTIC_ONLY/OUTPUT_MISSING」）: `run_measure_stage()` が `f0_unusable_instances` 上の F0 依存 candidate を skip するとき（`[UNDERSPEC-CAL-D61]`）、その `(row_id, probe_index, candidate_id)` cell は `records` から**丸ごと消える**——`MeasurementRecord` も ledger event も一切残らない。`selection_stage.build_candidate_criteria()`/`candidate_fail_filter_report()` は `records` のみから分母・誤差ベクトルを構築し（`missing_failure_rate` は `own_records` 内の missing/finite 比率であり、そもそも `own_records` に居ない instance は数えない）、positive-control filter（`positive_rows_absent`/`negative_controls_incomplete`）は「宣言された母集団が丸ごと不在」または「row_id 単位で 1 件も record が無い」場合のみ発火する row 粒度の検査であり、同じ row の他 probe_index に record があれば「seen」に数えてしまう instance 粒度の欠落を検出できない——結果として、候補は縮小された母集団の上で missing-rate ペナルティを一切受けずに selection へ勝ち残り得る。ルーリング: 正本は selection 段階固有の missing-rate 閾値を定義していないため、既定の fail-closed 規則を適用する——凍結 C3 測定集合の期待 `(instance, candidate)` セルのうち、評価対象 SELECTION split 内の当該 family 全 TRUTH_CORE instance が 1 件でも usable measurement を欠けば、その候補は ineligible とする（`COVERAGE_INCOMPLETE` 相当の新設 filter reason）。実装: (a) `measure_stage.run_measure_stage()` が skip した全 cell を `measurement_missing`（`reason: "F0_UNUSABLE"`、`cells: [[row_id, probe_index, candidate_id], ...]`、batched・1 呼び出し 1 event）として明示的に ledger 記帳する（`kind` が `meter_call` と異なるため `_completed_meter_call_records()`/`_reusable_f0_values_by_process()` の resume 判定には影響しない。ledger 走査で既存 `measurement_missing` cell を再構成し、resume 時に同一 cell を重複記帳しない）。(b) `selection_stage.candidate_fail_filter_report()` に新設 filter `coverage_incomplete` を追加（`FAIL_FILTER_NAMES` 8 種目）: 新設 kwonly 引数 `expected_truth_core_instances`（`fixtures.controls.positive_detection_instances()` が返す `(row_id, probe_index)` instance 集合——`positive_control_row_ids` の instance 粒度版）が非空にもかかわらず `own_records` がそのうち 1 件でも instance を欠けば発火し ineligible にする（`positive_rows_absent`/`negative_controls_incomplete` と同じ「宣言されたが観測から消えた」規約の instance 粒度版）。`cli._positive_instances_for_selection()`（旧 `_positive_row_ids_for_selection()` を改称・instance 版へ拡張）が `_run_c3a`/`_run_c3b` 双方から `_criteria_with_fail_filters(expected_positive_instances=...)` 経由で配線する。holdout（C4）は既存の `gates.absolute_gates()` gate 1（`expected_primary_instance_ids` の凍結母集団と実測 `per_instance` の完全一致を要求し、不一致なら G/BIAS 集計自体を行わず gate1 で FAIL）がこの規則を独立に既に体現していたため、production code の変更は不要——回帰テストで「F0-unusable による欠落 instance が `expected_primary_instance_ids` に残ったまま measured 集合から消える」形を再現し、`CALIBRATED_ABSOLUTE` に到達しないことを固定した。テスト: `run_measure_stage()` の skip 1 件が `measurement_missing` event 1 件（`reason`/`cells` 一致）を生む、同一 skip の 2 回目呼び出しで event が重複しない（resume 冪等性）、`candidate_fail_filter_report()` の `coverage_incomplete`（部分欠落で発火/全件充足で不発火/空母集団は不発火の 3 ケース）、`evaluate_absolute_meter()` に `expected_primary_instance_ids` へ margin 化されていない instance を 1 件混ぜると gate1 で FAIL し `CALIBRATED_ABSOLUTE` に到達しない |

| `UNDERSPEC-CAL-D65` | `campaign/cli.py`, `campaign/measure_stage.py`, `campaign/holdout_stage.py`, `tests/test_campaign_cli.py`, `tests/test_campaign_measure.py` | round 29 ADOPT finding「Stop dependent selection when F0 has no winner」（`campaign/cli.py` ~892、採用。**D61/D63/D64 終端宣言が定める再着手条件——「無効/欠測 F0 が実際に F0 依存 candidate へ到達する新しい具体的経路」——に該当する family の例外エントリ**）: C3a が `SELECTION_FAILED_CLOSED` を記帳する（F0 候補が 1 件も選出されない）と `cli._latest_f0_selection()` は `(True, None)` を返す。`_run_c3b`/`_run_c4` はこれまで `if f0_selected_id is not None:` の中でのみ `_build_f0_by_instance()` を呼んで `f0_unusable_instances` を構築していたため、`f0_selected_id is None` の枝では丸ごとスキップされ `f0_unusable_instances` は空集合のまま——F0 依存候補（CEPSTRAL_POLES 等）は `params["f0_hz"]` に何も注入されず呼ばれ、`formant_cepstral.cepstral_envelope_db()` が既定 lifter cutoff へ縮退代入して有限な出力を返すため、失敗した前提にもかかわらず F0 依存候補が selection を通過し holdout で凍結されうる（D61/D63/D64 が閉じたのは「F0 winner は存在するがその per-instance 値が不健全」という経路のみで、「F0 winner 自体が存在しない」経路は未網羅だった）。ルーリング: F0 winner が無ければ、C3b・C4 双方の全 instance を全 F0 依存候補について `F0_UNUSABLE`（reason `F0_SELECTION_FAILED`——D61/D63/D64 の per-instance reason `F0_UNUSABLE` とは区別する）として扱う。実装: `_run_c3b`/`_run_c4` は `f0_selected_id is None` の分岐で `f0_unusable_instances` をその stage の全 instance 集合へ設定し（`f0_by_instance` は空のまま）、`f0_dependent_selection_blocked`（`reason: "F0_SELECTION_FAILED"`, `stage`, `instance_count`）ledger event を 1 件記帳する。`measure_stage.run_measure_stage()`/`holdout_stage.render_and_measure_holdout()` に新設 kwonly 引数 `missing_reason`（既定 `"F0_UNUSABLE"`、後方互換）を追加し、`measurement_missing` event の `reason` フィールドをこの値でパラメタ化——呼び出し側が `"F0_SELECTION_FAILED"` を渡す。C3b はこれにより F0 依存候補を一切呼ばず（round 28 の skip 機構をそのまま再利用）、非依存候補は従来どおり測定する。selection 側は無改変——`[UNDERSPEC-CAL-D64]` の `coverage_incomplete` filter が「当該候補の `own_records` が期待 instance 集合を 1 件も充足しない」形でそのまま発火し F0 依存候補を ineligible にする。C4 も対称な分岐を持つ（defense in depth: 万一 stale な `selection_frozen` が F0 依存候補を named していても呼ばせない）。design 引用（`DESIGN_VG_METER_CAL_DEBT_v1.0.md` §11 該当行）:「selection 全 fail → campaign は SELECTION_FAILED_CLOSED、meter は NOT_EVALUABLE。」——family 全候補が F0 依存で C3a が fail-closed の場合、C3b の `select_across_ceilings()` は eligible 候補 0 件で `selected_by_family[family]=None`（`SELECTION_FAILED_CLOSED` と同じ形）となり、C4 は既存の `holdout_stage.selection_failed_closed_meter()`（`TerminalStatus.NOT_EVALUABLE`/`MissingReason.OUTPUT_NOT_EVALUABLE`）でその meter を閉じる——`holdout_executed_valid` event の `per_meter` に直接反映され、`close.close_campaign()` はこの payload をそのまま読む。テスト: C3a fail-closed で C3b が非依存候補のみ測定・依存候補の `meter_call` 0 件・`measurement_missing`（reason `F0_SELECTION_FAILED`）記帳・`coverage_incomplete` 発火・依存候補が selection で勝てないこと、family 全候補が依存のみのケースで C3b の `selected_by_family` が `None`・C4 が同じ family/meter を `NOT_EVALUABLE`/`OUTPUT_NOT_EVALUABLE` で閉じる・`f0_dependent_selection_blocked` が stage `c3b`/`c4` 双方に記帳されること、`run_measure_stage()` の `missing_reason` kwonly がイベントへ正しく伝播すること、F0 winner が実在する既存 production E2E（`test_c0_freeze.py`）が変わらず通過すること |

| `UNDERSPEC-CAL-D66` | `campaign/cli.py`, `tests/test_campaign_cli.py` | round 30 ADOPT (1) finding「Require records from the selected holdout candidate」（`campaign/cli.py` ~1149、採用。design 引用: `DESIGN_VG_METER_CAL_DEBT_v1.0.md` §11「score 計算可能だが PRIMARY 一部 output missing で gate 不通過 → DIAGNOSTIC_ONLY/OUTPUT_MISSING」）: `_run_c4` の per-family loop にある `if family.value not in records_by_family:` guard は「その family の**どれかの**候補が record を出したか」しか見ておらず、B0 は holdout でも常に呼ばれるため、選抜された F0 依存候補が C4 の**全** holdout instance で skip された（`render_and_measure_holdout()` が `f0_unusable_instances`/`[UNDERSPEC-CAL-D61]`/`[UNDERSPEC-CAL-D65]` により当該候補を一切呼ばない）場合でも family key は B0 由来の record で埋まっており、guard を素通りして `selected_id`（=出力 0 件の候補）のまま `DIAGNOSTIC_ONLY` へ closes していた。ルーリング: coverage 判定は `selected_id` **自身**の record 集合に対して行う——選抜候補自身が usable holdout record を 1 件も持たない、または `[UNDERSPEC-CAL-D64]` の期待 instance 集合（`workunits.c4_holdout_instances()`）の一部でも欠ければ、meter の終端 status は `NOT_EVALUABLE`（reason `OUTPUT_NOT_EVALUABLE`）とし、`holdout_executed_valid` event（および `close.close_campaign()` がそのまま複写する `campaign_closed` の close report）にその旨を記録する。実装: `records_by_family[family.value]` を `candidate_id == selected_id` で絞り込み、`(row_id, probe_index)` の被覆を `workunits.c4_holdout_instances(matrix_rows, assignment, family=family.value)` と突き合わせる（D64 と同じ record-presence-only 規約——値の欠損ではなく instance の不在のみを見る）。record が 0 件、または期待集合を 1 件でも欠けば `MeterHoldoutResult(terminal_status=NOT_EVALUABLE, reason_code=OUTPUT_NOT_EVALUABLE, ceiling=NONE, selected_candidate_id=selected_id, ...)` を積んで次の family へ進む（既存の `claim_scope_report`/`DIAGNOSTIC_ONLY` 経路には入らない）。完全被覆時は従来どおり無変更。テスト: 選抜候補が holdout 全 instance で skip（B0 のみ record あり）→ `NOT_EVALUABLE`/`OUTPUT_NOT_EVALUABLE`（`holdout_executed_valid` event と `campaign_closed` close report の双方で確認）、1 instance のみ欠落する部分被覆 → 同じく `NOT_EVALUABLE`（D64 規則適用）、全 instance 被覆 → 従来どおり `DIAGNOSTIC_ONLY`（無変更の対照実験） |
| `UNDERSPEC-CAL-D67` | `candidates/adapter.py`, `tests/test_adapters.py`, `tests/test_campaign_selection.py`, `tests/test_campaign_cli.py` | round 30 ADOPT (2) finding「Allow stable negative-control non-detections」（`campaign/selection_stage.py` ~411、採用）: negative control（例: silence）上で正しく `OUTPUT_MISSING` を返す候補は `candidate_fail_filter_report()` が `r.output.values`（missing 時は空 dict）をそのまま `adapter.within_fresh_process_mismatch()` へ渡し、旧実装は `v[field_name]` の `KeyError` を無条件に「不一致」とみなしていたため、negative control 上の**正しい**非検出が `within_fresh_process_mismatch` filter を発火させ候補を ineligible にしていた（逆に有限値を返す＝誤検出する候補は `negative_control_false_fire` で ineligible になるため、negative control をどの候補も通過できない構造的欠陥）。ルーリング: within/fresh 一致判定は missing-status を call ごとに一貫させて比較する——within 全 call・fresh 全 call のいずれでも対象 field が欠けている（missing-status が全 process で一致）のは negative control 上の**正しい**非検出であり不一致ではない。不一致は (1) 一部の call のみ値を報告し他は報告しない（missing-status が call 間で食い違う）、(2) 両側とも値を報告したが `tol` を超えて食い違う、の 2 パターンのみで発火する。同じ意味論は positive control 行にも適用する——positive 行で全 call が一貫して missing なのは `positive_control_non_fire`（既存 filter）が拾うべき「陽性対照の不発火」であり、`within_fresh_process_mismatch` の不一致ではない。実装: `adapter.within_fresh_process_mismatch()` を、値取得の前に `field_name in v` の有無を within/fresh 双方の call ごとに集計する形へ変更——「両側とも全 call で欠如」は不一致でなく `False`、「一部の call のみ有／無が食い違う」は `True`、「両側とも全 call で存在」のときのみ従来どおり値の spread 比較（`tol` 判定）へ進む。呼び出し側（`selection_stage.candidate_fail_filter_report()`、唯一の呼び出し元）は無改変——`within_fresh_process_mismatch()` 内で完結する修正のため、positive/negative 双方の呼び出し経路に自動的に一様適用される。テスト: negative control で within/fresh 全 call が `OUTPUT_MISSING` → 不一致なし・`negative_control_false_fire` 不発火・`eligible_after_fail_filters` True（`test_negative_control_consistent_missing_stays_eligible`）、1 call のみ値を報告 → 不一致（`test_negative_control_one_process_reporting_value_is_a_mismatch`）、positive 行で全 call が一貫して missing → 不一致ではなく `positive_control_non_fire` が ineligible にする（`test_positive_row_consistent_missing_is_non_fire_not_mismatch`）、`adapter.within_fresh_process_mismatch()` 自体の網羅ケース（`test_adapters.py`）、`librosa.pyin` の実測（fake ではない）が silence 上で 6 repeat 全て `OUTPUT_MISSING` を返す実 fixture 行を通した production E2E（`cli._run_c3a` 経由の real render+measure、修正前は `SELECTION_FAILED_CLOSED` に落ちることを対照確認済み）が `SELECTED` に到達すること（`test_c3a_f0_selection_passes_with_candidate_that_correctly_non_detects_on_silence`） |
| `UNDERSPEC-CAL-D68` | `fixtures/controls.py`, `campaign/selection_stage.py`, `campaign/cli.py`, `tests/test_controls.py`, `tests/test_campaign_selection.py` | round 30 self-review MAJOR finding #1「coverage fail-open on CONFOUND rows」採用（design 引用: `DESIGN_VG_METER_CAL_DEBT_v1.0.md` §10.1「control 出力の missing/invalid は分子に算入（分母から除外しない）」+ §11「score 計算可能だが PRIMARY 一部 output missing で gate 不通過 → DIAGNOSTIC_ONLY/OUTPUT_MISSING」）: `[UNDERSPEC-CAL-D64]` の `coverage_incomplete` filter は母集団を TRUTH_CORE 限定（`fixtures.controls.positive_detection_instances()`）にしていたため、hard CONFOUND 行（114 行）で一貫して `OUTPUT_MISSING` を返す候補は coverage 判定の対象外のまま、`build_candidate_criteria()` が縮小された instance 母集団の上で `primary_normalized_mae`/`signed_bias`/`primary_q95_ae` を計算し、`missing_failure_rate`（lexicographic 順位の末尾側、`selection.py` `_ranking_key`）による比較よりも**先に**正直に測定した候補へ勝ち得た——D64 が閉じたはずの「縮小母集団で勝つ」fail-open を、D64 が対象にしなかった CONFOUND 母集団の上で再現していた。ルーリング: D64 の coverage 母集団を TRUTH_CORE から **全非 BOUNDARY expected SELECTION instance（TRUTH_CORE + CONFOUND）** へ拡張し、判定を **record-presence** から **値の有無**（within/fresh 全 call を通じて有限な primary 値が 1 件も無い = 一貫した missing）へ切り替える。BOUNDARY-domain 行（`fixtures.matrix.compute_domain()` が negative control 行にも `Domain.BOUNDARY` を付与する）は §1 D2「domain 外は自動外挿せず NOT_EVALUABLE」により本 filter の対象外のまま——`missing_failure_rate` にのみ反映される。negative control は無影響（`[UNDERSPEC-CAL-D67]`）。実装: (a) `fixtures/controls.py` に新設 `non_boundary_selection_instances()`（`positive_detection_instances()` の `block == "TRUTH_CORE"` 限定を `mr.domain == Domain.PRIMARY` へ拡張した姉妹関数、`positive_control_row_ids` 用の TRUTH_CORE-only 集合とは別物として保持）。(b) `selection_stage.candidate_fail_filter_report()` の引数を `expected_truth_core_instances` → `expected_coverage_instances` へ改称し、`required_field` が定義される candidate では `measure_stage.primary_output_value()` + `math.isfinite()` で値の有無を判定（未定義の candidate は record-presence へフォールバック）。(c) `cli.py` に新設 `_expected_coverage_instances_for_selection()`（`_positive_instances_for_selection()` とは別関数——後者は `positive_control_row_ids` 用に TRUTH_CORE-only のまま据え置く）を追加し、`_run_c3a`/`_run_c3b` 双方（C3a にも同じ拡張済み意味論を適用）から `_criteria_with_fail_filters(expected_coverage_instances=...)` 経由で配線。テスト: `tests/test_controls.py` に `non_boundary_selection_instances()` の実データ検証（BOUNDARY/negative-control 行の除外、`positive_detection_instances()` の上位集合であること、CONFOUND 行到達の回帰ガード）、`tests/test_campaign_selection.py` に record-presence 由来のみだった D64 テストの改称・値ありは complete/値なしは present-but-missing でも incomplete という新規ケース、実 matrix 行（`APERIODICITY_GT`）を使った CONFOUND 行一貫欠測 → ineligible／BOUNDARY 行一貫欠測 → eligible(missing_failure_rate のみ反映)／TRUTH_CORE 行一貫欠測 → 既存どおり ineligible（`positive_control_non_fire` と `coverage_incomplete` の双方が発火するが結果は不変）の 3 対照 |
| `UNDERSPEC-CAL-D69` | `campaign/cli.py`, `tests/test_campaign_cli.py` | round 30 self-review MAJOR finding #2「holdout partial coverage status」採用（design 引用: `DESIGN_VG_METER_CAL_DEBT_v1.0.md` §11「critical output 全欠損 or 最小数割れで score/gate 計算不能 → NOT_EVALUABLE/OUTPUT_NOT_EVALUABLE」対「score 計算可能だが PRIMARY 一部 output missing で gate 不通過 → DIAGNOSTIC_ONLY/OUTPUT_MISSING」）: `[UNDERSPEC-CAL-D66]` の `_run_c4` per-family coverage check は「selected candidate の record が 0 件」と「1 件以上あるが期待 instance 集合の一部を欠く（部分被覆）」の両方を同じ `NOT_EVALUABLE`/`OUTPUT_NOT_EVALUABLE` 分岐へ落としていた——commit メッセージは §11 の後者の行を引用しながら前者の帰結を適用しており、1 instance の欠落は score 計算不能を意味しないため §11 の cascade は本来 item 2（NOT_EVALUABLE）ではなく item 5（DIAGNOSTIC_ONLY）へ落ちるべきだった（常に安全側=握り潰しなしの誤りだが、close report 上の終端 status が設計と食い違う）。ルーリング: 全欠損（usable instance が 0 件）のみ `NOT_EVALUABLE`/`OUTPUT_NOT_EVALUABLE`。部分被覆（1 件以上は usable だが期待集合の一部を欠く）は `DIAGNOSTIC_ONLY`/`OUTPUT_MISSING`（gate 1 不通過の事実を `gate_detail` に記録）。完全被覆時は無変更。実装: `_run_c4` の per-family loop を 3 分岐（全欠損 → NOT_EVALUABLE、部分被覆 → DIAGNOSTIC_ONLY/OUTPUT_MISSING、完全被覆 → 既存 DIAGNOSTIC_ONLY）へ分割。テスト: 全欠損 → NOT_EVALUABLE（既存 `test_c4_selected_candidate_fully_skipped_closes_not_evaluable` を維持）、部分被覆 → `test_c4_selected_candidate_partially_covered_closes_diagnostic_only`（旧 `..._closes_not_evaluable` を改称・帰結を修正）、完全被覆 → 無変更（既存 `test_c4_selected_candidate_fully_covered_is_unchanged`） |
| `UNDERSPEC-CAL-D70` | `campaign/cli.py`, `tests/test_campaign_cli.py` | round 30 self-review MINOR finding #3「gate_detail wording vs. value-aware check」+ MINOR finding #4「claim_scope dropped on NOT_EVALUABLE early continue」+ MINOR finding #5「test gaps」採用（design 引用は D69 と同一）: (3) `[UNDERSPEC-CAL-D66]`/`D69` の coverage check は record-presence のみで「no usable output」と述べていたが、実際に検査していたのは record の有無のみで、`missing_reason` 付きの present record は「usable」と誤カウントされ得た。ルーリング/実装: `measure_stage.primary_output_value()` + `math.isfinite()` で判定する value-aware check へ変更し、`gate_detail.reason` の文言を「no measurement record with a usable (finite, non-missing) primary value」へ是正（wording と実装の整合）。(4) 旧実装の早期 `continue`（NOT_EVALUABLE 分岐）は `selection_stage.claim_scope_report()` を計算せず、`gate_detail` に `claim_scope` を含めていなかった——D66 が新設した唯一の「非 M4 family だが claim_scope が記録されない」経路だった。ルーリング/実装: `claim_scope_report()` を分岐の**前**で計算し、NOT_EVALUABLE/DIAGNOSTIC_ONLY(部分被覆)/DIAGNOSTIC_ONLY(完全被覆) の全 3 分岐の `gate_detail` に `claim_scope` を含める。(5) D66 の 3 テストは `render_and_measure_holdout()` を丸ごと monkeypatch しており、選抜された F0 依存候補が実際の C4 内で `f0_unusable_instances` により skip される本番形状が一度も end-to-end で検証されていなかった。ルーリング/実装: `render_stage._refuse_if_pre_unseal_holdout()`（§7 leakage guard——全 canonical matrix を要求するため tiny fixture では原理的に満たせない、無関係な既存被覆済み関心事）のみを stub し、`render_stage.run_render_stage(stage="c4")`/`measure_stage.run_measure_stage()` は実行する new test（`test_c4_f0_unusable_selected_candidate_through_real_holdout_path_closes_not_evaluable`、`@pytest.mark.slow`）を追加。テスト: 上記 3 点それぞれに対応するアサーション（`gate_detail` の `claim_scope`/文言、実 holdout path 経由の E2E） |
| `UNDERSPEC-CAL-D71` | `campaign/selection_stage.py`, `tests/test_campaign_selection.py` | #344 round 2 ADOPT 分類③（frozen-contract contradiction / false terminal outcome。**D68/D70 を是正**）「Keep missing primary outputs in the selection ranking」採用（`campaign/selection_stage.py` ~487）: `[UNDERSPEC-CAL-D68]` が `coverage_incomplete` へ足した value-aware 判定（`measure_stage.primary_output_value()` + `math.isfinite()` で有限値が読み取れる record のみ「covered」と数える）は、design 引用 `DESIGN_VG_METER_CAL_DEBT_v1.0.md` §9 (~L300-305)「ABSOLUTE 族: primary-domain normalized MAE → \|signed bias\| → primary q95 AE → nuisance sensitivity max → **missing/failure rate** → complexity rank → candidate_id 字句順」「DIRECTIONAL 族: 同様に **missing/failure rate** を lexicographic ranking criterion として持つ」と矛盾する過剰締め付けだった——`OUTPUT_MISSING` を 1 件でも legitimately 返す（`meter_call` record は存在し `missing_reason` が説明する）候補が `coverage_incomplete` で ineligible になり、family 内の全候補が各 1 件ずつ想定内の explained miss を持つだけで、本来ならランキング可能な候補群が丸ごと `SELECTION_FAILED_CLOSED` に落ちる false terminal outcome を生みかねなかった。ルーリング: `coverage_incomplete`（D64 の absent-call 検出、D68 の母集団拡張）は **ABSENT な call のみ**を検出する——期待 cell に対して `meter_call` record が 1 件も無い、または `measurement_missing` skip record（F0_UNUSABLE/F0_SELECTION_FAILED 等、D64/D65）しか無い場合のみ発火する。`missing_reason` 付きの `meter_call` record は**測定済みの結果**であり coverage failure ではない——`missing_failure_rate`（および既存の positive-control/negative-control filter）へそのまま反映させる。D67 の within/fresh missing-status 整合規則、D68 の BOUNDARY 域 exemption はいずれも無変更で維持。実装: `candidate_fail_filter_report()` の `covered_instances` を D68 以前の record-presence（`{(r.row_id, r.probe_index) for r in own_records}`）へ差し戻し、value-aware 分岐（`required_field`/`primary_output_value()`/`math.isfinite()` を用いた if/else）を削除した。`expected_coverage_instances`（TRUTH_CORE ∪ CONFOUND、`non_boundary_selection_instances()`）自体は D68 のまま維持。テスト: PRIMARY instance 上に 1 件の explained `OUTPUT_MISSING`（present record）を持つ候補が `coverage_incomplete=False`・eligible のまま・`missing_failure_rate` にその miss が反映されること（`test_present_but_missing_valued_record_is_still_coverage_complete`、`test_confound_row_consistent_explained_missing_is_eligible`）、absent call / `measurement_missing` skip（record が丸ごと無い）は引き続き `coverage_incomplete=True` で ineligible のまま（`test_missing_expected_instance_record_is_reported_as_coverage_incomplete`、既存のまま無改変で通過）、family の全候補がそれぞれ 1 件ずつ explained miss を持つケースで `select_across_ceilings()` が `SELECTION_FAILED_CLOSED` に落ちないこと（`test_family_where_every_candidate_has_one_explained_miss_does_not_fail_closed`）。TRUTH_CORE 行の explained miss は `positive_control_non_fire` により従来どおり ineligible のまま（`test_truth_core_row_consistent_missing_behaviour_is_unchanged`、`coverage_incomplete` の寄与のみ False へ変更・結果は不変） |
| `UNDERSPEC-CAL-D72` | `campaign/cli.py`, `tests/test_campaign_cli.py` | #344 round 2 ADOPT 分類③（frozen-contract contradiction / false terminal outcome。**D69 を是正**）「Restrict holdout coverage failures to PRIMARY instances」採用（`campaign/cli.py` ~1235）: `[UNDERSPEC-CAL-D66]`/`[UNDERSPEC-CAL-D69]` の holdout 期待 instance 集合は `workunits.c4_holdout_instances()`（HOLDOUT split から**negative control 行のみ**を除外——`control_row_ids()` は `control_class is not None` の行のみを指す）由来のままだったため、control ではない BOUNDARY-domain 行（boundary-axis probe: 例えば境界 F0/SR/gain/duration/noise 値）が引き続き期待集合に含まれていた。design 引用（`DESIGN_VG_METER_CAL_DEBT_v1.0.md` §10.3 ~L351-361）: 「gate 1: 全 **PRIMARY** instance が eligible（critical missing/undefined なし）」——gate 1 は PRIMARY instance の網羅性のみを要求し、BOUNDARY は別扱い（§1 D2「domain 外は自動外挿せず NOT_EVALUABLE」）。BOUNDARY instance 上の正しい（想定内の）欠測が `missing_expected_instances` に混入し、`DIAGNOSTIC_ONLY`/`OUTPUT_MISSING`（または PRIMARY も同時に欠ける場合は `NOT_EVALUABLE`/`OUTPUT_NOT_EVALUABLE`）を偽って記録し得た。ルーリング: 期待 holdout coverage 集合を `fixtures.controls.non_boundary_selection_instances()`（selection 側 D68/D71 と同じ関数、`domain == Domain.PRIMARY` = TRUTH_CORE ∪ CONFOUND）を `Split.HOLDOUT` に対して呼んだ結果へ差し替える（BOUNDARY は既存の別ゲート/診断が扱う——本 filter の対象外のまま）。`workunits.c4_holdout_instances()` 自体は無変更で、実際に何を render/measure するか（`all_instances`）は従来どおり BOUNDARY 込みのまま——本 fix は coverage-**completeness 判定**の母集団のみを絞る。付随修正: 「usable output が 1 件も無い」判定（`if not usable_holdout_instances:`）はドメインを区別しないため、選抜候補が BOUNDARY instance 上でのみ usable な値を返し PRIMARY 上は全欠測、というケースで誤って `NOT_EVALUABLE` を回避し `DIAGNOSTIC_ONLY`/`OUTPUT_MISSING` 側へ落ちてしまう経路があった——`usable_primary_instances = usable_holdout_instances & expected_holdout_instances` を導入し、`not usable_holdout_instances or (expected_holdout_instances and not usable_primary_instances)` を `NOT_EVALUABLE` の判定条件とした（「候補が一切呼ばれなかった」場合の従来挙動は維持しつつ、「PRIMARY のみ全欠測・BOUNDARY のみ usable」も正しく `NOT_EVALUABLE` に倒す）。テスト: BOUNDARY instance のみ欠測（PRIMARY は完全被覆）→ coverage failure 無し（`reason_code is None`・`gate_detail` に `reason` キー無し、既存の完全被覆 unchanged 分岐と同一、`test_c4_boundary_only_miss_is_not_a_coverage_failure`）、PRIMARY 部分欠測（BOUNDARY は完全被覆で無関係を確認）→ `DIAGNOSTIC_ONLY`/`OUTPUT_MISSING`・`gate_detail` の expected/seen count が PRIMARY のみ（BOUNDARY 分を含まない）（`test_c4_primary_partial_coverage_closes_diagnostic_only_boundary_ignored`）、PRIMARY 全欠測（BOUNDARY は usable）→ `NOT_EVALUABLE`/`OUTPUT_NOT_EVALUABLE`（`test_c4_primary_all_missing_closes_not_evaluable_despite_usable_boundary`） |
| `UNDERSPEC-CAL-D73` | `gates.py`, `campaign/cli.py`, `tests/test_campaign_cli.py` | #344 round 3 ADOPT 分類③（frozen-contract contradiction / false terminal outcome。**D69/D72 を是正**）「Enforce minimum sample counts before diagnostic close」採用（`campaign/cli.py` ~1324）: `[UNDERSPEC-CAL-D69]`/`[UNDERSPEC-CAL-D72]` の部分被覆分岐は「usable PRIMARY instance が 1 件以上あるか」だけで `DIAGNOSTIC_ONLY`/`OUTPUT_MISSING` を確定していたが、design 引用（`DESIGN_VG_METER_CAL_DEBT_v1.0.md` §10.3 直下、~L375）「resolvable pair は各 sweep で >= 3」と §11 の missing-status cascade（~L395-396）「critical output 全欠損 **or 最小数割れ**で score/gate 計算不能 → `NOT_EVALUABLE`/`OUTPUT_NOT_EVALUABLE`」対「score 計算可能だが PRIMARY 一部 output missing で gate 不通過 → `DIAGNOSTIC_ONLY`/`OUTPUT_MISSING`」が示すとおり、usable instance が僅少（例: M5-type/DIRECTIONAL 族の meter で 2 件のみ）な場合はそもそも meter の frozen minimum-count/resolvable-pair 条件（`gates.directional_gates()` の per-sweep `>= 3` 判定）を満たし得ず、score/gate は計算不能——`DIAGNOSTIC_ONLY` ではなく `NOT_EVALUABLE` が正しい帰結だった。ルーリング: 部分被覆を `DIAGNOSTIC_ONLY`/`OUTPUT_MISSING` に分類する**前**に、usable PRIMARY observation の件数が meter の frozen minimum-count 条件を構造的に満たし得るかを検査する——閾値を CLI 側で再定義せず、`gates.py` が ABSOLUTE/DIRECTIONAL 両ゲートで使う frozen 実装を呼ぶ（閾値の二重定義を避ける）。条件を満たさなければ `NOT_EVALUABLE`/`OUTPUT_NOT_EVALUABLE`（`gate_detail` に未達条件を明記）、満たせば従来どおり `DIAGNOSTIC_ONLY`/`OUTPUT_MISSING`、完全被覆時は無変更。実装: `gates.py` に `MIN_RESOLVABLE_PAIRS_PER_SWEEP`（= `directional_gates()` が内部で使っていた直書きの `3` を名前付き定数へ抽出。値は無変更）と、coverage-count のみから「N 件の usable instance から作れる pair の理論最大数 `C(N,2)` が同定数を下回るか」を判定する `resolvable_pairs_possible(n_usable_primary_instances)` を新設（`directional_gates()` 自身も同じ定数を参照するよう置換——閾値の実体は 1 箇所のみ）。`campaign/cli.py` `_run_c4` の部分被覆分岐で、`DIAGNOSTIC_ONLY`/`OUTPUT_MISSING` を積む前に `gates.resolvable_pairs_possible(len(usable_primary_instances))` を呼び、`False` なら `NOT_EVALUABLE`/`OUTPUT_NOT_EVALUABLE`（`gate_detail` に `min_resolvable_pairs_per_sweep` と未達理由を記録）へ分岐する。テスト: usable PRIMARY instance が 2 件（期待 5 件、`C(2,2)=1 < 3`）→ `NOT_EVALUABLE`/`OUTPUT_NOT_EVALUABLE`（`test_c4_partial_coverage_below_minimum_count_closes_not_evaluable`、新規）、usable が 4 件（期待 5 件、1 件欠落、`C(4,2)=6 >= 3`）→ 従来どおり `DIAGNOSTIC_ONLY`/`OUTPUT_MISSING`（既存 `test_c4_selected_candidate_partially_covered_closes_diagnostic_only`、無改変で通過）、完全被覆は無変更（既存 `test_c4_selected_candidate_fully_covered_is_unchanged`）、D72 の BOUNDARY 系 3 テストは PRIMARY usable 件数が閾値を上回るため無影響のまま通過（既存、無改変）、`gates.py` 側は `directional_gates()` の既存 58 件のテストが定数抽出後も無改変で通過することを確認。**round 4 `[UNDERSPEC-CAL-D74]` により本 entry の閾値適用範囲（ABSOLUTE 候補にも適用していた点）と `test_c4_partial_coverage_below_minimum_count_closes_not_evaluable` の期待値は是正された——後続 entry を参照** |
| `UNDERSPEC-CAL-D74` | `gates.py`, `campaign/cli.py`, `tests/test_gates.py`, `tests/test_campaign_cli.py` | #344 round 4 ADOPT 分類③（frozen-contract contradiction / false terminal outcome。**D73 を是正**）2 件採用。(1)「Fix sweep-partition math in resolvable pair counting」（`gates.py` ~71）: D73 が新設した `resolvable_pairs_possible()` は宣言 sweep を無視し、meter 全体の usable PRIMARY instance 数 N から `C(N, 2)` を計算していた——design 引用（`DESIGN_VG_METER_CAL_DEBT_v1.0.md` §10.4 ~L375）「resolvable pair は各 sweep で >= 3」は sweep 間で集約した合計ではなく **宣言済みの全 sweep がそれぞれ独立に** 最小数を満たすことを要求する（`directional_gates()` 自身が実測 pair に対して行う `sweep_resolvable_counts`/`sweeps_below_minimum` の per-sweep 分割規約と同じ）。旧実装は 3 件の usable instance が 3 つの異なる宣言 sweep に 1 件ずつ分散している場合（各 sweep `C(1,2)=0`）でも「meter 全体では `C(3,2)=3 >= 3`」と誤って「構造的に達成可能」と判定し得た。ルーリング/実装: `resolvable_pairs_possible()` のシグネチャを `(usable_primary_instance_counts_by_sweep: Mapping[str, int], expected_sweep_ids: Collection[str]) -> bool` へ変更し、`directional_gates()` と同じ「宣言済み全 sweep を走査し、`usable_primary_instance_counts_by_sweep` に無い宣言 sweep は `n=0` として扱う」規約を適用——宣言済み各 sweep について `C(n, 2) >= MIN_RESOLVABLE_PAIRS_PER_SWEEP` を独立に要求し、1 つでも未達なら全体 `False`。宣言 sweep が 1 つも無ければ防御的に `False`（`directional_gates()` の "no expected sweep declared" と同じ fail-closed 側）。テスト: 3 件の usable instance が 3 sweep に 1 件ずつ分散 → `False`（`test_resolvable_pairs_possible_three_instances_spread_over_three_sweeps_fails`、新規）、1 sweep に 3 件集中 → `True`、複数 sweep が各々最小数を満たす → `True`、1 sweep のみ未達 → 全体 `False`、宣言 sweep に観測 0 件 → `False`、宣言 sweep 無し → `False`、しきい値ちょうど (`C(3,2)=3`) の境界値、いずれも新規（`tests/test_gates.py`、計 7 件）。(2)「Apply the pair minimum only to directional meters」（`campaign/cli.py` ~1348）: D73 の最小数チェックは effective ceiling を問わず全 meter に適用されていたが、design 引用（`DESIGN_VG_METER_CAL_DEBT_v1.0.md` §10.3 ABSOLUTE holdout gate、~L351-353）「gate 1: 全 PRIMARY instance が eligible（critical missing/undefined なし）」——§10.3 は上記 eligibility precondition 以外に最小サンプル数を一切課しておらず、MAE/BIAS/q95（§10.1）は usable instance が 1 件以上あれば計算可能（ゼロ件は既存の全欠損 `NOT_EVALUABLE` 分岐が別途処理）。「resolvable pair は各 sweep で >= 3」（§10.4）は DIRECTIONAL gate 専用の概念であり、ABSOLUTE 候補（例: `M2A-HARMONIC-RESIDUAL-*`）に適用すると、本来 score 計算可能な部分被覆を誤って `NOT_EVALUABLE` にしてしまう false terminal status だった。ルーリング/実装: `selected_candidate_obj`/`max_claim_scope` から `selection_stage.claim_scope_report()` で得る **effective ceiling**（`capped_ceiling()` による capping 後の値。旧実装は `_capped` として破棄していたのを `effective_ceiling` として保持するよう修正）が `ClaimCeiling.DIRECTIONAL` のときのみ `resolvable_pairs_possible()` を呼ぶ——それ以外（ABSOLUTE を含む）の effective ceiling では、部分被覆は無条件に `DIAGNOSTIC_ONLY`/`OUTPUT_MISSING`（§11「score 計算可能だが PRIMARY 一部 output missing で gate 不通過」）。DIRECTIONAL 側の呼び出しは、この CLI 呼び出し箇所に実測 instance ごとの宣言 sweep 分解が配線されていない（実 DIRECTIONAL gate 組み立て自体が `[UNDERSPEC-CAL-D17]` により CLI scope 外のまま）ため、meter の usable PRIMARY instance 全体を単一の宣言 sweep（`{"default": n}`/`{"default"}`）とみなす——D73 が exercise していた「meter 全体で 1 つの合算カウント」と同じ算術に退化するが、今回は DIRECTIONAL effective ceiling にのみ限定して適用される点が是正点。テスト: ABSOLUTE 候補（`M2A-HARMONIC-RESIDUAL-*`）が 2 件 usable + 3 件 explained miss（期待 5 件）→ `DIAGNOSTIC_ONLY`/`OUTPUT_MISSING`（`gate_detail` に `min_resolvable_pairs_per_sweep` キーが無いことも確認、D73 の誤った `NOT_EVALUABLE` 期待を是正、`test_c4_absolute_partial_coverage_below_minimum_count_still_closes_diagnostic_only`、旧 `test_c4_partial_coverage_below_minimum_count_closes_not_evaluable` を改名・期待値修正）、DIRECTIONAL 候補（`M2A-B0-AUTOCORR-PERIODICITY`）が同じ 2 件 usable → `NOT_EVALUABLE`/`OUTPUT_NOT_EVALUABLE`（`gate_detail.effective_ceiling == "DIRECTIONAL"`、`test_c4_directional_partial_coverage_below_minimum_count_closes_not_evaluable`、新規）、DIRECTIONAL 候補が 3 件 usable（`C(3,2)=3 >= 3`、最小数条件は満たすが 1 件欠落で部分被覆）→ `DIAGNOSTIC_ONLY`/`OUTPUT_MISSING`（`test_c4_directional_partial_coverage_at_minimum_count_closes_diagnostic_only`、新規）、既存の全欠損/完全被覆/BOUNDARY 系テストは無改変で通過、production E2E（`test_c4_f0_unusable_selected_candidate_through_real_holdout_path_closes_not_evaluable` 含む）も無改変で通過 |
| `UNDERSPEC-CAL-D76` | `fixtures/axes.py`, `fixtures/matrix.py`, `c0_freeze.py`, `c0_validate.py`, `vocab.py`, `campaign/cli.py`, `campaign/measure_stage.py`, `campaign/holdout_stage.py`, `tests/*` | #344 round 6–7 ADOPT 4 件（Fable design ruling。**D75 を SUPERSEDE**）。分類③ 2 件（frozen-contract contradiction / matrix generator structural defect・false terminal status）+ 分類② 2 件（実コード被害・致命的バグ相当の instance-usability collapse）。design 引用（`DESIGN_VG_METER_CAL_DEBT_v1.0.md` §10.4 ~L370-375）: 「`resolvable(i,j) <=> Delta_truth(i,j) > R_ij`」「resolvable pair は各 sweep で >= 3」——`Delta_truth(i,j) > R_ij >= 0` は truth が変動する pair を前提にしており `Delta_truth == 0` は原理的に resolvable になり得ない。詳細調査 = `sweep_truth_investigation.md`（file:line 証拠付き）。**(1) matrix/axes の revert**（分類③。D75 の対症療法を撤回）: D75 は「sweep = nuisance 軸」という誤った定義（Codex 指摘への対応として declared_sweeps_by_family() を D18 の confound_axes 写像から作った）の下で「gain 軸 2 水準では sweep 容量が足りない」と誤診断し、`axes.CANONICAL_NUISANCE_SEQUENCE` に gain 3 水準目を追加・単一 anchor family の `target_n` を引き上げて 456→462 セル化した（TILT_GT/RESONANCE_GT/TRANSITION_GT: 48→49、APERIODICITY_GT: 72→75）。`sweep_truth_investigation.md` Q3 の結論: nuisance 軸で group 化すると group 内 truth は family anchor に固定される（`_build_confound_block` の設計そのもの）ため全 pair `delta_truth == 0`——7 family 中 7 family が「nuisance 軸 = sweep」の下では構造的に resolvable pair 0 件であり、462 セル化はこの誤った定義への対症療法だった。ルーリング: `CANONICAL_NUISANCE_SEQUENCE`・`target_n` 各値・`FAMILY_COUNTS`・`TOTAL_LOGICAL_CELLS`・`BLOCKED_C0_SWEEP_CAPACITY_INSUFFICIENT` validator（`_check_sweep_capacity`）とそのテストを 728ef60 の値へ厳密に revert し、matrix 総数 456 logical cells / 2280 instances / 4560 renders / 13680 meter calls per implementation を復元する（`fixtures/axes.py`・`fixtures/matrix.py`・`fixtures/generators/resonance.py`・`__init__.py` 系 3 ファイルの `462`/`49`/`75` 表記も 728ef60 相当へ戻す）。**(2) declared sweep の再定義 = def A**（分類③。D18/D75 の写像を訂正）: `sweep_truth_investigation.md` の 3 傍証——§4.2 L169「content/F0/duration/SNR 固定の one-factor causal sweep」、§12 L421「事前 causal sweep の全 resolvable pair」（sweep 内に複数 resolvable pair が存在する前提）、§10.1 L325-326/L334「truth 自体が変わる軸は invariance 対象に混ぜない」+「invariance 軸ごとに >= 5 pairs」（nuisance 軸 = invariance 軸、sweep とは別語・別最小数）——から、sweep は「nuisance/covariate 設定を固定し truth 水準だけを動かす truth-core block の行集合」（= §2.7 truth-core 因子分解そのもの）と定義する（def A）。ルーリング/実装: `fixtures.matrix.declared_sweeps_by_family(matrix_rows)` を新設し、PRIMARY domain の `block == "TRUTH_CORE"` 行を「held-fixed field（`positive_control`/`block` を除く canonical field 一式から truth field を除いたもの）が同一」で group 化、sweep_id は held-fixed field のうち family 内で実際に変動する field を `field=value` 形式で連結して決定的に導出する（`truth_identity_for_row()` が group 内の truth 水準数を数える入力）。C0 freeze（`c0_freeze._fixture_specs()`）はこの出力を `frozen_design.fixture_spec.<FAMILY>.declared_sweeps`（sweep_id → member row_id 一覧、`manifest_core_sha` に含まれる）として新規記録し、C0 で `_check_declared_sweep_truth_levels()`（新設 `BLOCKED_C0_SWEEP_DECLARATION_INVALID`、旧 `BLOCKED_C0_SWEEP_CAPACITY_INSUFFICIENT`/`_check_sweep_capacity` を置換）が凍結 matrix 全体で全 declared sweep が truth level >= 3 を持つか検証する。旧 `confound_axes`（flat 6-tuple、gate4' invariance 軸宣言）は無変更のまま維持し、`declared_sweeps` という別 key へ分離した——D18/D75 の「confound_axes を DIRECTIONAL sweep_id としても再利用する」写像は誤りとして `campaign/holdout_stage.py` の D18 note を訂正した（`declared_axes_for_family()` 自体は gate4' 専用のまま無変更）。**per-family declared sweep 数 × sweep あたり行数**（def A、456 セル canonical matrix、`sweep_truth_investigation.md` Q3 と一致 — 検証手順は `tests/test_matrix.py`）: F0_CONTROL 3×4、FORMANT_GT 12×5、TILT_GT 6×5、APERIODICITY_GT 10×6、RESONANCE_GT 6×4、TRANSITION_GT 8×3、IDENTITY_CAUSAL_SWEEP 12×5（FORMANT_GT は truth identity に `pole_freqs_hz` tuple 全体を使うため常に 5 distinct level——F1 スカラーだけを見る `campaign.selection_stage.truth_value_for_row()` の代表値と異なり、既知の F1=500Hz 重複〔`[UNDERSPEC-CAL-D13]`〕による水準数低下が起きない）。**(3) C4 の構造下界 = 真値水準の distinct 数**（分類③。D75 ruling (1) の「usable instance の raw 件数」を是正）: `gates.resolvable_pairs_possible()` へ渡す per-sweep カウントを、usable PRIMARY holdout instance の raw 件数（probe repeat を含む）から、declared sweep ごとの **distinct truth level 数**（row-level、`truth_identity_for_row()` で同定、probe repeat は never カウント）へ改めた——同一行の複数 probe repeat が usable でも `delta_truth(i,i) == 0` で resolvable pair は増えない、という raw-count 版の見落としを閉じる（新規 regression: `test_c4_directional_sweep_with_repeated_truth_level_still_unresolvable`）。未達なら `NOT_EVALUABLE`/`OUTPUT_NOT_EVALUABLE`、`gate_detail.gate_detail_reason_code = "DIRECTIONAL_SWEEP_UNRESOLVABLE_ON_HOLDOUT"`（新設、閉語彙 `MissingReason` とは別に `gate_detail` 内の機械可読フィールド）。**holdout 上の DIRECTIONAL 到達不能性は設計事実であり回避しない**: def A を凍結 matrix 全体で評価すれば全 family が「各 sweep truth level >= 3」を満たすが、HOLDOUT split（`STRATUM_FACTOR_NAMES = (block, domain)` 層別 25%）は sweep 構造を層別因子に含まないため、各 sweep の holdout 残存行は構造的に 1–2 行へ薄まる（`sweep_truth_investigation.md` Q3「holdout split を通した後の実効容量」: ダミー secret 12 試行シミュレーションで 7 family 全て「全 sweep >= 3」不成立）。したがって DIRECTIONAL-ceiling 候補が holdout 上で `NOT_EVALUABLE`（`DIRECTIONAL_SWEEP_UNRESOLVABLE_ON_HOLDOUT`）に倒れるのは**この canonical matrix + 現行 split の下での正直な §11 帰結**であり、workaround すべきバグではない（sweep-aware stratification〔`STRATUM_FACTOR_NAMES` へ sweep key を足す〕は design v1.1 改訂候補として記録するのみ、本 D76 では未実装）。ABSOLUTE-ceiling 候補は本ルーリングの影響を受けない（§10.3 は sweep 構造に依存しない）。**(4) instance usability = 全 repeat 整合**（分類②。Codex round 7 #1、実コード被害/将来汚染: C4/selection 双方の集計が existential collapse で汚染された数値を produce しうる）: `campaign/cli.py` `_run_c4` の `usable_holdout_instances` は旧実装では instance の record を 1 件でも走査しその 1 件が有限なら instance 全体を usable とみなす existential collapse だった（6 repeat 中 1 件が有限なら残り 5 件が missing/不整合でも usable 側へ倒れる）。新設 `measure_stage.usable_primary_instances(candidate, records)` は record を `(row_id, probe_index)` でグループ化し、グループ内**全** record が有限な primary 値を返し、かつ within/fresh 両方の repeat 群が存在する場合は D67 一貫性規則（`candidates.adapter.within_fresh_process_mismatch()`）で不一致と判定されない場合にのみ usable とする（1 件でも欠落/不整合なら instance 全体を不採用、coverage は `OUTPUT_MISSING`（部分被覆）として正直に反映される）。within `WITHIN_PROCESS_REPEATS`+fresh `FRESH_PROCESS_REPEATS` の閉集合が実際に揃っているかどうかは検査しない——`measure_stage._completed_meter_call_records`/`StaleMeasurementError` が ledger 記帳時点で「6 repeat がちょうど揃うか測定を一切行わないか」を既に fail-closed で保証しているため、本関数が塞ぐのは「揃った 6 repeat の一部だけが有限/整合」というケースであり、この形は軽量な単一 record/instance のテスト fixture（既存 coverage テスト群が広く使う慣用句）とも両立する。`campaign/selection_stage.py` の `build_candidate_criteria()`/`two_stage_median` 入力集計は監査した結果、instance ごとに `per_instance_process_values` を record-by-record に安全側（missing/nonfinite は個別に除外し二段 median へ混入しない）で積み上げる既存実装であり、C4 と同型の existential collapse は確認されなかった（変更なし）。テスト: 全 4 ルーリングとも `voice_genesis/calibration/tests/{test_matrix,test_c0_validate,test_c0_freeze,test_gates,test_campaign_cli,test_campaign_workunits,test_vocab}.py` に反映（新規/改訂テスト一覧は commit body 参照） |
| `UNDERSPEC-CAL-D77` | `c0_validate.py`, `vocab.py`, `campaign/cli.py`, `tests/test_c0_validate.py` | #344 round 8 ADOPT 2 件。**(1) manifest declared_sweeps の必須化+完全一致検証**（分類②。実コード被害/provenance artifact contamination）: `c0_validate._check_declared_sweep_truth_levels()`（D76 ruling (2)）は manifest 引数を一切読まず凍結 matrix (`fixtures.matrix.build_matrix()`) から独立に再導出した値のみを検証していた——`FIXTURE_SPEC_REQUIRED_KEYS` にも `declared_sweeps` が未列挙だったため、manifest がこのフィールドを欠落・矛盾（宣言と実体の食い違い）させても `validate_c0_manifest()` は PASS し得た（`c0_freeze._fixture_specs()` 自体は既に正しい値を `manifest_core_sha` 対象として書き込んでいたが、validator 側がそれを一度も読んでいなかった）。ルーリング/実装: `FIXTURE_SPEC_REQUIRED_KEYS` へ `declared_sweeps` を追加（欠落/hollow は既存の `missing_required_keys`/`BLOCKED_C0_MANIFEST_INCOMPLETE` が捕捉）、`_MAPPING_SHAPE_FIELDS` へ追加（非 mapping/空 mapping も同じく `missing_required_keys` 側）、新設 `_check_declared_sweep_declaration_match()`（`_normalize_declared_sweeps()` で内側形状を `sweep_id -> (member row_id, ...)` へ正規化した上で）が manifest の宣言値を `fixtures.matrix.declared_sweeps_by_family(build_matrix())` からの導出値と **完全一致**（sweep_id 集合・member row_id の並び順まで）するか検査し、不一致（形状が壊れている場合も含む——「宣言が実体と一致しない」という同一の意味論として統合）は新設 `vocab.BlockedCode.BLOCKED_C0_SWEEP_DECLARATION_MISMATCH` で個別に fail-closed する（`BLOCKED_C0_SWEEP_DECLARATION_INVALID`——構造がそもそも §10.4 の truth-level 下限を満たせるかの manifest 非依存チェック——とは独立の軸）。`C0ValidationResult.sweep_declaration_mismatch_violations` を新設。テスト: `declared_sweeps` キー欠落 → `BLOCKED_C0_MANIFEST_INCOMPLETE`（MISMATCH は不発火、二重報告回避を確認）、非 mapping scalar → 同じく `BLOCKED_C0_MANIFEST_INCOMPLETE`（shape 検査経由）、member row_id 欠落/余分な sweep_id 追加/並び順入れ替えの 3 パターンいずれも `BLOCKED_C0_SWEEP_DECLARATION_MISMATCH`、canonical manifest（凍結 matrix からの実導出値をそのまま宣言）→ 無 BLOCK（`test_declared_sweeps_canonical_manifest_passes`、既存 `test_complete_manifest_has_no_blocks` も維持）。既存 `test_starved_matrix_blocks_with_sweep_declaration_invalid`（D76 ruling (2) の構造検査テスト）は、monkeypatch した飢餓状態 matrix と manifest の（非飢餓状態の）宣言値が今回追加した一致検査でも食い違うため `BLOCKED_C0_SWEEP_DECLARATION_MISMATCH` が独立に共起することをアサーションへ追記した（意味論的に正しい副作用——構造欠陥と宣言不一致は独立の軸であり両方とも実在する）。canonical manifest fixture（`tests/_full_fixture_spec()`/`_shape_valid_nested_value()`）は `declared_sweeps` キーのみ汎用 placeholder ではなく `fixtures.matrix.declared_sweeps_by_family(build_matrix())` の実導出値を返すよう拡張した（一致検証を満たす必要があるため）。**(2) C4 sweep 容量検査を被覆完全時も実施**（分類③。false terminal status）: `campaign/cli.py` `_run_c4` の per-sweep truth-level 容量検査（D76 ruling (2)/(3) が `resolvable_pairs_possible()` へ配線）は `if expected_holdout_instances and missing_expected_instances:`（部分被覆）ブロック内でのみ実行されていたため、DIRECTIONAL-ceiling 候補が **完全被覆**（`missing_expected_instances` が空）でも、宣言済み sweep の distinct truth level 数が構造的に `MIN_RESOLVABLE_PAIRS_PER_SWEEP` 未満（§10.4 ~L375）であれば検査自体がスキップされ、汎用 `DIAGNOSTIC_ONLY` placeholder（D17 note、「score/gate assembly 未配線」を示す）へフォールスルーしていた——score が構造的に計算不能であるという実態を「まだ計算していないだけ」の status で誤って表現する false terminal だった。ルーリング/実装: 容量検査（`usable_truth_level_counts_by_sweep`/`expected_sweep_ids`/`resolvable_pairs_possible()` の呼び出しと `directional_minimum_not_met` 判定）を `missing_expected_instances` の分岐の**外**（`if expected_holdout_instances and effective_ceiling is ClaimCeiling.DIRECTIONAL:` のみでガード）へ引き上げ、被覆完全性に関わらず先に評価する——未達なら（被覆が完全でも）`NOT_EVALUABLE`/`OUTPUT_NOT_EVALUABLE`（`gate_detail_reason_code = "DIRECTIONAL_SWEEP_UNRESOLVABLE_ON_HOLDOUT"`、新規 `coverage_complete` フィールドで被覆完全性の事実も記録）、達していれば従来どおり部分被覆 → `DIAGNOSTIC_ONLY`/`OUTPUT_MISSING`、完全被覆 → 既存の汎用 `DIAGNOSTIC_ONLY` placeholder（D17）を通る。ABSOLUTE-ceiling 候補は `effective_ceiling is ClaimCeiling.DIRECTIONAL` ガードにより無変更（§10.3 は sweep 構造に依存しない）。テスト: 完全被覆 + 宣言 sweep の distinct truth level が 3 未満 → `NOT_EVALUABLE`/`OUTPUT_NOT_EVALUABLE`（新規）、完全被覆 + 全 sweep が >= 3 → 既存どおり gate 評価（`DIAGNOSTIC_ONLY` placeholder）へ到達、部分被覆時の既存挙動（D76 ruling (2)/(3) のテスト群）は無改変で通過、production E2E（`test_c4_f0_unusable_selected_candidate_through_real_holdout_path_closes_not_evaluable` 含む）も無改変で通過を確認 |
| `UNDERSPEC-CAL-D78` | `vocab.py`, `c0_validate.py`, `tests/test_vocab.py`, `tests/test_c0_validate.py` | #344 round 9 ADOPT 1 件 + audit で発見した同型 1 件（分類②: contract vocabulary contamination）。Codex round 9 finding: `vocab.py` ~51 の `BLOCKED_C0_SWEEP_DECLARATION_MISMATCH`（D77 ruling (1) が追加）が `vocab.BlockedCode` を設計正本 §3.3（~L140-143「fail-closed code の閉語彙」6 値・事後追加禁止）を超えて事後拡張していた。**AUDIT**（本 ruling の一部として実施）: 現行 `BlockedCode` の全 8 メンバーを設計正本の凍結 6 値（`BLOCKED_DOMAIN_MANIFEST_INCOMPLETE`/`BLOCKED_C0_MANIFEST_INCOMPLETE`/`BLOCKED_C0_UNSEEDED_RNG`/`BLOCKED_C1_GENERATOR_NONDETERMINISTIC`/`BLOCKED_LEAKAGE`/`BLOCKED_CANONICAL_MUTATION_REQUIRED`）と突合した結果、round 9 finding 対象の `BLOCKED_C0_SWEEP_DECLARATION_MISMATCH`（D77）に加え、`BLOCKED_C0_SWEEP_DECLARATION_INVALID`（D76 ruling (2)。D75 の `BLOCKED_C0_SWEEP_CAPACITY_INSUFFICIENT` を rename した同型の事後追加）も同じ契約汚染であると判明した（`git log -p -S "BLOCKED_" -- vocab.py` で確認: いずれも設計正本 §3.3 自体の改訂を経由せず enum へ直接追加されていた）。両者とも `voice_genesis/calibration/` 配下のコード（`c0_validate.py` の `_check_declared_sweep_truth_levels`/`_check_declared_sweep_declaration_match`）でのみ発行され、`approvals/`・`candidates/impl/`・fixture データのいずれにも実体として参照されていない（`grep -rl` で repo 全体を確認。凍結済み provenance artifact による既成事実化なし）ため、両方とも最近傍の凍結コード `BLOCKED_C0_MANIFEST_INCOMPLETE` へ同一方針でマッピングした——`declared_sweeps` は `frozen_design.fixture_spec.<FAMILY>` という manifest 完全性検証の対象であり、両検査とも実質的に「manifest の該当箇所が欠陥/不完全である」ことの検出であるため。ルーリング/実装: `vocab.BlockedCode` から `BLOCKED_C0_SWEEP_DECLARATION_INVALID`/`BLOCKED_C0_SWEEP_DECLARATION_MISMATCH` の両メンバーを削除し設計正本どおりの 6 値に復元。新設 `c0_validate.SweepManifestViolationDetail`（frozen dataclass: `violation`/`family`/`sweep_id`/`expected_count`/`actual_count`/`detail`）を `C0ValidationResult.sweep_declaration_violations`（`violation="sweep_truth_level_insufficient"`、D76 ruling (2) の構造検査）と `sweep_declaration_mismatch_violations`（`violation="sweep_declaration_mismatch"`、D77 ruling (1) の完全一致検査。sweep_id 単位で `expected_count`/`actual_count` を個別記録するよう粒度を family 単位から sweep_id 単位へ改良）の要素型とし、`validate_c0_manifest()` はいずれかが非空なら（`all_missing` 経由で既発行済みでなければ）`vocab.BlockedCode.BLOCKED_C0_MANIFEST_INCOMPLETE` を追加する——downstream consumer は凍結 6 値のみを見れば良く、診断情報は detail 構造から失われない。テスト: `test_vocab.py` の `test_blocked_code_closed_vocab` を 8→6 件に更新し、設計正本の凍結 6 値とメンバー集合が厳密一致することを検証する新設 `test_blocked_code_equals_frozen_design_list`（回帰ガード）を追加。`test_c0_validate.py` の sweep 系テスト群（starved-matrix / missing-key / scalar-value / dropped-member / extra-sweep-id / reordered-members / canonical-manifest の 7 件）を、削除された 2 コードへの参照を `BLOCKED_C0_MANIFEST_INCOMPLETE` + `SweepManifestViolationDetail` の構造化 detail フィールド検証へ全面的に置き換えて維持した |
| `UNDERSPEC-CAL-D79` | `campaign/measure_stage.py`, `campaign/render_stage.py`, `campaign/baseline_stage.py`, `campaign/holdout_stage.py`, `campaign/cli.py`, `campaign/caps.py`, `campaign/time_budget.py`（新設）, `tests/*` | design memo `design_runner_robustness.md`（2026-09-03 user-approved「推奨で続行」）: rehearsal（`freeze_execution_13.txt`, 2026-09-02）で判明した production 実行の 2 つの隘路 — (1) 約 4 分ごとの kill が meter_call group を途中で切り、次回起動が `StaleMeasurementError`（fail-closed by design）で詰まり手動 ledger 修復を要する、(2) instance 境界で stage を安全に止める手段が無い — への runner 堅牢化。**design decision**（本 UNDERSPEC の核）: (a) 部分 group の復旧は**明示的な operator action として ledger に記録する**（`--discard-partial-groups` + `meter_call_group_discarded` event。設計正本のいかなる箇所も「欠けている分だけ測定して埋める」自動再開を認めていないため、無言の fill-in ではなく operator の判断を ledger 上の 1 件の event として可視化する——default OFF、無指定時の挙動は不変）。(b) slice は**instance 境界**で終わる（wall-clock budget: `--time-budget-seconds` が新規 instance のdispatch を止める境界であり、instance の途中では絶対に止めない——render は 1 render 単位、measure は 1 `(row_id, probe_index)` の全 candidate 測定単位。budget 切れは `PARTIAL_SLICE` として exit 0 で正直に報告し、phase transition も `stage_summary` も記帳しない一方、parent CPU は通常の transition と同額だけ課金する——caps がスライスを跨いで正直であり続ける）。(c) ledger 走査は**stage 呼び出しあたり 1 回**（`MeterCallIndex`: 初回の 1 回だけ `campaign.ledger.entries` を読み、以降は `Ledger.append()` の戻り値を index へ直接反映する増分更新——`_completed_meter_call_records()` が instance ごとに ledger 全体を再走査する superlinear コストを解消）。R1 の reconstruction rule（discard event 以降の `meter_call` のみが完全性・scoring 対象。discard 前の記録は append-only で ledger には残り、`caps.cap_counters_from_ledger()` の dedup epoch もリセットされ両エポック分が課金される）を含め、詳細は実装マップ §6.5 参照。**boundary declaration**: 本 UNDERSPEC が対処するのは observed interruption（プロセス kill — 完全な行を残したまま中断する）のみ。VM 再起動時の page-cache 損失による ledger 末尾の tear（不完全な JSONL 行）は本設計の対象外——`provenance.Ledger.append()` の `LedgerTruncatedTailError`（既存の fail-closed 経路）がこれを検出した場合、それは新規の finding として扱う（rehearsal では一度も観測されていない）。**追補（Codex #345 レビュー採用、2026-09-03）**: 第 1 巡 ③×2 件（`cli._build_f0_by_instance()` の C3b/C4 呼び出しが `meter_call_index` を渡していなかったため上記 (c) の superlinear 解消が F0 再開 path では実際には効いていなかった欠落の修正——呼び出し元で `MeterCallIndex` を 1 回構築して素通しする；`PARTIAL_SLICE` の parent CPU が `counters.json` にのみ課金され ledger 側に記帳されず `cap_counters_from_ledger()` が undercount していた欠落への新規 `slice_summary` ledger event 追加）と第 2 巡 ③×1 件（`render_stage.run_render_stage()` の resume ループが完了済み unit も毎回 `render_instance()` 経由でフル ledger 再走査 + PCM 再ハッシュしていたため resume を繰り返すほど budget が完了済み prefix の処理だけで尽きていた欠落への per-invocation render index 追加）を採用。第 3 巡 ③×1 件（F5: 第 2 巡で追加した index skip が completing invocation でも一切検証を行わず、削除・破損した PCM の上に `fixture_valid`/c4-holdout render サブフェーズ遷移が falsely advance し得た欠落——`c1-fixtures` が一度 FIXTURE_VALID に達すると `NOOP_ALREADY_COMPLETE` になり render へ戻る道が失われる——への、遷移直前 1 回のみの skip unit 全数検証 + `stop_event`/`RenderResumeIndexIntegrityError` fail-closed の追加。回復は ledger 非破壊の決定論的再 render 書き戻しのみ——2 件目の `render` event 追記は `_recorded_render_sha`/`_render_index_from_ledger` の「最初の一致を採用」実装により既存 reader 全てから無視されるため不採用）と②×1 件（F6: `SliceStatus.instances_completed_this_run` が index skip 分まで含めて過大計上していた欠落を新規 render 分のみの計上へ修正）を採用。**リハーサル 4 追補（`freeze_execution_15.txt`, 2026-09-03）**: ③×1 件（D: `measure_stage.run_measure_stage()`——c2/c3a/c3b/c4 measure サブフェーズ共有——が完了済み instance を毎回フル `MeasurementRecord` へ再構成し続けていた欠落を、新設 `MeterCallIndex.is_complete()` による O(1) skip + completing invocation 時の 1 パス再構成へ修正)と②×1 件（G: `instances_remaining` が「このスライスが実際に歩いた instance 数」ベースの引き算で、budget が最初の instance 前に尽きると完了済み分を無視し過大報告していた欠落——render_stage/measure_stage 双方を index からの直接算出へ修正)を採用。**operator rule**（境界宣言、コード変更なし）: `--time-budget-seconds` は dispatch 開始境界のみを縛るため wall time は「残り budget + 最長 in-flight instance 所要時間」に達し得る——外部プロセス寿命制限の下で選ぶ値は `制限 - 最長 in-flight instance 所要時間` 以下にする（c3b は `--workers 3` で約 70s/instance、`--workers 1` で約 105s/instance)。completing invocation は phase transition と同時に `stage_summary`（自身の parent CPU 全量)を記帳し `slice_summary` は記帳しない（`slice_summary` は `PARTIAL_SLICE` exit 専用)。詳細は実装マップ §6.5/§6.5.1/§6.5.2 参照。**第 4 巡追補（Codex #345 レビュー採用、2026-09-03）**: ②×1 件（`render_stage.run_render_stage()` の c4-holdout render サブフェーズで、`instances_remaining` が全 ledger 走査ベースの `completed_units`（当該 stage 外の C1 render event も含む）をそのまま使っていたため、当該 stage の `units` に属さない完了数まで差し引かれ過小算出・負値になり得た欠落——`completed_units` を当該 stage の `units` との積集合に限定するよう修正）と③×1 件（render_stage/measure_stage 双方の instance 境界 budget 検査が、完了済み unit/instance の index 参照より**先に**実行されていたため、直前呼び出しが最終 unit/instance の完了後・phase transition 前に中断され、かつ index 再構築自体が budget を使い切ると、真に pending な dispatch が無いにもかかわらず `PARTIAL_SLICE` を報告し続け同一の小さい budget での再開が完了しない欠落——budget 検査を index 参照の**後**（genuinely pending な dispatch のみを対象）へ順序修正)を採用。**#345 第 5 巡 4 件採用（③×3、②×1）; slice/resume ファミリー（budget 前の再開状態参照 / 新規分のみの進捗計上 / 完了時整合検証の 1 回化 / 検証項目の loader 同値）を全 stage で掃討し終端宣言**。詳細は実装マップ §6.5.3 参照。#345 第 6 巡: ③ 1 件採用（discard 時の within CPU 計上）、1 件境界宣言 (D80)。#345 第 7 巡: ③ 2 件採用（discard 時の within CPU 課金を、当該 partial group を書いた invocation の epoch に `stage_summary`/`slice_summary` が存在しない場合のみに限定——round 6 の「hard-kill のみが partial group を残す」という前提は誤りで、`KeyboardInterrupt` 等 catchable な中断も `cli.py main()` の `finally` を通常どおり通り自身の epoch を summary で閉じるため、そこに discard 課金を無条件加算すると二重計上になる；`caps.cap_counters_from_ledger()` に ledger 走査順のみから導出する `dispatch_epoch` を追加、新規 payload field なし。`measure_stage.MeterCallIndex.is_complete()` が重複 repeat key を `False`（pending 扱い）ではなく `StaleMeasurementError(kind="duplicate")` で即座に fail-closed するよう修正——`run_measure_stage()` の instance 境界 budget 検査がこの index 参照より後にあるため、短い budget と重複 key が重なると `completed_records()` に到達する前に無害な `PARTIAL_SLICE` として抜けてしまい重複が無期限に隠蔽されていた欠落。#345 第 8 巡: ③ 3 件採用・割込み invocation 跨ぎ cap 会計ファミリー終端（invocation_id による明示的 invocation 識別への置換——round 7 の `dispatch_epoch` ledger 順序ヒューリスティックは、SIGKILL された writer と discard フラグ無しで再試行した別 invocation の `stage_summary` を取り違え得たため、`cli.py` main() が process ごとに生成する `invocation_id` を cap 会計対象の全 event へ付与し、discard 時の pairing を「discard される group の WRITER 自身の invocation_id と同じ invocation_id を持つ summary が存在するか」の同一性判定へ改めた；discard 時に live cap_counters へ即座に課金し remeasure 前に cap を再検査するよう修正；`--discard-partial-groups` 指定時、discard（非 dispatch 操作）を budget 境界検査より先に処理するよう修正。以降は新規の具体的な偽成功/偽失敗経路を示す指摘のみ採用）。#345 第 9 巡 ③ 1 件採用: 完了済み family の再構成を完了 invocation まで遅延 + リハーサル 5 ②（遷移 event にも invocation_id 付与）。#345 第 10 巡 ② 1 件採用（family 事前判定の重複検出にも stop_event 記帳）。レビュー対応は第 10 巡で上限到達、以降は③のみ。#345 第 11 巡: 1 件境界宣言 (D81)。#345 第 12 巡 ③ 1 件採用（未 summary invocation の完了グループ within CPU を直接計上）。#345 第 13 巡 ③ 1 件採用（第 12 巡の deferred pass が PARTIAL group まで無条件に計上していたため、`--discard-partial-groups` 復旧が reconcile 時の deferred 計上と discard event の計上を二重に加算し得た欠落——deferred pass を COMPLETE group（期待される全 repeat key が揃っている group）限定へ改め、PARTIAL group は discard event 経由のみで計上する不変条件を明記）。**#345 指摘: summary event を counters cache 保存より先に記帳（hard-kill 窓の cap 過小計上を排除）** |
| `UNDERSPEC-CAL-D80` | (docs only — no code change) | #345 第 6 巡境界宣言: `render_stage.py` ~913 「C4 render を最終 holdout 遷移時に再検証する」は不採用。測定後に消失/破損した render 成果物の最終遷移時再検証は行わない。測定値は測定時に入力検証済み（`_verify_and_load_rendered_pcm`）・ledger の sha が正本。成果物ストア（PCM ファイル）の完全性は campaign 正当性契約の外——測定後の消失はどの測定結果も改変しない。再入条件 = 測定前の破損経路が新たに示された場合のみ |
| `UNDERSPEC-CAL-D81` | (docs only — no code change) | #345 第 11 巡境界宣言: `campaign/measure_stage.py` ~1836「Surface partial groups before enforcing the slice budget」は不採用。フラグなし実行で index 構築が budget を使い切った場合、非重複の部分グループは pending 扱いとなり PARTIAL_SLICE で終了する（fail-closed は次の通常 budget の invocation で必ず表面化。遷移は起きない）。マスクされるのは budget < index 構築時間という運用外の設定のみ。slice/resume ファミリーは第 5 巡・cap 会計ファミリーは第 8 巡で終端宣言済み。再入条件 = 通常 budget で stale 状態が遷移まで隠蔽される具体経路が示された場合のみ |
| `UNDERSPEC-CAL-D82` | (docs only — no code change) | #345 第 14 巡境界宣言: `campaign/cli.py` ~672 「Log duplicate failures from the F0 resume precheck」。`_build_f0_by_instance()` の index 参照で重複 repeat key が `StaleMeasurementError` を送出する経路は fail-closed 自体は正しく動作（exit 非 0・遷移なし）し、欠けるのは ledger 上の説明用 `stop_event` のみ。C0 freeze 後の校正コード変更は正本変更 = 新 campaign を要するため本 campaign では修正せず、次 campaign 前の修正候補として記録。**→ 2026-09-04 適用済み**（v1.1 §V5、commit `8cd4c5c`: `_record_f0_resume_precheck_duplicate_stop_event()` を append→raise で配線。本行の当時の境界宣言は歴史的記録として保存） |
| `UNDERSPEC-CAL-D83` | (docs only — `GATE1_DECISION_RECORD.md` §10.8 正誤表 + E_use restamp) | #345 第 14 巡指摘②採用（campaign close 後に適用）: §10.7 の「偽成功経路」は誤りで正は「偽失敗経路（偽 `COST_CAP_EXCEEDED`）」。記録は E_use table で digest 固定され campaign 凍結入力だったため close（ledger seq 70627）後の errata として §10.8 に追記し restamp |
| `UNDERSPEC-CAL-D84` | `provenance.py` | 実測事実: campaign `RUN10-CAL-20260903-9bcbbf86` の c2-baseline（33 slices、315/1140 instances 完了、エラーなし）が 2026-09-03 に途中中断。原因は `provenance.Ledger.append()` が既存台帳の全 chain を毎 append `_verify_chain_prefix()` で再検証し（`_entry_sha` の canonical_json+sha256 を既存全行に対して再計算）、さらに `_read_all()` で全行を再パースしていたこと（append 1 回あたり O(n)、n=既存エントリ数。c2 は group あたり 6 meter_call append のため O(n²)/campaign）。ledger copy（11,220 行）での実測: append 1 回 912ms（うち `_verify_chain_prefix` 636ms + `_read_all()` 153ms）、group（6 append）5.47s。本番 `slice_summary.parent_cpu_seconds` を予測式 `6·b·n`（b≈0.0813ms/既存行）で再現し誤差数% で一致（inv1: predicted 1.03s/obs 0.88s、inv31: predicted 5.32s/obs 5.16s）。残り作業量（c2 残 825 instances + c3a/c3b/c4）を前提にした投影は append 単体で ≈112h CPU（≈2,700 slices）——実測定作業（≈4.1h）の ≈27 倍——完走不能と判定し、この campaign は本欠陥により c2 途中で中断・破棄した（新 campaign で再 freeze する）。fix: `Ledger` に per-instance の検証済み watermark（バイト位置・`seq`・末尾 `entry_sha`・末尾改行有無。`__init__`/`load_with_verification` が構築時の full-chain 検証から確立し、`chain.ok=False` の場合は「未検証」sentinel を設定して次回 append を旧来のファイル全体検証へ自動フォールバックさせる——fail-closed は弱めない）を追加し、`append()` は watermark **以降**の suffix のみを `_verify_chain_prefix`（`start_seq`/`start_prev_sha` を新設し任意の chain 位置から検証できるよう拡張）で検証してから `self._entries` を増分更新する方式へ変更した（append 1 回あたり O(1)。実装後の実測（本番 ledger のスクラッチパッドコピー、n=11,257/2,000 行）: OLD 相当（全 chain 再検証+全行再パース、write 抜き）は 733ms/append（n=11,257）・105ms/append（n=2,000）。NEW（本実装）は 1.84ms/append（n=11,257、398 倍）・0.61ms/append（n=2,000、171 倍）——existing entry 数に依らず定数（回帰テスト `test_ledger_append_cost_independent_of_ledger_size` が `_verify_chain_prefix` に渡る行数が n=5/300 いずれでも 0 であることを固定）。台帳フォーマット・`verify_chain()`・構築時 full-chain 検証は無変更（`Ledger.load_with_verification()` を使う `campaign/state.py::load_frozen_campaign` の毎 invocation full 検証は従来どおり）。詳細: `docs/`（本 README 経由で辿れる範囲）に加え、投入前の実測ログはコミット履歴（本 UNDERSPEC 採用コミット）参照。**#345 指摘: watermark 未満へのロールバックは fail-closed（在メモリ状態を再同期しない）**。**#345 指摘: 末尾空行のある有効 ledger で append が偽失敗する欠陥を修正（watermark を最終非空行に固定）**。**#345 指摘: watermark 以前の同一長改ざんに対し、stat 変化検出時の全検証（G1）と遷移 event 追記前の全検証（G2）で fail-closed。残余露出 = stat 不変の同一長改ざん後の非遷移 append（次の遷移/起動で検出）** |
| `UNDERSPEC-CAL-D85` | `campaign/unseal.py` | #345 指摘②採用: Gate 3 は freeze 後承認であることを unseal 時に検証（`approved_at_utc` > `c0_freeze` event の `event_time_utc`）。IMPLEMENTATION_MAP_v1.md §6.4 は Gate 3（seal 保護水準の受容）を「C0 freeze **後**に成立する」承認として定義するが、`campaign.unseal.unseal_campaign()` はこれまで `seal_protection_level_accepted`/hash/approver の存在のみを検証し、承認ファイル自体が freeze より前に発行されていても `gate3_accepted`/`holdout_unseal` を記帳できていた。実測: `RUN10-CAL-20260903-9bcbbf86`（D84 により c2 途中で中断・破棄）の Gate 3 承認記録（`approved_at_utc=2026-09-03T03:31:26Z`）は Gate 2 承認（`15:57:46Z`）・武装 C0 freeze のいずれよりも前に発行されていた。この記録自体は中断済み campaign に属する破棄対象であり、再 freeze 後に新規発行される（procedural 対応、本項目の範囲外）。fix: `unseal_campaign()` に `gate3.approved_at_utc` と campaign の `c0_freeze` ledger event の `event_time_utc`（`FrozenCampaign.freeze_event["event_time_utc"]`、`c0_freeze.armed_freeze()` が刻む値）の順序検証を追加し、Gate 3 が freeze 以前（早い/同時刻）またはいずれかのタイムスタンプがパース不能なら fail-closed で拒否する（新規 `BlockedCode` は追加せず、既存の "Gate 3 not approved" 拒否と同じ pre-dispatch 拒否スタイル・missing-factor 表記 `approval_file:gate3_predates_freeze` を踏襲。ledger には一切書き込まない）。`approvals.py`/`provenance.py` は本 fix の編集対象外のため、両モジュールが既に採用している「ISO 8601 UTC パーサを独立実装として重複させる」方針を `unseal.py` 内の `_parse_iso8601_utc()` でも踏襲した。テスト: gate3 が freeze より前 → 拒否・`gate3_accepted`/`holdout_unseal` とも未記帳、gate3 と freeze が同時刻 → 拒否、gate3 が freeze より後 → 成功、freeze event に `event_time_utc` が無い（legacy/malformed ledger を模す）→ 拒否（`tests/test_campaign_unseal.py` 新規 4 件）。既存の unseal/holdout/close/cli 系テストは共有 fixture ヘルパー（`tests/_campaign_fixture.py`）の既定値更新（`build_tiny_campaign()` に `freeze_event_time_utc` 引数追加・既定 `2026-09-02T00:00:00+00:00`、`write_gate3_approval()` に `approved_at_utc` 引数追加・既定を freeze 既定より後の `2026-09-02T01:00:00Z` へ変更）で freeze 後発行のまま素通りする。**#345 指摘: 未来日付の Gate 3 も拒否（freeze 時刻 < approved_at_utc ≤ 現在時刻）** |
| `UNDERSPEC-CAL-D86` | `campaign/cli.py`, `campaign/holdout_stage.py`, `tests/test_campaign_cli.py` | 実測事実: campaign `RUN10-CAL-20260903-591cadcd` の c4-holdout 開始時（unseal 直後、初回 invocation）が `FileNotFoundError`（`pcm not rendered for row_id=... probe_index=...`）で決定論的に停止した。原因: `cli._run_c4` が選抜済み F0 candidate の instance 単位 F0 前処理（`_build_f0_by_instance`、C4 HOLDOUT instance の**音声そのもの**を測定する）を、`holdout_stage.render_and_measure_holdout()`（`render_stage.run_render_stage(stage="c4")` の唯一の呼び出し元）を呼ぶ**前**に実行していた。本番の C1 は CALIBRATION/SELECTION 行のみを render し（HOLDOUT 行は C4 が render する設計どおり）、この F0 前処理を隠していたのは `tests/test_campaign_cli.py` の tiny-campaign fixture 側だけだった（`test_c4_f0_reuse_builds_and_reuses_single_meter_call_index` 等が `_run_c4` を呼ぶ前に `render_stage.run_render_stage(..., stage="c4")` を直接呼んで HOLDOUT PCM を先回りして render しており、この呼び順バグを一度も踏まなかった）。fix: `holdout_stage.render_and_measure_holdout()` に新設の `f0_prepass`（zero-arg callable、既定 `None`）引数を追加し、渡されれば自身の render サブフェーズ完了後・family ごとの measure サブフェーズ開始前に一度だけ呼ぶよう構造化（`f0_prepass` 未指定時は従来どおり静的な `f0_by_instance`/`f0_unusable_instances`/`f0_missing_reason` を使う——後方互換）。`cli._run_c4` は `_build_f0_by_instance()` の呼び出しをこの `f0_prepass` closure へ包んで渡すよう変更し、F0 選抜が失敗している（`f0_selected_id is None`、音声を一切読まない）分岐は従来どおり即時実行のまま維持した。`holdout_stage.render_and_measure_holdout()` 自身は監査の結果、元から render→measure の順序を正しく守っていた（共有 `TimeBudget` の expiry が測定サブフェーズの新規 dispatch を安全に無効化する設計のため、render が部分完了で打ち切られても後続の measure/F0 前処理が未 render PCM に触れることはない）ため、この 1 箇所のみの是正で足りる。テスト: 既存の tiny-campaign fixture ベースの c4 単体テストは `render_and_measure_holdout` を丸ごと monkeypatch しているため無改修で通過（`f0_prepass` closure は fixture の import 経路上呼ばれない）。F0-reuse を検証する 1 件（`test_c4_f0_reuse_builds_and_reuses_single_meter_call_index`）のみ、stub 内で `kwargs["f0_prepass"]()` を呼ぶよう更新（実 `_build_f0_by_instance` 呼び出しを維持するため）。新規 in-process E2E（`test_c4_holdout_render_precedes_f0_prepass_end_to_end`, `@pytest.mark.slow`）: 実 `c1-fixtures→c2-baseline→c3a-f0-selection→c3b-selection→unseal→c4-holdout（`--time-budget-seconds` で複数 invocation にスライス）→close` を、HOLDOUT 行が c4 以前に一切 render されていない・real F0 selection が成立した tiny matrix（APERIODICITY_GT 11 行、既定 split secret で HOLDOUT=3 行）上で駆動し、`FileNotFoundError` を起こさず・render_holdout_valid が厳密に 1 回・HOLDOUT PCM が実在・holdout_executed_valid が記帳されることを確認（実測: 全段合計で約 6 分、real audio synthesis 込みのため slow）。campaign `591cadcd` は c4 開始時に中断・破棄し、本 fix 後の新 campaign で再 freeze する |
| `UNDERSPEC-CAL-D87` | (docs only — no code change) | #345 freeze 後（campaign 実行中）Codex 指摘 5 件の境界宣言（cap 会計ファミリー、第 8 巡終端宣言済み）: `cli.py` ~2698「Include slice-summary verification in compute accounting」/ `cli.py` ~2575「Charge pre-dispatch work in every slice」/ `render_stage.py` ~927「Recheck the compute cap before C1/C2 transitions」/ `cli.py` ~917「Persist checkpoint CPU before exposing the transition window」/ `provenance.py` ~524「Recheck the compute cap before completing unseal」。いずれも stage 遷移・summary 記帳の前後で親プロセスの検証/事前処理 CPU が cap 判定に算入されない窓を指す同型指摘。campaign `RUN10-CAL-20260904-862dec28` の実使用 compute 33,798 s は cap 172,800 s の 19.6% で、未算入 CPU（ledger 全検証は秒オーダー）が cap 判定を覆す経路は存在しない。freeze 後の校正コード変更は新 campaign を要するため本 PR では修正せず、次 campaign 前の修正候補（cap 判定前に checkpoint を永続化する順序の統一）として記録。再入条件 = 未算入 CPU が cap 残余を超えうる具体的計測が示された場合のみ。**→ 2026-09-04 適用済み**（v1.1 §V5、commit `8cd4c5c`: pre-dispatch CPU 課金 + c1/c2/unseal の遷移前 checkpoint callback + summary 直前 delta 計上、`d5f4152`: checkpoint の `parent_cpu_checkpoint` ledger event 永続化と invocation 単位の summary 優先集計。本行の当時の境界宣言は歴史的記録として保存） |
| `UNDERSPEC-CAL-D88` | (docs only — no code change) | #345 freeze 後 Codex 指摘 2 件の境界宣言（検証器 parity / 縮退入力）: `unseal.py` ~168「Enforce Gate 3 ordering in the ledger verifier」— D85 の順序検査は `unseal_campaign()` 実行時に fail-closed で強制済みで、実行経路では freeze 前 Gate 3 が記帳され得ない。replay 検証器が同検査を再実行しないのは防御の重複不足であり偽成功経路ではない。`provenance.py` ~1225「Allow appending to a blank-only verified ledger」— 空行のみの ledger は `c0_freeze` event から始まる実 campaign では到達不能で、発生しても偽失敗（append 拒否）方向。いずれも次 campaign 前の修正候補として記録。**→ 2026-09-04 適用済み**（v1.1 §V5、commit `8cd4c5c`: replay 検証器へ freeze<gate3≤unseal+60s の順序 parity（新 writer の `event_time_utc` 前提）+ genesis watermark の空 ledger append 修正、`d5f4152`: `event_time_utc` を持たない legacy `holdout_unseal`（閉 campaign `862dec28` 含む）への fallback 救済を実測確認込みで追加。本行の当時の境界宣言は歴史的記録として保存） |
| `UNDERSPEC-CAL-D89` | (docs only — no code change) | #345 freeze 後 Codex 指摘の実測付き境界宣言: `cli.py` ~1187「Skip completed F0 reconstruction on nonterminal slices」— 完了済み F0 group の再構成が budget を消費し `PARTIAL_SLICE` が収束しない懸念は、campaign `RUN10-CAL-20260904-862dec28` の c3b（39 invocations、残数 425→0 で単調減少、throughput 5→60 instances/slice に上昇）と c4（12 invocations、805→0）で不発を実測。再構成は第 3 巡で index 化済みで残作業に比例しない。再入条件 = 通常 budget で残数が減らない具体経路が示された場合のみ。 |
| `UNDERSPEC-CAL-D90` | (docs only — no code change) | #345 freeze 後 Codex 指摘 ② 将来汚染・次 campaign 前修正候補: `cli.py` ~759「Deduplicate F0 rejection events across slice resumes」— resume した c3b/c4 invocation が prefix 全体の `f0_injection_rejected` を再記帳し、破棄 campaign `RUN10-CAL-20260903-591cadcd` の ledger に 89 件（同一集合の反復 71 件）の重複 event が残る。閉じた campaign `RUN10-CAL-20260904-862dec28` では当該 event 0 件で記録は清浄。event 数を統計に使う下流を汚染しうるため ② に分類するが、freeze 後の校正コード変更に当たるため本 PR では修正せず、次 campaign 前の修正（記帳前に既存 event を index 照合）として登録。**→ 2026-09-04 適用済み**（v1.1 §V5、commit `8cd4c5c`: 既記帳 instance との差集合のみ記帳・空差集合は無記帳。本行の当時の境界宣言は歴史的記録として保存） |
| `UNDERSPEC-CAL-D91` | `campaign/cli.py`, `campaign/selection_stage.py`, `tests/test_campaign_cli.py`, `tests/test_campaign_selection.py` | v1.1 §V1 実装（commit `1527a41`）: F0_CONTROL C3a の negative control fail filter を control class で分割。**観察**（設計 v1.1 §V1.1 の一次事実）: C3a の negative control 評価は split 非依存で全 3 行×5 probe を評価するが、NOISE_ONLY への any-fire ゼロ許容フィルタは噪音実現（campaign ごとに異なる HKDF seed）の確率事象に左右され、選定結果が campaign 間で反転していた（`591cadcd`: F0-PYIN-FRAME2048-HOP256 のみ `negative_control_false_fire=False` で SELECTED / `862dec28`: 全5候補 True で `SELECTION_FAILED_CLOSED`）。v1.0 §8 は「voiced false detection」を順位基準に宣言しながら共通 fail filter が any-fire で全滅させ、宣言済み順位基準を構造的に無意味化していた内部矛盾でもある。**ruling**: `selection_stage.candidate_fail_filter_report()` に kwonly `noise_only_control_row_ids`（既定空 = C3b は無分岐で不変）を追加し、any-fire フィルタの母集団から NOISE_ONLY を除外する一方、completeness（`negative_controls_incomplete`）判定の母集団は和集合のまま維持（record 欠落は any-fire 免除と無関係に fail-closed で捕捉し続ける）、`(row_id, probe_index)` instance 単位の `noise_only_instances_total`/`_detected`/`noise_only_false_detection_rate` を新規 audit キーとして返す（`FAIL_FILTER_NAMES` 8 種には非算入=`eligible_after_fail_filters()` の判定に無影響）。**配線**: `cli._run_c3a()` が `negative_controls_by_class(f0_rows)`（既存関数、無改変）で SILENCE/TOO_SHORT（決定論的縮退 control = zero-tolerance）と NOISE_ONLY を分離し、`_criteria_with_fail_filters()` の新規 kwonly `noise_only_negative_control_ids` 経由で report を取得、rate が non-None のときのみ既存の未配線 placeholder スロット `CandidateCriteria.nuisance_sensitivity_max`（新規フィールド追加なし、`selection.py` は無改変）を `dataclasses.replace` で上書きし、v1.0 §8 の lexicographic 順位「cents error → octave-error rate → voiced false detection rate → process reproducibility」の該当位置で消費させる。C3b（`_compute_family_criteria`）は `negative_control_row_ids()` 単一集合呼び出しのまま無改変。`fixtures/controls.py`/`campaign/workunits.py` は既存関数の再利用のみで無改変。**テスト**: `test_campaign_selection.py` 6件（zero-tolerance class は依然 reject / noise-only false-fire は非 reject・rate のみ記録 / record 欠落は依然 incomplete / 未宣言時は no-op / rate による lexicographic tie-break / `f0_selection_frozen` payload への内訳記録）+ `test_campaign_cli.py` 2件（`_run_c3a` 統合、`measure_stage.run_measure_stage` monkeypatch で NOISE_ONLY 発火を固定 2/5 に再現）。対象4ファイル+関連135件全 green。設計 v1.1 §V1 として承認済みの ruling の直接実装（起草段階 Codex 第6巡 P1 が §V1.3 の正直性宣言の前提となる観察を採用）。 |
| `UNDERSPEC-CAL-D92` | `fixtures/matrix.py`, `splitter.py`, `c0_freeze.py`, `c0_validate.py`, `campaign/cli.py`, `campaign/state.py`, 対応テスト群 | v1.1 §V2 holdout sweep pinning（初版 commit `d672e16` + 縮退規則 `7e192f2` + 検証強化 `5028cc6`(R10)/`2af2bde`(R11)）: **段1 pin**（`fixtures.matrix.pin_holdout_sweeps_by_family()`）が family ごとに declared sweep から `k_hold` 個を HOLDOUT 専属として層内選抜。sweep stratum key は coverage 軸（`generator_impl`）+ claim 構成次元（IDENTITY の `founder_id`/`trait`）。単一 field family（FORMANT_GT）は field 値ごとの largest-remainder（全値≥1 先取り→残余配分、同点は字句順）+ stratum 内 HMAC 昇順、複数 field family（IDENTITY）は founder×trait の決定論的ラウンドロビン。claim-relevant field は凍結許可語彙 `_CLAIM_DIVIDING_FIELD_NAMES`（join_type/duration_class/bandwise_band/founder_id/trait）との積集合として機械導出（`claim_relevant_fields_by_family()`）。456セル実測 k_hold は F0=1/FORMANT=3/TILT=2（→§V3.5 縮退で1）/APERIODICITY=2/RESONANCE=2/TRANSITION=2/IDENTITY=4 で設計文書の予告値と完全一致。**段2**（`splitter.realize_split()` に kwonly `pinned_holdout_row_ids`/`hold_forbidden_row_ids` を追加、既定空=無 pin 呼び出しは無分岐で完全後方互換）が残余行を従来の層別 largest-remainder+coverage+swap で割当（pin 行は不動）。**縮退規則**（`7e192f2`、CI flaky 根治）: `cap=floor((N_hold-1)/r)<1` は無条件 pin 免除（k_hold=0）、段2 `CoverageRepairInfeasible` 発生時は `degradation_floor`（claim 被覆 family=max_field_cardinality、他=0）まで k_hold を1ずつ決定論的に縮退して段1から再選抜・段2再試行、floor 到達でも不能なら新設例外 `HoldoutPinDegradationExhausted` で fail-closed。根治した flaky の実体は独立な2欠陥の組合せ: (i) 段2 `CoverageRepairInfeasible` の未捕捉、(ii) `c0_validate.py` のテストが `build_matrix` 差し替えで frozen-declaration 系検査（`_canonical_build_matrix` に固定すべき）まで巻き込んで束縛混同——`c0_freeze.py` 既存の `build_matrix`/`_canonical_build_matrix` 分離規約を `c0_validate.py` へも導入して解消。10連続実行×2回でクリーン状態下0失敗を確認。**manifest 配置**（設計文書からの意図的逸脱、報告義務）: 文書 §V2.2 は `frozen_design.fixture_spec.<FAMILY>.holdout_sweeps`（core 節）を明記するが、pin 選抜は armed freeze 後にのみ生成される `split_secret` に依存するため「Gate2 承認対象の core に、Gate2 承認後にしか存在しない値を含める」ハッシュ循環で構造的に実装不能——**実装時訂正**としてトップレベル非-core キー `holdout_sweeps`（`realized_split` と同格、`manifest_core_sha` 対象外・`manifest_sha` 対象）へ変更し、整合性を (i) `armed_freeze()`/staging 読み戻しの2箇所での secret 依存完全再導出照合、(ii) secret 非依存の構造検査（宣言 sweep_id が `declared_sweeps` に実在・member 一致・宣言数=k_hold）を `validate_c0_manifest()` が常時実行、(iii) pin member が全て `realized_split.assignment` 上で HOLDOUT 所属（§V2.3）、の3層で担保。**検証側の公称kからの完全再導出**（R10 `5028cc6`）: `_check_holdout_sweeps_declaration_match()` の secret 依存経路は、宣言された縮退カウントを override として信頼する旧ロジック（FORMANT_GT で正当な3-sweep pinから1件落とした2件宣言の脆弱性を実演確認——旧ロジックなら pass していた）を廃し、公称 k_hold から `splitter.pin_and_realize_holdout()`（`c0_freeze.py` から `splitter.py` へ移設・公開化、`STRATUM_FACTOR_NAMES`/`row_inputs_for_split()` も同様）を検証側で完全再実行し、`HoldoutPinInfeasible`/`HoldoutPinDegradationExhausted` を送出すれば宣言のある全 family を無条件 fail-closed mismatch とする。secret 非依存経路は従来どおり nominal k_hold との単純一致のみ。**空/欠落宣言の fail-closed 化**（R11 `2af2bde`）: 非 pin 免除 family（k_hold≥1）の宣言欠落/空は `holdout_pin_declaration_mismatch` で拒否、pin 免除 family の非空宣言（免除偽装）も同様に検出。secret 非依存（dry-run）経路・v1.0 形式 manifest は従来どおり「宣言があれば照合」の緩い規約を維持（既存テスト無改変）。**`campaign/state.py` の副次修正**（wp2a 実装時発見、致命的回帰の未然防止）: `_realized_split_from_manifest()` が `pinned_holdout_row_ids` を読み戻していなかったため、v1.1 pin を使う本番 campaign で `RealizedSplitMap` を再構築するたび（`provenance.Ledger.check_leakage()` の毎回の leak 検査経路）正しい v1.1 realized split を `splitter.verify_split()` が pin なしアルゴリズムで再実行して「改竄」と誤検出し `BLOCKED_LEAKAGE` になる致命的回帰を、1行の読み戻し追加で修正（`provenance.py`/`holdout_stage.py`/`selection_stage.py`/`approvals.py` は無改変）。テスト: `test_holdout_sweep_pinning.py`/`test_c0_validate_holdout_pin.py` 新規計140件超、フルスイート `-m "not slow"` 1081 passed（作業ツリー clean 時）。Codex 第2/3/5巡（設計起草段階、実行不能の指摘）+ 第10/11巡（実装段階、③致命的バグ・虚偽縮退宣言のすり抜け）採用。 |
| `UNDERSPEC-CAL-D93` | `campaign/cli.py`, `campaign/holdout_stage.py`, `c0_freeze.py`, `c0_validate.py`, `e_use_table.py`, `fixtures/matrix.py`, `splitter.py`, 対応テスト群 | v1.1 §V3 c4 実gate配線（`c8dbd5b`）+ §V3.3 U_GT/U_num凍結（`744a3fb`）+ §V3.5/§V3.6（`59ad7e1`）: **§V3 D17閉塞** — `cli._run_c4` の D17 placeholder（被覆完全時に全 meter を無条件 `DIAGNOSTIC_ONLY` で閉じ、実 gate（`holdout_stage.evaluate_absolute_meter`/`evaluate_directional_meter`）が campaign 実行経路から一度も呼ばれず `DEBT_DISCHARGED` が恒久 false になる構造的欠陥）を、`effective_ceiling` 分岐（ABSOLUTE→`evaluate_absolute_meter_from_campaign()`、DIRECTIONAL→`evaluate_directional_meter_from_campaign()`、それ以外→正直な理由付き `DIAGNOSTIC_ONLY`）へ置換。欠落の終端写像は v1.0 §11 の一意写像に完全に従う（入力側 critical 欠落→`NOT_EVALUABLE/INPUT_MISSING`、output 全欠損/最小数割れ→`NOT_EVALUABLE/OUTPUT_NOT_EVALUABLE`、PRIMARY 一部 missing→`DIAGNOSTIC_ONLY/OUTPUT_MISSING`）。gate 入力の出所: E_use=`load_e_use_rows()`→`absolute_e_use_value()`、invariance 軸 pair=`build_invariance_pairs_for_family()`、gate5 control=`control_detection_for_family()`、DIRECTIONAL sweep 単位=manifest `holdout_sweeps` 優先（§V2.3 の `expected_sweep_ids` 入口を再利用）。M6 は precondition 判定のみ実評価し常に `NOT_EVALUABLE/INPUT_MISSING`（cross-family 測定・A/B pair 実験設計は v1.0/v1.1 で未規定のため v1.2 へ境界宣言で繰延）。**§V3.3 U_GT/U_num凍結** — 現行 `c0_freeze` が v1.0 §10.2 の U_GT/U_num を一切凍結していなかった実装欠落（`862dec28` の manifest 実測で発見。凍結しない限り全 ABSOLUTE gate が入力欠落で終端し debt が構造的に返済不能）を埋め、`frozen_design.fixture_spec.<FAMILY>.u_gt_bound`/`.u_num_bound`+導出式文字列を core 節へ追加。U_num=`tolerance.derive_floor()`（PCM 量子化項を worst-case gain=-36dBFS で相対化、伝達係数 unity-gain 簡略化=`[UNDERSPEC-CAL-V01]` で保守側に倒す/float eps 項/宣言分解能は C0 時点 0 固定=`[UNDERSPEC-CAL-V02]`）、U_GT=F0_CONTROL/FORMANT_GT/TILT_GT=0（float64 解析合成）・APERIODICITY_GT=`fraction*3*sqrt(2/N)`・TRANSITION_GT=0（`discontinuity_magnitude`、`join_time_s` は単位不一致のため informational note 止まり）・RESONANCE_GT/IDENTITY_CAUSAL_SWEEP=ABSENT。456セル実値を regression lock で固定。**§V3.5 invariance軸の機械導出** — `_CONFOUND_AXES` の旧・全 family 一律6軸宣言（f0_hz/sr_hz 含む）が正典456セルの CONFOUND 行実体（f0_hz/sr_hz は単一軸行を持たず targeted interaction 限定）と矛盾し、全 ABSOLUTE 候補が「軸あたり5pair 未満」で構造的に偽失敗していたことを実 gate 配線後の監査で発見。`invariance_axes_by_family()`（`fixtures/matrix.py` 新設、truth 軸除外・CONFOUND 単一軸行から機械導出）へ置換、`c0_validate._check_invariance_axes_match()` で宣言=実体を強制。pair は instance 単位（1 holdout 行=5pair）。**coverage 制約**: `splitter._COVERAGE_AXES` へ `nuisance_axis` 追加も、RESONANCE_GT/IDENTITY_CAUSAL_SWEEP は gate4'（ABSOLUTE ceiling）に到達せず保護効果ゼロで、課すと456セル campaign が secret に依らず恒久的に `HoldoutPinDegradationExhausted` に陥ることを実測、`splitter._GATE4_INAPPLICABLE_FAMILIES` で2 family を coverage 対象からのみ除外（manifest の宣言軸自体は不変）。この対処後 TILT_GT が nominal k_hold=2→1 へ全 secret で決定論的に縮退（degradation_floor=0 内、既存被覆保証は不変。**2026-09-05 実装時確定として追補**: §V2.2 表の TILT=2 はこれで上書き）。**anchor 共有測定**: pair の anchor 側は split 非依存の共有 control として扱い、`build_invariance_pairs_for_family()` の varied（CONFOUND）行のみ HOLDOUT を要求（旧実装は両側 HOLDOUT 要求で anchor が他 split に home すると pair が黙って全滅していた）。**§V3.6 control欠落の分子算入** — `control_detection_for_family()` の `_all_detected()` を非対称2関数へ分割: positive=`_positive_detected()`（ALL repeat detected のみ成功、無改変）、negative=`_negative_fired()`（新設。ANY repeat 発火または record 0件も発火=失敗——旧 `all()` 実装は一部発火を他 repeat の missing で握りつぶす/record 0件を非発火=成功に誤写像する2バグを持っていた）。**M6 precondition→terminal_status判定**: `critical_ceilings`（gate 評価前の ceiling 宣言）ではなく `critical_terminal_statuses==CALIBRATED_ABSOLUTE`（gate 評価の実結果）で判定するよう是正——旧実装は ABSOLUTE 候補が holdout gate で `DIAGNOSTIC_ONLY` に終端しても `ceiling` フィールドが残るため precondition 充足を偽装していた。**gate fail理由コード訂正**: cascade5（else→`DIAGNOSTIC_ONLY`）の全経路を一律 `OUTPUT_MISSING` としていたのを、ABSOLUTE=gate1 不通過時のみ `OUTPUT_MISSING`（他 gate の正直な performance fail は `reason_code=None`）、DIRECTIONAL=expected sweep 未観測時のみ `OUTPUT_MISSING`、へ是正（v1.0 §11 の `OUTPUT_MISSING` 予約先=PRIMARY 一部 missing 専用、と整合）。**claim縮小の列挙義務**（§V2.4）: `directional_claim_shrinkage_detail()` が全 DIRECTIONAL 終端 status の `gate_detail` へ `evaluated_sweep_contexts`/`prohibited_interpretations` を必ず付与。**境界宣言**（本 entry の見送り事項）: M6 の cross-family 測定単位・A/B pair 設計は v1.2 へ繰延、TILT_GT k_hold=1 縮退は上記のとおり実装時確定、R15-D（pre-dispatch 早期 return の cap 会計、詳細は D96）の `NOOP_ALREADY_COMPLETE`/`PHASE_ORDER_VIOLATION` 等 no-op 系3経路は ledger でなく counters.json（cache）のみへ課金。テスト: `test_campaign_holdout.py`（130件超）/`test_c0_freeze.py`/`test_c0_validate.py`/`test_campaign_cli.py`（slow E2E 含む）ほか多数、全 green。Codex 第12巡（設計 §V3.5/V3.6 追補、①構造的偽失敗）+ 第13巡（③ M6 precondition 偽装・gate fail 理由コード誤り）+ 第15巡（anchor pair 全滅・cap 会計、詳細は D96）採用。 |
| `UNDERSPEC-CAL-D94` | `tools/archive_aborted_ledger.py`, `provenance.py`, 対応テスト群 | v1.1 §V4 破棄 campaign ledger の原子的 gzip 保全（初版 `98e8eff` + Codex 各巡: R9=`c019dba`/R11=`2af2bde`/R12=`b4ba478`/R14=`7b18bd8`/R15=`e43dcd3`/R17=`b688bcc`）。**初版**（`tools/archive_aborted_ledger.py` 新設）: 同一ディレクトリ内固定名 staging（`ledger.jsonl.gz.archiving` 等）へ決定論的 gzip（`mtime=0`）+ sidecar を書込→伸長・sha256・chain 検証→`fsync`+`os.rename` で両者同時公開→検証済み公開物確認後にのみ原本削除。`ensure_archived()` が中断回復込みの単一入口（「原本 or 検証済み公開物のいずれかが必ず存在する」不変条件、故障注入テスト13件で固定）。**閉campaign拒否**（R9）: `_ledger_bytes_contain_campaign_closed()` を関数冒頭（一切の staging 書込前）と回復経路の両方に追加し、`CAMPAIGN_CLOSED` ledger の archive 試行をディレクトリ完全無変更のまま `ArchiveError` で拒否（回復経路では検証済み gz から原本を復元してから拒否）。**残存ledger照合保全**（R11）: 回復分岐で原本 unlink 前に sha256 を公開済み sidecar と照合、不一致かつ byte-prefix 拡張+chain valid なら再 archive、それ以外は原本・公開物とも変更せず保全して `ArchiveError`。**排他ロック**（R12）: `provenance.Ledger.append()` と同一 inode 上の `fcntl.flock` を `_ledger_write_lock()` で取得し、snapshot→unlink 区間全体を appender と排他化+削除直前の hash 再照合。**安定ロックファイル**（R14）: ロック対象を `ledger.jsonl` 自身の fd から専用 `ledger.jsonl.lock`（`c0_freeze.py::_publish_lock()` と同流儀）へ変更し、「unlink 前の旧 inode を open してから unlink 後に flock 取得する」経路を構造的に消去。`Ledger.append()` にも open 直後の inode 再検証（`os.fstat`/`os.stat` 照合、不一致は `LedgerChainInvalidError`）を defense-in-depth で追加。**回復経路の統一ロック**（R15）: `_ledger_write_lock()` 取得位置を `ensure_archived()` 入口（closed 検査の後・全ブロック読取の前）へ統一し、フレッシュ archive/回復判定の二重取得（同一ロックファイルへの二重 flock=自己デッドロック）を解消。**closed検査もロック内へ**（R17）: closed-campaign 検査自体がロック取得**前**の unlocked read だったため、「closed でない」判定直後・ロック獲得前の一瞬に `campaign_closed` が追記される race（R9 が塞いだはずの窓の再発）を検出、検査をロック取得後の最初のステップへ繰り上げ。**実適用結果**: 破棄 campaign 2本（`RUN10-CAL-20260903-9bcbbf86`: 7,534,399B→755,721B、`RUN10-CAL-20260903-591cadcd`: 70,354,422B→6,290,526B）を実際に archive し、`zcat ledger.jsonl.gz \| sha256sum` と sidecar の一致を実測確認（sha256=`2be10706717efd51bc08069d73143fb4d99bfc50bc7f078785a75ccdbb184221`/`c02f513b086ade0b2e23e2153adb009bf51204dfb3bfb9d61ff8a3828e969580`）。閉鎖 campaign `862dec28` は全コミットで1バイトも変更されないことを `git diff` で確認済み。テスト: `test_archive_aborted_ledger.py` が初版13件→最終30件（各巡の脆弱性再現+修正確認込み。R17 は修正前コードで新規テストが実際に FAIL することも確認した上で採用）、`test_provenance.py` に R14 の2件追加。全 green。Codex 第9/11/12/14/15/17巡 採用（③致命的バグ・実コード被害相当: 閉campaign誤archive・正典喪失race・appender/archiver 競合の6段階が互いに異なる機構上の欠陥）。 |
| `UNDERSPEC-CAL-D95` | `approvals.py`, `voice_genesis/calibration/__init__.py`, `c0_validate.py`, `c0_path_inventory.json`, 対応テスト群 | v1.1 §V6 統治文書切替+基底pin（`c57ba40`、単一読取化=R16 `d06e800`、path inventory union=`d5f4152`）: `approvals.DESIGN_DOC_RELATIVE_PATH` を v1.1（`DESIGN_VG_METER_CAL_DEBT_v1.1.md`）へ切替、新設 `BASE_DESIGN_DOC_RELATIVE_PATH`（v1.0）。`_parse_front_matter()`（`yaml.safe_load`）+ `_verify_base_document_pin()` を `load_approval()` の検証チェーンへ組込み、v1.1 front matter の `base_document_sha256` と checkout 上の v1.0 実測 sha256 の一致を毎回検証（v1.0 の承認後改変を無効化する唯一の経路）。**単一読取化**（R16、Codex 第16巡指摘=hash 照合と front matter 解析が別々の `read_bytes()` 呼出しに基づいており、両呼出しの間で v1.1 統治文書が差し替わると「hash は版A・base pin は版B」の組合せで承認が通り得た承認 provenance 汚染）: `_read_design_doc(repo_root)->bytes` を唯一の入口として新設し、`load_approval()` が1回だけ read した同一バイト列を hash 照合と `_verify_base_document_pin(repo_root, design_doc_bytes)` の両方へ渡すよう変更。回帰ガード `test_load_approval_reads_design_doc_exactly_once`（`Path.read_bytes` を monkeypatch して呼出し回数=1を固定）。**path inventory union**（`d5f4152`）: `c0_validate.scan_calibration_tree_inventory()` の戻り値へ `approvals.DESIGN_DOC_RELATIVE_PATH`/`BASE_DESIGN_DOC_RELATIVE_PATH` を union（`.py` 拡張子限定の走査ロジック自体は無改変、新規2文書は `c0_freeze.py::_path_hash_maps()` 側で自動的に既定カテゴリ=`schema_paths_sha256` へ分類されるため producer 側コード変更は不要）。既存の `gate1_campaign_execution.2026-09-02.json`（v1.1切替前の実履歴レコード）は歴史的事実として書き換えず、比較対象を新設 `_base_design_sha()`（v1.0実測値）へ変更して整合を維持。テスト: `test_approvals.py`（v1.0改変拒否/front matter欠落/`base_document_sha256`フィールド欠落等5件+単一読取回数固定1件）、`test_c0_path_inventory_sync.py` は走査ロジックの union がそのまま期待値計算にも反映されるため無改変で green。Codex 第16巡 採用（③実コード被害相当: 承認 provenance 汚染の実際の攻撃経路）。 |
| `UNDERSPEC-CAL-D96` | `campaign/cli.py`, `campaign/caps.py`, 対応テスト群 | cap会計のR7/R9/R15-D修正（`d5f4152`(R7)、`c019dba`(R9)、`59ad7e1`(R13/R15)）: **R7 P1** — 遷移前 checkpoint の ledger 永続化。`_checkpoint_parent_cpu_before_transition()` が delta 加算→`campaign.ledger.append({"kind":"parent_cpu_checkpoint","stage":...,"parent_cpu_seconds":delta,"invocation_id":...})`→cap 検査の順に変更（旧実装は ledger 記帳なしで cap 検査のみ行い、遷移 event 記帳前に kill されるとその区間の CPU が再構成不能だった）。`caps.cap_counters_from_ledger()` が新 kind を扱い、invocation_id ごとの**最大値**（累積値なので sum ではなく max）を deferred pass で「summary 未カバーの invocation のみ」加算。**R9** — `parent_cpu_checkpoint` と既存の `meter_group_within_cpu`（round12/13 由来の COMPLETE group within CPU 計上）の2本の独立 deferred pass が同一 invocation に対し共に発火すると、checkpoint より前に完了した group の within CPU が二重計上される欠陥を発見。`meter_group_last_seq`（各 key の最新 `meter_call` record の `entry.seq`）と `checkpoint_seq_by_invocation`（checkpoint 記帳時の `entry.seq`。CPU は同一 invocation 内で単調非減少なので max 値を持つ checkpoint は必ず時系列上も最新）を ledger 順序で比較し、`meter_group_last_seq[key] < checkpoint_seq`（=checkpoint より前に完了）の場合のみ deferred pass で skip（invocation 単位の一律 skip ではなく group 単位）。`None` invocation_id 同士は意図的に非マッチ（未識別 writer の誤 skip 防止）。**R15-D** — `campaign/cli.py::main()` の pre-dispatch 早期 return 7経路を User 裁定（2026-09-05）で3段階に分類: (1) canonical-drift/environment-drift/cap-loading(`BudgetAccountingUndeclaredError`)/counters-corrupt(`CountersCorruptError`) の4経路=ledger 記帳+counters.json 永続化（`_charge_pre_dispatch_cpu(..., record_ledger_event=True)`）、(2) `NOOP_ALREADY_COMPLETE`/`PHASE_ORDER_VIOLATION` の2経路=counters.json のみ永続化（round19/20 が凍結した「ledger 増分ゼロ」契約を尊重、`reconcile_cap_counters()` の per-dimension max により次回 armed 起動の cap 判定に反映）、(3) `binding_violation`（gate1 frozen binding）/`_stage_already_complete` の `COST_CAP_EXCEEDED` 変種/dispatch 直前の最終 `COST_CAP_EXCEEDED` 事前拒否=境界宣言で見送り——実装過程で `test_completed_stage_noop_retry_still_refuses_on_persisted_cap_breach`（idempotent 判定が前回 stop_event の counters/caps フィールドと厳密一致で重複防止するため、ここで cap_counters を加算すると2回目リトライで `compute_used` が僅かに進み「新しい breach」と誤認して stop_event を重複記帳する回帰、`999999.0`→`999999.001088` で不一致）等の既存テストとの衝突を実測し、実害を「真に cap 超過でロック済み campaign の、その事実の記帳がわずかに遅延する」ことに限定して見送りを裁定（実データ破壊・誤 CALIBRATED は生まない）。テスト: `test_campaign_caps.py` に R7=5件・R9=4件・R15-D=2件を追加、既存47件/43件は無改変で green。Codex 第7巡（②将来汚染: cap 会計の recovery gap）+ 第9巡（③実コード被害: within CPU 二重計上）+ 第15巡（境界宣言、実害限定を確認の上で見送り）採用。 |
| `UNDERSPEC-CAL-D97` | `provenance.py`, `tests/test_provenance_unseal_prerequisites.py` | replay検証器のlegacy互換（R7 P2、commit `d5f4152`）: D88 で追加した Gate3順序 parity 検査（`freeze_time < gate3_time <= unseal_time+60s`）は `holdout_unseal` event の `event_time_utc` フィールド（D88 で新設したフィールド、旧 campaign には存在しない）を前提にしており、`_c0_freeze_ordering_violation()` の初期実装は `unseal_time is None`（フィールド自体が存在しない）を無条件 fail-closed としていたため、実在する唯一の閉 campaign `862dec28`（`c0_freeze` seq0=`2026-09-04T08:19:55.278186+00:00`、`gate3_accepted` seq68168=`2026-09-04T08:20:24Z`、`holdout_unseal` seq68169=`event_time_utc` フィールド不在）の replay 検証を偽って拒否する（`freeze_time < gate3_time` の下限自体は本来成立していたにもかかわらず）ことが判明した。**ruling**: `holdout_unseal_payload.get("event_time_utc") is None`（フィールド自体の不在。空文字列等「存在するが不正」なケースとは判定を区別）の場合のみ legacy パスへ分岐し、`freeze_time < gate3_time` の下限比較のみを行い上限比較を skip。フィールドが存在するが不正な値は従来どおり fail-closed のまま。**実測確認**: 862dec28 の実 ledger（70,630行）を `provenance._verified_holdout_unseal_detail()` へ直接通し、`verified_seq=68169`（`holdout_unseal` 自身の seq と一致）・`gate3_candidate_unverified=False` で replay valid を確認（修正前は `verified_seq is None` で棄却されていたはずの経路）。テスト: 既存 `test_replay_verifier_rejects_holdout_unseal_missing_event_time_utc` は本修正が意図的に変更する挙動そのものを固定していた旧テストのため `test_replay_verifier_accepts_legacy_holdout_unseal_missing_event_time_utc` へ書き換え、`test_replay_verifier_rejects_legacy_holdout_unseal_when_gate3_predates_freeze`（legacy でも freeze<gate3 下限は強制）・`test_replay_verifier_rejects_holdout_unseal_malformed_event_time_utc`（missing vs malformed の区別）・`test_replay_verifier_accepts_real_862dec28_campaign_shape`（実 campaign 3タイムスタンプの回帰pin）を新規追加。Codex 第7巡 採用（③致命的バグ相当: 唯一実在する閉 campaign の replay 検証が偽って失敗する経路）。 |

**holdout coverage-classification family（`[UNDERSPEC-CAL-D66]`/`[UNDERSPEC-CAL-D69]`/`[UNDERSPEC-CAL-D72]`/`[UNDERSPEC-CAL-D73]`/`[UNDERSPEC-CAL-D74]`/`[UNDERSPEC-CAL-D76]`/`[UNDERSPEC-CAL-D77]`）終端宣言**（2026-09-02、D77 追記で再終端。**D75 は本 family の一部として数えない**——D75 は matrix/sweep 定義そのものの誤りで、D76 (1)/(2)/(3) により撤回・訂正済み。D76 (4) の instance-usability は本 family と同じ「C4 coverage-completeness」領域の是正だが、collapse していた対象は record 集計であり分岐ロジックではないため、本家系の是正段数には含めず別枠の Codex round 7 #1 指摘として扱う）:
`_run_c4` の部分被覆判定はこの 5 entry（D66/D69/D72/D73/D74）で「record presence →
値の有無 → PRIMARY 限定 → frozen minimum-count 条件 → 最小数条件は DIRECTIONAL
effective ceiling 限定」の 5 段の是正を経て収束し、D76 (3) が「最小数条件のカウント
対象は usable instance の raw 件数ではなく declared sweep 内の distinct truth level
数」へ是正した。D77 (2) は 2026-09-02 時点で本 family が「上記 6 entry のいずれかを
覆す新しい false terminal status 経路が具体的に示された場合」の再開条件そのものに
該当した唯一の追加指摘: 容量検査が **完全被覆の分岐そのものを一度も通っていなかった**
（部分被覆ブロック内限定）という、これまでの 6 entry のいずれも扱っていなかった経路
（timing/coverage-gating の欠陥であり、6 entry のいずれかの帰結の再修正ではない）。
family は**この 7 entry で終端を維持する**：全欠損/最小数割れ(DIRECTIONAL, distinct
truth level 基準・被覆完全性に関わらず)/部分被覆(ABSOLUTE 含む)/完全被覆の各通りが、
design §10.3/§10.4/§11 の cascade と閾値の唯一の実体（`gates.py`/`fixtures.matrix.
declared_sweeps_by_family()`）に対して整合する形で切り分けられている。以降この
family（C4 holdout の coverage-completeness 分類、および D77 (1) が追加した C0
manifest 側の宣言一致検証）で見つかる指摘は、D66/D69/D72/D73/D74/D76/D77 のいずれかが
見落とした**新しい具体的な false terminal status 経路（または provenance
contamination 経路）**を示すもの以外は境界宣言で見送る（`[UNDERSPEC-CAL-D57]`/F0
注入 family と同じ運用）。
再着手条件: 上記 7 entry のいずれかを覆す新しい false terminal status 経路、または
manifest 宣言と実体の新しい不一致経路が具体的に示された場合。

**F0 注入 family（`[UNDERSPEC-CAL-D61]`/`[UNDERSPEC-CAL-D63]`/`[UNDERSPEC-CAL-D64]`/`[UNDERSPEC-CAL-D65]`）終端宣言**（2026-09-02、D65 追記）:
D61 が固定した「注入される F0 は repeat/aggregate 双方が有限かつ厳密に正でなければ
ならない」規則の未網羅ケースは D63（空 repeat 集合）・D64（skip の下流不可視化）の
2 件で尽きた——F0_UNUSABLE の判定自体（cli.py `_build_f0_by_instance`）と、その
判定が selection/holdout の母集団会計に正しく伝播することの両方をそこで閉じた。
D65 はこの終端宣言が明示していた再着手条件「無効/欠測 F0 が実際に F0 依存
candidate へ到達する新しい具体的経路」に該当する唯一の例外——「F0 winner が
一切存在しない」（C3a が `SELECTION_FAILED_CLOSED`）経路は D61/D63/D64 のいずれの
per-instance guard も対象にしておらず、`_build_f0_by_instance()` の呼び出し自体が
スキップされていた——を塞いだ。family は D65 を含めて**終端を維持する**：以降
この family で見つかる指摘は、D61/D63/D64/D65 のいずれかを覆す**新しい具体的
到達経路**を示すもの以外は境界宣言で見送る（`[UNDERSPEC-CAL-D57]` 系列と同じ
運用）。再着手条件: 上記 4 entry のいずれかを覆す新しい到達経路が
具体的に示された場合。

**v1.1 実装ファミリー終端宣言**（`[UNDERSPEC-CAL-D91]`〜`[UNDERSPEC-CAL-D97]`、
2026-09-05）: 設計正本 v1.1（§V1–V6）の実装に対する bot レビューは、設計起草
段階（Design Memo 起草時、Codex 第1〜6巡）と実装段階（PR #346、Codex 第7〜18巡）
の全巡にわたり、CLAUDE.md/`AGENTS.md` §3 の3分類基準（実コード被害/将来汚染・
致命的バグ）で採否判定し、該当する指摘は巡数によらず全て採用した（見送りは
境界宣言として D93/D94/D96 の各 entry に明記済み: M6 cross-family 測定の v1.2
繰延、TILT_GT k_hold=1 縮退〔実装時確定〕、R15-D の no-op 3経路の counters.json
のみ課金、binding_violation の CPU 計上見送り）。`archive_aborted_ledger.py` の
排他/原子性ファミリーだけで R9/R11/R12/R14/R15/R17 の6巡を要し、CLAUDE.md の
bot レビュー対応上限（10巡、同一領域3巡）を実装段階のみで超過したが、これは
規約の「新しい具体的な偽成功/偽失敗/正典喪失経路を示す指摘は巡数に依らず採用
する」という打ち切り例外に該当する——各巡は前巡までの修正が塞いでいなかった
新しい具体的経路を示していた（closed campaign 誤 archive〔R9〕→ 残存 ledger
照合漏れ〔R11〕→ appender/archiver 競合〔R12〕→ ロック対象自体の不安定性
〔R14〕→ 回復経路の二重ロック〔R15〕→ closed 検査自体の unlocked read race
〔R17〕の6段階はいずれも異なる機構上の欠陥であり、前巡の修正のやり直しでは
ない）ため、上限到達で打ち切らず全巡採用した。**打ち切りは3分類そのものを
上書きしない**——今回超過した各巡はいずれも②/③に該当する具体経路を伴って
いたことを、上記の各 D entry に個別の実測/再現手順とともに記録した。
再入条件: D91–D97 のいずれかが見落とした新しい具体的な偽成功/偽失敗/正典
喪失/承認 provenance 汚染経路が示された場合のみ。同型経路の言い換えや
スタイル指摘は境界宣言で見送る。

以下は Codex レビュー（2026-09-01、複数巡）で採用され `IMPLEMENTATION_MAP_v1.md`
に凍結された仕様であり、上表の UNDERSPEC には数えない（正本の一部として実装
済み）: `canonical.py` の `vgcal-canon/1` 版管理と `-0.0` 正規化、
`provenance.py` の単一 writer 境界（fcntl 排他 + fsync）と `verify_chain()` の
truncated/tamper 区別、`gates.py` の DIRECTIONAL resolvability 単位健全二連言
分解、`splitter.py` の重複 `row_id` 拒否、`selection.py` の ceiling 階級間裁定
規則、`observables.py` の `u_rep` singleton 除外、`m6_identity.py` の
CLAIM_CRITICAL_SET 全 member ABSOLUTE 必須化。

## テスト

```bash
python -m pytest voice_genesis/calibration/tests -q
ruff check voice_genesis/calibration/
```

依存は numpy / scipy / stdlib のみ（`svp_rpe` / `voice_genesis.harness` を
import しない）。
