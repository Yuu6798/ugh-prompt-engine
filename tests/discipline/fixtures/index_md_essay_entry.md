# Session Memory Index

各セッションの 1 行要約。詳細は同ディレクトリの `YYYY-MM-DD.md` を参照。

- 2026-01-01: 正常な短いエントリ。[詳細](2026-01-01.md)
- 2026-01-02: このエントリは意図的に 500 文字を超えるエッセイ化した違反サンプルである。セッション中に何が起きたかを延々と物語る長文をインデックスに直接書き込むと、セッション起動時に毎回読み込まれる _index.md が肥大化し、コンテキスト予算を圧迫する。本来この詳細は dated session log 側に書き、インデックスには 1 行の要約だけを残すべきである。ここからは文字数を稼ぐための反復である。設計判断の経緯、レビューで指摘された点、修正の往復、マージされた PR の番号、次セッションへの引き継ぎ事項、雑多なメモ、これら全てをインデックス行に書いてはならない。これら全てをインデックス行に書いてはならない。これら全てをインデックス行に書いてはならない。これら全てをインデックス行に書いてはならない。これら全てをインデックス行に書いてはならない。これら全てをインデックス行に書いてはならない。これら全てをインデックス行に書いてはならない。これら全てをインデックス行に書いてはならない。これら全てをインデックス行に書いてはならない。これら全てをインデックス行に書いてはならない。これら全てをインデックス行に書いてはならない。これら全てをインデックス行に書いてはならない。
