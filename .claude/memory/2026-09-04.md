## Session Summary — 2026-09-04

（注: 2026-09-02 / 09-03 の RUN10-CAL セッションは wrap-up 未実施で dated log がない。本記録は PR #342〜#345 の系列全体を 1 本で補う）

### Context

VoiceGenesis の meter 校正負債キャンペーン **RUN10-CAL**（設計正本
`voice_genesis/calibration/DESIGN_VG_METER_CAL_DEBT_v1.0.md` =
`VG-METER-CAL-DEBT-DESIGN-v1.0`、read-only）を、設計 → 実装 → C0 freeze →
campaign 実行 → close まで Claude 完結ルートで完走し、PR #342〜#345 を全マージした。

- #342: 校正キャンペーン基盤（設計 v1.0 準拠のスキーマ・ledger・gate 登録）
- #343: Phase D 未武装の実行基盤（C0 freeze producer + campaign runner）
- #344: 第 30 巡修正 + C0 freeze 記録
- #345: runner 堅牢化（`--time-budget-seconds` / `--discard-partial-groups` /
  `MeterCallIndex`）+ Gate 1 再承認 + C0 freeze 3 回 + campaign
  `RUN10-CAL-20260904-862dec28` の c1→close 完走（49 commits、187,724 行追加のうち
  177,836 行は ledger.jsonl 3 本、コード側 8,913 行）

**結果（`campaign_closed`、ledger seq 70627 原文）**: F0_CONTROL / M2_SPECTRAL_TILT /
M3_FORMANTS / M5_TRANSITION = `NOT_EVALUABLE`（`SELECTION_FAILED_CLOSED`）、
M2_APERIODICITY = `NOT_EVALUABLE`（候補 `M2A-B0-AUTOCORR-PERIODICITY` は選定されたが
`DIRECTIONAL_SWEEP_UNRESOLVABLE_ON_HOLDOUT`、85/85 instances、10 sweep 中 9 が最小 3 ペア未満
= D77 の設計上の帰結）、M4_RESONANCE = `DIAGNOSTIC_ONLY`（§16-1）、M6_IDENTITY =
`NOT_EVALUABLE`（claim-critical meter が ABSOLUTE ceiling 未達）。`debt_discharged = false`。
chain verify ok（70,630 entries）、compute 33,798 s / cap 172,800 s、stop_event 0、
`StaleMeasurementError` 0、discard 0。split secret reveal は seq 70628（本文はどこにも引用せず）。

### Design Decisions

- **freeze 後の校正コード変更 = 正本変更 = 新 campaign**。この規律で campaign を 2 回破棄した:
  `9bcbbf86`（D84: `Ledger.append()` が毎回全チェーン再検証で O(n²)、残作業外挿 CPU 約 112 h →
  watermark 増分検証へ）、`591cadcd`（D86: `cli._run_c4` が F0 前処理を c4 render より先に呼び
  `FileNotFoundError`。fixture が holdout を c1 で先回り描画していたため本番でのみ発現）。
  3 回目の freeze `862dec28`（HEAD `a9d411b`）で完走。
- **Gate 3 は armed freeze 後に発行**（D85: `freeze_time < gate3_time ≤ now+60s` を
  `unseal_campaign()` で fail-closed 検証）。Gate 3 を freeze 前に切っていた運用ミスを機械化で塞いだ。
- **F0 選定は split 依存**（campaign 2 で F0-PYIN-FRAME2048-HOP256 選定、campaign 3 で全 5 候補
  `negative_control_false_fire`）。下流 4 ファミリーが `f0_dependent_selection_blocked` で連鎖
  fail-closed するのは設計どおりで、結果は改竄せず設計 v1.1 の観察として記録した。
- **bot レビュー**: #345 は 20 巡 + 本番実測 2 件を 3 分類で処理（③ 致命 = 採用、他は境界宣言）。
  巡数上限 10 到達後は「新しい具体経路を示す ③ のみ採用」。freeze 後・campaign 実行中に来た Codex
  指摘 9 件は subscribe 停止（User 指示）で未 triage のまま残っていたので、マージ前に境界宣言
  D87–D90（cap 会計ファミリー 5・検証器 parity/縮退入力 2・実測付き 1・② 将来汚染 1 = D90）を
  README + PR 本文 + スレッド返信で付けてからマージ可と判定した（resolve せず残置）。
- **記録の digest 固定と errata**: `GATE1_DECISION_RECORD.md` は E_use table で digest 固定され
  campaign の凍結入力になるため、§10.7 の誤記（偽成功経路 → 正は偽失敗経路）の訂正は close 後に
  §10.8 正誤表 + restamp で行った（D83）。閉じた campaign dir の凍結コピーは不変、close 後 plan =
  campaign_state OK / drift []。
- **runner 運用**: 環境が約 4 分でプロセスを kill するため、`--time-budget-seconds 140`
  （budget ≤ 外部上限 − 最長 instance）で PARTIAL_SLICE を回し、20 slice ごとに snapshot commit、
  ledger 95 MB size guard、hourly send_later で生存確認。委譲エージェントは Opus 固定・同期実行。

### Artifacts

- マージ: PR #342 / #343 / #344 / #345（main `b08590b`）
- campaign dir: `voice_genesis/calibration/campaigns/RUN10-CAL-20260904-862dec28/`（ledger 50 MB、
  closed）。破棄 campaign `9bcbbf86`（7 MB）/ `591cadcd`（68 MB）は abort record として残置
- README ledger 行 D79–D90、`GATE1_DECISION_RECORD.md` §10.1–§10.8、`IMPLEMENTATION_MAP_v1.md` §6.5.x、
  approvals/records に Gate 1/2/3 記録（secret は `~/.vg_cal/` のみ、checkout 外）
- CI: test-calibration を 2 shard 化・timeout 45 分（#345 `56e088e`）
- **Fable 直接実行監査**: runner 生存確認の tail / git 状態確認、CLI 語彙の grep、PR 状態 get、
  wrap-up のメモリ編集。実装・実行・検証・PR 本文更新・スレッド返信はすべて Opus / Sonnet に委譲。
  改善余地 = wrap-up の材料集め（3 コール）は Sonnet に事前収集させてよかった。

### Handoff

- **設計 v1.1 の材料**（記録のみ、コード変更なし）: (a) F0 選定の split 依存（negative control の
  割当で結果が反転）、(b) DIRECTIONAL ceiling 候補は正準 456 セル (block, domain) holdout split で
  sweep を解決できない → sweep-aware 層化、(c) ledger サイズ運用（破棄 campaign の ledger は gzip か
  Git LFS へ。GitHub 50 MB 推奨を 2 本が超過、blob は push 済み）。
- **次 campaign 前の修正候補**: D82（重複 repeat key の説明用 `stop_event` 不在）、D90
  （resume 時の `f0_injection_rejected` 重複記帳。破棄 campaign に 89 件）、D87（cap 判定前の
  checkpoint 永続化順序統一）、D88（replay 検証器の Gate 3 順序 parity / 空行のみ ledger）。
- **運用教訓**: subscribe を止めたら bot 指摘が未 triage で溜まる。マージ判定の前に
  「未 resolve スレッド全件が境界宣言か resolve 済みか」の棚卸しを必ず 1 回入れる。
- PR #345 の subscribe は User 指示で停止済み。予約 check-in は全削除済み。
