# 2026-08-31

## Session Summary — 2026-08-31

### Context

RUN9 の実行セッション（2026-08-30 15:00Z 開始 → 08-31 01:00Z 完結）。User 指示
「ポッド起動」を受け、success-only pod を 6 attempt・4 修正のループで同日中に
**PASS まで完走**した。entry script は初実走で、環境・実装の未検証領域が段階的に
露出 → ライブ監視で原因確定 → Sonnet 委譲で修正 → 再起動、を反復。完走後は
bundle 回収・検証・Drive 恒久保全・最終レポート化まで実施し、
**User 裁定 2026-08-31「レポートの Drive 保存をもって RUN9 完結」**で閉じた。

### 経緯（6 attempt）

1. attempt 1 (hdih5cgpe7aroo): 失敗・証跡消滅（CPU pod は volume 非割当・停止で
   コンテナディスク消失。60 分後チェックインでは 900 秒の公開ホールド窓に間に合わず）
2. attempt 2 (h9mcz219gi043u): 45 秒ポーリングのライブ監視を導入し console 全量捕捉
   → fresh-export で `ModuleNotFoundError: _bz2`（ソースビルド CPython の dev ヘッダ不足）
3. attempt 3 (pb2jaavoyww9xy): render-network-isolation で iptables Permission denied
   = **RunPod CPU pod は CAP_NET_ADMIN 無し**
4. attempt 4 (knoeallarqdgl1): preflight 前置 probe が早期 named failure —
   **unshare -n/-rn も runtime 拒否**（隔離機構全滅を 3 分で特定）
5. attempt 5 (i4n3ewckub6v61): **seccomp 隔離が実走成立**。admission 内
   `mora_phonemes` で `'str' has no attribute 'onset'`（executor が pin manifest の
   kana を構造化 Mora へ変換していない型境界バグ = Birth Probe executor 初 E2E で露出）
6. attempt 6 (vly56753h3eqwq): pin `1682153` で **PASS**（18:55:24Z success-published・
   admission 57 分・19:40:36Z self-stop）

修正 5 commits（すべて PR #341）: `b3cc34e` stdlib ヘッダ+fail-fast / `15a2468`
隔離フォールバック+probe 前置 / `5ad13bf`+`fd55777` seccomp モード+照合モード対応 /
`5ba2b09` kana→Mora 正規パーサ変換（phoneme_jp provenance pin 追加）/ `1682153`
BPF 許可リスト化（AF_UNIX/AF_NETLINK のみ）+ socket(2) 全空間掃引照合 ≈13.5 万 probe。

### Design Decisions

- **User 裁定 2026-08-31: RUN9 完結**（最終レポートの Drive 保存をもって。正本 =
  Drive `VoiceGenesis/RUN9_success_20260830/RUN9_FINAL_REPORT_20260831.md`）
- **隔離契約は保持・機構は差し替え可**（Fable 設計判定）: 遮断の意図（admission 中の
  外部通信の物理的不能化）を保ったまま iptables → seccomp 許可リストへ。ブロック
  リスト BPF は AF_SMC(43, TCP フォールバック)/AF_XDP を素通しする自己検出穴があり、
  **許可リスト＝ドメイン軸の構造的終端**が正。照合は「生成不能」の実効検査
  （errno 非依存・socket(2) は 3 引数で第 4 軸なし）
- **撤回 2 件を明記**: attempt 1 の stage 推定誤り（マシン速度の過小評価）/
  Codex 第 2 巡返信の「(family,type) 行列で終端」宣言は早計（protocol 軸 +
  enforcement 穴を見落とし）→ 第 3 巡で終端形へ再構成
- bot レビュー: Codex P2 計 3 巡すべて採用（「新しい具体経路は巡数非依存で採用」を適用）
- 保全設計: bundle のバイト正準は success-only 裁定の意味論上サルベージ必須と判定。
  30MiB 送付上限に対し「独立 zip 群 + ONNX のみ .001 バイト分割」方式を採用
  （**Info-ZIP の分割ボリューム機能は再結合検証で CRC 不一致 → 不採用**）

### Artifacts

- **PR #341 マージ**（2026-08-31 00:09Z・CI green・Codex 所見ゼロで収束・スレッド全 resolve）
- **成功 bundle**（9 model / 84 WAV / 85 feature・181 ファイル manifest 全一致検証済み）:
  Drive `VoiceGenesis/RUN9_success_20260830/`（10_登録記録 / 20_WAV / 30_モデル に整理済み・
  全 zip サイズ照合済み）+ チャット送付分。acoustic ONNX `80a40f9e…` は 08-28 replay と
  一致 = 決定論 export のクロス走行再現
- Birth Gate evidence: **ESTABLISHED (BY_MACHINE_FEATURE)・learning_progression_allowed=true**・
  d12=7.06・PJS confuser 62.90/62.68・全 take distance 0.0
- 費用 ≈ **$2.11**（6 走行・self-stop 6/6 正常・fail-closed 4/4 設計どおり）
- 転送時の転記破損 2 件（gzip base64 の手動転記）をサイズ照合で捕捉 → 破棄・再送で解決

### Fable 直接実行監査

委譲: 実装 4 件（Sonnet: stdlib ヘッダ / 隔離フォールバック / seccomp 2 段 / mora 境界）。
直接実行: run9 テスト 2 回・pod 起動/監視スクリプト作成・bundle 回収/検証/zip 再パッケージ・
Drive 20+ 操作。**bundle 保全系（分割・zip 化・Drive 整理）は委譲可能な規模で、
逐次判断の必要を過大評価した** — 次回は保全作業も Sonnet 委譲が適。

### Handoff

- **RUN9 は完結（User 裁定）**。次フェーズ = PJS 学習進行（evidence が解錠済み）と
  登録検分の運用。着手は User 指示による
- 環境契約（RunPod CPU pod・次の pod 設計で前提にする）: CAP_NET_ADMIN 無し /
  unshare 拒否 / **seccomp フィルタは導入可** / volumeInGb 要求は無視される
  （成果物は稼働中ホールド窓での回収が唯一 = 45 秒ポーリング監視を標準運用に）
- bot レビュー standing rule 提案 2 件（巡目 20 打ち切り / 敵対的自己検査必須化）は
  引き続き CLAUDE.md 未収載（Queue に残置）
