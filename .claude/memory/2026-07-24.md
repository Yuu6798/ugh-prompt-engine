## Session Summary — 2026-07-24

### Context

外部設計書「主旋律観測センサー M0/M1（成立帯域の発見）」を Claude 完結ルートで実装し、
**PR #215 を単一トラックで完走・マージ**（branch `claude/design-doc-implementation-nxakv8`）。
Codex レビュー **全 23 ラウンド**をきり上げなしで対応。設計の核心は「主旋律 *保存* を
保証する」のではなく**「比較の前に、そもそも旋律を観測できるかを問う観測ゲート」**を作る
こと（Phase 0 メロディスパイク #199/#201 の負の結果＝pyin が和音パッドで 1–4 音へ縮退、を
「比較器の問題ではなく入力帯×抽出器の問題」と再定義した再入）。

### Design Decisions

- **観測ゲート（M1 本体）**: `melody/observability.py`。抽出器非依存の共通中間表現
  `MelodyObservation` → `MelodyObservabilityReport`（note/phrase/被覆/信頼/オクターブ誤り/
  cross_extractor_agreement/status/reasons）。比較（M3）を一切呼ばない。閾値は M0 で
  registry に事前登録し M1 で緩めない（one_way_rule）。
- **抽出経路 routing**: `melody/routing.py`。入力種別→経路候補。旋律不在入力は
  `not_applicable`(applies=False) で抽出せず not_observed へ落とすが、診断用に
  `pyin_negative_control` も併走させ「ゲートが抽出器の false positive を弾く」ことを
  harness 出力で自己実証。
- **slow-lane / learned 隔離**: pyin だけが CI 経路（core librosa）。CREPE/Melodia/Demucs/
  basic-pitch は optional。標準 CI は重依存なしで green。抽出結果は決定論 RPE へ書き戻さない。
- **ライセンス規律（Codex round-22）**: CREPE の *コード* は MIT でも同梱重み（model-*.h5）の
  ライセンスを別途 inspect/pin していなかった→ policy §4（permissive コード ≠ permissive
  重み）に照らし、`[crepe]` published extra を**削除して Melodia と同じ manual/external 統合へ
  降格**。`_WEIGHTS_LICENSE="UNVERIFIED…"` を fail-closed で明示。重みライセンス実確認は
  machine-dependent な slow-lane 課題（Codex/User 側）と位置づけ。
- **provenance pin 網（Codex 全ラウンド通底）**: registry_sha256 の single-read 束、external
  audio_sha256 の byte-freeze、manifest_sha256、そして round-23 で **synthetic waveform_sha256**
  も registry.provenance と照合（drift を fail-closed reject）し report へ serialize。閾値
  override は asdict で「実際に使った閾値」を pin し registry snapshot の誤主張を排除
  （round-21）。公開は staging→os.replace の atomic + manifest-last。round-21 の symlink
  EXDEV は staging を out_dir 内へ置き symlink/マウント非依存の同一 fs を担保。

### Artifacts

- **PR #215 マージ**: `melody/`（observability/routing/extractors/__init__）+ learned adapters
  3 種（source_separation/crepe/melodia）+ `scripts/`（run_melody_observability /
  build_melody_bench / make_melody_pairs）+ `tests/fixtures/melody_bench/`（registry.yaml 事前
  登録閾値・synthesis_specs.yaml・waveform_sha256 pin）+ `tests/test_melody_observability.py`
  （42 not-slow + 5 slow）+ `docs/melody_observability.md`。CLAUDE.md/README に索引・tree 行。
- **M1c Go/No-Go 実測**: pyin 経路のゲート機構を実測確立（synth_mono_phrased=sufficient 15
  notes/3 phrases・synth_chord_pad/unison_drone=insufficient）。実利用入力帯（Suno vocals
  stem を Demucs→CREPE/Melodia）は machine-dependent で **slow-lane 繰延・M2 非自動進行**を
  正直に記録（捏造しない）。
- CI: 全マージ前に ruff clean + discipline 12 + melody 47 pass 確認。

### Handoff

- **melody M2 は未着手・自動進行しない**。次の再入条件＝実利用入力帯（Suno vocals stem 等）で
  Demucs→CREPE/Melodia 経路を実推論し、事前登録ゲートを安定して満たす経路が 1 本以上あるか
  を dated 実測（machine-dependent・重依存導入環境が必要）。全経路 insufficient を確認した
  わけでもないので「強化版 not_observed」も宣言しない。
- CREPE 重みライセンスの実確認＋pin（registry.provenance.model_weights.crepe="TBD" のまま）が
  付けば `[crepe]` extra 昇格を検討可（policy §4 記録が前提）。
- 23 ラウンドは全て provenance/fail-closed 同型（waveform pin・symlink EXDEV・閾値 pin・重み
  ライセンス・glob メタ文字・TOCTOU）で、#203 で §8 恒久化した emit 前チェックリストの妥当性を
  再確認する事例。新規 policy 逸脱の externalize は不要（既存規律の適用）。
- マージはユーザーが実施（規律どおり Claude からはマージせず）。

### Fable 直接実行監査

本セッションは Opus 稼働（Fable 非稼働）につき代行。実装・実行・検証・レビュー返信は
すべて Opus が直接実施（委譲対象の Fable 設計判断は発生せず）。スレッド ID 取得の重い
get_review_comments ページングのみ subagent へ委譲しコンテキストを温存。
