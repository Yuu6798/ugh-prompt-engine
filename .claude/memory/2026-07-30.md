# Session Summary — 2026-07-30

## Context

M2 トラック（メロディ抽出精度検証）を M2b 実測確定から M2d 誤差モデルまで一気に完結
させた長大セッション。7 PR（#226–#232）をすべて Claude 完結ルート（Fable 設計 →
Sonnet/Opus 委譲実装・実行 → Fable 判定）で作成〜マージ監視まで完走した。

1. **#226 M2b 記録 commit**: run7 確定実測（環境ゼロ再構築・CREPE model-full.h5 sha256
   前回一致・run×2 各 ~24 分 + evaluate green・判定 7 回不変）→
   `docs/measurements/m2b_2026-07/` に dated 記録 + `test_m2b_committed_record.py`
   （verdict bytes digest 凍結・report_pins sha256 照合・導出値読み戻し・failures verbatim）。
   判定: S-direct **fail**（VFA 0.2588 > 0.15 単独超過・RPA 1.000）/ S-fullstack diagnostic_only。
2. **#227/#228 TB-30（テスト 30 分化）**: User 上限指示 30 分に対し全件 2h20m だった
   test_m2_accuracy_harness を、**二重 forensics のセッション凍結 stub**（`_scorer_pins`
   ≈13s/test + `bind_inference_code_pins` ≈5-6s/run_accuracy を session fixture 1 回実測 +
   autouse monkeypatch + `@pytest.mark.real_forensics` opt-out 15 件 + spy テストで rglob
   0 呼び出し保証）で 13m29s へ。全件スイート 39m24s は CI 2 シャード分割
   （test-m2-harness / test-rest、timeout-minutes 35）で壁時計 30 分内。カバレッジ不変・
   新規依存ゼロ。
3. **#229/#230 M2c（V 帯実測）**: registry.yaml 凍結チェーン罠を検出し独立登録ファイル
   `m2c_external_fixtures.yaml`（40 clip audio/annotation sha256）を新設。vocadito
   （Zenodo・cc-by-4.0 原文記録）で事前登録 commit `1cbd448` → 実測 → 記録の一方向順序
   厳守。V_direct **pass**（RPA 0.9886 / octave_gap 0.0015 / 40 clip 全バー上・
   VFA median 0.44 = 分散主軸）。squash 禁止（2-commit attestation 構造保存）を README に
   明記し merge commit 方式でマージ。
4. **#231 M2d 誤差モデル doc**: `docs/m2_error_model.md` — 誤差 4 軸（cent 中心誤差 /
   オクターブ予算 / voicing=系統的弱点軸 / 素材別ばらつき）+ 帯域地図
   （crepe_direct=単離 clean lead 限定・stem 被覆外）+ §7 分岐決裁待ち構造
   （(a) 非前進 / (b) User 承認で撤回範囲分離改訂）。Codex 11 巡 22 件全採用で統計・
   スコープの正直会計に精緻化。
5. **#232 hotfix（main 赤）**: 真因は**壁時計タイムボム**（テストが committed=
   2026-07-30T00:00Z 固定・report started_utc が実時刻 → UTC 30 日到達で DID NOT RAISE）。
   started_utc を `"2026-07-29T12:00:00+00:00"` に固定 + shallow 縮退挙動の pin テスト追加。
   当初仮説（shallow checkout 縮退）は委譲診断の full clone 再現で**誤りと切り分け**られた。

## Design Decisions

- **凍結チェーン分離**: M2c 外部素材 pin は registry.yaml（m1real verdict が sha pin 済み・
  変更禁止）に足さず独立ファイル新設。「凍結済み入力には追記もしない」を規約化。
- **セッション凍結 stub の設計原理**: 実測は session で 1 回だけ行い deepcopy を配る。
  seam の実在は spy テスト（harness 側 from-import 束縛 `harness.package_code_sha256` を
  patch — provenance 側を patch しても観測できない束縛罠に注意）で保証。
- **V 帯評価はネイティブタイムライン**（vocadito hop 256/44100 ≈ 5.805ms・リサンプル
  なし）、集計は per-clip 算術平均（clip 等重・MIREX 慣行。フレーム加重と区別を明記）。
- **Codex 採否規律の運用実績**: 約 30 巡 50 件を「検証力への実害のみ採用 / 終端機構
  （digest 凍結・列挙付き監査閉鎖）と境界宣言で treadmill 阻止」で処理。#231 は解釈 doc
  の統計過大主張（打ち切り median 推奨・片側 2.5%・双安定「再現」等）を全採用で撤回。

## Artifacts

- マージ済み PR: #226（M2b 記録）/ #227・#228（TB-30）/ #229（M2c-1 ハーネス+事前登録）/
  #230（M2c-2 実測記録）/ #231（M2d 誤差モデル doc）/ #232（タイムボム hotfix）
- 新規: `docs/measurements/m2b_2026-07/`・`docs/measurements/m2c_2026-07/`・
  `docs/m2_error_model.md`・`tests/test_m2b_committed_record.py`・
  `tests/test_m2c_committed_record.py`・`tests/fixtures/melody_bench/m2c_external_fixtures.yaml`
- 大改修: `tests/test_m2_accuracy_harness.py`（セッション凍結 stub×2 + spy）・
  `.github/workflows/ci.yml`（2 シャード）・`scripts/run_melody_accuracy.py`（external 経路
  +~1900 行）

### Fable 直接実行監査

- M2b run7 / M2c 実測の長時間 run を委譲エージェント孤児化（5 回超）の後始末として
  Fable が直接 background 実行・完了判定した場面が複数（緊急回復ではあるが委譲原則
  からの逸脱。孤児化耐性のある委譲レシピ整備が引き継ぎ課題）。
- #231 doc の最終編集・#229 の squash 整理 + force-with-lease push を直接実行
  （doc 解釈は職務内、git 整理はマイクロ操作超過気味）。

## Handoff

- **M3（旋律比較器）は §7 分岐の User 決裁待ち**: (a) 非前進=代替抽出器待ちのみ /
  (b) 撤回範囲分離の改訂で vocals stem / clean lead 入力限定の先行設計（推奨 (b)）。
- V-fullstack は MedleyDB アクセス申請ゲートで User 律速（未測定と正直記録済み）。
- M1-real 実測（Suno stems）= machine-dependent slow-lane、scorer pin 後続 3 件（P3）は
  STATUS.md queue 参照。
- 委譲運用の教訓: 長時間検証はバックグラウンド化前提で成果物（EXIT 行・結果ファイル）
  判定、孤児化したら SendMessage で同期再実行（CLAUDE.md 既記載を再確認した形）。
- §8 提案 2 件（AGENTS.md への追記は User 決裁待ち・本セッションでは編集していない）:
  (a) 解釈 doc の emit 前チェックリスト（規範的拘束・統計過大主張・未 commit データ依拠・
  被覆過大表記の 4 点検査 — #231 の 11 巡指摘の頻出ジャンル）、
  (b) テストに未来固定時刻を置かない規律（壁時計タイムボム教訓 — #232）。

## Session 2

### Context

M3（旋律比較器）を設計書 `DESIGN_M3_melody_comparator.md`（Cowork 発行・User アップロード）
に従い M3-0〜M3d の全フェーズ実装し、PR #233 として作成→Codex レビュー 21 ラウンド対応→
マージ完了。User 決裁 2026-07-30（選択肢 (b)=§7 撤回範囲の音高系/voicing 系分離・
clean lead 帯限定の M3 先行）を M3-0 で dated 記録し、前セッションの「§7 分岐 User 決裁
待ち」を解消した。

### Design Decisions

- **M3 凍結定数は新規 sibling `m3_comparison_registry.yaml`**（registry.yaml 追記は
  M1-real の hash pin 済み verdict を壊すため。m2_accuracy_bars 前例踏襲）
- **NW 整列は chroma 折返し量子化音程上**（オクターブアーティファクト頑健）。interval 軸=
  折返し一致率・raw は §3.2 ガード専用、contour 軸=raw 由来（真の跳躍方向を折返しで壊さない）
- **evidence 未校正時は "none" + `evidence_thresholds_uncalibrated`**（未校正の計器は
  証拠を出せない、が正直な意味論）。axis_evidence 併記で「割れたまま報告」を実装
- **半音丸めは `floor(x+0.5)`**（偶数丸めは半音境界で移調不変性を破る — Codex 指摘の
  反例 (60.5,61.5) で確認。M3a は未マージ PR 内コードのため凍結違反に当たらず修正可）
- **狙い撃ち negative の軸スコープ化**（cross→全軸 / rhythm→rhythm / interval→
  interval+contour）。全軸投入は保存軸の negative_max を汚染し校正を構造的に不可能に
  していた（第 11 R で tuning 必須化した帰結。放置なら slow-lane 校正が必ず全滅）
- **軌跡 hash は丸めなし `float.hex()`**（M2d の軌跡レベル決定論残課題を正しく閉じる形）
- **レビュー採否の引き締め（第 19 R〜）**: 採用=①設計意味論バグ ②当方対応の回帰
  ③自明修正のみ。改竄耐性ラチェット延長は境界宣言で不採用（M2a 第 28 巡打ち切り前例）。
  不採用 4 件=holdout 永続マーカー（見せかけの保証）/ ギャップ隣接被覆除外（M2d 頑健性
  要件と逆行）/ phrase 境界のテンポ正規化・IOI 量子化境界安定化（凍結値の意味論変更は
  実測が先行条件・M3d 校正分布に折り込まれる）→ docs/melody_comparator.md に dated 記録

### Artifacts

- **PR #233 マージ**: 実装 5 コミット（M3-0/M3a/M3b/M3c/M3d）+ レビュー対応 12 コミット。
  新規: `melody/representation.py`・`alignment.py`・`comparison.py`・
  `scripts/run_melody_comparison.py`・`m3_comparison_registry.yaml`・
  `docs/melody_comparator.md`・`docs/DESIGN_M3_melody_comparator.md`・テスト 4 ファイル
  241 件（全 fake-backend 約 5 秒・TB-30 影響なし）。DESIGN_M2 §7 改訂 + m2_error_model
  決裁記録。M1/M2 凍結値 diff ゼロ維持
- Codex 21 R・47 指摘: 採用 42・部分採用 1・不採用 4（全採用は実コード検証済・false
  positive ゼロ。対応は毎 R PR コメントに明記・スレッド 27+ resolve・不採用は残置）
- **Fable 直接実行監査**: 実装・検証は全て Sonnet 委譲（8 エージェント）。Fable 直接=
  git 状態確認/rebase --reset-author/push・PR 作成・スレッド resolve（22 件一括含む）・
  PR コメント投稿・セルフチェック管理。改善点: レビュースレッド取得（get_review_comments）
  を毎 R 直接実行した（戻り値が重く CLAUDE.md 運用細則では委譲対象。次回は委譲か、
  修正エージェントに取得込みで依頼する）

### Handoff

- **M3d slow-lane 実測が次の実質マイルストーン**（machine-dependent）: 実 crepe +
  vocadito で positive 変形対（±2..5 半音 / rate 0.85–1.15）+ negative（cross/狙い撃ち）
  の manifest を組み、run×2（repeats）→ evaluate（tuning マージン表 + 凍結提案 +
  floor 候補）→ registry 凍結 commit → holdout 一度検証。ハーネスは protected-path・
  pin 検証・holdout ロック・kind×expected 検査等を fail-closed で強制するので、素材と
  重みさえあれば手順どおりで回る。校正まで比較器の evidence は "none"（正直な未校正状態）
- 検証孤児化 2 回発生（環境再起動）→ SendMessage 同期再実行で回復（CLAUDE.md 手順どおり）。
  委譲プロンプトに「フォアグラウンド同期・timeout 明示」を先回りで入れる形が有効だった
- §8 提案（Session 1 の (a)(b) に追加・User 決裁待ち）: (c) レビュー採否の引き締め基準
  （設計意味論バグ/当方回帰/自明修正のみ採用・ラチェット延長は境界宣言で終端）を
  AGENTS.md §8 に一般規律として明文化するか

## Session 3

### Context

リポジトリ整備デー（Claude 完結ルート・Fable 設計専任）。User 依頼=「ドキュメント/コード/テストのリファクタリング」。診断 3 本（docs 冗長性 / radon 複雑性 / テスト効率）を Sonnet 並列委譲 → Fable 判読・設計 → 実装 3 WS 委譲 → PR #234。続いて積み残しの recast 巨大関数分割を PR #235 で完走。締めに User 決定のレビュー運用規律 3 件を AGENTS.md §8 へ恒久化（PR #236・マージ待ち）。

### Design Decisions

- **docs**: README 索引 48 行を `docs/README.md`（新設・カテゴリ別入口）へ移送し README 212→158 行。CLAUDE.md 400 行 cap 張り付きを索引長文セル圧縮で 371 行へ（削減分は移送先 docs に既存照合＝情報消失ゼロ）。README 350 行 cap discipline テスト新設
- **コード**: atomic write 7 並行実装（docstring 自認の意図的複製）を `utils/atomic_io.py` へ集約（既存関数は薄ラッパ化で挙動・import 経路保存）。clamp 直書き 3 箇所置換。**見送り**=sha256 1 行イディオム ~50 箇所（効果薄）・意図的防御コード（provenance/ELF/override 解決）
- **テスト**: 削除対象なし（無意味な重複は不在と診断確定）。slow 付け漏れ 11 件降格（not-slow 2816→2805・約 70 秒短縮・CI は全件実行でカバレッジ不変）。ci.yml の ruff 4 重実行を lint 専用ジョブ化
- **巨大関数分割（#235）**: 構造分析（ブロックマップ+順序制約 20 件超の引用列挙）→ 分割設計 → 逐字ブロック抽出。`build_recast_plan_artifacts` F(57)→C(12) / `recast_ingest_cmd` F(44)→C(13)。核心設計=`_finalize` クロージャ温存+callable 渡し（自由変数 9 個の配線爆発回避）・staging スコープを関数境界で割らない・`typer.Exit(0)` 経路はオーケストレータ残置。tests/ 無改変 green + `recast --help` バイト一致を挙動保存の証拠に
- **#234 Codex 対応（7R 計 8 指摘・全採用）**: atomic_io の契約強化連鎖（protected_inputs 必須化→トラバーサル字句拒否→snapshot prev/ 隔離→payload/prev 二重サブディレクトリ→catch 撤去=常時 BaseException rollback→register-before-syscall）+ architecture.md 欠落補完。**境界宣言 2 件で系統終端**（フラット名契約=containment 一般化拒否 / 非同期例外 rollback=best-effort）。第 3 ラウンドの「構造的に衝突不能」自己判定が第 4 ラウンドで反証された（サブディレクトリ自体が名前空間を占有する見落とし）— 正直に認めて修正

### Artifacts

- **PR #234 merged**: docs スリム化+atomic_io 集約+テスト/CI 効率化（10 commits・+1094/−374）
- **PR #235 merged**: recast 2 巨大関数の挙動保存分割（全 2940 テスト 0 failed・被覆照合完全一致）
- **PR #236 open・監視中**: AGENTS.md §8「Claude 自己 PR のレビュー運用規律」恒久化（User 決定 3 件: ①PR 作成〜subscribe デフォルト化 ②オーバーエンジニアリング回避第一 ③対応済み=返信+resolve 3 点セット）
- 委譲 pytest 自動バックグラウンド化が再発（計 4 回）→ SendMessage 分割同期指示+成果物判定で全回復（§8 レシピどおり）。検証エージェントには「成果物判定を最初から指示に織り込む」形が有効だった

### Handoff

- **PR #236 のマージ確認**（docs のみ・CI green 待ち。マージされたら監視終了）
- 次のリファクタ候補（緊急性なし・価値順）: ① D ランク CLI 3 関数（observe_cmd D30 / _audit_root D28 / recast_init_cmd D25）— #235 レシピ流用可、**該当ファイルへ機能追加が入るタイミングで同梱**が推奨 ② CI シャード実測時間ベース 3–4 分割（テスト 1.5–2 倍時に発動）③ compare_melodies D26 は M3d 校正確定まで凍結
- **Fable 直接実行監査**: レビュー対応中の `grep -n "prev" atomic_io.py`（第 3 指摘の事実確認 1 コール）と AGENTS.md §8 追記の直接編集（micro 逸脱気味・次回は Sonnet 委譲も検討）。他は診断/実装/検証/スレッド操作すべて委譲
