## Session Summary — 2026-08-24

### Context

PR #311（VG-DET0 事前登録ドラフト + VG-DEBT-006 の machine-independent pin／
再生成器）の最終レビュー対応を引き継ぎ、User のマージ完了報告まで閉じた。
本引き継ぎで処理した新規 P1 は 2 件で、いずれも実害基準を満たすため採用した。

- commit 済み Phase B validator が可変な real-render baseline を期待値として再利用し、
  baseline・packaged manifest・report の協調差し替えで偽の `14/14 RESOLVED` を作れる
- `git show 8a14ca97...` に依存する正常系が GitHub Actions の depth-1 checkout では
  固定commit objectを読めず、通常CIとshallow checkoutの再生成を実行不能にする

PR #311 は merge commit `89e5c3e5` で main へ入った。ただし
`VG-DEBT-006` の台帳 status は **open のまま**で、
`reproducibility_reconciliation.status = PENDING_PHASE_B`、real-render acoustic は
`BLOCKED_MISSING_PINNED_ACOUSTIC`。本マージは Phase B 実測完了や負債完済を意味しない。

### Design Decisions

- real-render baseline は単一readし、コード内固定SHAと fixed probe pin の一致を確認後に
  だけparseする。同じparse結果を recovery 検証と14条件再計算へ再利用し、producer／
  committed validator の両側を同じ契約へ束縛した
- 正本・packaged manifest・reportを同時改変して再束縛する負例と、正常な
  producer→validator往復（baseline 1 read）の正例を同じ回帰テストへ固定した
- CIの履歴取得は、D6テストを実行する `test-rest` だけ `fetch-depth: 0` とした。
  履歴不要の11 matrix実行へ広げず、provenance保証とcheckoutコストを分離した
- depth-1 cloneで失敗を再現し、完全履歴取得後に同じ正常系が通ることを確認した。
  discipline testは正常設定とdepth-1退行の両方向を検査する

### Artifacts

- PR #311 マージ（merge `89e5c3e5`、最終head `bd491d16`）
- 主な最終修正: `d6_regenerate.py` の固定baseline検証、
  `test_d6_fixed_probe_pins.py` の協調改変負例、`ci.yml` のtest-rest完全履歴取得、
  `test_ci_foundry_coverage.py` の履歴契約ガード
- ローカル: D6 + discipline **77 passed**、Ruff／JSON・YAML／diff検査成功
- GitHub Actions: 同一headで **13/13 jobs success**、未解決review thread 0、競合なし
- 再レビュー依頼は User 指示に従い発行しなかった

**Fable 直接実行監査**: 該当なし（Codex が実装・検証・GitHub対応を実行）。

### Handoff

- VG-DEBT-006 の次の完了条件は、固定acoustic回収後の Phase B 実走、23証拠成果物の
  publication、commit済み report の再検証、台帳 status 更新。今回の防御はその前提
- 5巡超のレビューで得た教訓はコード・policy文字列・回帰テスト・discipline testへ
  外部化済み。追加の `AGENTS.md`／`CLAUDE.md` 改訂は不要
- `test-rest` が完全履歴を取得できるようになったため、snapshot退場キューの
  「CI履歴基盤」条件は成立した。ただし凍結コピーからnamed commit照合への置換自体は未着手

## Session Summary — 2026-08-24 Session 2

### Context

2026-08-23 Session 2（VG-DEBT-008 export-device probe）の継続セッション。User
実行委任（「では、それを実行してください」）に基づき、PR #312 で境界宣言残置
していた freeze_mode 語彙問題を v1.1 追補裁定 §8 として起草し、PR #314 で
main へ収載した。Codex レビューは 10 巡 = 採用 13 / 見送り 0（うち 1 件部分
採用）で、打ち切り上限ちょうどで収束。マージ後に PR #312 の照会スレッドを
裁定参照つきで resolve し、VG-DEBT-008 フォローアップの照会を全て閉じた。

### Design Decisions

- **マージ済み凍結記録の再分類は参照方式**（第 2 巡採用で確立）: 当初の
  三面協調更新（record 書き換え + 台帳 sha + FROZEN 定数更新、新 sha
  a9056022…）は immutability 規約と User 裁定 C（2026-08-21「確定記録は
  attestation 止まり」）に反すると判定し撤回。凍結バイト（sha 47879666…）を
  復元し、分類の正本 = §8 + debt_adjudication_v1.1.yaml の
  freeze_mode_assignments（shape test 4 本で exact pin）。適用境界
  「三面協調更新はマージ前の記録に限る」を runbook §8・テスト docstring・
  §8 項 4 に規約化（PR #312 の 2 回運用はマージ前で正当と注記）
- **D0 の BLOCKED 表記は誤りと確定**（第 2 巡部分採用）: v1.1 §6 の閉塞条件
  「v1.1 裁定追補が正典に入るまで」は v1.1 収載（PR #305）自体で解消済みで
  freeze_mode 裁定と独立。ただし D0 PASS 宣言は台帳に未記録のため
  「閉塞解除済み・PASS 未宣言」が正（Codex の「passed と記せ」は過剰主張で
  不採用 = 正直会計）
- **追補の provenance はスコープ分離**（第 10 巡）: yaml トップレベルの
  date/adjudicator（2026-08-22・User）を追補キーに継承させず、amendments
  ブロック（2026-08-24・Fable = User 実行委任・adds 2 キー）で機械可読化
- レビュー中盤以降は「一手遅れの整合性」型（会計行・索引・PR 本文の陳腐化）
  に逓減 → 会計同期を同一コミットに含める + ファミリー単位の同語全数掃討
  （裁定待ち残置 / 三面無境界 / stale D0 の 3 系統を終端）で収束させた

### Artifacts

- PR #314 マージ（squash 4c2dc9b）: DEBT_ADJUDICATION_v1.1.md §8 /
  debt_adjudication_v1.1.yaml（vocabulary + assignments + amendments）/
  shape test 4 本 / runbook §8 適用境界 / STATUS・_index・2026-08-23.md の
  supersession 注記
- PR #312 の freeze_mode 照会スレッドを resolve（裁定参照リプライ）

### Handoff

- freeze_mode 裁定は完結。VG-DEBT-008 は accepted_residual 不変・
  reentry_condition (a-1)/(b) は有効のまま
- D0 = 閉塞解除済み・PASS 未宣言。PASS を宣言する場合は台帳側に宣言記録を
  作ってから（STATUS Phase の文言が正）
- **Fable 直接実行監査**: 採否判定・記録起草・PR 本文改訂は職務内。
  マイクロ操作超過の直接実行 = 各巡の修正 edit + 検証 pytest + commit/push
  サイクル（9 コミット、1 巡 3–5 コール。記録文言と修正が不可分なため直接
  実行を選択したが、純機械的な修正巡は Sonnet 委譲が原則どおり）。
  スレッド取得・返信投稿・resolve は全巡 Sonnet 委譲（原則遵守）
