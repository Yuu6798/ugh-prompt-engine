# Session Summary — 2026-04-27

## 修正・訂正

- **AGENTS.md エスカレーション規則のデッドロック**: Codex P1 指摘 — 旧 #4「依存追加 = 即停止」が roadmap_goal1.md の予定依存追加（Q0-4 mir_eval / Q1-1 pyloudnorm / Q2-1 madmom / Q3-1 Demucs）と矛盾。修正: Task Brief に `Allowed Dependencies` フィールド新設、Escalation #4 を「Brief で許可されていない依存追加」に限定（PR #6, commit `0b6e826`）
- **PR #7 mojibake**: parser.py に `ﾎ忍` `窶｢` という Shift-JIS 誤解釈の痕跡が混入していた（おそらく Windows 編集環境由来）。レビューで P1 として指摘 → Codex 修正後マージ
- **librosa 接続認識ミス（ユーザー側）**: 「librosa が接続されていない」という前提でディスカッション開始したが、実際は physical_features.py で 9 種類の librosa API が深く統合済み。ユーザーが認識ミスを認め、調査範囲を「Essentia/madmom/Demucs 等の未統合ライブラリ」に再フォーカス
- **Q0-1 F# minor 和音**: Codex 初版で `F#-A-C` を生成 → diminished で minor ではない。レビューで `F#-A-C#` に補正（Codex セルフレビュー）
- **Codex Summary のファイル数誤り（PR #7）**: "7 ファイル変更" と記載されていたが実際は 9 ファイル（render_yaml.py, parser.py の変更が summary から漏れていた）

## 成功パターン

- **設計と実装の役割分担**: Claude（Opus）が設計・レビュー、Codex が実装、User がループのトリガーという 3 者オーケストレーションを CLAUDE.md の Workflow 節 + AGENTS.md として明文化。Task Brief / Completion Summary の固定フォーマットでメッセージ伝達を標準化
- **API stream timeout 対策**: 大きなマルチファイル PR（5 ファイル）を 1 commit で push しようとして stream idle timeout 発生 → 1 ファイルずつ create_or_update_file で commit する方式に切り替えると安定
- **PR レビューの深掘り**: PR #7 の mojibake は単なる pattern 確認だけでは見逃すレベル。diff 全体を逐行で読むことで P1 を捕捉できた
- **壁打ち → Brief 化のループ**: SVP schema 共通/固有切り分けの壁打ち分析が、後段の DomainProfile 実装（PR #7）と Evidence-bearing Semantic 提案の両方に直接効いた
- **Codex を信頼しつつ独立検証**: PR #9 の Codex 自己レビュー（F# minor 和音修正、metadata mismatch 検出強化）は実装者の自己監査として機能し、Claude の二次レビュー負荷を軽減
- **roadmap_goal1.md の各 Q-ID を独立 Brief に分解**: 各フェーズの Acceptance Criteria を Brief にそのまま転記できる粒度で書いてあり、Codex への引き渡しがスムーズ

## 設計判断

- **Workflow 役割分担を CLAUDE.md に明文化**: Claude × Codex × User の 3 者循環を「Advisor Strategy」直後に新節として配置。「Claude は本リポジトリでコードを書かない」原則を明示（docs/CLAUDE.md/AGENTS.md/設計仕様の作成は可）
- **AGENTS.md は root に配置**: Codex が読む前提で慣習に従い root へ。CLAUDE.md (Claude 用) と AGENTS.md (両方の通信プロトコル) で関心分離
- **Task Brief / Completion Summary の固定フォーマット**: 各セクション必須化。Phase / Goal / Acceptance Criteria / Scope / Allowed Dependencies / Implementation Hints / Required Outputs / Done When の 8 セクション
- **roadmap_goal1.md (Q0–Q5) を目的軸ロードマップとして新設**: 既存 roadmap.md (PoC/Pre-prototype 段階軸) と相補。「目的1 完成」を 5 条件で定義（ground-truth 検証 / 業界標準指標 / per-stem 観測 / ジャンル別 baseline / 検証データセット）
- **Allowed Dependencies フィールド追加**: roadmap で予定された依存追加（mir_eval/pyloudnorm/madmom/Demucs）が escalation で止まらないよう Brief で事前許可する仕組み
- **Q4 を「Evidence-bearing Semantic Layer」に置換予定（保留中）**: GPT-5.5pro 提案を評価。perceptual / structural / semantic_hypothesis / cultural_context の 4 層分離 + evidence + confidence の設計は本質的。ただし破壊的変更で PR #7 マージ後でないと衝突する。User 判断でプロンプト保存 → 着手は後回し
- **synth サンプル音源の真値スキーマ**: `ground_truth.yaml` に BPM / key / mode / 拍子 / duration / 構造境界 / brightness band / SHA-256 を併記。`--verify` でハッシュ + metadata 両方の改竄を検出

## 未解決課題

- **PR #8 (本日の roadmap Q4 改訂 PR、もし作っていたら) の処遇**: 実際には User が「Q4 は保留、ロードマップ通り進める」と判断したため、roadmap_goal1.md の Q4 を Evidence-bearing 版に更新する PR は **作成していない**。後段で着手する場合は別 PR で
- **GPT-5.5pro の Evidence-bearing Semantic Layer Brief**: User 側で保存。PR #7 マージ後 + Q3 完了後あたりで再浮上する想定
- **CC0 実音源サンプルの追加**: Q0-1 では合成サイン波のみ対象。CC0 音源は別 Brief で扱う予定（roadmap_goal1.md Q0-1 の補完タスク）
- **次の Brief: Q0-2 / Q0-3**: 本日追加した 5 WAV を使って expected_output JSON 生成 + snapshot test。これが次回の最初の作業
- **PhysicalDiff 命名のクロスドメイン違和感**: `domain="video"` で `PhysicalDiff` を返すのは命名上の歪み。将来 `MetricsDiff` 等への rename 余地（P3、follow-up）
- **DomainProfile の `_format_if_complete` silent failure**: template フィールド欠損時に何も表示しないため、デバッグが困難になる可能性。debug log 追加の余地（P3）

## CLAUDE.md 更新候補

- **特に追加なし**。今日のセッションで CLAUDE.md は Workflow 節追加 + 索引更新で既に肥大化方向。これ以上の追記は管理ポリシーに反する
- 余地があるとすれば「Codex PR レビュー時の観点リスト」（mojibake / encoding 検査、ファイル数の summary 整合確認、validator の失敗パステスト等）。ただしこれは AGENTS.md 側に記述する方が自然

## 本日の主要 PR

- **PR #6** (`claude/japanese-greeting-BF7XW`) ✅ マージ済 — AGENTS.md / Workflow / roadmap_goal1.md / 索引同期
- **PR #7** (`codex/svp-domain-profiles`) ✅ マージ済 — DomainProfile 抽象化（mojibake fix 後）
- **PR #9** (`codex/q0-1-synth-samples`) ✅ マージ済 — Q0-1 完了、tests 79 → 110

## 進行状況

- roadmap_goal1.md Q0-1: ✅ 完了
- Q0-2 / Q0-3 / Q0-4 / Q0-5: 未着手（次回以降）
- Q1 以降: 未着手
- Q4 (Evidence-bearing 版に置換予定): 設計案あり、保留中
