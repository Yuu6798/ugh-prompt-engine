## Session Summary — 2026-08-28

### Context

RUN10（`VG-R10-AQUEST-VG-PHENOTYPE-COMPATIBILITY`）の実装着手。User の
「Google ドライブの Run10 フォルダを確認して実装実行に着手してほしい」から
開始し、2026-08-27 15:49Z 〜 2026-08-28 にまたがる（08-25〜08-27 の
session log は存在しない）。

RUN10 は AquesTalk 由来 UTAU デフォルト音声（A0）を未知標本として観測し、
VoiceGenesis 形質体系がどこまで説明できるかを監査する Run。本セッションは
**測定を一切行わず**、測定を始める前に満たすべき契約・公開境界・凍結検証
だけを機械化した（§29 実行順の手順 2/3/5 + 手順 6 第 1 段）。

PR **#330 マージ済み**（`1404635`・25 commits・+7,644）、PR **#332 open**
（フォローアップ）。

### Design Decisions

**R10-PUB-1（公開境界）**: 本リポジトリが public であることに気づいた時点で
黙って進めず §33 の User 裁定に上げた。裁定 = `APPROVED_CODE_ONLY_PUBLIC`
（実装コードのみ public）。後日 User 再確認「UTAU デフォルト音源は個人非公開
でのみ分析や抽出、合成を許されてる。リポジトリに乗せるのはダメだ」を受け、
同じ保守的姿勢を VG 側の Evolution Theory v0.3 本体にも適用した。

**A0 は生成できない**（User 質問への回答）。合成すれば比較が VG 対 VG になり、
それは §12.3 / H10 / Outcome C がまさに検出対象とする measurement overfit
そのもの。§7.1 の provenance pin と §32 Stop Rule 3 により合成代替は不可。

**Gate を捏造しない**。R10-G0 / G1 / G2 はいずれも BLOCKED のまま。PASS を
作れる材料（A0 実体・AF01 bundle・設計文書実体・cost cap 裁定）が無いことを
3 値（PRESENT / ABSENT / UNRESOLVED）で正直に記録する方式に統一した。

**bot レビューの終端方針**。Codex 24 巡（#330）+ 2 巡（#332）で、同型の欠陥が
繰り返し出た。個別修正をやめてファミリー単位で終端する判断を 2 回行った:

1. **「開いたキー集合」ファミリー**（第 12/13/15/17/19/20 巡＝6 度再発）→
   検証で降りる mapping を `SHAPE`（キーが設計の固定語彙 → 未知キー拒否）と
   `INDEX`（キーが trait id 等のデータ識別子 → 閉じられない。行を SHAPE として
   閉じる）に分類し `MAPPING_CLOSURE_INVENTORY` へ全数登録。**閉世界化の正しい
   形は「全部閉じる」ではなく「種別を宣言し SHAPE だけ閉じる」**（INDEX を
   閉じると測定結果が記録できなくなる）。
2. **「宣言されたが適用されていない検証器」ファミリー**（第 4/22/23 巡）→
   `tests/test_enforcement_wiring.py` で ast 静的解析（走査範囲は非テスト .py
   全数発見・到達可能性は公開関数起点の呼び出しグラフ）。例外は理由付き登録制。

**#332 第 2 巡で収束しないと判定**。3 件とも監査の解析精度への指摘で、完全な
呼び出しグラフ解析器を作る方向ではなく **主張を実態まで狭める**方を採った
（指摘自身が代替として挙げた選択肢）。`AUDIT_LIMITATIONS` に再入条件付きで
3 件の限界を宣言。**PR #330 第 23 巡の欠陥（private 述語の分岐単位の適用漏れ）
は本監査の範囲外**であることを明記した — 動機に挙げながら捕捉できない、という
指摘は正しい。

**pin の出所を 1 箇所に**（第 18 巡）。Evolution Theory の digest をモジュール
定数と契約 YAML の 2 箇所に持つ設計だったため、乖離すると R10-G0 が片方を検証
しながら R10-G2 が別バイトを PRESENT と書ける。定数を廃止し契約を唯一の出所に
した。**乖離の「検出」ではなく乖離し得る状態そのものを無くす**方向で終端。

### Artifacts

**PR #330（マージ済み）** — RUN10 Phase 0 スキャフォールド:

- `run10_schema.py` — §23 契約の fail-closed ローダ / §21 Gate 登録簿
  R10-G0..G22 / §27 results schema / 閉世界表群
- `private_boundary.py` — §2.2/§24/§26 の公開境界。**閉世界 allowlist**
  （公開してよいものを列挙。`PUBLISHABLE_SUFFIXES = (".py",)` のみ）
- `af01_freeze_verifier.py` — 台帳自己検証 / bundle 実体照合 / 決定論 replay
- `pre_run/build_pre_run_inventory.py` — R10-G2 を 3 値で記録
- `RUN10_CONTRACT.yaml` / `inputs/` / `tests/`（4 ファイル）

**実測で PASS した唯一の結果**: AF01 v1.0 payload ledger 検証 15/15 checks
（同梱台帳 8,025 bytes の実バイト sha256 が凍結値と一致・101 エントリの構造
一致・canonical 4 点一致）。

**§29 手順 5 完了**: User が Drive の複製 2 件を raw 照合（25,119 bytes・
IDENTICAL）→ `vg_evolution_theory_ref_sha =
87f4208ffdd213099977c4b5a1ee5d06852524036c818e4b14ce6b0e355b2e93` を PINNED。
CORE pin 未充足 37 → 36 件。

**PR #332（open）** — 検証器配線監査 + `CONTRACT_STAGES` の二重管理解消
（監査自身が検出した）。

**Codex レビュー**: #330 = 24 巡 / 採用 55 / 見送り 1（第 8 巡 TOCTOU の脅威
モデル境界）。#332 = 2 巡 / 採用 3 / 主張の縮小 2。

**Fable 直接実行監査**: 本セッションは Fable 非稼働（Opus 代行）。かつ
システムプロンプトが AgentTool の使用を明示的に禁じていたため、実装・実行・
検証・Drive 調査のすべてを直接実行した。CLAUDE.md § Advisor Strategy の
委譲原則は本セッションでは適用不能だった（環境制約であり判断の逸脱ではない）。

### Handoff

**次セッションへの引き渡し = 依頼書を発行済み**（会話内に Design Memo 形式で
出力。`codex/run10-a0-manifest` 想定）。内容:

1. `pre_run/build_a0_manifest.py` の新規実装（§7.1 pin 材料を決定論 JSON へ。
   **private staging へのみ書き出す**）— この道具が未実装なのが §29 手順 4 の
   律速
2. 実体照合の実行（手順 4 / 6 第 2 段 / 7）— 実体を持つ環境でのみ成立

**User 確認済み実体（2026-08-28）**: A0 ZIP 3,700,398 bytes /
`e247ec34...c34f`、AF01 bundle 8,955,391 bytes / `c69ac23c...ae90`、
設計文書 v0.4 95,709 bytes / `cc05f025...0957`（凍結値と一致）。保管は
owner-only Drive。**A0 は「User 供給待ち」ではなく取得済み・inventory と
hash freeze が未実行**。

**依頼書に書いた 2 つの罠**（どちらも自分で作り込んだ自己検査が反応する）:
(1) 追跡中の `pre_run/inventory.json` を資産つきで上書きすると
`test_committed_inventory_matches_current_generation` が落ちる。資産つきの
実行は必ず `--out <staging>` へ。(2) `assert_private_staging_path` を配線
したら `UNWIRED_REGISTRY` から行を消す（消し忘れると陳腐化検査が落ちる）。

**User 裁定待ち**: `cost_cap` 4 欄（`cpu_hours_max` / `gpu_hours_max` /
`storage_bytes_max` / `render_count_max`）。有限の正数のみ。R10-G0 の律速。

**着手していないもの**: §11 measurement family M0–M6。測定器の定義は正本設計
文書の内側にあり、その文書はリポジトリに置かない裁定のため、仕様を持たずに
実装すると存在しない測定契約を発明することになる。**設計文書を読める実行者
（User / Codex）の担当**。

**PR #332 は unsubscribe 済み**（User 指示「修正して自分でレビュー呼ぶ巡目が
2 連続したら解除」の条件成立）。未解決スレッド 2 件は境界宣言つきで意図的に
残置。

## Session Summary — 2026-08-28 [Session 2]

### Context

RUN9（`VG-R9-DUAL-FOUNDER-PJS`）Claude 完結ルートの継続セッション（08-27〜08-28、
コンテキスト圧縮 2 回・コンテナ再起動 2 回を跨いで完走）。この窓で User 裁定 4 件を
受領し、PR 3 本をマージした:

1. **PJS 再取得 / Lesson Channel Freeze 裁定**（08-27）→ HARNESS-3b（PR #329、
   10 巡・採用 24+2/見送り 1）で education_technique_lesson_manifest_sha PINNED。
2. **Learning Recipe 残 5 キー裁定**（08-27）→ HARNESS-3c(1/2)（PR #331、13 巡・
   採用 26/見送り 0）で 5 manifest（score_axis_catalog / loss_evaluator /
   candidate_generation / compute_budget / learning_data_binding）PINNED。
3. **Identity Calibration Degeneracy / rev 0.6 裁定**（08-27）→ HARNESS-3c(2)
   （PR #333、16 巡・採用 30/見送り 0）で identity_decision_protocol_v0.6.json 発行・
   hypothesis_algebra_sha PINNED・design_revision 0.5→0.6 昇格。
4. **PRACTICE Alignment / Teacher Annotation Boundary 裁定**（08-28）→ 第 3 PR は
   未着手（W2 実測エージェントを起動したが User 操作で停止 → 指示待ちのまま終了）。

### Design Decisions

- **calibration scale の粒度原理**（PR #331 第 6 巡）: scale の標本粒度 = 当該
  channel の loss に入る原子残差標本の粒度。relative_f0/normalized_energy を frame
  プールから mora 粒度へ切替（エージェントが矛盾を flag し Fable が判定した好例）。
- **候補生成の決定論意味論の完全凍結**（#331）: 正準候補列 L・digest 写像・決定論
  プロービング・NO_BEST sentinel（None=恒等との多義性排除）・NO_SCORABLE_CANDIDATE
  終端・exploratory の適用範囲 2 項列挙・note_count 三者一致検査。
- **rev 0.6 protocol の判定構造**（#333 レビューで完成）: 二層 outcome（既存 frozen
  tuple 無改変 + IDENTITY_PROTOCOL_* detail 層）/ Birth Gate = identity_establishment
  （validity ∧ d12>0 有限 ∧ PJS>0 の連言）と birth_gate_overall_pass（+ C0/C1/
  positive の停止条件非該当 + 両 Founder 分の closed-world 完了証跡）の**会計分離**/
  重複述語対の優先順・値域被覆（non-finite d12/PJS 含む）のファミリー全数掃討。
- **supersede の範囲限定**: 裁定 §7 の supersede は閾値推定・判定意味論のみ。bridge が
  消費する生成定義は preserved_generation_definitions の閉包列挙で有効維持（bridge 消費
  path ⊆ 列挙の cross-document 閉包検査）。
- **bot レビュー運用の実績と学習**: 上限 10 巡到達後は 3 分類（実コード被害/将来汚染/
  致命的バグ）の新規具体経路のみ採用で #333 は 16 巡まで全採用が続いた（毎巡実欠陥）。
  第 4 巡実装が排他 conjunct という実行不能バグを自ら導入 → 以後フェーズ 1 に「文字通り
  消費者シミュレーション」の敵対的自己検査を必須化して有効だった。**User 裁定（08-28）:
  巡目 20 到達で subscribe 停止・残レビューはフォローアップ PR 送り**（#333 は 16 巡で
  レビュー収束・マージにより発動せず。将来 PR の standing rule として CLAUDE.md への
  収載を提案中）。
- **speaker map の design_revision 据え置き**: 裁定 §7 の不変対象のため 0.5 のまま
  （四点同期規約の適用条件をコメントで明文化 — 機械追随は裁定違反になる事例）。

### Artifacts

- **PR #329**（HARNESS-3b education lesson、merged 08-27）/ **PR #331**（HARNESS-3c
  5 manifest、merged 08-27）/ **PR #333**（rev 0.6 事前登録、merged 08-28、
  run9 テスト 2402→2751 件）。
- pre-run PENDING: 8→**6 件**（education + hypothesis_algebra が PINNED。新設
  hypothesis_threshold_calibration_sha が PENDING 追加で実質 7 欄。残 = attempt_id /
  repository_commit_sha / config_sha / dependency_pins_sha / learning_recipe_sha /
  measurement_spec_sha / hypothesis_threshold_calibration_sha）。
- **Fable 直接実行監査**: 委譲原則は概ね維持（実装/検証/返信投稿は全て Sonnet 委譲、
  フェーズ 1→Fable 検分→commit/push→フェーズ 2 の 2 フェーズ運用を PR 3 本 39 巡で
  反復）。Fable 直接実行はマイクロ操作の範囲内（sha 連鎖検分スニペット・git commit/
  push・PR 作成・トリガー管理・speaker map コメント 1 箇所の Edit）。逸脱なし。

### Handoff

1. **第 3 PR = PRACTICE Alignment 裁定の実装が次タスク**。設計正本はセッション
   scratchpad の h3c_practice_alignment_design_spec.md（揮発）— 要旨: W2 実測
   （prototype DP alignment + score projection 生成器の決定論/成立性を合成 fixture +
   training 数曲 smoke で確認、.lab はアルゴリズム入力禁止・参考比較のみ）→
   practice_alignment_spec_v1 / practice_actor_input_manifest_v1 /
   practice_audit_annotation_manifest_v1 新設 + loss_evaluator / learning_data_binding
   改訂 + .lab fail-closed ガード + contract 新 pin 3 欄。**W2 エージェントは起動直後に
   User 操作で停止された — 再開はユウさんの指示待ち**（進め方に変更希望の可能性）。
2. 第 3 PR 完了 + Birth Gate PASS（rev 0.6 規約）で learning_recipe_sha build/PIN →
   LEARN_PERFORMANCE へ（裁定 §8 の実行順）。
3. PR #331 の PRACTICE alignment スレッドは裁定受領済み・PR マージ済みのため、第 3 PR
   完了時に当該スレッドへ対応返信 + resolve する（未了）。
4. 裁定逐語 4 件のうち PJS freeze / 5keys / rev 0.6 は repo 収載済み。**PRACTICE
   Alignment 裁定は repo 未収載**（第 3 PR で USER_ADJUDICATION_20260828_PRACTICE_
   ALIGNMENT.txt として収載予定）— 逐語を下記 Appendix に保全。

### Appendix — User 裁定逐語（PRACTICE Alignment / Teacher Annotation Boundary、2026-08-28）

> 【RUN9 User裁定 — PRACTICE Alignment / Teacher Annotation Boundary】
>
> 選択肢Aを採用する。
>
> PRACTICE_FROM_AUDIOのlearner / Founder-local actorへ、PJSの .lab または
> そこから導出された教師演奏の個別境界情報を入力してはならない。
>
> PRACTICEで許可する教師側入力は、
> - training raw audio
> - 教師Performanceから導出されていないscore contextの凍結projection
> のみとする。
>
> score contextはmora順序、mora数、nominal duration ratio、phrase grouping、
> lyrics/phoneme列、nominal pitchに限定する。
>
> 1. Founder-local alignment
> 教師音声のmora/phrase境界は、Founder-local actorがraw audioとscore contextから
> 自律推定する。決定論的alignment algorithmについて、入力特徴、cost function、
> constraints、tie-break、failure policyを学習開始前にpractice_alignment_spec_v1
> として凍結する。.lab fallback、外部alignment、teacher-derived boundary、
> random fallbackを禁止する。
>
> 2. Mandatory 5 channel
> PRACTICEでも以下5 channelをすべて測定する。
> - relative F0 contour
> - note/mora duration ratio
> - phrase-normalized energy envelope
> - attack timing
> - phrase-end timing
> これらはFounder-localに推定した境界から算出する。alignment不能時は
> ALIGNMENT_FAILED / NOT_SCORABLEとして記録し、ゼロ補完、推測補完、予算追加を
> 行わない。
>
> 3. PRACTICE / EDUCATION非対称
> PRACTICE: raw audioからFounder自身が境界・特徴・差分を推定する。
> EDUCATION: .lab整列済みの凍結Technique Lessonを受け取る。
> 両者のreference精度差は欠陥ではなく、PRACTICEとEDUCATIONの情報量差という
> 実験変数として記録する。
>
> 4. 検索と監査の分離
> PRACTICE検索中はFounder-local推定値だけを用いる。r_practice凍結後の共通外部
> 監査では、.labをmeasurement-onlyの基準として利用してよい。ただし監査結果を
> 学習、候補選択、追加trial、ControlProfile更新、閾値・重み・探索範囲の変更へ
> 戻してはならない。
>
> 5. Data binding
> 既存pjs_consumed_inputs_manifest_shaは上流provenanceとして維持するが、その
> 全内容をPRACTICE actorの検索入力として公開してはならない。新たに、
> - practice_actor_input_manifest_v1
> - practice_audit_annotation_manifest_v1
> を分離する。practice_actor_input_manifestにはWAVとsanitized score projection
> だけを含める。practice_audit_annotation_manifestの.labは
> POST_FREEZE_AUDIT_ONLYとする。builder / learnerが.labを開こうとした場合は
> 学習開始前にfail-closedで停止する。
>
> 6. Freeze
> loss_evaluator_spec_v1、learning_data_binding_manifest_v1、
> practice_alignment_spec_v1、practice_actor_input_manifest_v1、
> 関連validator / loaderを再凍結するまでlearning_recipe_shaをPINNEDにしない。
> 本裁定は既存PoRの入力境界を実装へ適用する訂正であり、新しい学習channelや
> 実験目的を追加するものではない。

（同日 User 指示: 「巡目が20に到達したらサブスクライブ停止。残レビューを
フォローアッププルリクエストとして後日対応に回してください」— #333 では未発動。
将来 PR の運用規則として扱う）
