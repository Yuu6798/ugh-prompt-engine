# STATUS.md — ugh-prompt-engine プロジェクト状況

## Phase

2026-07-09（Session 2・Opus 代行＝Fable レートリミット）、**壊れた先行セッションを引き継ぎ PR #164 を Codex 9 レビューラウンド全 P2 対応で完走マージ**（merge b849cc9）。核=**K2-seg Exclude 欄併用追試の fixture 収載**: 先行セッション判読済みの事前登録比較 2 本（比較1 `calm_avoid_excl` vs `calm_avoid` d=−1.66 / 比較2 vs `calm` d=+1.64）の生データは scratchpad 消失していたが、**ユーザー再アップロードの実音源 4 本を再抽出→判読値と完全一致**（centroid 2794.315 / R2-2f 候補対 161.5/234.91 / key F#m 2:Am 2）で同定確定し fixture 化。P2 対応: **① expected_sign** を観測方向 +1 から事前登録極性 −1 へ戻し dead 化（net-effect 失敗が classify_grip 上 tight に化ける自己成就的誤りを是正）。**② semantic.core config 撤回**（アーキ的に重く AskUserQuestion で User 決裁）=#162 SEM-1 の loose 入場は grip がカスタムモデル実測（stock 未検証）なのに external→suno 解決で stock 全 render の truncation 優先度を変えていた→「実測なき横展開をしない」規律で config 反映撤回（測定値は docs/fixture 保持・再入場は stock 検証 or model-scope 後）。**③〜⑦ cross-batch 交絡**（本セッションの核心的教訓）=Exclude 追試は excl セル（モデル未確認ブラウザ生成）と baseline（batch-1 流用）を跨ぐため d=−1.66/+1.64 を Exclude 効果と generator/model drift に分離不能→§7.6「解消」を「未確定」へ格下げし全 docs（比較表/caveat・§7.6・control_profile.md・README・excl_plan.yaml・**tests/test_grip.py docstring**・PR 本文）で断定を交絡明記 or batch-1 論拠へ統一。比較2 の dead も交絡（前回「交絡非影響」は誤り）。batch-2 structure 設計も同型交絡のため**同一バッチ low+high 両生成の隔離設計を primary**へ是正。**omit_body_negative(#163) は batch-1 内 attractor（同一モデル d=+4.03）に立つため交絡と独立して無傷**。運用: Sonnet ~13 委譲＋許可後 Opus 直接 Edit 3 箇所。プロセス知見: **ハーネスが同期実行文言下でも長時間 pytest を自動バックグラウンド化**（#158 以降の事故の真因はハーネス変換の可能性大）。先行同日成果（STATUS 参照）: #162（K2-seg バッチ 1・Avoid=attractor d=+4.03）/ #163（omit_body_negative suno 反映）/ #161（07-08 政策恒久化）。active_rate / valley_depth / time_signature / structure はバッチ 2 繰越。残＝全て人手・実音源律速。

## Next-Issue Queue

| ID | Title | Priority | Notes |
|---|---|---|---|
| DD-D 解除（意味フィールド write-through） | CLAP 読みの semantic.* 自動記入解禁の是非 | P3 | 軸バッテリー本体は #151 で v1.1 closeout（probe チューニング第一巡・7 軸・sweep 手順制度化＝追加軸は config 追記+`--axes-config` sweep+校正ログ再生成のみ）。write-through 解禁は promotion gate G1-G5 + 校正基準文書化が前提（score_centric §5）。valence 軸の本来検証（セクション別感情アーク）は実制作音源律速 |
| K2-seg Suno 転移 | プロンプト欄 grip 地図の Suno 側検証 + 本文 Avoid 検証 | P2 | **MusicGen 側は #152 で実測済み**（5 欄地図・Avoid=attractor d=+1.10）。**バッチ 1（avoid+core）は #162 で closeout**（attractor d=+4.03・omit_body_negative suno 波及は #163）。**Exclude 欄併用追試は #164 で fixture 収載したが cross-batch 交絡で未確定**（excl=モデル未確認ブラウザ生成 × baseline=batch-1 流用→Exclude 効果と generator/model drift 分離不能）＝Exclude-channel 単独 grip の確定には**同一モデルで excl と baseline を揃えた isolated 追試**が要。**semantic.core config 反映は #164 で撤回**（custom-model 実測の stock グローバル適用回避・再入場は stock 検証 or model-scope 後）。残=バッチ 2: active_rate / valley_depth / time_signature / structure。**structure は #164 で同一バッチ low+high 両生成の隔離設計に是正済み**（batch-1 calm 再利用の経済化は confounded pre-mark 必須・センサー設計は match_rate 規約済）。律速=Suno 人手生成バッチ |
| lyrics センサー実音源 E2E | vocals 分離込み実 E2E + committed fixture | P2 | **機械配線は #149 で完走**（`extract --lyrics`/`lyrics-adherence`・schema 1.2・全チェーン MIT 実確認）。実推論はフルミックス経路のみ検証済（検証環境に ffmpeg なし）。残＝歌入り実音源での Demucs 分離込み E2E、real fixture 化、順序診断の実測（反復歌詞の同点限界の実挙動確認）。律速=実音源 |
| v2 ブロック Drive 化 | #159 の 8 音源 drive_file_id 回収 | P3 | lyrics_symmetric_block_2026-07-08.yaml / CLAP v2 manifest は drive_file_id 未取得のまま merged（捏造せず省略・follow-up 明記済み）。#94 方式（SendUserFile→Drive→search 回収・byte/sha 照合）で追記。律速=人間作業 |
| K3-2b フル Suno 行列 | Suno 実測でフル直交性行列 | P2 | **設計指示 (a)(b)(c) と計器は #137 の MusicGen 行列で実証済み**（ノイズ天井・known_dead 行・key センサー化、controllability_poc.md §5.5）。Suno 実測が揃えば bpm→centroid 符号反転=機種依存の裁定と cross_couplings の unresolved 昇格が可能。律速=Suno 人手生成バッチ（5 ノブ×2×R≥8） |
| Q1-5 Ph2 | spectral 帯域 magnitude 再校正(残) | P3 | **大半 closeout 済**: brightness=B-3 / `high_ratio==0.0` 前提見直し=#107 / low/mid_ratio 据え置き finding=#108(low はゲートで健全・mid は未発火で繰越・general アンカー欠如)。残=`screen_2026-06-16.yaml` 再採取 + mid_ratio/magnitude 軸の bias 検証(Phase C と統合・licensing 律速)。**BPM**: 自動化は #155 R2-2f で closeout（3:2 帯 (1.35,1.6) 限定の prior-disagreement 検出を extractor 配線）。残=実音源 live E2E（punk 22050+start_bpm=180 の同条件実測、R1-audio 律速）と窓外の未観測比（screener 診断で継続監視） |
| Genre Calib seed follow-up | EDM 5本の Drive 化 + Suno prompt 転記 | P3 | #103 で measured はインライン保全済。残=`electronic_dance_*` の Drive アップロード→`drive_file_id` 回収 + `prompt:` 本文の PENDING 解消。律速=人間作業 |
| Genre Calib 4th genre | acoustic へ域拡張 | P3 | acoustic(low_ratio<0.4 の general 落ち領域を突く coverage 拡張型)を rock(#105)と同じ協働フロー(物理層明示プロンプト→user 生成→抽出→分離分析→ルール反映)で追加。律速=人間生成バッチ |
| 意味層トラック lyrics 残 | 歌詞=意味層アンカー検証続行 + lyrics_presence 昇格セル埋め | P2 | **(b) control_profile ノブは SEM-1(#126) で実装済み**（`SemanticLayer.lyrics_presence`・loose 固定・昇格ゲート制度化=docs/control_profile.md）。残: (a) 検証デザイン B/C=同一アレンジ×【歌詞なし/原曲無関係歌詞/原曲由来歌詞】で「原曲寄り度」を人間評価×物理乖離(dynamic_range は #124 で棄却済につき別指標を探す)。(c) は **#159 で closeout**: mid_ratio 昇格は両ジャンル棄却が確定（absent alt 込み n≥2×2 実測）・センサーは CLAP vocal contrast へ DD-4 改訂済み（tight 昇格は条件 2 formal 充足を経て **#160 で config 反映済み**＝SEM-1 昇格ゲート初完走）。**dynamic_range 再検証はしない**(棄却済)・**「ボーカル=主音の錨」は棄却済**。**新展開(07-05 #149)**: 歌詞転写＋検収は `extract --lyrics`/`lyrics-adherence` として制度化済み＝検証デザイン B/C の「原曲由来歌詞」素材の機械生成・機械検収が CLI で可能に。律速=人間生成バッチ+主観評価 |
| 発注書生成スクリプト再作成 | scratchpad 消失分の再作成（必要時のみ） | P3 | 旧「neutral ラベル化の本体還元」行の残置項目（本体は #156 で closeout）。Suno カバー発注書生成スクリプトは scratchpad 消失につき、次に必要になった時点で再作成 |

## Recently Merged

| PR | Title | Date | Phase |
|---|---|---|---|
| #164 | feat(control): K2-seg Exclude 欄併用追試の fixture 収載 + semantic.core config 撤回 + cross-batch 交絡格下げ（Codex 9 ラウンド全 P2）。再アップロード実音源 4 本の再抽出が判読値と完全一致（centroid 2794.315 / d=−1.657/+1.643 / R2-2f 候補対 161.5/234.91）で excl_01–04 同定→fixture 化。① expected_sign を dead 是正（net-effect 失敗の classify tight 化を防止）/ ② semantic.core 撤回（AskUserQuestion で User 決裁・custom-model→stock 外挿回避・実測値は docs/fixture 保持）/ ③〜⑦ Exclude 追試の cross-batch 交絡を全 docs+test docstring+PR 本文で「未確定」へ格下げ（omit_body_negative は batch-1 内 attractor 同一モデル d=+4.03 で無傷）+ structure 設計を同一バッチ隔離へ是正。全件 1070 passed | 2026-07-09 | 制御トラック K2-seg Exclude 追試 + 交絡格下げ |
| #163 | feat(compose): suno backend の本文 Avoid 送出を停止（`omit_body_negative=True`・external/musicgen 不変・negative_tags=Exclude チャネル維持）— #162 attractor d=+4.03 の事前登録裁定を反映（#153 musicgen の対）。Codex P2×1「除外要求の silent loss」採用: compose text 出力で negative_tags を stderr 可視化（#128 advisory 経路・チャネル名ラベル付き・stdout/JSON 不変）+CLI テスト 4 件。933 passed | 2026-07-09 | 制御トラック K2-seg follow-up |
| #162 | feat(control): K2-seg Suno 転移バッチ 1 — 実 Suno 12 曲（カスタムモデル・Exclude 空）で本文 Avoid=attractor d=+4.03 確定（→omit_body_negative suno 波及裁定）・core=物理 loose(+0.23)×CLAP tight(+2.45)。fixture k2_suno_segments/（schema 1.1・sha256 インライン・honesty a–g）+snapshot テスト 2 本+docs 2 本。検算照合ゲート 3/3 通過・926 passed。副次: R2-2f live 初発火・key 相対調化・core→centroid 交差結合 | 2026-07-09 | 制御トラック K2-seg Suno 転移 |
| #161 | docs: 委譲同期実行文言の追補（timeout は Bash ツールの timeout パラメータで明示・シェル `timeout` ラッパ代用不可）+ 孤児化回復手順（SendMessage 同期再実行）を CLAUDE.md 運用細則へ恒久化 + AGENTS.md §8 に「計測系委譲の検算照合ゲート（設計値の独立検算）」新設。400 行 cap は Agent spawn コード例のプロース化で捻出（399/400 行）。ruff+discipline 12 passed・Codex 指摘ゼロ | 2026-07-09 | プロセス規律（07-08 Handoff 政策 closeout） |
| #160 | feat(compose): lyrics_presence を tight 昇格 — DD-4 条件 2（K3 干渉分離）を事前登録規約で formal 充足（CLAP: present 干渉 0.0142<ノイズ 0.0320 / absent 0.0343<0.0826・効果は干渉の 4.0–10.9×。対照 mid_ratio は干渉 0.0195≥効果で不成立=棄却裏付け）。SEM-1 昇格ゲート（#126 制度化）の初完走。suno device profile（二重コピー同期）+ midnight_signal example を tight/clap_vocal_contrast へ。回帰は昇格直接帰結 4 件のみ・プロンプト出力不変（他 backend byte-identical 検証込み）・1059 passed。検算照合ゲートが設計側事前値の丸め誤差 ±0.0001 を検出→厳密値 canonical | 2026-07-08 | 意味層 SEM-1 昇格ゲート closeout |
