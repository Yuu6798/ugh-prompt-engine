# STATUS.md — ugh-prompt-engine プロジェクト状況

## Phase

2026-08-20、**run 7 closeout（教師交代 = あみたろ実録音教師）+ PR #289 マージで VG-L0 実装第 1 タスクが一段落**: run 7 は起動 2 回（初回は素材命名の不一致で fail-closed → PR #288 で根治）・NaN ゼロ・40K 完走・**self-stop 成功**・≈$1.40。実測正本 = `voice_genesis/foundry/results_s6/s6_record_2026-08-20.md`。**Q12 = 部分裁定**（2026-08-20 に 2 段階で訂正 = s6_record §5-1）: 言えるのは**事前登録した局所退行 2 点のうち「す」は改善した**（ritsu「す」改善 / pjs 改善のみ）までで、**事前登録の articulation 測度（さ行 onset 明瞭性）は `undetermined`**（さ→あ 未裁定 = §5-1b）。**撤回 2 件** = 初版の「合成教師の代替として機能する」/ 第 1 訂正の「articulation 側に限り達成」（終端の所見を onset の代理に使っていた）。sustain 側は非改善で user には連続発声が新規発生。user の音量交絡解消は run 6 の介入効果で、user 入力は run6→run7 で 15/15 バイト同一につき教師交代に帰属できない。一方**「り→ん破綻は教師交代の副作用」は撤回** — ③三世代判定で run5→7 一貫して未解決と判明し、`score_d3_sustain.py` が「り→ん破綻対策の主力データ」と宣言していた対策は**投入されただけで効果が検証されていなかった**ことが露呈した（→ 申し送り「対策は投入でなく効果で終端宣言する」）。**破綻を免れるのは pjs 系のみ = 話者依存**で、さくら破綻/うみ成立の曲依存も未判定。VG-L0 側は記録を 3 度撤回して確定/未判定/撤回の三分類へ整理し（正規化 artifact / 軸定義の誤り / 同一プロセス反復）、probe 2 本と結果 JSON 4 本と pin 整合 CI テストをコミット化、次段 schema v0 の BLOCKER 7 件を記録した。bot レビュー運用規約も新設（採否基準 3 分類・上限 10 巡・採否はコミットメッセージ・**打ち切りは実害基準を上書きしない**）。**(1) フォローアップ PR は 2026-08-20 に完了**（記録を accepted へ昇格 + Codex 7/9 巡目の 4 件を全採用 + 実測を生んだ版を `probes/snapshots/` へ凍結する機構を新設 + s3_record へ Drive 実在確認と回収指示書を追記）。**次の主戦線 = (1) run 8 = **り→ん破綻の専用調査**〔S3 以来の最古の未解決課題・話者依存まで絞れた。設計書 = `voice_genesis/foundry/DESIGN_S7_run8.md`（PR #292・マージ待ち）: 観測子先行の 2 段〔Run 8-0 診断 GPU $0 → Gate → Run 8-B dosage-fixed targeted partial replacement〕・**E=3 conditional approval**（承認条件 7 項目のうち 6 は PR-1 作業）・B-1 TRF 測定仕様 → B-2 H0–H5 裁定代数の**二段階凍結**（meta-contract は凍結済み・数値は PR-1 の校正後）〕、(2) VG-E1（founding 3 体の台帳接続・軸凍結）、(3) **s3_record 未転記は 1〜3 完了（2026-08-20）。残 = 費用の確定値 + 未実施と判明した節目 NaN ゲート（state_dict 非有限値チェック）**、(4) VG-L0 probe/checker live 版の再実測（Pod/Drive 律速）**。

## Next-Issue Queue

| ID | Title | Priority | Notes |
|---|---|---|---|
| run 5 継続項目 | ②無留保確定 **達成（2026-08-20）** + B 側 5K/10K/20K ckpt ローカル再実測 | P3 | **②は run 6 で無留保確定（s5_record §6.2）— 本項目の主目的は達成**。残は B 側中間 ckpt のローカル再実測のみ（run 5/6 とも Drive に 5K/10K/20K を保全済み・優先度低）。run 5 の Q6/Q7 裁定と判定材料 pin は `results_s4/s4_record_2026-08-19.md` が正本 |
| run 4 費用の確定値転記 | ダッシュボード実測値で概算 ≈$2.95 を置換 | P3 | **2026-08-20: s3_record 未転記 1〜3 は転記完了**（`s3_record` §7.4）。User が AI-Drive → Google Drive へ実体を移送し、**バイトが手元にある状態で `sha256sum -c` を実行**したため照合つきで閉じられた（checkpoint 4 点 = 4/4 OK / 学習 log・TB = 実体から算出 / NaN スキャンは pin 済み `train_run4.log.gz` に束縛 — **ただしログの文字列走査であって checkpoint `state_dict` の非有限値検査ではない**。後者は未実施で、実施先の `.ckpt` は sha で pin 済み）。**`anchors_provenance` 凍結（VG-E1）のブロッカーは外れた**。副産物: **§7.1 の wav pin 10/10 が生成ログ側からも一致**（tar.gz provenance と生成ログの 2 系統）。**ただしこれは出力の同一性であって入力の provenance ではない** — どちらも記録しているのは出力 wav の sha で、どのモデル/config のバイトが生んだかは決まらない（ONNX 群とスクリプトは pin 無し）。**wav ↔ model/config の provenance edge は閉じていないので VG-E1 は閉じたものとして消費しないこと**。**宣言済みの欠落 1 件**: 生成コマンドは 10/10 逐語確定だが、**「その ONNX が 40K checkpoint 由来」の根拠は run4 の 6 本のみ確立・run3 の 4 本は未確立**（`--skip-export` では `--step` は出力先を選ぶだけで由来を示さない。確立には `gate_synth_summary.json` の `input_sha256.acoustic_onnx` が要るがサルベージ対象外）。訂正 2 件 = checkpoint のサイドカーは分割 part ではなく連結後の `.ckpt` を参照 / 「ログは anchors tar.gz 同梱」仮説は反証。**本項の残作業は費用のダッシュボード確定値のみ** — Runpod への login 済みアクセスが要り、AI-Drive にも確定値を示す資料が無い。VG-E1 の前提ではない。**なお run 4 全体としては別途「節目 NaN ゲート未実施」が残る**（次行） |
| run 4 節目 NaN ゲート（未実施） | 5K/10K/20K/40K の checkpoint `state_dict` 非有限値チェック | P2 | **runbook が定める節目ゲート**（`S3_RUN4_RUNBOOK.md:388-390`「5K/10K/20K/40K の節目で checkpoint 回収 + state_dict の非有限値チェック」）が **run 4 では未実施のまま**であることが 2026-08-20 の転記作業で判明した。走行時に実施されたのは `train_run4.log.gz` の**文字列走査**で（`nan_scan_run4.txt` = No NaN/Inf matches found）、state_dict に非有限の重みがあってもログに文字列は出ないため**種類が違う**。**実施は可能** — checkpoint 4 点は §7.4-1 で sha 照合済み・Drive 上に実体があるので、同じバイト列に対して `torch.isfinite` を全テンソルへ適用して件数を記録すればよい。**§3 の「NaN ゼロ」はこのゲートに支えられていない**ので、run 4 を「完全に閉じた」と扱わないこと |
| VG-L0（学習回路） | 制御層「稽古」1 遷移の最小実験 — 実装第 1 タスクは**「制御 I/F 探索」の範囲まで**（本タスク自体は継続中） | P2 | **[accepted — 2026-08-20 昇格。PR #289 + フォローアップ PR で 7/9 巡目の 4 件を反映済み。accepted の射程は §0 の三分類そのもの = 確定は C1–C7 のみ・U1–U6 は未判定のまま]** 実測正本 = `voice_genesis/evolution/records/vgl0_control_axis_probe_2026-08-20.md`（run 6 40K ONNX・CPU・GPU ゼロ。**DESIGN の前提 pin は run 5 checkpoint**。run 6 代替が許される射程は**入力 I/F・介入点の探索まで**で、音質/知覚/可動範囲/WAV sha は checkpoint 依存につき持ち出さない）。probe 実装と結果 JSON は `evolution/probes/` / `evolution/records/` に**コミット済み**（monkeypatch は sha だけではレビュー不能・過去に probe 自身の欠陥で誤った実測をした経緯があるため）。**確定**: (1) duration 経路に子音時間配分 `consonant_duration_scale` の介入点が存在し出力まで届く、(2) 制御量は入力係数どおり単調（子音比 0.0562→0.4167）、(3) **Render Reproducibility PASS を独立プロセス間で確認**（計 10 プロセス: fresh-process 反復 4/4 + 実行順反転 17/17 + ExecutionProfile 一致。同一プロセス内反復は independent replay の証拠にならないため別枠へ分離）、(4) フレーズ間ブレス/終端伸長は**尺としては厳密**（挿入フレーム数・対象ノート数と総尺増が誤差 0。フレーズ境界で切った `notes_limit=6/10` でも同一規則）、(5) gate_synth は `ScoreNote.phrase_index`/`is_phrase_final` を受け取れない（`_NoteWithMs` が 3 属性しか写さない・grep 0 件）、(6) 音素長の総和保存は 3 走行・全 51 条件で成立し `ph_dur<1` は 0 件。**撤回**: 「候補 3 軸すべて可動」（ブレス軸=`HEAD/TAIL_FRAMES` は曲全体の前後 SP パディング / 終端軸=`note_target_frames[-1]` は `notes_limit=8` ではフレーズ途中の「よ」— いずれも軸の定義が誤り）/「音響応答は非単調」（ピーク 0.6 正規化後 RMS の artifact）/「選抜方式の妥当性が実測で裏づけ」/「直交性確認済み」/ 知覚意味名「アタック強度」（機構名へ格下げ — 未校正の値に知覚名を付けない）。**未判定**: ブレスの**音響実現**（尺は厳密だが SP 区間が無音になるかは境界で割れた — 片方 −26 dB / もう片方 +9.8 dB）/ 音響応答の単調性 / 直交性（併用でブレスの効き方が変わる = 否定側の観測）/ 安全な可動範囲（0.25〜3.0 は `experimental_observed_range` であって `safe_range` ではない）/ SB0 発動（算定不能）/ **Profile Transition Determinism**（DESIGN §6 は Render Reproducibility との混同を禁じている）。**次段 schema v0 を canonical 凍結する前の BLOCKER 7 件**（record §5）: 制御意味の正本を 1 箇所へ（**既に repo 内で breath/jitter/vibrato/attack/drift/onset の 6 語が別単位・別意味で衝突している** — 3 バックエンド並存）/ renderer adapter 方式 / `perf_genes` と学習 profile の優先規則（perf_genes は継承物ではなく preset JSON・genome 台帳の `performance_prior` は 14 件すべて空）/ phrase metadata の共通中間表現（gate_synth 専用パッチで直さない）/ 音素長の下限ガード（現行コードに無い。×0.25 の最小子音は notes_limit 8 で 3 フレーム・**10 で 2 フレーム = 下限まで残り 1** — ノートを増やすだけで余裕が縮む）/ 機構名と意味名の分離 / monkeypatch の canonical 経路禁止。**User 採用 2026-08-19**・設計正本=`voice_genesis/evolution/DESIGN_VG_L0.md`。判定は SPR 行 2（LearningGain）。**残** = **probe/checker live 版の再実測**（フォローアップ PR で 7/9 巡目の 4 件を是正した結果、live 実装はどの正本からも pin されていない = 実測証拠が無い。実測を生んだ版は `probes/snapshots/` に凍結・`index.json` の `live_unmeasured` に理由と再検証条件を宣言済み。律速 = run 6 40K ONNX + canon + vocoder の再用意 = Pod/Drive）+ phrase metadata を保持する前処理 I/F + ブレス音響実現の判定（評価軸選定 + 耳） + 制御プロファイル schema v0 + `learning-transition/0.1` 台帳 + 前処理層の製品実装（gate_synth の read-only 契約を壊さない差し込み口・既定 None で現行とバイト同一）+ 可動範囲の聴取評価（`safe_range` 確定）+ うみでの音素長不変条件検査 + 稽古 1 遷移 + held-out A/B |
| VG-TR0（Trial 層） | 実戦試験層の最小 PoC — 再現可能な Failure Profile の取得 | P2 | **User 採用 2026-08-19**・正本=`voice_genesis/evolution/DESIGN_VG_TR0.md`（観測/採点/処遇の三分離・SPR 全行の証拠生産層・Suite 6 群）。schema 5 種は新系列台帳（VG-E0 非改変）。前提 = run 5 checkpoint。実装第 1 タスク = (a) gate_synth song loader の TrialScore 対応拡張（現状 sakura/umi ハードコード実測）+ (b) score レベル決定論変換の表現力実測（統合注記 S5・Trial runner は --skip-export 強制 = §11）。④の審査は SPR 行 1 の弱圧のみ（既存耳判定様式・TR0 実装後に TRS-BIRTH として形式化） |
| VG-E1 | Probe 合成配線 + 評価軸語彙凍結 + 第 0 世代取り込み | P3 | **run 5 checkpoint 律速**（④三角形補間が第 0 世代を兼ねる・DESIGN_S4 §4）。評価軸=LRA 単独禁止・ノイズ/区間レベル軸必須（run 4 評価器ギャップ）。**軸凍結は SPR Routing 表の行 4（絶対 Floor）/行 5（niche 相対）を区別して宣言**（`evolution/DESIGN_SPR.md` が評価設計の正本）。**④の審査は SPR 行 1 = 弱 viability 圧のみ**（S2 型誤配線疑いの再発防止・第三の声性は観測記録のみで reject 条件に不使用）。d_min 校正・VG-E0 §8 の「run 4 判定材料④」文言更新もここで |
| snapshot 機構の退場 | 再実測で凍結を廃する + git 履歴方式への置換 | P3 | **User 承認 2026-08-20 = 時限措置**（`evolution/probes/snapshots/README.md` / `index.json` の `policy`）。(a) run 6 の 40K ONNX + canon + vocoder が再用意でき次第 live 実装で正本 4 本を再生成 → 凍結は**機械強制で自動退場**（帰属検査 → measured_results 空 → 行削除 → 実体削除の連鎖）。(b) CI が履歴を取得できるようになったら（`actions/checkout` の `fetch-depth` を深くできたら）凍結コピーを廃し **named commit の git blob 照合**へ置換（重複 1,175 行がゼロになる）。**1 スクリプト 1 世代の上限は台帳検査が強制**（2 世代目が要る = 再実測を先送りしている合図） |
| VG follow-up 11 件 | PR #262/#263 由来のコード修正キュー | P3 | #264 には含めず繰越（#264 本文 Notes for Reviewer 参照）。次の VG 実装 PR で対応 |
| Intent Graph ループ 1 周目 | frontier=intent.ai_score の E2E 受け入れ条件定義 | P2 | 台帳は PR #256 でマージ済み（31 ノード・`svprpe intent-status`）。frontier が示す次作業=「AI 著述 → 決定論演奏者検収 → Score-level identity（sidecar 会計）」一気通貫デモの受け入れ条件を Design Memo 化。blocked=melody 系 3（m3d 起因・恒久）。台帳更新は PR レビュー経由のみ・claim 起草は一次ソース照合必須（08-10 の 6 件訂正の教訓） |
| WI3 第 2 トランシェ | 実 Suno P5 ペア + tie 規則 v1 で人間校正を再事前登録 | P2 | 律速=実 Suno 4 take（A1/A2/B1/B2）の音源回収（sha pin のみ・Drive 未回収を #202 で実地確認。#94 方式で回収可）。v1 規則=作品単位同値類+距離マージン（#202 docs §3 の構造的棄権対策）・synth スタイル変換ペア拡充（p10 が唯一の「作品保存×yes」）。事前登録はし直し（事後変更なし） |
| WI4 制度化 | proxy advisory 配線・per-work identity budget | P3 | **第 2 トランシェで proxy 軸が非空になるまでブロック**（v0 は空集合につき配線対象なし）。材料=WI2 弁別表（bpm のみ）+ WI3 誤り機構台帳（アトラクタ/長さバイアス/平行調） |
| lyrics 転写の精度実測 | 歌入り+歌詞 pin 音源での転写精度実測 | P3 | #199 で defer（素材律速）。配線・境界挙動（instrumental ハルシネーション・no_speech_prob ゲート）は WI0-b で実測済み |
| Metrics v2 再ベースライン | Pro n=20 で v2 基準値化 + legacy_valley_depth 一括撤去 | P2 | #188 の凍結措置の解凍タスク。Pro 群 n=20 を v2 で再計測→ジャンル別 crest/valley_db 分布→`config/*_baseline.yaml` の実置換（active_rate_ideal/valley_depth_pro 系の凍結解除）+ semantic rules/domain profile の valley 閾値 v2 スケール移行 + `legacy_valley_depth()` と `valley_depth_legacy` の一括撤去（時限マーカー: rpe/models.py:191 / semantic_ci/observed_adapter.py:22 / baseline yaml 4 本 / docs/metrics.md）。CompositionScore 計器の v2 移行は target 帯再ベースラインと同時。律速=Pro 実音源 n=20 の入手（licensing） |
| DD-D 解除（意味フィールド write-through） | CLAP 読みの semantic.* 自動記入解禁の是非 | P3 | 軸バッテリー本体は #151 で v1.1 closeout（probe チューニング第一巡・7 軸・sweep 手順制度化＝追加軸は config 追記+`--axes-config` sweep+校正ログ再生成のみ）。write-through 解禁は promotion gate G1-G5 + 校正基準文書化が前提（score_centric §5）。valence 軸の本来検証（セクション別感情アーク）は実制作音源律速 |
| K2-seg Suno 転移 | プロンプト欄 grip 地図の Suno 側検証（バッチ 4 = 残 3 ノブ）+ structure4 タグチャネル A/B | P2 | **MusicGen 側はバッチ M2（#173/#174・2026-07-13）で closeout** — 3 ノブとも M1 規律 canonical で再計測済み（active_rate loose 0.414 / valley_depth dead→loose 0.3518 / time_signature dead・device profile 反映済み）。**残は Suno 側のみ**: バッチ 4 は時刻証跡前提（画面録画 or 40 分超バッチ）、**structure4 発注書（タグチャネル A/B・non-canonical 事前登録・instrumental 固定）はコミット済み=発注可能状態**（律速=Suno 人手生成 8 本）。バッチ 1（avoid+core）=#162 / Exclude 追試=#164（confounded・isolated 追試待ち）は従来どおり。副次の種: バッチ 3 novelty d=+0.71 loose の追検討・M1 の区間別非対称（quiet 通る/loud 通らない=デフォルト形状引力と整合） |
| lyrics センサー実音源 E2E | vocals 分離込み実 E2E + committed fixture | P2 | **機械配線は #149 で完走**（`extract --lyrics`/`lyrics-adherence`・schema 1.2・全チェーン MIT 実確認）。実推論はフルミックス経路のみ検証済（検証環境に ffmpeg なし）。残＝歌入り実音源での Demucs 分離込み E2E、real fixture 化、順序診断の実測（反復歌詞の同点限界の実挙動確認）。律速=実音源 |
| v2 ブロック Drive 化 | #159 の 8 音源 drive_file_id 回収 | P3 | lyrics_symmetric_block_2026-07-08.yaml / CLAP v2 manifest は drive_file_id 未取得のまま merged（捏造せず省略・follow-up 明記済み）。#94 方式（SendUserFile→Drive→search 回収・byte/sha 照合）で追記。律速=人間作業 |
| K3-2b フル Suno 行列 | Suno 実測でフル直交性行列 | P2 | **設計指示 (a)(b)(c) と計器は #137 の MusicGen 行列で実証済み**（ノイズ天井・known_dead 行・key センサー化、controllability_poc.md §5.5）。Suno 実測が揃えば bpm→centroid 符号反転=機種依存の裁定と cross_couplings の unresolved 昇格が可能。律速=Suno 人手生成バッチ（5 ノブ×2×R≥8） |
| Q1-5 Ph2 | spectral 帯域 magnitude 再校正(残) | P3 | **大半 closeout 済**: brightness=B-3 / `high_ratio==0.0` 前提見直し=#107 / low/mid_ratio 据え置き finding=#108(low はゲートで健全・mid は未発火で繰越・general アンカー欠如)。残=`screen_2026-06-16.yaml` 再採取 + mid_ratio/magnitude 軸の bias 検証(Phase C と統合・licensing 律速)。**BPM**: 自動化は #155 R2-2f で closeout（3:2 帯 (1.35,1.6) 限定の prior-disagreement 検出を extractor 配線）。残=実音源 live E2E（punk 22050+start_bpm=180 の同条件実測、R1-audio 律速）と窓外の未観測比（screener 診断で継続監視） |
| Genre Calib seed follow-up | EDM 5本の Drive 化 + Suno prompt 転記 | P3 | #103 で measured はインライン保全済。残=`electronic_dance_*` の Drive アップロード→`drive_file_id` 回収 + `prompt:` 本文の PENDING 解消。律速=人間作業 |
| Genre Calib 4th genre | acoustic へ域拡張 | P3 | acoustic(low_ratio<0.4 の general 落ち領域を突く coverage 拡張型)を rock(#105)と同じ協働フロー(物理層明示プロンプト→user 生成→抽出→分離分析→ルール反映)で追加。律速=人間生成バッチ |
| 意味層トラック lyrics 残 | 歌詞=意味層アンカー検証続行 + lyrics_presence 昇格セル埋め | P2 | **(b) control_profile ノブは SEM-1(#126) で実装済み**（`SemanticLayer.lyrics_presence`・loose 固定・昇格ゲート制度化=docs/control_profile.md）。残: (a) 検証デザイン B/C=同一アレンジ×【歌詞なし/原曲無関係歌詞/原曲由来歌詞】で「原曲寄り度」を人間評価×物理乖離(dynamic_range は #124 で棄却済につき別指標を探す)。(c) は **#159 で closeout**: mid_ratio 昇格は両ジャンル棄却が確定（absent alt 込み n≥2×2 実測）・センサーは CLAP vocal contrast へ DD-4 改訂済み（tight 昇格は条件 2 formal 充足を経て **#160 で config 反映済み**＝SEM-1 昇格ゲート初完走）。**dynamic_range 再検証はしない**(棄却済)・**「ボーカル=主音の錨」は棄却済**。**新展開(07-05 #149)**: 歌詞転写＋検収は `extract --lyrics`/`lyrics-adherence` として制度化済み＝検証デザイン B/C の「原曲由来歌詞」素材の機械生成・機械検収が CLI で可能に。律速=人間生成バッチ+主観評価 |
| 発注書生成スクリプト再作成 | scratchpad 消失分の再作成（必要時のみ） | P3 | 旧「neutral ラベル化の本体還元」行の残置項目（本体は #156 で closeout）。Suno カバー発注書生成スクリプトは scratchpad 消失につき、次に必要になった時点で再作成 |
| recast マシン依存 E2E | musicgen backend 実配線 + 実 Suno manual フロー一周 | P3 | musicgen はスタブ（重み/extra 律速・docs/musicgen_backend.md）。実 Suno=注文書 6 ファイル→人間生成→ingest→report の実地一周（素材/人手律速）。**melody は #237 で M1–M3 経路の experimental anchor として配線済み**（Phase 0 の LCS スパイクとは別経路・docs/DESIGN_M4_recast_melody_anchor.md）＝M3d closeout（2026-08-09・docs/m3d_calibration_record.md）により不点灯が恒久確定。identity_assessment 解禁は WI4 後 |
| M2e §12 User 決裁 | 実運用帯素材の扱い（①宣言素材限定 or ②MedleyDB 申請再開） | P3 | **M2e r0–r7 は 2026-08-14 全段階完了**（census 1280/1280 = `census.json`・判読正本 = `m2e_r7_breakdown_curve_record_2026-08-14.md`・closeout PR #260）。残るのは設計 §12 の User 決裁のみ: 判断材料 = 破断の膝が 0dB と −6dB の間（clear_lead 帯は膝から遠い）＝①でも `clear_lead`/`vocal_track` 宣言素材の射程は曲線が支持。M4 は crepe_direct しか束ねないため①でも前進（ただし M4 不点灯恒久 = m3d closeout は不変）。律速=User 判断 |
| D ランク CLI 関数分割 | observe_cmd D30 / _audit_root D28 / recast_init_cmd D25 の挙動保存分割 | P3 | #235 レシピ（構造分析→逐字ブロック抽出→tests 無改変 green）流用可。安定稼働中のため**該当ファイルへ機能追加が入るタイミングで同梱**が推奨（単独実施は churn 優位）。CI シャード再設計（実測時間ベース 3–4 分割）はテスト 1.5–2 倍時に発動 |
| scorer pin 後続 3 件 | #225 から切り出した P2 群 | P3 | (1) カテゴリ跨ぎの `numeric_runtime_config` 不変性（17 巡目・複数カテゴリを 1 プロセスで回す運用導入時に必要）(2) `threadpool_info()` の検査失敗を absent と区別して fail-closed（18 巡目）(3) **threadpoolctl を scorer 閉包から外す**（scoring 経路から import されず presence が instrumentation のタイミングを測る＝閉包メンバーとして機能していない。技術的により正しいがテスト 15 箇所以上の整合コストで #225 では見送り） |
| CI m2-harness シャード予算再点検 | test-m2-harness の 35 分 timeout が main で常態化 | P2 | 初出=2026-08-10 PR #255(cancelled ×2)。**08-14 に常態化を確認**: main は 8/5 以降 30 run 中 success 7 のみ・PR #261 ブランチも 13:42 以降 17 run 連続 cancelled(失敗ゼロのまま 84% 地点で 35 分打ち切り。同一コードで 3.11 が 22.5 分 success の実例=ランナー速度変動で境界跨ぎ)。当該シャードは tests/test_m2_accuracy_harness.py 専用。恒久対応=ci.yml 冒頭方針どおり timeout 増でなく shard 再分割(file→module/クラス分割)。#261 スレッドに分析+選択肢コメント済み(issuecomment-5298008557)。P3→P2 昇格(08-14: merge blocking が実害化) |

## Recently Merged

| PR | Title | Date | Phase |
| #289 | VG-L0 制御軸 probe（記録 3 度撤回 + probe/結果 JSON コミット化 + pin 整合 CI）+ **S6 run 7 closeout**（教師交代・Q12 = ①矯正の保存は**部分裁定** 〔2026-08-20 訂正・s6_record §5-1: 局所退行「す」の改善までで articulation 測度は `undetermined`〕 / り→ん破綻は run5→7 一貫の継続課題と判明し「教師交代の副作用」を撤回）+ bot レビュー運用規約（採否基準・上限 10 巡・実害例外） | 2026-08-20 | VG-L0 / VG run 7 |
| #285/#286 | run 7 の Design Memo（教師交代 = d3synth 引退 → あみたろ実録音教師・同 dosage・spk_id 4 で id 3 恒久欠番・規約 2026-08-11 改訂版の逐語 pin）+ 実装（convert_amitaro〔決定論選定・dosage 帯 fail-closed・被覆検査・カナ→かな 2 段 lookup〕/ assemble --profile run7〔num_spk 明示・構成比会計 amitaro<50% assert〕/ bootstrap RUN_PROFILE=run7〔素材 ID 指定取得・verify_spk_map〕/ run7_dataset_pins）。セルフレビュー memo 5 件 + 実装 7 件（採用 6・見送り 1） | 2026-08-20 | VG run 7 |
| #283/#284 | run 6 の Design Memo（単一介入 = user 宅録の投入前ラウドネス正規化）+ 実装（convert_user の BS.1770 正規化段〔線形ゲインのみ・ピーク/無音 fail-closed〕/ run6_dataset_pins〔目標 -26.1 LUFS = ritsu+pjs 743 本の結合中央値実測〕/ bootstrap RUN_PROFILE 機構 / salvage (c) / ANALYSIS_STACK_PIN 新設）。走行結果は run 6 closeout（s5_record） | 2026-08-19 | VG run 6 |
| #276 | 評価設計バンドル: SPR（淘汰圧の配線 7 行×5 つ組・隔離規則 7 件・④審査=行 1 弱圧のみ）+ v0.3 補論 A 収載（G/A/R/D 4 分類・理論正本）+ Trial 層 VG-TR0（観測/採点/処遇の三分離・Suite 6 群・schema 5 種）。セルフレビュー 2 回転 + 外部レビュー 6/6 採用。gate_synth の read-only 契約違反（既定 export/publish）と song loader ハードコードをレビューで先取り | 2026-08-19 | 評価理論 SPR/TR0 |
| #275 | 進化理論 v0.2 収載（採用注記 4 点・D3/D4 訂正）+ 学習回路 VG-L0 開始（制御層採用・TRANSFER_SKILL 語彙化・H16・成果の過大定義禁止）。外部レビュー 6 件 + セルフレビュー 12 件対応 | 2026-08-19 | VG-L0 / v0.2 |
