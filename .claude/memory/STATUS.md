# STATUS.md — ugh-prompt-engine プロジェクト状況

## Phase

2026-07-14、**Arrangement Identity Track（C5 具体化）を 0→M2 目前まで 6 PR で立ち上げ**（#175 AR1-1 resolver / #176 AR1-2 `svprpe arrange` CLI+provenance bundle / #177 AR1-3 EDM·Jazz variants fixture=**M1 達成** / #178 AR2-1 IdentityManifest sidecar / #179 AR2-2 anchor preservation contract / #180 AR3-1 input capability profile）。設計原則を全 PR で貫徹: canonical `CompositionScore` 無変更（sidecar-first・planning D1）・fixity/preservation/capability/adherence の 4 軸分離・hard=resolver 契約であって生成保証でない・決定論（タイムスタンプ/絶対パス非埋め込み・byte-identical を fixture/テストで pin）・推測補完禁止（省略 anchor は contract に含めない・省略チャネル=unknown・変形語彙は planning 7 種限定で語彙なきドメインは elastic 不可）。Codex P2 計 14 件・9 ラウンドを当日全採用 — 頻出型は①path 封じ込め（絶対/`../`/symlink。字面遡上判定は base 不要=R3 訂正）②JSON 読み戻し経路の invariant safety net（validator 二重化）③多ファイル書き込みの原子性（staging+rollback・残余窓=二重障害と honesty 明記）④packaged 配布整合（package-data+evidence 検証の明示 base 限定）⑤hash とパースの同一バイト列。運用: Fable 設計専任×Sonnet 実装×babysit 分業を 6 PR 連続で初完走（実装 4+返信専任 1 エージェント再利用）。M2 残=AR3-2 PerformancePackage compiler のみ（AR2-3 は AR4 実データ待ち保留）。負債=AR0 計画文書未コミット+CLAUDE.md 索引 cap。政策提案（ユーザー判断待ち）: Codex 頻出型①〜⑤の AGENTS.md §7 チェックリスト encode。

## Next-Issue Queue

| ID | Title | Priority | Notes |
|---|---|---|---|
| Arrangement Identity Track 続き | AR3-2 PerformancePackage compiler（M2 最終ピース）+ AR2-3 保留 | P2 | AR1 全部+AR2-1/2-2+AR3-1 は #175–#180 で merged（M1 達成）。AR3-2=既存 compose 経路再利用+capability profile 突合（hard anchor×unsupported/unknown→advisory・`--strict-capabilities` で error 化・hard melody の prompt 暗黙降格禁止=planning D7）。**負債: AR0 計画文書 docs/arrangement_identity_planning.md 未コミット**（全 AR PR の Phase 参照先が repo 内に無い）— docs 化は CLAUDE.md 索引 1 行追記が要るため 400 行 cap の外出しとセットで docs-only PR。AR2-3（structure anchor）は AR4 の form artifact 実データ待ち推奨 |
| DD-D 解除（意味フィールド write-through） | CLAP 読みの semantic.* 自動記入解禁の是非 | P3 | 軸バッテリー本体は #151 で v1.1 closeout（probe チューニング第一巡・7 軸・sweep 手順制度化＝追加軸は config 追記+`--axes-config` sweep+校正ログ再生成のみ）。write-through 解禁は promotion gate G1-G5 + 校正基準文書化が前提（score_centric §5）。valence 軸の本来検証（セクション別感情アーク）は実制作音源律速 |
| K2-seg Suno 転移 | プロンプト欄 grip 地図の Suno 側検証（バッチ 4 = 残 3 ノブ）+ structure4 タグチャネル A/B | P2 | **MusicGen 側はバッチ M2（#173/#174・2026-07-13）で closeout** — 3 ノブとも M1 規律 canonical で再計測済み（active_rate loose 0.414 / valley_depth dead→loose 0.3518 / time_signature dead・device profile 反映済み）。**残は Suno 側のみ**: バッチ 4 は時刻証跡前提（画面録画 or 40 分超バッチ）、**structure4 発注書（タグチャネル A/B・non-canonical 事前登録・instrumental 固定）はコミット済み=発注可能状態**（律速=Suno 人手生成 8 本）。バッチ 1（avoid+core）=#162 / Exclude 追試=#164（confounded・isolated 追試待ち）は従来どおり。副次の種: バッチ 3 novelty d=+0.71 loose の追検討・M1 の区間別非対称（quiet 通る/loud 通らない=デフォルト形状引力と整合） |
| lyrics センサー実音源 E2E | vocals 分離込み実 E2E + committed fixture | P2 | **機械配線は #149 で完走**（`extract --lyrics`/`lyrics-adherence`・schema 1.2・全チェーン MIT 実確認）。実推論はフルミックス経路のみ検証済（検証環境に ffmpeg なし）。残＝歌入り実音源での Demucs 分離込み E2E、real fixture 化、順序診断の実測（反復歌詞の同点限界の実挙動確認）。律速=実音源 |
| v2 ブロック Drive 化 | #159 の 8 音源 drive_file_id 回収 | P3 | lyrics_symmetric_block_2026-07-08.yaml / CLAP v2 manifest は drive_file_id 未取得のまま merged（捏造せず省略・follow-up 明記済み）。#94 方式（SendUserFile→Drive→search 回収・byte/sha 照合）で追記。律速=人間作業 |
| K3-2b フル Suno 行列 | Suno 実測でフル直交性行列 | P2 | **設計指示 (a)(b)(c) と計器は #137 の MusicGen 行列で実証済み**（ノイズ天井・known_dead 行・key センサー化、controllability_poc.md §5.5）。Suno 実測が揃えば bpm→centroid 符号反転=機種依存の裁定と cross_couplings の unresolved 昇格が可能。律速=Suno 人手生成バッチ（5 ノブ×2×R≥8） |
| Q1-5 Ph2 | spectral 帯域 magnitude 再校正(残) | P3 | **大半 closeout 済**: brightness=B-3 / `high_ratio==0.0` 前提見直し=#107 / low/mid_ratio 据え置き finding=#108(low はゲートで健全・mid は未発火で繰越・general アンカー欠如)。残=`screen_2026-06-16.yaml` 再採取 + mid_ratio/magnitude 軸の bias 検証(Phase C と統合・licensing 律速)。**BPM**: 自動化は #155 R2-2f で closeout（3:2 帯 (1.35,1.6) 限定の prior-disagreement 検出を extractor 配線）。残=実音源 live E2E（punk 22050+start_bpm=180 の同条件実測、R1-audio 律速）と窓外の未観測比（screener 診断で継続監視） |
| Genre Calib seed follow-up | EDM 5本の Drive 化 + Suno prompt 転記 | P3 | #103 で measured はインライン保全済。残=`electronic_dance_*` の Drive アップロード→`drive_file_id` 回収 + `prompt:` 本文の PENDING 解消。律速=人間作業 |
| Genre Calib 4th genre | acoustic へ域拡張 | P3 | acoustic(low_ratio<0.4 の general 落ち領域を突く coverage 拡張型)を rock(#105)と同じ協働フロー(物理層明示プロンプト→user 生成→抽出→分離分析→ルール反映)で追加。律速=人間生成バッチ |
| 意味層トラック lyrics 残 | 歌詞=意味層アンカー検証続行 + lyrics_presence 昇格セル埋め | P2 | **(b) control_profile ノブは SEM-1(#126) で実装済み**（`SemanticLayer.lyrics_presence`・loose 固定・昇格ゲート制度化=docs/control_profile.md）。残: (a) 検証デザイン B/C=同一アレンジ×【歌詞なし/原曲無関係歌詞/原曲由来歌詞】で「原曲寄り度」を人間評価×物理乖離(dynamic_range は #124 で棄却済につき別指標を探す)。(c) は **#159 で closeout**: mid_ratio 昇格は両ジャンル棄却が確定（absent alt 込み n≥2×2 実測）・センサーは CLAP vocal contrast へ DD-4 改訂済み（tight 昇格は条件 2 formal 充足を経て **#160 で config 反映済み**＝SEM-1 昇格ゲート初完走）。**dynamic_range 再検証はしない**(棄却済)・**「ボーカル=主音の錨」は棄却済**。**新展開(07-05 #149)**: 歌詞転写＋検収は `extract --lyrics`/`lyrics-adherence` として制度化済み＝検証デザイン B/C の「原曲由来歌詞」素材の機械生成・機械検収が CLI で可能に。律速=人間生成バッチ+主観評価 |
| 発注書生成スクリプト再作成 | scratchpad 消失分の再作成（必要時のみ） | P3 | 旧「neutral ラベル化の本体還元」行の残置項目（本体は #156 で closeout）。Suno カバー発注書生成スクリプトは scratchpad 消失につき、次に必要になった時点で再作成 |

## Recently Merged

| PR | Title | Date | Phase |
|---|---|---|---|
| #180 | feat(arrange): AR3-1 input capability profile — 入力チャネル存在（supported/experimental/unsupported/unknown）を grip と分離した別モデル `InputCapabilityProfile` + config/capability_profiles/{suno,musicgen}.yaml（supported は repo 実測 evidence 主義・省略チャネル=unknown フォールバック）。Codex P2×4・3R（evidence 封じ込め→package-data→packaged 実在検証の明示 base 限定→字面遡上は base 不要）。1271 passed | 2026-07-14 | Arrangement Identity Track AR3-1 |
| #179 | feat(arrange): AR2-2 anchor preservation contract — `PreservationSpec.identity_anchors`（additive・後方互換 CLI byte 不変 pin）+ `build_preservation_contract`（unknown anchor→required 網羅→domain 語彙の cross-validate・manifest 順保持・省略=推測補完なし・hash 明示注入）。変形語彙は planning 7 種限定・lyrics/rhythm/motif は elastic 不可。Codex P2×2（読み戻し invariant validator・anchor_id 重複拒否）。1243 passed | 2026-07-14 | Arrangement Identity Track AR2-2 |
| #178 | feat(arrange): AR2-1 IdentityManifest sidecar — hash 検証 loader（単一読み取り TOCTOU 排除・manifest 親基準相対・rights_basis/domain 明示 enum・anchor ID 一意）。canonical schema 変更ゼロ・compose 非依存。Codex P2×2（locator/artifact の封じ込め・manifest 読み失敗のラップ対称化）→最終レビュークリーン。1210 passed | 2026-07-14 | Arrangement Identity Track AR2-1 |
| #177 | feat(arrange): AR1-3 EDM/Jazz deterministic variants fixture — 1 Base Score から 2 Derived + field-level diff + provenance bundle を実 CLI 生成でコミット（source_score.sha256 両 variant 同一・semantic.core/physical.key を preservation-only hard で共有=identity anchor 契約・2 回生成 sha256 一致確認後にコミット）。**M1（C5 minimum）達成**。Codex 指摘なし。1184 passed | 2026-07-14 | Arrangement Identity Track AR1-3（M1 closeout） |
| #176 | feat(arrange): AR1-2 `svprpe arrange` CLI + provenance bundle — derived_score.yaml/bundle/diff の 3 成果物を決定論生成（タイムスタンプ/絶対パス/output_dir 非埋め込み・byte-identical）・入力 sha256 記録。Codex P2×4・4R: staging+os.replace+snapshot/rollback の原子的公開（残余窓=二重障害と honesty 明記）・hash/パース同一バイト列・入出力パス衝突拒否・OSError 捕捉。1175 passed | 2026-07-14 | Arrangement Identity Track AR1-2 |
