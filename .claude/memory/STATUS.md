# STATUS.md — ugh-prompt-engine プロジェクト状況

## Phase

2026-08-08、**M3d slow-lane 校正実測が手順 1/5（事前登録）まで完走**: #252 で 98 対 pairs manifest + 決定論 builder + 実行設計を実測前 commit し順序証明の起点が成立（Codex 12R 29 指摘対応込み。evaluator material 別会計・pin 検証の run→evaluate 強制チェーン・vocadito スナップショット束縛・rhythm 負例是正・起動数会計 460 へ訂正）。L 系列は #245–#250 で非旋律軸実証まで完結済み（残 = L0c のみ・M3d+M4 校正待ち）。M2e-r4 は 08-05 の条件付き決裁のまま変化なし。**次の主戦線 = M3d 手順 2–5 実測（crepe run×2 → evaluate → 凍結 → holdout・--pins 必須・460 crepe 起動）と M2e r4–r7 実測（いずれも machine-dependent）**。M3d 校正が通れば M4 anchor が追加統合なしに点灯し L0c が解禁される。

## Next-Issue Queue

| ID | Title | Priority | Notes |
|---|---|---|---|
| WI3 第 2 トランシェ | 実 Suno P5 ペア + tie 規則 v1 で人間校正を再事前登録 | P2 | 律速=実 Suno 4 take（A1/A2/B1/B2）の音源回収（sha pin のみ・Drive 未回収を #202 で実地確認。#94 方式で回収可）。v1 規則=作品単位同値類+距離マージン（#202 docs §3 の構造的棄権対策）・synth スタイル変換ペア拡充（p10 が唯一の「作品保存×yes」）。事前登録はし直し（事後変更なし） |
| WI4 制度化 | proxy advisory 配線・per-work identity budget | P3 | **第 2 トランシェで proxy 軸が非空になるまでブロック**（v0 は空集合につき配線対象なし）。材料=WI2 弁別表（bpm のみ）+ WI3 誤り機構台帳（アトラクタ/長さバイアス/平行調） |
| lyrics 転写の精度実測 | 歌入り+歌詞 pin 音源での転写精度実測 | P3 | #199 で defer（素材律速）。配線・境界挙動（instrumental ハルシネーション・no_speech_prob ゲート）は WI0-b で実測済み |
| Metrics v2 再ベースライン | Pro n=20 で v2 基準値化 + legacy_valley_depth 一括撤去 | P2 | #188 の凍結措置の解凍タスク。Pro 群 n=20 を v2 で再計測→ジャンル別 crest/valley_db 分布→`config/*_baseline.yaml` の実置換（active_rate_ideal/valley_depth_pro 系の凍結解除）+ semantic rules/domain profile の valley 閾値 v2 スケール移行 + `legacy_valley_depth()` と `valley_depth_legacy` の一括撤去（時限マーカー: rpe/models.py:191 / semantic_ci/observed_adapter.py:22 / baseline yaml 4 本 / docs/metrics.md）。CompositionScore 計器の v2 移行は target 帯再ベースラインと同時。律速=Pro 実音源 n=20 の入手（licensing） |
| DD-D 解除（意味フィールド write-through） | CLAP 読みの semantic.* 自動記入解禁の是非 | P3 | 軸バッテリー本体は #151 で v1.1 closeout（probe チューニング第一巡・7 軸・sweep 手順制度化＝追加軸は config 追記+`--axes-config` sweep+校正ログ再生成のみ）。write-through 解禁は promotion gate G1-G5 + 校正基準文書化が前提（score_centric §5）。valence 軸の本来検証（セクション別感情アーク）は実制作音源律速 |
| K2-seg Suno 転移 | プロンプト欄 grip 地図の Suno 側検証（バッチ 4 = 残 3 ノブ）+ structure4 タグチャネル A/B | P2 | **MusicGen 側はバッチ M2（#173/#174・2026-07-13）で closeout** — 3 ノブとも M1 規律 canonical で再計測済み（active_rate loose 0.414 / valley_depth dead→loose 0.3518 / time_signature dead・device profile 反映済み）。**残は Suno 側のみ**: バッチ 4 は時刻証跡前提（画面録画 or 40 分超バッチ）、**structure4 発注書（タグチャネル A/B・non-canonical 事前登録・instrumental 固定）はコミット済み=発注可能状態**（律速=Suno 人手生成 8 本）。バッチ 1（avoid+core）=#162 / Exclude 追試=#164（confounded・isolated 追試待ち）は従来どおり。副次の種: バッチ 3 novelty d=+0.71 loose の追検討・M1 の区間別非対称（quiet 通る/loud 通らない=デフォルト形状引力と整合） |
| lyrics センサー実音源 E2E | vocals 分離込み実 E2E + committed fixture | P2 | **機械配線は #149 で完走**（`extract --lyrics`/`lyrics-adherence`・schema 1.2・全チェーン MIT 実確認）。実推論はフルミックス経路のみ検証済（検証環境に ffmpeg なし）。残＝歌入り実音源での Demucs 分離込み E2E、real fixture 化、順序診断の実測（反復歌詞の同点限界の実挙動確認）。律速=実音源 |
| v2 ブロック Drive 化 | #159 の 8 音源 drive_file_id 回収 | P3 | lyrics_symmetric_block_2026-07-08.yaml / CLAP v2 manifest は drive_file_id 未取得のまま merged（捏造せず省略・follow-up 明記済み）。#94 方式（SendUserFile→Drive→search 回収・byte/sha 照合）で追記。律速=人間作業 |
| K3-2b フル Suno 行列 | Suno 実測でフル直交性行列 | P2 | **設計指示 (a)(b)(c) と計器は #137 の MusicGen 行列で実証済み**（ノイズ天井・known_dead 行・key センサー化、controllability_poc.md §5.5）。Suno 実測が揃えば bpm→centroid 符号反転=機種依存の裁定と cross_couplings の unresolved 昇格が可能。律速=Suno 人手生成バッチ（5 ノブ×2×R≥8） |
| Q1-5 Ph2 | spectral 帯域 magnitude 再校正(残) | P3 | **大半 closeout 済**: brightness=B-3 / `high_ratio==0.0` 前提見直し=#107 / low/mid_ratio 据え置き finding=#108(low はゲートで健全・mid は未発火で繰越・general アンカー欠如)。残=`screen_2026-06-16.yaml` 再採取 + mid_ratio/magnitude 軸の bias 検証(Phase C と統合・licensing 律速)。**BPM**: 自動化は #155 R2-2f で closeout（3:2 帯 (1.35,1.6) 限定の prior-disagreement 検出を extractor 配線）。残=実音源 live E2E（punk 22050+start_bpm=180 の同条件実測、R1-audio 律速）と窓外の未観測比（screener 診断で継続監視） |
| Genre Calib seed follow-up | EDM 5本の Drive 化 + Suno prompt 転記 | P3 | #103 で measured はインライン保全済。残=`electronic_dance_*` の Drive アップロード→`drive_file_id` 回収 + `prompt:` 本文の PENDING 解消。律速=人間作業 |
| Genre Calib 4th genre | acoustic へ域拡張 | P3 | acoustic(low_ratio<0.4 の general 落ち領域を突く coverage 拡張型)を rock(#105)と同じ協働フロー(物理層明示プロンプト→user 生成→抽出→分離分析→ルール反映)で追加。律速=人間生成バッチ |
| 意味層トラック lyrics 残 | 歌詞=意味層アンカー検証続行 + lyrics_presence 昇格セル埋め | P2 | **(b) control_profile ノブは SEM-1(#126) で実装済み**（`SemanticLayer.lyrics_presence`・loose 固定・昇格ゲート制度化=docs/control_profile.md）。残: (a) 検証デザイン B/C=同一アレンジ×【歌詞なし/原曲無関係歌詞/原曲由来歌詞】で「原曲寄り度」を人間評価×物理乖離(dynamic_range は #124 で棄却済につき別指標を探す)。(c) は **#159 で closeout**: mid_ratio 昇格は両ジャンル棄却が確定（absent alt 込み n≥2×2 実測）・センサーは CLAP vocal contrast へ DD-4 改訂済み（tight 昇格は条件 2 formal 充足を経て **#160 で config 反映済み**＝SEM-1 昇格ゲート初完走）。**dynamic_range 再検証はしない**(棄却済)・**「ボーカル=主音の錨」は棄却済**。**新展開(07-05 #149)**: 歌詞転写＋検収は `extract --lyrics`/`lyrics-adherence` として制度化済み＝検証デザイン B/C の「原曲由来歌詞」素材の機械生成・機械検収が CLI で可能に。律速=人間生成バッチ+主観評価 |
| 発注書生成スクリプト再作成 | scratchpad 消失分の再作成（必要時のみ） | P3 | 旧「neutral ラベル化の本体還元」行の残置項目（本体は #156 で closeout）。Suno カバー発注書生成スクリプトは scratchpad 消失につき、次に必要になった時点で再作成 |
| recast マシン依存 E2E | musicgen backend 実配線 + 実 Suno manual フロー一周 | P3 | musicgen はスタブ（重み/extra 律速・docs/musicgen_backend.md）。実 Suno=注文書 6 ファイル→人間生成→ingest→report の実地一周（素材/人手律速）。**melody は #237 で M1–M3 経路の experimental anchor として配線済み**（Phase 0 の LCS スパイクとは別経路・docs/DESIGN_M4_recast_melody_anchor.md）＝点灯は M3d 校正後。identity_assessment 解禁は WI4 後 |
| melody M1-real 実測→M2 判定 | Suno vocals stem 等を Demucs→CREPE/Melodia で実推論し凍結 Go バーを機械適用 | P2 | **機械独立な配線は完了**（#215 M1c(pyin) / #216 凍結バー+評価器 / #217 重みゲート+stem・weights・コード pin の emit）。残＝**実測のみ（machine-dependent）**: (1) 重みを事前取得して `.[separate]` 導入（docs §6.4 レシピ）、(2) `describe_separation_weights()` で version/sha256 を採り registry の `provenance.model_weights` へ記録（記録済み値は評価器が行と一致要求）、(3) 凍結素材の `expected_audio_sha256` を registry へ記録、(4) `--external` 実測→`evaluate_m1_real_go_bar` 適用。**1 run = 1 プロセス**で回す（load-time pin と in-memory cache の対応保証）。分離 pin は現状 **Linux/ELF 限定**（非 ELF は closure を読めず fail-closed）・入力は soundfile で読める形式に限定（AAC/M4A は fail-closed なので素材は WAV/FLAC で回収）。**M2 へ自動進行しない**（明示 Go 判定要求）。副次: CREPE 重みライセンス実確認→pin できれば `[crepe]` extra 昇格検討。**07-30 追記: M2a–M2d は #224–#231 で完結**（S-direct fail=VFA 単独超過 / V-direct pass・誤差モデル=`docs/m2_error_model.md`・CREPE 重み取得レシピは m2b 記録参照）。本行の残＝ M1-real Go バーの実測のみ |
| M2e r4–r7 実測 | r2-0 校正 → シャード地図 → 1280 セル実測 → 破断曲線 | P2 | **帯セル実測は 0/1280**。**08-05: r4 は決裁消化済み**（R_max=18・P=2 降格・S=337.4s 遅延ロード補正・ゲート 49/49・A-1 スポットチェック合格・条件付き決裁=N_shards≤22 なら R_max=N_shards+2 で自動進行）。残手順=C（P=1 vs P=2 ゲート）→ S 補正 rev + 決裁 dated 記録 → env_digest/lockfile → 地図生成（fixtures canonical rename 同時 landing）→ r6 本測定 ~20 シャード（各セッション冒頭 2 波再校正+S 再導出）→ r7 evaluate+census。停止条件=P=2 direct 再超過（→P=1≈50h・環境変更再検討）/ ゲート不一致 / N_shards>22。律速=machine-dependent 実測 |
| M3d slow-lane 校正実測 | 実 crepe + vocadito で比較器を校正し閾値凍結 → holdout 一度検証 | P2 | **機械配線は #233、手順 (1) 事前登録は #252 で完走**（98 対 manifest + builder + 実行設計を実測前 commit。Codex 12R 29 指摘対応込み: evaluator material 別会計＝synth は診断専用で凍結可否に不影響・pin 検証の強制チェーン（--check-only → run 強制 preflight `--pins` 必須 → 凍結コピー照合 → evaluate 証跡要求）・vocadito スナップショット束縛・rhythm 負例 fixture の構造的無効を是正・起動数会計を pair 基準へ訂正＝tuning 264 + holdout 196 = 460 起動、半減 fallback 292）。残=実測のみ（machine-dependent）: (2) crepe_direct で run×2（**--pins 必須**）、(3) evaluate → マージン表+凍結提案+floor 候補、(4) registry 凍結 commit（tuning 由来等値は evaluate が検証）、(5) holdout 一度検証。校正まで evidence="none"（正直な未校正）。**M4（Recast 配線）は #237 で先行マージ済み**＝校正が入れば追加統合なしに点灯。**校正と同時に G2 帯域語彙の解像度是正が必要**（下行）。**校正が事前登録バー（separation margin 0.15）を通らない場合は melody トラック全体の closeout 判断**（Phase 0 で同曲/異曲分布が完全重複した前例あり・structure 欄 dead #170/#171 と同規律） |
| G2 帯域語彙の解像度是正 | `melody_take_band` の宣言語彙を M2 実測区分まで細分 | P2 | **M3d 校正と同時に実施**（#237 で判明した新規事項）。M4 の G2 は backend 宣言 `melody_take_band == "clear_lead"` を見るが、M2 実測は同じ clear_lead 帯の中で **S-direct（合成単旋律）= fail（VFA 0.259 単独超過）/ V-direct（実声 vocadito）= pass（RPA 0.9886）** に割れる。校正後は合成素材が clear_lead を名乗って通過し、実声で校正した閾値が合成音の分布へ誤適用されうる（**Codex R1-1 の route 束縛と同型**）。処方=帯域語彙の細分 or 素材種の束縛。M4d E2E は抽出器注入で配管のみ検証しており**この問題を検出できない**点に注意 |
| D ランク CLI 関数分割 | observe_cmd D30 / _audit_root D28 / recast_init_cmd D25 の挙動保存分割 | P3 | #235 レシピ（構造分析→逐字ブロック抽出→tests 無改変 green）流用可。安定稼働中のため**該当ファイルへ機能追加が入るタイミングで同梱**が推奨（単独実施は churn 優位）。CI シャード再設計（実測時間ベース 3–4 分割）はテスト 1.5–2 倍時に発動 |
| L0c 旋律軸 | L 系列の旋律軸拡張（M3d+M4 待ち） | P3 | **M3d 校正+M4 点灯までブロック**（L0-s〜L0b=#245–#248、L0b-R=#249、L0a v2+BR-D1 再測=#250 で完走済み）。**L0a v2 は #250 で消化**（BR-D1 2/2 reached）。v3 契約材料=#250 記録 §4–§5（高→低遷移側 bridge の erratum 是正義務・終端 tier3↔5 無差別帯・intent の parse 要求 or 自由文格下げの二択）。#247 残置 2 件（take.wav フルハッシュ pin / package ディレクトリ単位公開の境界宣言）も必要時に同梱 |
| scorer pin 後続 3 件 | #225 から切り出した P2 群 | P3 | (1) カテゴリ跨ぎの `numeric_runtime_config` 不変性（17 巡目・複数カテゴリを 1 プロセスで回す運用導入時に必要）(2) `threadpool_info()` の検査失敗を absent と区別して fail-closed（18 巡目）(3) **threadpoolctl を scorer 閉包から外す**（scoring 経路から import されず presence が instrumentation のタイミングを測る＝閉包メンバーとして機能していない。技術的により正しいがテスト 15 箇所以上の整合コストで #225 では見送り） |

## Recently Merged

| PR | Title | Date | Phase |
|---|---|---|---|
| #252 | feat(m3d): 校正実測の事前登録 — pairs manifest builder + 98対 manifest + 実行設計。孤児 commit d4e2eea をトピックブランチへ載せ替え同日マージ。Codex 12R 29 指摘=採用 28（二分割案はハーネス構成契約との矛盾実測で evaluator material 別会計へ設計転換・同一バイト原則を全入力へ強制・pin 検証の run→evaluate 強制チェーン・rhythm 負例是正・起動数 180→460 訂正）。テスト 13→296 passed | 2026-08-08 | M3d 手順 1/5 |
| #250 | feat(l0a-v2): 著述契約 v2（開示 6 項目のみの改訂）+ BR-D1 再測バッテリー — 2/2 reached（各 3 周回）で事前登録仮説 supports（交絡限定付き=非対応比較・著者揺らぎ未分離）。二層証明（判定器 pin 機械証明+src 閉包 attestation）・凍結判定器再評価/Pareto 再計算拘束テスト・v3 材料 3 件（高→低 bridge erratum / tier3↔5 無差別帯 / intent 形式二択）。Codex 12 巡 20 スレッド（採用 16・部分採用 3・不採用 1=事前登録防衛） | 2026-08-07 | L 系列 L0a v2 |
| #249 | feat(l0br): L0b-R 単一著者頑健性バッテリー — 3 課題×2 系列・到達 5/6・Pareto 改善 5 件・全周回ツール使用 0。唯一の未到達=contract_defect（ヒント非加算）→ L0a v2 の入力に。到達率会計は User 決裁で 5/6 維持。記録=docs/l0br_robustness_record.md。Codex 21 巡 38 スレッド（採用 33・部分 2） | 2026-08-07 | L 系列 L0b-R |
| #248 | docs(agents): L 系列 3 PR の教訓を政策化 — §8 新設 5 則（例文も仕様 / truthy は不在チェック限定 / 情報遮断ペイロード機械化 / 撤回・訂正の repo 全域掃討+監査証跡保持例外 / parse 可能≠形状正しい）+ §3 採否方針 2 点（正典記録の自己矛盾=採用対象・見送りスレッド残置）+ §6 Tier A に §8 見出しカタログ走査を常時必須化。Codex 4 巡 4 指摘（P1 監査証跡破壊 / P2 非空制約巻き込み / P2 掃討範囲字義 / P2 Tier A 到達性）**全採用**=いずれも政策文の字義矛盾型 | 2026-08-06 | L 系列政策編入 |
| #247 | feat(l0b): L0b 閉ループ（非旋律軸）— 基盤+陽性対照+スパイク T1/T2。T1=L0S 同一課題 1 周回成功（契約 v1 仮説と整合・交絡あり）。T2=散文注記を off-contract 裁定 → クリーン系列 round4c→5c 再実行の Pareto 改善で §5 操舵証拠充足（原系列改善は会計除外）。記録=`docs/l0b_closed_loop_record.md`。レビュー 27 巡（残置 2 件=take.wav フルハッシュ pin / package 単位公開の境界宣言） | 2026-08-06 | L 系列 L0b |
