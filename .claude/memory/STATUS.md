# STATUS.md — ugh-prompt-engine プロジェクト状況

## Phase

2026-07-04 Session 2、**意味層のスカウト → CLAP 意味センサー増設を「抽出→採譜→区間粒度」の 3 段で完走（#145/#146/#147 マージ）**。本筋＝CLAP を「生成物への post-hoc 比較器」から「抽出段階で SOURCE 音声を読む意味層センサー」へ（従来 CLI 未配線＝`learned/__init__.py` deferred 注記を解消）。#145 `extract --clap-semantic`（`LearnedSemanticAxis`+`semantic_axes`・軸バッテリー `config/semantic_probe_axes.yaml` 5 軸・schema 1.0→1.1）/ #146 `transcribe --clap-semantic`（DD-D 尊重の advisory コメントブロック＝意味層フィールドは auto-fill せず TODO 保持・draft は loader-valid のまま）/ #147 `extract --clap-sections`（区間別＝感情アーク・`embed_audio_file` を `_embed_waveform_vector` へ挙動保存リファクタ＝`test_clap_adapter` 33/33 unchanged・whole-track superset）。全編で isolation（`SemanticRPE`/`PhysicalRPE` へ write-through なし）・決定論・fake-backend CI 契約を厳守。Codex P2 通算 6 件（#145×4/#147×1＝Rich markup/dep fail-fast/probe 検証×3/重複軸名 clobber）全てマシン非依存で Opus 直対応・回帰テスト付き・各 1 ラウンド解決。実装 Sonnet／設計・数値判読・レビュー Opus。残る意味層タスク＝実 CLAP 推論での軸校正（単調性・効果>再生成ノイズ）で `semantic-embed` extra + Suno/実音源律速。

## Next-Issue Queue

| ID | Title | Priority | Notes |
|---|---|---|---|
| CLAP 意味軸の実推論校正 | 抽出段階センサーの各軸を「本物のセンサー」と実証 | P2 | **#145/#146/#147 で機械配線は完走**（`extract --clap-semantic`/`--clap-sections`・`transcribe --clap-semantic` advisory・軸バッテリー5軸 config・区間別=感情アーク・全て isolation/決定論/fake-backend CI 契約）。残＝**実 CLAP 推論**で各軸の目盛り資質（単調性・再現性・効果>再生成ノイズ）を検証し probe 文言を校正、ジャンル/年代/情緒など軸追加。律速＝`semantic-embed` extra（torch+2GB 重み・当環境未導入）+ Suno/実音源バッチ。DD-D 解除（意味フィールド write-through）は promotion gate G1-G5 + 校正基準文書化が前提（score_centric §5） |
| CLAP 残③ | absent 側 alt の完全対称化（Suno 人手生成） | P2 | **② は #138 で closeout**（MusicGen K2 バッチへ拡張・bpm 学習版 d=2.60=halving 第三証拠・brightness は物理センサー優位=センサーは相補、musicgen_backend.md §7.5）。残 ③ のみ: 揃えば SEM-1 昇格ゲートと CLAP の n≥2×2 が同一バッチで埋まる。律速=Suno 人手生成。将来枠: K3 行列への意味センサー列追加 |
| K3-2b フル Suno 行列 | Suno 実測でフル直交性行列 | P2 | **設計指示 (a)(b)(c) と計器は #137 の MusicGen 行列で実証済み**（ノイズ天井・known_dead 行・key センサー化、controllability_poc.md §5.5）。Suno 実測が揃えば bpm→centroid 符号反転=機種依存の裁定と cross_couplings の unresolved 昇格が可能。律速=Suno 人手生成バッチ（5 ノブ×2×R≥8） |
| Q1-5 Ph2 | spectral 帯域 magnitude 再校正(残) | P3 | **大半 closeout 済**: brightness=B-3 / `high_ratio==0.0` 前提見直し=#107 / low/mid_ratio 据え置き finding=#108(low はゲートで健全・mid は未発火で繰越・general アンカー欠如)。残=`screen_2026-06-16.yaml` 再採取 + mid_ratio/magnitude 軸の bias 検証(Phase C と統合・licensing 律速)。**BPM**: #106 で sr/閾値の単一ノブ修正は大域回帰と確定(finding #6)。自動化は multi-prior/sr ensemble+octave tie-break を全 fixture 回帰検証付き Design Memo 化(K2 #117 でも bpm 素朴センサーの prior アトラクタが grip を圧縮することを再確認) |
| Genre Calib seed follow-up | EDM 5本の Drive 化 + Suno prompt 転記 | P3 | #103 で measured はインライン保全済。残=`electronic_dance_*` の Drive アップロード→`drive_file_id` 回収 + `prompt:` 本文の PENDING 解消。律速=人間作業 |
| Genre Calib 4th genre | acoustic へ域拡張 | P3 | acoustic(low_ratio<0.4 の general 落ち領域を突く coverage 拡張型)を rock(#105)と同じ協働フロー(物理層明示プロンプト→user 生成→抽出→分離分析→ルール反映)で追加。律速=人間生成バッチ |
| 意味層トラック lyrics 残 | 歌詞=意味層アンカー検証続行 + lyrics_presence 昇格セル埋め | P2 | **(b) control_profile ノブは SEM-1(#126) で実装済み**（`SemanticLayer.lyrics_presence`・loose 固定・昇格ゲート制度化=docs/control_profile.md）。残: (a) 検証デザイン B/C=同一アレンジ×【歌詞なし/原曲無関係歌詞/原曲由来歌詞】で「原曲寄り度」を人間評価×物理乖離(dynamic_range は #124 で棄却済につき別指標を探す)。(c) **mid_ratio 昇格セル埋め**: 各ジャンル instrumental alt 込み n≥2×2 セルで効果>再生成ノイズ + **K3 干渉分離**(genre も mid_ratio を動かす=matched-pair 統制) → 満たせば example の suno プロファイルを loose→tight 更新。**新展開(#131/#132)**: CLAP vocal contrast が「効果>再生成ノイズ」を**両ジャンル充足**(EDM 15.1×/Rock 10.9× vs mid_ratio 0.8×/1.3×)＝昇格判定のセンサーを mid_ratio から CLAP へ切替える選択肢が実データで立った(センサー変更は昇格ゲート定義の改訂=Design Memo 要)。**dynamic_range 再検証はしない**(棄却済)・**「ボーカル=主音の錨」は棄却済**。**新展開(07-03 S3)**: Demucs(torchaudio HDEMUCS)+faster-whisper で歌詞転写の機械経路が成立し、実 Suno カバーで発注歌詞のフレーズ単位再現まで確認＝検証デザイン B/C の「原曲由来歌詞」素材を機械生成できる。律速=人間生成バッチ+主観評価 |
| デモ 4 部作ブランチ処遇 | `claude/fable-demo-project-t6czdl` のマージ判断 | P3 | showcase スクリプト 4 本+出力 gitignore（07-03 S3）。PR 化するか一過性デモとして放置かはユーザー判断。scratchpad 側の CLAP 転写 runbook / 発注書生成 / 検収スクリプトを制度化するなら Design Memo 起点（transcribe の中立帯 TODO→neutral ラベル化も同梱候補） |

## Recently Merged

| PR | Title | Date | Phase |
|---|---|---|---|
| #147 | feat(learned): セクション別 CLAP 意味センサー（感情アーク・`extract --clap-sections`）— 意味軸バッテリーを構造セクションごとに評価し intro→verse→chorus の意味推移を読む。`LearnedSemanticSection`+`semantic_axis_sections`・whole-track superset・`embed_audio_file` を `_embed_waveform_vector` へ挙動保存リファクタ（`test_clap_adapter` 33/33 unchanged で証明）+`embed_audio_segments`（1回デコード→区間スライス→決定論）。Codex P2×1（重複軸名 clobber→`_validated_axes` fail-fast で両センサー単一点ガード） | 2026-07-04 | 意味層センサー増設 区間粒度 |
| #146 | feat(transcribe): `transcribe --clap-semantic` で CLAP 意味読みを採譜下書きに DD-D 準拠 advisory 添付 — 意味層フィールド（core/grv/delta_e）は auto-fill せず TODO 保持し、CLAP 読みを YAML コメントブロックとして draft 先頭に添える（「計器＝作曲前のパラメータ取得道具」）。全行コメントゆえ draft は loader-valid のまま。`render_semantic_axes_advisory()`・fail-fast プローブ踏襲 | 2026-07-04 | 意味層センサー増設 採譜段 |
| #145 | feat(learned): CLAP を抽出段階の意味層センサーへ配線（`extract --clap-semantic`）— 従来 CLAP は生成物への post-hoc 比較専用・CLI 未配線（`learned/__init__.py` deferred）。SOURCE 音声を固定の意味軸バッテリー5軸（`config/semantic_probe_axes.yaml`）に A/B `contrast_fit` で読む `LearnedSemanticAxis`+`semantic_axes`（schema 1.0→1.1・isolation 厳守で `SemanticRPE`/`PhysicalRPE` 非汚染）。Codex P2×4（Rich markup/dep fail-fast/probe スカラー拒否/空バッテリー拒否）全直接対応・回帰テスト付き | 2026-07-04 | 意味層センサー増設 抽出段 |
| #144 | ci: extra 導入環境の回帰を検出する nightly ジョブ追加 — 通常 CI（torch なし・`.[dev]` のみ）では動かない「本物 adapter コード経路」（#138 CLAP / #139 basic_pitch・panns・beat_this ×3 の環境依存回帰型）を、重い extra を入れて 1 日 1 回回して先回り検出。**install-only**（fake backend なので重み DL・実推論なし）。Codex P2×2 で **2 ジョブ scoped 化**（`test-extras`=core は必ず緑 / `test-clap-extra`=CLAP は fatal だが独立ジョブ=install 失敗を握りつぶさず可視化）。**手動 dispatch（run 28695987277）全 4 ジョブ緑で実働検証**＝semantic-embed の py3.11 install は clean runner で成功と実測（「ビルド不能」は proxy env 固有と訂正） | 2026-07-04 | 負債繰越 closeout 3/3 |
| #143 | refactor(clap): CLAP 埋め込みを `*.embeddings.json` サイドカーへロスレス退避 — 巨大 `audio_embedding`（32×512=326KB の主因）は消費側精査で **provenance 専用**と確定（実計算は保存済み cosines/contrast_fit を使用）。text JSON サイドカー（diffable/sha ethos・binary npz 不採用）へ既存値のまま退避（CLAP 再実行なし・数値再生成なし）。schema 1.0→1.1・split/merge round-trip 恒等。musicgen_k2 326KB→24KB / lyrics 62KB→6KB。Codex P2×1（重複 sample_id fail-fast） | 2026-07-04 | 負債繰越 closeout 2/3 |
