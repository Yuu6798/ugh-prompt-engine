## Session Summary — 2026-06-16

### Context
Codex 不在日に Claude 単独で 3 スレッドを完走。(1) 2026-06-15 積み残しの非ブロッカー
P3 fix-up 2 件（PR #79）。(2) R2-2a「BPM 半折り（×2 オクターブ）検出」スライスを
Design Memo→実装→Sonnet 独立レビュー→PR→Codex 自動レビュー→修正のフルサイクルで
完走（PR #80）。(3) R2-1（89.1 アトラクタ再現確認）の実行可能性を調査し、**合成再現は
不可能・R1-audio 必須**を実証で確定（コード変更なし）。Codex 不在を「Claude が設計と実装を
同一セッションで担い、Sonnet subagent + Codex bot の二段レビューで独立性を確保」する形で代替。

### Design Decisions
- **PR #79（P3 fix-up）**:
  - `TODO(transcribe):` センチネル（定数 5 + ヘルパ 5 重複）を依存ゼロの leaf
    `svp_rpe/sentinels.py`（`TODO_SENTINEL_PREFIX` / `is_todo_sentinel`）へ集約。
    `compose → semantic_ci` の既存循環を踏まないよう **compose 配下でなくトップレベル**配置。
  - R1 corpus の C-gen brightness を `qualitative_cue → numeric_knob` 再分類（C-gen は
    compose 出力 = prompt_renderer が brightness を数値ノブ送出, case study §3）。bright→neutral
    の非保存が比較表から脱落していたのを復活。`send_form` 運用規約も docs 明文化。
- **PR #80（R2-2a 半折り検出）**:
  - 検出は onset-strength 自己相関の「×2 subdivision lag vs primary lag」比。**overlap 数
    `(n-lag)` で正規化**（Codex P2②: 生の `librosa.autocorrelate` は非正規化和で短尺クリップが
    上方バイアス→誤検出）。threshold 1.15、**×2 方向のみ**（÷2 は自己相関振幅から復元不能）。
  - `compute_bpm` のシグネチャ・契約は不変（beat 規則性の純測定）。曖昧性は直交軸として
    extractor で合成し `bpm_octave_ambiguous` / `bpm_candidates` を `PhysicalRPE` に追加。
  - **trust 配線（Codex P2①）**: cap 0.5 は `_is_untrusted_confidence`(<=0.0) に効かず inert
    だったため、`score_draft._bpm_untrusted()` で `bpm_octave_ambiguous` を sensor-blind 扱いに
    配線→曖昧 BPM は `TODO_BPM_UNDETECTED`(unlocked) 転記。flag が trust を担い cap は記録信号。
  - 二段レビュー: Sonnet が 6 件（dead guard / tuple 不変性 / extractor cap 未テスト等）、
    Codex が 2 件の実質 P2（trust 配線・正規化）を補完的に検出。両方妥当→全反映。
- **R2-1 調査（コード変更なし）**: 推定器は **明瞭に速い合成信号を halving しない**
  （アクセント付き 150/160/170/175 すべて正答）。halving するのは純インパルス列＝
  本質的にオクターブ曖昧な信号のみ（180→89.10 だが 89/90 も妥当な解釈でバグでない）。
  → Suno 175→89 の真病理（明瞭に速いトラックの誤読）は合成で再現不能、**R2-1 は R1-audio
  必須**を仮定でなく実証で確定。併せて当初疑った「検出器ギャップ」は誤認と訂正：検出器は
  設計ケース（subdivision 支配的）では発火するが、そのケースを推定器が halving する形で
  合成できないため、**実効力は Suno 問題音源が無いと検証不能**（これも R1-audio 待ち）。

### Artifacts
- **PR #79 (merged)**: refactor センチネル集約 + fix C-gen send_form 整合 + send_form 規約 docs。
- **PR #80 (merged)**: R2-2a 半折り検出（`detect_bpm_octave_ambiguity` / `PhysicalRPE`
  2 フィールド / trust gate 配線 / `docs/metrics.md`「BPM Half-fold Detection」/
  `docs/roadmap_goal2.md` R2-2a）。新規 `tests/test_bpm_octave_ambiguity.py` ほか。
- R2-1: 成果物なし（調査のみ、working tree クリーン）。

### Handoff
- **R1-audio が単一クリティカルパス**: R2-1（アトラクタ再現確認）/ CV-scale 実校正 /
  ÷2 方向検出 / R2-2a 検出器の実効力検証 — **すべて R1-audio（人間が Suno 問題ケース音源を
  保存付き確保）待ち**。合成での代替は調査で否定済み（再試行しないこと）。
- **R2-2a は ×2 方向のみ shipped・実効力未検証**。real Suno 音源が入ったら manifest に
  calibratable レコード追加 →（a）推定器が実際に halving するか、（b）検出器が flag するかを
  記録するのが R2-1 の本体。
- **次の Claude 単独タスク候補**: クリティカルパス外の tech-debt / docs 整理（R1-audio 非依存）。
  R2 系はこれ以上 Claude 単独では進められない。
- **プロセス学習**: Codex 不在でも「Claude 設計+実装 → Sonnet 独立レビュー → PR → Codex
  bot レビュー → 修正」の二段レビューループが 1 セッションで回り、補完的にバグを捕捉できた。

## Session Summary — 2026-06-16 (Session 2)

### Context
テーマ「テストで設計知見を増やす（ツールを使ったテスト）」。決定論パイプラインの研究課題
（grip/校正/直交性/往復保存）が**メタモルフィックテストの問いそのもの**である点に着目し、
2 つの計器を新設。さらにセッション途中で User が**実 Suno 音源（R1-audio）を初めて提供**し、
STATUS.md の「R2 は R1-audio 待ち」が解消。実音源調査が**抽出器 BPM halving**を発見し、
当日中に修正の Design Memo（R2-2b）まで起案した。全作業は `claude/testing-design-insights-l5l0rz`
ブランチ（未マージ）。

### Design Decisions
- **計器 2 本**: (1) `scripts/metamorphic_probe.py`＝合成音(`render_sample`)×実 extract を
  Hypothesis 掃引する「配線テスター」（grip/直交性/決定論、`derandomize=True` で CI 決定論化）。
  (2) `scripts/screen_corpus.py`＝実音源を「指示 vs 検出」の保存率テーブルにする R1 スクリーナ。
- **provenance 規律**: ファイル→曲は**特徴自己同定**で対応づけ、ファイル名に依存しない。
  最初の Remix テイクは同一仕様の J-ebm 変種と取り違えリスクが切れず**破棄**（ノイズ化回避）。
- **仮説の反証フロー（本セッションの核心）**: N=1→2 で過度な一般化 2 件（bpm 系統誤差・key
  非保存）を反証。**A/B/C 対照実験**（key=Dm/疎電子/テンポ変化なし固定、BPM とドラムのみ変更）で
  「breakbeat が collapse 主因」仮説を**棄却**（breakbeat 保存・単純拍崩壊）。**高 prior 診断**
  （`start_bpm=180`）で高速 4 曲が全て真テンポ 172.3 を回復 → **崩壊は Suno 不忠実でなく抽出器
  halving**を確定。「117.45 アトラクタ」の正体＝prior(~120)×BPM グリッド選択。
- **R2-2b Design Memo は Option A（flag-recall のみ・値不変・低リスク）**で起案。reported bpm の
  値補正は snapshot/transcribe 波及のため別スライス（R2-2c）に分離。

### Artifacts（すべて claude/testing-design-insights-l5l0rz、未マージ）
- `metamorphic_probe.py`＋`test_metamorphic_probe.py`（Hypothesis 8 件）、`docs/metamorphic_probe.md`
- `screen_corpus.py`＋`test_screen_corpus.py`（純関数 5 件）、`docs/roundtrip_corpus_screen.md`
- `examples/roundtrip/screen_2026-06-16.yaml`（実 Suno 6 テイクを sha256 固定・observation_log、
  音源非同梱＋A/B/C＋高 prior 回復診断）
- `docs/briefs/R2-2b_bpm_octave_neighborhood.md`（Codex ハンドオフ）
- `hypothesis>=6.0` を dev 依存追加、CLAUDE.md/README 索引同期
- **主要発見**: spectral_centroid=tight grip / 高域 brightness は合成音では input 飢餓・実音源で
  健全（編成順に並ぶ校正スケール）/ key 保存は実曲で頑健 / R2-2a は固定 2× lag のため
  グリッド量子化された 1.93× 実パルスを外す具体バグ。

### Handoff
- **R1-audio 入手で R2 のブロックが解けた**。実 Suno 6 テイクを screen yaml に hash 固定済
  （observation_log。calibratable 化＝音源 repo 同梱は **licensing 判断待ち**で保留）。
- **次の実装**: R2-2b（Design Memo 済、Codex 実装待ち）。判断事項: Option B（reported bpm の
  値補正）をやるか別 memo 化。`screen_corpus` に「高 prior 真テンポ回復チェック」内蔵も推奨。
- **未完の実験**: 「数字 vs 構造」の残セル（高速 >170 の 4 つ打ち / 中速 120-140 の breakbeat）。
  ただし高 prior 診断で主因は抽出器側と判明したため、優先度は実装（R2-2b）に移ってよい。
- **ブランチ処理**: 本セッションの実装コードは `claude/` 例外 fix-up 枠の範囲を超える規模。
  PR 化するか、docs/計器のみ別建てにするか User 判断が要る（実装は本来 Codex 担当）。
