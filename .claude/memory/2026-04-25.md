# Session Summary — 2026-04-25

## 修正・訂正

- Codex P2 #1: Agent spawn 例の Python 構文不正 — bare identifier をdictキーに使っていた (`{ model: "sonnet", ... }`) ため `NameError`。クオート付き dict literal に修正。commit `c2aa40e`
- Codex P2 #2: main push policy の矛盾 — Branches 節は `.claude/memory/` 例外を許容するが PR 節は絶対禁止と書いており非整合。PR 節側で例外を明示する形で統一。commit `58dcbc4`
- マージ後にブランチが auto-delete されている事に気づかず `create_or_update_file` が 404 で失敗 (B 着手時)。新タスクの先頭で必ず `list_branches` で現状確認する運用に切り替えた

## 成功パターン

- 「移植元」「移植先」を最初に明示してから差分プランを提示すると承認が早い (CLAUDE.md 移植時)
- CLAUDE.md のような大きい単一ファイル更新は `mcp__github__create_or_update_file` の full-content 上書きで進める。差分はサーバ側で計算され conflict が起きにくい
- レビュー指摘は (a) 確信が持てる小さい修正なら即対応 + 簡潔な返信、(b) 曖昧なら確認、というルールで Codex 自動レビュー 2 件とも 1 ターンで片付いた
- 索引表のエントリは docs 実物を読んで要約を書く（推測で書かず、ファイル冒頭〜中盤を読んで主旨を抽出）

## 設計判断

- CLAUDE.md は svp-video-pipeline 由来の「汎用ポリシー版」と ugh-prompt-engine の「project 固有」をマージする方針を採用 (audit-core 版は project 固有が混ざっており不適切と判断)
- `.claude/memory/` 直接 commit 例外は維持（運用ログ自動化のため）。PR 節側で例外を明示する形で policy を一意化
- Architecture ツリーは `__init__.py` を列挙しない（audit-core スタイルに揃える）。視覚ノイズ削減
- 「設計ドキュメント索引」表は Architecture セクション内 (`### 設計ドキュメント索引`) に配置。ドキュメント管理ポリシーで参照されるため近接させる
- レビュー応答時は「指摘ありがとうございます + 何をどう直したか + commit ハッシュ」の3要素で簡潔に返す

## 未解決課題

- 同種の Python 構文不正 (`{ model: "sonnet", ... }`) は移植元 svp-video-pipeline/CLAUDE.md にも残存。次回そちらを触る時に揃える
- ugh-audit-core の CLAUDE.md は project 固有内容が混ざっており、汎用ポリシーの蒸留余地あり
- 次の選択肢として「C: 個別モジュール深掘り」を提示したまま終了

## CLAUDE.md 更新候補

- 特になし（今日のセッション自体が CLAUDE.md 整備の連続だったため、運用観点での新規追記事項は発生せず）

## マージ済み PR

- #3: docs: 汎用運用ポリシーを svp-video-pipeline から移植
- #4: docs: Architecture 節を実態と同期
- #5 (相当): docs: 設計ドキュメント索引表を Architecture 節に追加 (commit `6da6eaf`)
