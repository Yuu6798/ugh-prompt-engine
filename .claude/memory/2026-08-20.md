# 2026-08-20 — run 6 closeout（②無留保確定）+ run 7 起動・走行中

## run 6 完走と耳判定（走行は 08-19 17:09→23:30Z）

- **起動 1 回・fail-closed ゼロで 40K 完走・NaN ゼロ・≈$1.40**（cap $4 の 35%）。
  run 5 の素材搬入層 3 件根治（PR #271/#272/#274）が効いた初の走行
- 実測正本 = `voice_genesis/foundry/results_s5/s5_record_2026-08-20.md`
  （4 項目同時転記 = checkpoint sha / 学習 log・TB sha / wav コマンド対応表 /
  費用 + 耳判定逐語 + Q10/Q11 裁定）
- **耳判定（User 逐語）**: ①「dssynthは改善　ほかは改善無し」/ ②「別人判定　
  前回より明瞭」/ ③「ノイズは無し　相変わらず音量交絡が区間ごとにバラバラで
  安定せず　ここは変わり無し」+ 追確認「留保に関しては前回より良くなってたので
  無留保とした」

## 裁定

- **Q10 = 達成**: ② A/B/C 再判定（run 5 と**同一区間 A 2-7s/B 8-13s/C 14-19s**・
  各区間 -22.5 LUFS 完全一致・連結様式も同一）で 3/3 別人 + 明瞭度向上 +
  **留保撤回 = 無留保の「第三の声」確定**。プロトコル不変ゆえ**測定側の交絡は
  除外できる**が、run 6 は同一契約の別走行なので「正規化のみが効いた」とは
  帰属できない（データ不変の ritsu の区間幅も 4.43→1.86 LU と動いている）。
  主張できるのは「**正規化を含む run 6 の条件下で無留保に到達した**」まで
- **Q11 = 入口正規化の射程外で決着**: ③は**聴感**「変わり無し」。実測は
  user の区間幅 8.99→7.36 LU・1 ファイル内 17.21→14.54 LU と**わずかに縮小**
  だが、(i) 聴感で拾える水準に達していない、(ii) データ不変の ritsu も
  4.43→1.86 LU（区間幅）・1 ファイル内では **−2.88 LU と user の −2.67 LU
  より大きく**縮んでおり、**user の縮小は走行間変動に埋もれて介入効果として
  取り出せない** → 「交絡が解消した」とは言えないし「正規化で縮んだ」とも
  言えない。run 6 の正規化はカード単位の統合ラウドネスを揃える設計で
  カード内変動は保存する仕様 → 残存の発生源は (a) 収録のカード内
  ダイナミクス or (b) モデル/曲側。④ user 寄りセル不安定の再評価は
  VG-E1 へ送り・**T3 は引き続き任意**
- **①**: 劣化なし = 回帰合格。局所退行 2 点の消長は一様でない —
  **みわたす語尾「す」= 維持 / d3synth さ→あ = 改善**（User が d3synth を
  「ほか」から切り出して明言）。ただし d3synth 改善の帰属は**未確定**:
  (i) 走行間変動（d3synth のデータ・spk_id・レシピは不変）と
  (ii) 共有デコーダ経由の間接効果（user wav は 15/15 変化）が交絡する。
  run 7 Q12 はこの背景変動を差し引いて読む
- **③**: ノイズ不在維持 → Q7「ノイズ = D3 起因」の帰属が強化

## 新機構が全て初実走で機能（資産化ループの回収）

- phase 別 namespace: 同名 checkpoint 衝突が**再発せず**（run 5 は手動 copyto 保全）
- train.log 捕獲（14.6MB）・実行 manifest（checkpoint/TB/material の sha を
  自動記帳 = s3_record 型の未転記問題を構造的に解消・failure_history 空）
- **salvage (c)**: export に必要な spk_map/lang_map/dictionary が退避済みで、
  判定材料生成が手戻りゼロ（run 5 は一次記録から再作成を要した）
- ANALYSIS_STACK_PIN の Pod 側初実走・salvage 67/67（run 5 は 57/57）
- **lock PENDING 解消**: gates/binarize 2 段の実測 freeze から。当初「2 環境が
  共存」と書いたが**誤りで、セルフレビューで訂正**: 両ログとも
  `/usr/bin/python`・bootstrap は venv を作らず全段 `sys.executable` なので、
  **同一環境の時系列 2 断面**（binarize の `pip install -r` が numpy 2.4.6→
  1.26.4 等を in-place 降格する）。決定論の担保は「環境分離」ではなく
  **STAGE_PLAN の順序不変条件**（変換系の pin 照合が binarize より前に完了）
  に依存する、と lock に明記。praat-parselmouth 0.4.7 も**観測値であって
  gate 強制ではない**ため確定 pin 行から降格（強制は別 PR）

## 正直会計 1 件

当初「run 5 の判定材料はローカル作業コピーの改変で生成された」と書いたが
**誤りで、セルフレビュー 2 巡目で訂正**: run 5/6 とも `gate_synth_run4.py`
経由で、同スクリプトは run 5 の pin `9899f3c` 時点で既に 4 話者 choices を
持っていた（s4_record のコマンド表は repo 正本のまま再現可能）。choices が
2 話者だったのは判定材料経路では未使用の `gate_synth.py` 側で、run 7 実装 PR の
拡張はそちらの将来利用に備えたもの。run 7 実装 PR のコミットメッセージに
残る「場当たり改変」の記述は誤りとして本記録で訂正する。

## run 7 起動（同日 03:35:56Z）

- PR #285（memo）/ #286（実装）マージ後、`create_pod_run7.py` の PIN_COMMIT を
  `8ef874b` へ差し替えて起動。Pod `hc0x4t5lf48foz` / RTX 3090 / $0.22/h /
  `RUN_PROFILE=run7` / cap $4
- 起動前チェック全通過: Drive `run7/amitaro_sources/` 324 本・pin commit から
  bootstrap と pod_entry が raw 取得可能・run7 プロファイル在中
- **新規リスク 6 点が準備段に集中**（amitaro_sources の ID 指定取得 /
  convert_amitaro の Pod 実走 / run7 pins 照合 / assemble --profile run7 /
  binarize 後 verify_spk_map / preflight の比較相手が run6 か）。完走見込み ~09:15Z
- s5-record（PR #287）は #285/#286 が STATUS を触った後の分岐だったため
  コンフリクト → rebase で解消（run 6 行 = closeout・run 7 行 = 走行中へ統合）

## VG-L0 実装第 1 タスク: 制御軸表現力の実測（run 7 走行中の待機作業）

**[PROVISIONAL / REVIEW_PENDING — PR #289]**。正本 =
`voice_genesis/evolution/records/vgl0_control_axis_probe_2026-08-20.md`。
実測資産は `voice_genesis/evolution/probes/` にコミット済み
（`vgl0_control_axis_probe.py` / `vgl0_reproducibility_check.py`）。

- 前提 = run 6 40K ONNX・さくら・CPU 推論・**GPU ゼロ**。DESIGN の pin は
  run 5 checkpoint なので、run 6 代替の射程は**入力 I/F・介入点の探索まで**
  （音質・知覚・可動範囲・WAV sha は checkpoint 依存）
- **確定**: 子音時間配分 `consonant_duration_scale` の介入点が duration 経路に
  存在し出力まで届く / 制御量は単調（子音比 0.0562→0.4167）/
  **Render Reproducibility PASS を独立プロセス間で確認**（計 10 プロセス:
  fresh-process 反復 4/4 + 実行順反転 17/17 + ExecutionProfile 一致）/ フレーズ間ブレスと終端伸長は**尺としては厳密**（誤差 0）/
  gate_synth はフレーズ情報を受け取れない（`_NoteWithMs` が 3 属性しか写さない）/
  音素長の総和保存は 3 走行・全 51 条件で成立（`ph_dur<1` は 0 件）
- **撤回（3 巡目・外部レビュー 2 件）**: 「候補 3 軸すべて可動」—
  **ブレス軸と終端軸の定義そのものが誤っていた**。`HEAD_FRAMES`/`TAIL_FRAMES` は
  曲全体の前後 SP パディングでフレーズ間ブレスではない。`notes_limit=8` の
  最終ノートはフレーズ 2「やよいの」の 2 音目で終端ではない
  （`is_phrase_final=false` を機械実測で確認）。正しい定義で測り直した
- **測り直しの結果**: 尺は厳密に動くが、**挿入した SP が無音として鳴るかは
  境界で割れた**（境界 1 = 直前比 −26.0 dB で無音 / 境界 2 = **+9.8 dB** で
  無音になっていない）。しかも子音軸と併用すると境界 2 の実現が変わる
  （+9.8 → −1.0 dB）= **直交性は否定側の観測**。よって「ブレスとして成立するか」は未判定
- **Render Reproducibility の測り直し**: 2 巡目は**同一プロセス内の反復**で
  PASS としていた（monkeypatch 状態・module global・セッション・乱数状態が
  共有される）。independent replay の証明にならないので、1 条件 1 サブプロセス
  + forward/reverse 順の 2 プロセスで測り直し、同一プロセス内反復は結果 JSON でも
  `in_process_repeat` として別枠へ分離した
- **命名の格下げ**: 実際に操作したのは子音時間配分であって知覚上の
  「アタック強度」ではない（しかも変更後 duration は pitch predictor にも入る）。
  未校正の値に知覚名を付けると LearningTransition の意味汚染になるため
  `consonant_duration_scale` / 条件ラベル `cdur_x*` へ改名
- **provenance の穴を閉じた**: monkeypatch は「本体 sha が同じでも実行時挙動が
  変わる」ため、結果 JSON に probe 自身と gate_synth 本体の sha を**モデルと
  同格で**束縛（+ score/acoustic/embed/vocoder/canon 各 onnx + 入力パラメータ +
  ExecutionProfile）。製品実装では monkeypatch 禁止を BLOCKER に記載
- **音素長の下限ガードが現行コードに無い**ことを実測で確認: ×0.25 の最小子音は
  `notes_limit=8` で 3 フレーム、**10 では 2 フレーム（下限まで残り 1）**。
  ノート範囲を広げただけで余裕が縮んでおり、さくら全長や短いノートを持つ曲では
  0 に到達しうる。「さくら 8 ノートで通った」は安全性の根拠にならない。`0.25〜3.0` は `experimental_observed_range`
  であって `safe_range` ではない
- **既存 Performance 層との意味衝突は仮説ではなく既発生**（調査で確認）:
  repo には演奏制御が**3 バックエンド**に分かれて存在し（singer 自前
  ソースフィルタ 22050Hz / WORLD 24000Hz / DiffSinger ONNX 44100Hz）、
  gate_synth は `performance.py` も `perf_genes.py` も import していない。
  そのうえで **breath / jitter / vibrato / attack / drift / onset の 6 語が
  別単位・別意味で衝突済み**（例: `jitter_amount`=無次元比 0.006 vs
  `jitter_cents`=3.0 セント ≈ 170 倍差 / `BREATH_DURATION_SEC`=秒 vs
  `breath_lift`=WORLD ap の無次元底上げ）。VG-L0 が gate_synth 内部へ独自の
  breath/attack を足すと 7 語目の衝突になる → ControlProfile を renderer の
  外側に置く設計を BLOCKER 化
- **STATUS 昇格フローを新設**: experiment → self-review → PR review →
  accepted record → STATUS。レビュー前に STATUS へ載せる場合は
  `[PROVISIONAL]` / `[REVIEW_PENDING]` を必須にする（今回「3 軸すべて可動」が
  レビュー前に STATUS へ入り、他セッションが前提として拾いうる状態だった）
- 実測スクリプトの自己欠陥も 1 件訂正済み: セッション生成が `build_inputs`
  より先に起きるため is_vowel_flags の先読み実装では patch が一度も適用されず
  「アタック軸は動かない」という誤った実測になっていた（遅延参照へ修正）

## run 7 closeout（教師交代・11:22:53Z self-stop）

正本 = `voice_genesis/foundry/results_s6/s6_record_2026-08-20.md`。
**起動 2 回**・NaN ゼロ（train.log 14.4MB 全走査）・40K 完走・self-stop 成功・
**≈$1.40**。namespace 分離が 2 回目の実証。

- **初回起動は fail-closed**（`hc0x4t5lf48foz` 03:35:56Z → 04:24:57Z・49 分
  ≈$0.18）。04:19:34Z に `amitaro_sources: file set mismatch` — ID 指定取得へ
  替えた際に `plan_drive_id_fetch` の `{i:03d}_{name}` 採番を考慮していなかった。
  PR #288（`0b994a94` ほか）で sha256 索引経由の解決へ根治
- **s6 record の当初版は「起動 1 回・fail-closed ゼロ・≈$1.22」と誤記していた**。
  Codex レビューの指摘で RunPod API 実測から訂正。run 6 が「起動 1 回」だった
  のと取り違えた。**「無事故で通った」のではなく「事故を検出して止めた」**

- **Q12 = 矯正効果の保存は達成**。ritsu「す」改善 / pjs 改善のみ /
  user が三世代で最大改善（音量交絡解消・第三の声確認）。**実録音教師は
  合成教師の代替として機能する**
- **当初の見立てを撤回した**: 「り→ん破綻（かぎり→かぁんん）は教師交代の
  副作用」と組み立てたが、③三世代判定で **run5→7 一貫して未解決**と判明。
  d3synth を投入していた run5/run6 でも解決していなかった。
  `score_d3_sustain.py` の docstring が「り→ん破綻対策の**主力データ**」と
  宣言していたのを読んで因果を作ったが、docstring が言うのは**意図**であって
  **効果**ではない。実測で確かめずに機構から因果を構成した — VG-L0 の記録で
  自分が何度も撤回してきたのと同型の誤り
- **設計上の発見**: 対策データが「投入」だけで「効果」を検証されないまま
  対策済み扱いになっていた（DESIGN_S3_backfill の り→ん対策）
- **り→ん破綻は話者依存**（免れるのは pjs 系のみ）。さくら破綻/うみ成立の
  曲依存も観測されたが交絡分離できず未判定
- **教師交代の等価性は dosage だけでは足りない**: DESIGN_S6 は時間を
  1200.5s→1124.5s と厳密に合わせたが素材種別の被覆は合わせていない
  （d3synth = カナ+サステイン+曲レンダ / あみたろ = 朗読のみ・持続音ゼロ）
- ②は User 裁定で不要（run 6 と同一区間・同一 LUFS で生成済み・保全）
- 監視ギャップ: `MILESTONE_STEPS` に 30000 が無く 20000→40000 の約 2 時間
  マーカーが出ない。実際に停滞と誤認しかけた

## PR #289（VG-L0）レビュー打ち切り

Codex 自動レビューは **6 巡で打ち切り**（User 裁定）。1〜6 巡の指摘は全採用・
コミット済み（見送り 0）。7 巡目以降はフォローアップ PR 送りとし、内容は
record §9-1 に記録した。7 巡目の 3 件目（`load_song_module` の `_shas` を
捨てている）は **§9 の member 5 境界宣言の前提を一部崩す**材料なので、
フォローアップで §9 の表を改訂する可能性がある。

resolve は実行環境の制約で不可（REST repo スコープ 403 / GraphQL は
pinned set のみ / gh CLI 無し / GitHub MCP 無し。`/user` と `/rate_limit` は
200 なので認証自体は通っている）。CLAUDE.md の規約に前提条件を明記した。

---

## Session Summary — 2026-08-20 (Session 3)

### Context

run 7（教師交代 = d3synth 引退 → あみたろ実録音教師）の監視・完走・判定材料
生成・耳判定・closeout を実施。並行して VG-L0 制御軸 probe（PR #289）の
外部レビュー 2 通 + Codex bot 9 巡への対応を行い、#289 をマージした。

### Design Decisions

- **VG-L0 記録は 3 度撤回した**。(1)「音響応答は非単調」= ピーク 0.6 正規化後
  RMS の artifact、(2)「候補 3 軸すべて可動」= ブレス軸/終端軸の定義そのものが
  誤り（HEAD/TAIL_FRAMES は曲全体の SP パディング・notes_limit=8 の最終ノートは
  フレーズ終端でない）、(3) 決定論 PASS = 同一プロセス内反復では independent
  replay の証明にならない。いずれも**機構から因果を組み立てて実測で確かめて
  いなかった**のが共通の原因
- **り→ん破綻の帰属も同型の誤りで撤回した**。`score_d3_sustain.py` の docstring
  （「り→ん破綻対策の主力データ」）を読んで「外したから再発した」と組み立てたが、
  ③の三世代判定で run5→7 一貫して未解決と判明。docstring が言うのは**意図**で
  あって**効果**ではなかった。派生して「対策データは投入ではなく効果で終端宣言
  する」という設計原則を s6 record の申し送りに入れた
- **bot レビューの採否基準を明文化**（クリティカルな欠陥 / 意味的汚染 /
  将来のバグ要因の 3 つに絞り、他は設計範囲として据え置く）。上限 10 ラウンド・
  採否はコミットメッセージに記載・対応済みは resolve、を CLAUDE.md へ新設
- **pin ファミリーの全数掃討**を 4 巡目で宣言したが 5 巡目に漏れが出た。
  「同じ検査を全メンバーへ機械的に横展開したか確認してから宣言する」を記録へ
- **②の A/B/C は User 裁定で破棄**。判定しない材料を保全し続けると「未判定の
  在庫」が「判定済み」と誤認される危険があるため、使わないと決めた時点で捨てる

### Artifacts

- **PR #289 マージ**（`ba1379f9`）: VG-L0 制御軸 probe 記録 + probe 2 本 +
  結果 JSON 4 本 + pin 整合 CI テスト + S6 run 7 closeout 記録 +
  bot レビュー運用規約（CLAUDE.md / AGENTS.md）
- `voice_genesis/foundry/results_s6/s6_record_2026-08-20.md`（新規・220 行）
- run 7 成果物: 40K checkpoint / train.log / ONNX export（話者 4 種）/
  判定材料 wav 8 本 / 三世代比較 6 本。すべて sha256 を record へ転記

**Fable 直接実行監査**: 本セッションは Fable が probe 実装・実測（6 回の全再取得）・
判定材料生成・記録起草をすべて直接実行した。委譲が妥当だったのは
「Performance 制御実装の横断調査」のみで、実際に Explore agent へ委譲している。
実測の再取得（1 回 35 分 × 6）は待機が主で委譲の利得が薄く、直接実行で妥当。

### Handoff

- **フォローアップ PR**（未着手）: (1) 記録の PROVISIONAL → accepted 昇格
  （§8 昇格フローの未執行分・**自分で決めた規約を守れていない状態**）、
  (2) checker の派生入力保護、(3) 所有外 result ファイルの unlink 拒否、
  (4) `load_song_module` の `_shas` 検証 = **§9 の境界宣言を見直す材料**、
  (5) 正本結果ごとの geometry 検査
- **run 8 の主題候補 = り→ん破綻の専用調査**。S3 以来の最古の未解決課題で、
  今回「話者依存（pjs のみ免れる）」「run5〜7 一貫」まで絞れた
- s3_record 未転記残 3 項目（run 4 分）は本セッションでも未着手
- VG-E1（founding 3 体の台帳接続・評価軸凍結）は未着手
- **出力の不安定さについて User から指摘あり**。次セッションへ引き継ぐ

