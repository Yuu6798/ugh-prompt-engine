# 2026-08-09(跨ぎ → 08-10)

## Session Summary — 2026-08-09

### Context

Task Brief「M3d slow-lane 校正実測(手順 2–5)+ G2 帯域語彙是正」をリモートコンテナで
完走した。結果は **melody トラック closeout**(User 決裁・選択肢 a)——校正不成立の性質は
「分離不能」ではなく**観測律速**。v1(#252 の盲選定 98 対)は run×2 完走・bit 一致まで
健全だが evaluate が凍結拒否(real positive 46/48 が観測ゲート `phrase_count 1 < min 2`)。
User 承認の v2 再事前登録(観測ゲート・スクリーニング選定)も 40 clip census で
survivor 3/40 → 自らの停止条件で fail-closed 終端。判定・再入条件 =
`docs/m3d_calibration_record.md`。PR #255 は Codex **14 巡・36 スレッド**対応の末、
User が merge commit 方式で手動マージ(祖先証明の main 保存を機械確認済み)。

### Design Decisions

- **停止条件の運用**: v1 凍結拒否・v2 survivor 不足とも事前登録ガードの正常動作として
  即停止・User 報告(事後調整ゼロ)。診断は vocadito 正解アノテーション突合で機構を
  素材特性 6 clip × crepe voicing false-alarm 融合 4 clip の複合と特定(M2d 整合)
- **v2 選定規則**: 類似値を参照しない観測ゲート S1/S2 スクリーニング + sha256 順分割 +
  survivor 下限の停止条件。17/22 汚染防御は「clip 固有項なしの機械規則」で担保
- **closeout の帰結**: M4 不点灯恒久・L0c 非解禁 = L 系列は非旋律軸(#245–#250)で完結・
  G2 是正 moot 化(設計判定 = 素材種束縛を記録)・M1-real Go バー moot 化。registry は
  uncalibrated のまま不変更(一方向規則)
- **レビュー採否(14 巡)**: 採用 27・部分採用 3・不採用/見送り 6。終端機構 = 脅威モデル
  境界(第 3 巡)・保護集合の有界宣言(第 8 巡)・診断計器ファミリー完結(第 12 巡)+
  preflight 最小複製境界(R2/W2)。正直会計の 2 段是正(98/98 → 66/66 → 段階別
  60/2/4: 全チェーン n=4)は自らの過大主張の訂正として透明記録
- **実行基盤の系統的死因を特定**: バックグラウンドタスク TTL ≈4200 秒(run1 が 2 回
  70 分ちょうどで死亡・ログ凍結は stdout バッファの錯覚)→ **setsid デタッチ +
  成果物ベース完了判定**で克服。全件 pytest も同 TTL に収まらないため対象テスト +
  CI 委任へ矯正(委譲エージェントが 2 回この罠に停滞し SendMessage 介入で回復)

### Artifacts

- **PR #255 マージ**(merge commit `b1947a1`): docs/measurements/m3d_2026-08/ 実測記録
  一式(run×2・verdict・診断・census・provisioning)+ preregistration_v2.md +
  docs/m3d_calibration_record.md + スクリーナ/builder v2/診断導出スクリプト + テスト
  約 100 件 + 索引/STATUS 消し込み
- 実測: crepe 460 起動計画中 tuning 264 消化(holdout 検証権は未消費のまま終端)・
  S1/S2 スクリーニング約 90 起動・全 pin 一致(crepe 重み/vocadito/40 clip)
- **Fable 直接実行監査**: 逸脱 2 件——(1) run1 死因調査で ps/loadavg 直接確認を複数回
  (孤児化回復の緊急判断・許容範囲と判断)(2) wrap-up 前の閉め作業で PR 本文更新・
  マージ方式検証・cancelled ジョブ再実行トリガを直接実行(1–2 コール級の連鎖だが
  総量はマイクロ例外を超過気味。次回は閉め作業も委譲を検討)。実装・実測・返信は
  全て Sonnet 委譲(11 体)

### Handoff

- **次の主戦線 = M2e r7 完走 → census verdict → 破断曲線の判読(設計側・Fable)**
- CI 観察: `test-m2-harness` シャード(PR 無関係ファイル)が 35 分 timeout で 2 回
  cancelled(ランナー速度変動 + TB-30 予算の際どさ)。rerun で再検証中に User マージ。
  シャード予算の再点検を queue 化済み(P3)
- 政策編入の提案 2 件(User 採否待ち): (1) 実行基盤 TTL ≈4200s と setsid デタッチ
  レシピの AGENTS.md §8 恒久化 (2) 08-08 から持ち越しの provenance 計器チェック
  リスト外部化(今回 14 巡の指摘が全て同ファミリーで再演——外部化の価値を再実証)
- melody 再入条件 = M0/M1 ゲート意味論の再設計登録のみ(判定 doc §4)
