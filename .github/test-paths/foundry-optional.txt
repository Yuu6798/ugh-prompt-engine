# Foundry files excluded because module collection needs an optional runtime.
# Empty by design: optional runtime use is isolated to individual tests.
# Adding a file here requires an explicit reviewed diff.
